# Full-deck 98–116 page outline

Status: `DIRECTLY TEACHABLE DONOR-TAIL OUTLINE / CURRENT CONTENT STILL TO BE FROZEN`

This file defines the donor-owned tail of the full deck: `P098–P116` (19
pages). Together with the course-owned `P001–P097` modules, the maximum build is
116 pages, inside the accepted 98–116 range. The normal 120-minute route may
omit pages marked `APPENDIX` or `FAST`; they are named optional capacity, not
duplicate filler.

The page-level route is written for the current package identity:
`https://github.com/cedarwud/lora-energy-lab`, root `lora-energy-lab/`, Python
module `lora_energy_lab`, with the marked region of `student_policy.py` as the
only learner code-edit surface. No obsolete C-120 ZIP/path/hash, old BeamShift
button, old T1–T6 session state, or donor KPI appears in the teachable route.

## Placement contract

| Range | Owner/role | Use |
|---|---|---|
| P001–P027 | Part A | Opening, package boundary, Windows/WSL/Python setup, `.venv`, verify, and safe policy reading. |
| P028–P063 | Part B | Endpoint model plus Lab A pace/rest and Lab B enter/exit/hold, including frozen/withheld evidence. |
| P064–P097 | Part C | Lab C, result/replay import, workbook, recovery, evidence clinic, transfer, and hypothesis. |
| **P098–P116** | **Donor lane** | Optional source/technical appendix, two causal deepening branches, transfer, and claim-safe handoff. |

The donor tail must be interleaved only at the places identified in
[`donor-insertion-map.md`](donor-insertion-map.md). Each page below has one
teaching job and one dominant visual. All current command names, API versions,
line numbers, artifact IDs, screenshots, and numeric results remain
`EVIDENCE PLACEHOLDER` until the current package/replay gates pass.

## Directly teachable page-level outline

| Page | Route | Title and learner-facing anchor | Dominant visual | Teaching operation and expected evidence | Interpretation/recovery | Donor role |
|---:|---|---|---|---|---|---|
| P098 | APPENDIX | **TLE 是固定來源，不是即時位置** | Read-only two-line source card with `source_id`, epoch, raw bytes, and scenario anchor | **Do:** open the pinned scenario source read-only. **Why:** preserve the exact model input. **Mechanism:** source bytes and epoch feed later state derivation. **Expect:** source identity matches the current scenario. **Interpret:** this page establishes provenance, not a live pass or measured position. | Missing source, epoch, or identity stops the appendix; keep the page as a placeholder and do not fetch a replacement source during class. | `e2` 7 or 98; choose one, because they are a duplicate TLE opening. |
| P099 | APPENDIX | **SGP4：平均元素加目標時間，才得到狀態** | `TLE/GP + target time → SGP4 → position/velocity` flow | **Do:** point to the input and target time, then read the derived state. **Why:** show where contact context comes from. **Mechanism:** the model advances mean elements to a requested time. **Expect:** a derived state record with source/time lineage. **Interpret:** derived is not measured telemetry. | If target time, model version, or units are absent, stop at the source card and label the state `EVIDENCE PLACEHOLDER`. | `e2` 8 or 107; rewrite without donor satellite values. |
| P100 | APPENDIX | **TEME 不是地面站方向** | `TEME → ECEF → ENU → az/el/range` conversion chain | **Do:** follow one state vector through the frame chain. **Why:** avoid treating a reference-frame vector as an observer reading. **Mechanism:** frame conversion and observer location produce local direction values. **Expect:** frame/unit labels remain attached to each step. **Interpret:** only the final observer frame can answer local elevation/azimuth questions. | Frame or unit mismatch means no policy comparison; preserve the error and return to the current scenario contract. | `e2` 9–10 and 108–109; optional source vocabulary only. |
| P101 | APPENDIX | **可見性、角度、距離、品質是不同 gate** | Four-stage gate: elevation → boresight angle → range/path → quality | **Do:** classify a field before using it in a policy explanation. **Why:** prevent one geometric angle from standing in for service qualification. **Mechanism:** each field enters a different model branch. **Expect:** a legal service opportunity has the required gates. **Interpret:** passing visibility alone does not prove delivered service. | Unknown field mapping or units keeps the candidate unqualified; do not import donor thresholds or formulas. | `e2` 5, 11, 30–31, 110–111; redraw as one non-specialist page. |
| P102 | APPENDIX | **quality / dB 只是 action context** | Small `quality observation → linear interpretation → policy threshold` ladder | **Do:** read one current quality observation and its unit. **Why:** let policy use only information available at decision time. **Mechanism:** dB/log representation is interpreted before any threshold or rate explanation. **Expect:** an observation field, not a delivered-bit or energy result. **Interpret:** quality is not service, endpoint J, or `bit/J`. | If units or observation timing are unknown, mark the field unavailable and return to the policy API page; never guess a conversion. | `e2` 13, 15, 22–24; keep the concept, not donor RF values. |
| P103 | APPENDIX | **throughput、delivered bits、service 不同欄** | Timeline with rate, delivered packet, and service-pass gate | **Do:** read rate and delivered outcome separately. **Why:** a high rate with failed delivery is not service. **Mechanism:** packet outcomes accumulate into delivered work only when the course service boundary passes. **Expect:** distinct rate, delivered bits, freshness/deadline, and service fields. **Interpret:** no service pass means no fair energy-efficiency claim. | Missing packet or service evidence invalidates the comparison; keep the result/replay pair and use matching fallback if needed. | `e2` 14, 32, 37; merges the useful distinction without repeating the core unit page. |
| P104 | FAST | **切換太慢會中斷，太快會 ping-pong** | Two trace lanes: delayed switch versus A→B→A chatter | **Do:** inspect two frozen-policy traces. **Why:** expose the cost of a timing rule without adding a new control. **Mechanism:** margin/hold conditions change switch timing and service continuity. **Expect:** transition, continuity, and quality evidence from the current replay. **Interpret:** event count alone is not energy; judge service continuity and bounded policy behavior. | No frozen policy or trace identity means return to Lab B freeze; never retune after seeing the held-out trace. | `e2` 12; one optional counterexample before/after the current Lab B pages. |
| P105 | APPENDIX | **功率與能量要有明確帳本範圍** | `P(t)` strip with `W = J/s`, area under the curve, and endpoint/system boundary | **Do:** name the power scope before reading a number. **Why:** separate instantaneous power from time-accumulated energy. **Mechanism:** state duration and modeled power terms accumulate into endpoint J. **Expect:** a scoped ledger with units and source/assumption labels. **Interpret:** RF output, PA input, static terms, and endpoint J are not interchangeable. | If a term is not modeled, leave it absent rather than writing zero; if scope is ambiguous, stop before computing `bit/J`. | `e2` 16/17, 21, 25–29, 112; one rewritten accounting page. |
| P106 | FAST | **公平 A/B：固定 job、window、boundary** | Four-lock comparison gate followed by one changed policy block | **Do:** identify fixed scenario, seed, window, units, and predecessor, then mark one allowed policy change. **Why:** make a causal result attributable. **Mechanism:** only the marked policy bytes may alter actions. **Expect:** matching baseline/candidate identity and a consequential diff. **Interpret:** mismatch or null diff is a failed gate, not a successful energy result. | Restore the matching checkpoint or same-scenario fallback; do not merge donor tables or silently change the scenario. | `e2` 20, 36–37, 70–75; deepens the existing fair-baseline page rather than duplicating it. |
| P107 | FAST | **控制 → 狀態 → 欄位 → 解釋** | One causal spine with a highlighted intermediate state | **Do:** annotate one `student_policy.py` change from action to state to packet/service/energy evidence. **Why:** keep explanation causal. **Mechanism:** the intermediate state is the bridge between code and result. **Expect:** policy identity, state event, and result/replay field align. **Interpret:** if no intermediate evidence changes, the change is not consequential. | Keep the prediction and null diff; return to the marked block/API card instead of adding a visual-only claim. | `e2` 38–39; use as a single debrief visual. |
| P108 | APPENDIX | **一個低能量數字不等於節能** | Four-way verdict: service, quality, delivered work, energy | **Do:** compare at least delivered work, service/quality, and scoped J. **Why:** prevent denominator-only conclusions. **Mechanism:** numerator and denominator must refer to the same qualified window. **Expect:** a bounded verdict with the changed mechanism named. **Interpret:** lower J with lost service is a trade-off or failure, not a saving claim. | If service shrinks or a field is missing, label the comparison invalid/incomplete and preserve the original record. | `e2` 70–75; 73 is the direct question, but all current values must be regenerated. |
| P109 | APPENDIX | **一筆 evidence record 要能自己說明** | Source → identity → mechanism → evidence → limitation record card | **Do:** fill one current record from source mode through result/replay. **Why:** make a handoff reproducible. **Mechanism:** policy hash, scenario identity, run/replay IDs, units, and scope bind the interpretation. **Expect:** another reader can reconstruct what changed and what was observed. **Interpret:** a sentence without lineage is not a result claim. | Missing identity, scope, or source mode remains `INCOMPLETE`; do not fill the gap with a donor screenshot or expected value. | `e2` 96–97; rewrite as a current endpoint record. |
| P110 | APPENDIX | **measured、derived、assumed、simulated 要分級** | Four-class claim classifier with current course ceiling | **Do:** classify every displayed field or sentence. **Why:** keep evidence strength proportional to source. **Mechanism:** source/model/assumption lineage determines the permissible claim. **Expect:** endpoint runner/replay output is labelled simulated teaching data unless owner evidence changes it. **Interpret:** classification is a boundary, not a quality ranking. | Unclear class lowers the claim; return to provenance and leave a placeholder rather than upgrading it. | `e2` 113; one compact classification page. |
| P111 | APPENDIX | **endpoint J 不等於 system consumed J** | Two ledgers sharing scenario/clock but separated by scope | **Do:** place each field in endpoint or system layer. **Why:** avoid same-unit aliasing. **Mechanism:** endpoint replay and existing C-120 system replay have distinct authorities. **Expect:** no endpoint field writes into canonical/system fields. **Interpret:** shared anchor does not mean shared semantics. | If a field's scope is unknown, stop all `bit/J` interpretation and return to the schema/authority map. | `e2` 19–21, 28–29, 32–33; current authority overrides donor semantics. |
| P112 | APPENDIX | **policy bytes → result/replay → Leo import** | Current package lineage spine, with `student_policy.py` as the only edit node | **Do:** follow one policy identity through a deterministic run and JSON import. **Why:** distinguish learner control from generated evidence. **Mechanism:** policy hash and scenario identity bind the result/replay pair. **Expect:** Leo receives JSON only and never executes learner Python. **Interpret:** a changed label or animation without a consequential artifact diff fails the teaching gate. | Version/policy/hash mismatch returns to the release checkpoint or matching fallback; never edit generated JSON. | Topic rewrite using `e2` 96–97 and current ADR/SDD; no old package path or hash. |
| P113 | APPENDIX | **scenario、result、receipt、replay、workbook 是一條 contract** | Five-node contract flow with all-or-nothing import boundary | **Do:** read the identity spine before opening a replay. **Why:** preserve provenance across tools. **Mechanism:** scenario fixes inputs; result records the run; receipt freezes lineage; replay materializes events; workbook stores the learning record. **Expect:** matching IDs, units, seed, policy, and source mode. **Interpret:** validation failure leaves session/workbook unchanged. | Preserve the error and select the exact matching artifact or same-scenario fallback; do not hand-edit schema fields. | Current SDD plus donor evidence-qualification ideas from 70–75. |
| P114 | APPENDIX | **source、model、assumption、license 各有邊界** | Three-party seam: pinned upstream ↔ course wrapper ↔ JSON ↔ Leo | **Do:** identify which statement comes from source, model, course assumption, or result. **Why:** keep license and trust boundaries visible. **Mechanism:** documented JSON separates the GPL upstream runner from the Leo application and server execution. **Expect:** source disclosure and claim class travel with the artifact. **Interpret:** course learners edit only the bounded policy surface. | Any request to edit upstream, Leo source, scientific formulas, schemas, or generated artifacts returns to owner review. | `e2` 113–114; rewritten around current ADR-004 boundary. |
| P115 | FAST | **同一機制可以搬到另一個 IoT 場域** | LEO window → smart farm/HVAC/edge transfer map | **Do:** replace the LEO label with one application and state its job, opportunity, policy, service, and energy boundary. **Why:** demonstrate mechanism transfer. **Mechanism:** changing opportunity still drives send/wait/sleep/batch/urgent choices. **Expect:** one hypothesis and one falsifier, not a copied number. **Interpret:** a renamed scenario without a new boundary is incomplete transfer. | Return to the endpoint model and redefine service, deadline, traffic, and energy scope; do not transplant donor values. | `e2` 2, 4, 19; transfer prompt only. |
| P116 | APPENDIX / EXIT | **留下可重開、可反駁、可追溯的 handoff** | Adopt/Appendix/Retire legend joined to workbook, hypothesis, and claim shield | **Do:** verify the evidence record, frozen policy/result identity, workbook reopen state, and claim class. **Why:** close with a safe handoff rather than a pretty final number. **Mechanism:** every surviving claim points to current source, mechanism, evidence, and limitation. **Expect:** `COMPLETE` only when required evidence exists; otherwise explicit `INCOMPLETE`. **Interpret:** donor material is a reference trail, not current course evidence. | Preserve the missing gate and return to the affected page or same-scenario fallback; never claim live, measured, classroom-ready, or canonical parity without its owner gate. | `e2` 96–97, 113–114 plus this audit's disposition map. |

## Direct-teaching notes for the assembler

- The visual treatment should vary: source cards, model flows, gates, trace
  timelines, ledgers, comparison locks, record cards, and transfer maps. Do not
  reproduce the donor's repeated blank/filled table rhythm.
- The page title and notes must be regenerated in Traditional Chinese for the
  current deck. `student_policy.py`, `scenario_id`, `service_pass`, `consumed J`,
  and `bit/J` may remain canonical English identifiers where necessary.
- The current deck's claim ceiling is
  `SIMULATED TEACHING DATA / NOT LIVE / NOT MEASURED / NOT CANONICAL-PARITY-VERIFIED`
  until the corresponding evidence and owner gates change it.
- The final assembler should include P098–P116 only when the page has a clear
  learning, recovery, evidence, or source role. The delivery variant may inform
  layout comparison but supplies no notes and no current page evidence.
