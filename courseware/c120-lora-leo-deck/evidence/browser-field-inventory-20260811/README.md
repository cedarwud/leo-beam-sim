# Current `/course` field inventory

Controller-owned browser evidence captured from
`http://120.126.151.102:4191/course` on 2026-08-11 (Asia/Taipei).

Purpose: refresh the direct-teaching deck after the current website changed.
The captures use the site's built-in same-scenario fallback unless a filename
explicitly says `uploaded`.  They are simulated teaching evidence, not live or
measured data.  The field inventory records only labels and values that were
present in the actual DOM at capture time.
