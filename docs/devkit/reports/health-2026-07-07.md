# P2 全面健檢報告 — leo-beam-sim（2026-07-07）

> 方法：5 路並行唯讀評估（A=validator 資產＋工作樹、B=測試跑道＋CI、C=src 結構、D=文檔債、E=依賴工具鏈），L 級預算經 gr-2026-07-06-01 核准（est 1.2M token、實花約 0.6M agent tokens）。各路完整筆記在安裝 session scratchpad（會隨 reboot 蒸發；本報告是持久摘要，關鍵數字已全部抄入）。評估與執行拆開：**本報告只列發現與 backlog，使用者圈選後才動刀。**

## 總判定（一句話）

**資產本體品質高（載重 validator 層健康、零循環依賴、依賴面乾淨、lane 紅線零違規），但「安全網」有一個系統性假象：CI 出生至今 25/25 全紅、static:all 自發現有 src/ 盲區、235 支 validator 零型別檢查——三個洞疊起來，「唯一不可繞層」目前是一面沒砌的牆。** 修復鏈全部便宜（最大單項 0.5-1 天），先砌牆再做別的。

## 六維速覽

| 維度 | 判定 | 最重發現 |
|---|---|---|
| 安全網（B＋A） | 🔴 假象 | CI 三層獨立死因：①src 兩檔跨 repo **值** import ntn-sim-core（`visual-showcase-contract.ts:57`、`loadShowcaseArtifact.ts:36`）→ runner 上 tsc 必死；②workflow 引用已刪 script（`governance.yml:73`，`8cdb8c8` 刪除後未同步）→ visual-gates 必死；③13+ 支 validator 硬依賴 `/home/u24`／`/tmp` 本機路徑 → hosted runner 上 static:all 必死。branch protection 未設（API 404） |
| validator 資產（A） | 🟡 健康但有盲區 | static:all regex 只認 `scripts/` → 14 支 src-test validator＋2 支 aggregate 內嵌**永遠不被自動執行**（=6/14 rot 事故復發通道）；27 支 browser validator（vc* 19 支全數）無 runner；scene-lane-governance 3,345 行/598 needles 等未排程的 S6/C1/C2 |
| 測試跑道（B） | 🟡 能跑沒接 | 17 支 `*.test.ts` 全部能跑（實跑 2 支 PASS）、0 支被任何 gate 執行、1 支全孤兒（`seriesModel.test.ts`）；三選項評估：**擴既有 tsx 跑道勝**（~10 行 diff），vitest（新依賴）與判死都不成立 |
| src 結構（C） | 🟡 好架構+增生 | 「好架構」判定仍成立（0 循環依賴、S3 純度已修、lane 紀律活著）；但 god-file 已兩支：App.tsx 2279 行（上週 +208）＋MainScene.tsx 1950 行；>700 行檔 5→11 支；逆向邊僅 3 條（可 30 分鐘級清） |
| 文檔債（D） | 🟡 有權威失真 | 175 份全歸層：ACTIVE 48／REFERENCE 102／SUPERSEDED 19／CANDIDATE-ARCHIVE 6。**北極星計畫鏈 5 份不在任何路由面**；sdd-index 自身過時（「only these define next sequence」已為假）；13 份 superseded 無 banner；3 份殭屍「awaiting sign-off」 |
| 依賴工具鏈（E） | 🟢 乾淨小修 | lint 實為 `tsc --noEmit`（從無 ESLint）且只蓋 src/；vite 7.1.12 落在 dev-server 漏洞公告內且綁 `0.0.0.0:3000`（in-range 升 7.3.6 即清）；satellite.js 幽靈依賴；15/15 無 deprecated、audit 7 筆全 in-range 可歸零；tsc 基線 0 錯 |

## 分級 backlog（圈選單）

### 第零優先：安全網包（制度規定永遠排第一；全部 non-heavy、本機可做）

| # | 項目 | 證據 | 驗收條件 | 預算 |
|---|---|---|---|---|
| SN-1 | static:all 發現 regex 擴 `src/` | `validate-static-all.mjs:54-55`；乾跑重演證實 14+2 支被跳過 | 發現數 140→~154 全綠；negative-control（故意弄壞一支 src-test 要紅） | S（~0.5 天含兩輪 8min 驗證） |
| SN-2 | `seriesModel.test.ts` 補 npm script | 全 repo 僅自引用 | static:all 計數含它 | S（5 分鐘，併 SN-1） |
| SN-3a | vendor `visual-showcase-v1` contract 檔（走 CLAUDE.md §4 vendor 流程＋kill-switch audit） | 491 行 0 頂層 import；兩處值 import 是 CI 死因① | src 零跨 repo import；tsc 綠；ntn-sim-core 側 validator 先跑 | M（0.5-1 天；一模組一 PR） |
| SN-3b | workflow 清單與 package.json 對齊（刪 `governance.yml:73` 死引用） | 死因②；Rule#9 違反 | Actions 該 job 過此行 | S |
| SN-3c | 資料依賴 validator 的 CI 環境語義：缺 `/tmp`／sibling 資料 → **可見 SKIP＋計數**（不可 silent-pass） | 死因③；13+ 檔硬 readFileSync | 乾淨容器 static:all 綠且 SKIP 數可見 | M |
| SN-4 | CI 連續 3 綠後綁 `static-gates` required check（步驟已寫好照抄）＋**制度選擇**：全面 PR flow（勾 no-bypass）vs owner 直推軟模式 | branch protection API 404；25 run 全是 push-to-main | required check 生效；一次真實攔截演練 | S（使用者 5 分鐘＋決策） |
| SN-5 | `tsconfig.scripts.json` 給 235 支 script 型別覆蓋 | E＋A 同源發現；58,974 行零檢查＝rot 同構敞口 | `tsc -p tsconfig.scripts.json` 綠＋掛進 gate | M（0.5-2h；首跑可能翻存量錯） |

### P1（非安全網的高優先）

| # | 項目 | 證據 | 驗收 | 預算 |
|---|---|---|---|---|
| D-1 | 北極星鏈 5 份補路由（CLAUDE.md §6＋AGENTS.md 鏡像＋sdd-index 入口） | 只讀制度檔的 agent 會按已收攤的 showcase D0-D7 行事 | 路由表含 final-plan | S |
| D-2 | sdd-index 權威修真（Active 入口表重寫） | 「唯一 doc-tier 權威自身失真」 | 與 P2-D 分層一致 | S/M |
| W-1 | 工作樹 15 檔 untracked 全部 commit（**4 支 .githooks 是 git-lfs hooks**——repo 用 LFS、fresh clone 缺 pre-push 失去 LFS 上傳防護；11 支探針含 2 支 committed golden 的出生工具＝provenance） | A 路逐檔讀過、無一建議刪；untracked 被吃前科 | git status 乾淨 | S |

### P2（重要不急）

- vite in-range 升 7.3.6＋audit 歸零＋satellite.js 移除（S，30-45min 一次做）
- superseded 13 份補 banner＋3 份殭屍 status 修正＋`hobs-current-status` 指向新 SDD（S）
- god-file 止血：「新功能不落 App.tsx/MainScene.tsx」review 規則入 frontend contract（S 制度層）；S6/C1/C2 排程決策另開（實作 L，牽 tangle-lock 15 blocks） 
- browser validator 27 支 triage（M）；ω validator 4 支處置三擇一（S）
- lint 改名 typecheck 或文檔註明（S；Rule#9 原子連動）
- `beam-display-control-surface-sdd` status PROPOSED→SHIPPED 等狀態修真（S）

### P3（撿便宜）

逆向邊 3 條清理（30 分鐘級 ×3）；alias 雙跑 2 鍵；`:browser` 後綴補；static-all 紅時印 stderr tail；quarantine expected-failure-signature；13 支孤兒 script 處置記錄；engines pin；isolatedModules；scene/types.ts hub 拆分；HandoverEventRail model 抽出；frontend-ux-redesign 路由降級為歷史 ledger。

## fitness function 缺口（提案新 validator，照 repo 慣例）

1. **workflow↔package.json 對齊 gate**（Rule#9 的可執行化——這次 visual-gates 死因②就是它會抓的）。
2. **src→ui／engine→modqn 逆向 import 禁令**（C 路發現 3 條，趨勢要鎖）。
3. **>700 行檔案 watch**（readability ratchet：新進榜需附拆分計畫；現況 11 支入基線）。
4. **needle-count 趨勢**（scene-lane-governance 598 needles 入健檢趨勢，防 tangle-lock 無感膨脹）。

## 覆蓋面與誠實條款

- 已覆蓋：204 條 validator 名冊級全分層＋37 支深讀（19%）；175 份文檔全歸層（深讀 17）；src 全 import 圖＋SCC；15/15 依賴逐條；CI 100-run 歷史＋3 run step 級屍檢；17 支 test 逐支＋實跑 2。
- 未做：validator 全量實跑（唯讀原則）；vite build 半段（會寫 dist/）；ntn-sim-core 側源檔現況；Actions 線上 UI 實錄（gh API 離線側查）。
- 已知偏差：CI「不可繞」的完整語意只在 PR flow 下成立——owner 直推放行是 GitHub classic protection 的預設（SN-4 的制度選擇）。
