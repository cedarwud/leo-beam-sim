依據您提供的 32 條已通過三輪驗收之提案，以下整理為符合既有 Playwright 風格、嚴格遵守不可協商紀律（不自創 selector、原子讀取防 torn read、單一斷言單一職責、未寫明前置條件標註「前置條件不明」、無法轉換者誠實標註不適用）的完整對照表。

---

### 32 條驗收提案之 Playwright 行為斷言實作對照表

| # | 原斷言 | 建議放入的 validator | 程式碼 | 前置條件（要先到哪個 lane／做什麼操作） |
|---|---|---|---|---|
| 1 | `validate-phase-h-s5-orbit-trail-modqn-demo.ts:L27`<br>`appMode !== 'modqn-demo' \|\| reducedMotion` | 無（提案結論為無法轉換，建議改判 S） | `// 不適用：純記憶體狀態與 R3F primitive 無 DOM telemetry，經驗收確認無法轉換為 browser 斷言，改由 pure resolver 單元測試 (S) 守護。` | 不適用（改判 S 單元測試） |
| 2 | `validate-phase-h-s5-orbit-trail-modqn-demo.ts:L31`<br>`orbitTrail: false` | 無（提案結論為無法轉換，建議改判 S） | `// 不適用：OrbitTrail 為 Three.js 幾何且無 DOM testid，經驗收確認無法轉換為 browser 斷言，改由 resolver 單元測試 (S) 守護。` | 不適用（改判 S 單元測試） |
| 3 | `validate-phase-h-s5-orbit-trail-modqn-demo.ts:L35`<br>`spineParticles: true` | 無（提案結論為無法轉換，建議改判 S） | `// 不適用：設定值與 render plan 實際掛載邏輯矛盾且無 runtime 訊號，經驗收確認無法轉換為 browser 斷言，改判 S 單元測試。` | 不適用（改判 S 單元測試） |
| 4 | `validate-phase-c-scene-scale-override.tsx:L195`<br>`visualScaleMultipliers.beamFootprintMultiplier` | 無（提案結論為無法轉換，建議改判 S 或 V） | `// 不適用：footprint 幾何無 runtime 觀察點且已不讀取 multiplier，拒絕以 shell key 製造假綠，改判 S 或 V 守護。` | 不適用（改判 S 或 V） |
| 5 | `validate-phase-i-s7b-serving-count-selector.tsx:L241`<br>`runtime.cellServingCount ?? DEFAULT_SERVING_COUNT` | 新增獨立驗證腳本（`validate-phase-i-s7b-serving-count-selector.tsx`） | <pre><code class="language-ts">// 1. 驗證 null fallback 預設值（原子讀取 canvas 與 HUD）<br>const defaultSnap = await page.evaluate(() => ({<br>  canvasServingCount: document.querySelector('canvas')?.getAttribute('data-cell-serving-count'),<br>  hudServingCount: document.querySelector('[data-testid="hud-serving-count"]')?.getAttribute('data-serving-count') ?? document.querySelector('[data-testid="hud-serving-count"]')?.textContent?.trim(),<br>}));<br>assert.equal(defaultSnap.canvasServingCount, defaultSnap.hudServingCount, 'canvas and HUD serving count match DEFAULT_SERVING_COUNT on null fallback');<br><br>// 2. 切換至 L=8 radio<br>await page.locator('input[type="radio"][value="8"]').click();<br>const updatedSnap = await page.evaluate(() => ({<br>  canvasServingCount: document.querySelector('canvas')?.getAttribute('data-cell-serving-count'),<br>  hudServingCount: document.querySelector('[data-testid="hud-serving-count"]')?.getAttribute('data-serving-count') ?? document.querySelector('[data-testid="hud-serving-count"]')?.textContent?.trim(),<br>}));<br>assert.equal(updatedSnap.canvasServingCount, '8', 'canvas cellServingCount updates to 8');<br>assert.equal(updatedSnap.hudServingCount, '8', 'HUD cellServingCount updates to 8');<br><br>// 3. 點擊 clear 按鈕後驗證回到預設值<br>await page.locator('button:has-text("Clear")').click();<br>const resetSnap = await page.evaluate(() => ({<br>  canvasServingCount: document.querySelector('canvas')?.getAttribute('data-cell-serving-count'),<br>  hudServingCount: document.querySelector('[data-testid="hud-serving-count"]')?.getAttribute('data-serving-count') ?? document.querySelector('[data-testid="hud-serving-count"]')?.textContent?.trim(),<br>}));<br>assert.equal(resetSnap.canvasServingCount, resetSnap.hudServingCount, 'clearing storage falls back to DEFAULT_SERVING_COUNT');</code></pre> | 前置條件不明（材料僅標註透過 storage 預置與清空操作，未提供確切 lane / URL / storage key） |
| 6 | `validate-modqn-handover-story-layer.ts:L558`<br>`visible={showCellReassignmentEventArcs}` | 無（提案結論為無法轉換，不放入 browser validator） | `// 不適用：canvas[data-modqn-handover-cues-visible] 為同源推導屬性，直接讀取同源 dataset 會造成假綠，經驗收確認無法轉換為 browser 斷言，建議改判 S。` | 不適用（改判 S 單元測試） |
| 7 | `validate-modqn-handover-story-layer.ts:L562`<br>`beamConeScope={modqnVisualLayers.beamConeScope}` | 無（提案結論為無法轉換，不放入 browser validator） | `// 不適用：現有 telemetry 僅為預算值而非 mesh 回報，prop 斷線仍會假綠，無獨立觀察點，經驗收確認無法轉換為 browser 斷言。` | 不適用 |
| 8 | `validate-phase-h-s8-hud-camera-default.ts:L41`<br>`mainScene.includes("'paper-faithful-closeup'")` | 無（提案結論為無法轉換，不放入 browser validator） | `// 不適用：該 preset 缺乏 UI 觸發入口，為未被使用的 source shape，無 runtime 入口或 selector，經驗收確認無法轉換為 browser 斷言。` | 不適用 |
| 9 | `validate-phase-i-s5b-cell-beam-cones.tsx:L379`<br>`beamConeScope={modqnVisualLayers.beamConeScope}` | 無（提案結論為無法轉換，不放入 browser validator） | `// 不適用：同源預算 dataset 無法作為獨立證據，現有 telemetry 無法反映實際 mesh，經驗收確認無法轉換為 browser 斷言。` | 不適用 |
| 10 | `validate-phase-g-ue-mobility-step.tsx:L262`<br>`runtime.ueMobilityMode` | 無（提案結論為無法轉換，不放入 browser validator） | `// 不適用：canvas 僅暴露 primary UE，但 mobility 僅作用於 secondary UE（缺乏 telemetry），且 rail events 為平行計算無法代理 MainScene wiring，經驗收確認無法轉換為 browser 斷言。` | 不適用 |
| 11 | `scene-lane L2309`<br>`visible={(focusBeamLoad?.load ?? 0) > 0}` | 無（提案結論為無法轉換） | `// 不適用：全域 contention 數無法等價證明焦點 beam load，且現有 UI 無法受控設定輸入條件，經驗收確認無法轉換為 browser 斷言。` | 不適用 |
| 12 | `scene-lane L2396`<br>`beamConeScope: 'focus-satellite'` | 無（提案結論為無法轉換） | `// 不適用：一般 cone scope 屬性不屬於上傳粒子元件，若強行替代會造成粒子錯誤擴散時仍假綠（削弱防護），經驗收確認無法轉換為 browser 斷言。` | 不適用 |
| 13 | `scene-lane L2686`<br>`sceneFrame?.sceneSource === 'artifact-replay'` | 無（提案結論為無法轉換） | `// 不適用：目前 App 缺乏可到達的 artifact 命名路由，無法以 query string 建立有效測試，經驗收確認無法轉換為 browser 斷言。` | 不適用 |
| 14 | `scene-lane L2691`<br>`<ArtifactSceneContent` | 無（提案結論為無法轉換） | `// 不適用：目前無穩定 artifact 入口，維持 blocked 待入口提供，經驗收確認無法轉換為 browser 斷言。` | 不適用 |
| 15 | `scene-lane L2696`<br>`liveSimulationEnabled="0"` | 無（提案結論為無法轉換） | `// 不適用：artifact 前置狀態無法自公開 UI 建立，避免只測預設值造成假綠，經驗收確認無法轉換為 browser 斷言。` | 不適用 |
| 16 | `scene-lane L2701`<br>`liveSimulationEnabled={simSource === 'live' ? '1' : '0'}` | 無（提案結論為無法轉換） | `// 不適用：homepage 凍結 TLE 入口導致 else 分支不可達，拒絕只斷言單邊行為，經驗收確認無法轉換為 browser 斷言。` | 不適用 |
| 17 | `scene-lane L2775`<br>`<DecisionHandoverCue` | 無（提案結論為無法轉換） | `// 不適用：R3F group 無法由 DOM 查詢，且呼叫端的 event count 無法證明元件實際渲染，經驗收確認無法轉換為 browser 斷言。` | 不適用 |
| 18 | `scene-lane L2826`<br>`multiCandidateCentralOverlayActive && !multiCandidateServing...` | 無（提案結論為無法轉換） | `// 不適用：canvas 屬性僅為呼叫端布林式的直接投影，未能證明 fallback mesh 實質渲染，經驗收確認無法轉換為 browser 斷言。` | 不適用 |
| 19 | `scene-lane L2847`<br>`enabled: simSource === 'live' && sceneFrame.sceneSource ...` | 無（提案結論為無法轉換） | `// 不適用：場景語意旗標不能證明 publisher 停止呼叫 onSimUpdate，需補 publisher sequence receipt，經驗收確認無法轉換為 browser 斷言。` | 不適用 |
| 20 | `scene-lane L3159`<br>`if (targetLane === 'artifact-replay') { if (handoverMode...` | 無（提案結論為無法轉換） | `// 不適用：切換路徑不可達，且單純斷言 banner 消失為可空斷言（無法證明 policy 被正確正規化），經驗收確認無法轉換為 browser 斷言。` | 不適用 |
| 21 | `phase-f L257`<br>`runtime.ueDistributionMode` | 無（提案結論為無法轉換） | `// 不適用：distribution 僅影響 secondary UEs，現有 canvas 僅發布固定 primary UE 位置，無有效遙測可核對，經驗收確認無法轉換為 browser 斷言。` | 不適用 |
| 22 | `phase-g L227`<br>`runtime.ueMobilityParams` | 無（提案結論為無法轉換） | `// 不適用：UI 控制項已刻意移除且無 secondary 軌跡 telemetry，經驗收確認無法轉換為 browser 斷言。` | 不適用 |
| 23 | `L1594`<br>`teachingPolicySection={isWalkerSceneActive ? (` | 無（提案結論為無法轉換，改判 V） | `// 不適用：showTeachingAuxiliaryUi 在呼叫端寫死為關閉，runtime 不可達；若寫缺席斷言只會得到恆真假綠結果，經驗收確認改判 V 靜態檢查。` | 不適用（改判 V 靜態檢查） |
| 24 | `L1739`<br>`if (!showSinrServingMosaic) return null;` | 無（提案結論為無法轉換，改判 S） | `// 不適用：color-count 來自獨立 telemetry 計算而非 mesh mount 訊號，無法守住 guard 刪除或接錯的破壞（有假綠風險），經驗收確認改判 S 單元測試。` | 不適用（改判 S 單元測試） |
| 25 | `L1744`<br>`buildSinrServingUeColorMapFromCells(cellFrame.ues)` | 無（提案結論為無法轉換） | `// 不適用：distinct-color count 無法證明色圖是從 cellFrame.ues 建立，亦無法驗證各 UE 色彩映射正確性，有假綠風險，經驗收確認無法轉換為 browser 斷言。` | 不適用 |
| 26 | `L2246`<br>`visible={showCellReassignmentEventArcs}` | 無（提案結論為無法轉換，改判 S） | `// 不適用：Canvas 屬性僅為設定態而非真實 arc mesh 渲染計數，prop 接線損壞時仍會假綠，經驗收確認改判 S 單元測試。` | 不適用（改判 S 單元測試） |
| 27 | `L2261`<br>`{presentationPlan.visible['serving-beams'] && showCellOverlay && modqnVisualLayers.beamCones && (` | 無（提案結論為無法轉換） | `// 不適用：三個 cell beam cone 屬性是 pre-mount resolver 計算所得而非真實 mesh 掛載訊號，無法驗證 JSX gate，經驗收確認無法轉換為 browser 斷言。` | 不適用 |
| 28 | `L2272`<br>`beamConeScope={modqnVisualLayers.beamConeScope}` | 無（提案結論為無法轉換） | `// 不適用：DOM 與 WebGL userData 隔離，同源 telemetry 無法偵測 prop 接線遺失，經驗收確認無法轉換為 browser 斷言。` | 不適用 |
| 29 | `Y6:L664`<br>`const liveTimelineWindowStartSec = demoStartOffset;` | 專用 Live Timeline 驗證腳本（`validate-phase-y6-live-timeline.ts`） | <pre><code class="language-ts">// 一次讀取兩個屬性（避免跨 render 的 torn read）<br>const snap = await page.evaluate(() => {<br>  const rail = document.querySelector('[data-testid="walker-results-rail"]');<br>  const bar = document.querySelector('[data-testid="timeline-bar"]');<br>  return {<br>    simTimeSec: rail ? Number(rail.getAttribute('data-sim-time-sec')) : null,<br>    currentTimeSec: bar ? Number(bar.getAttribute('data-current-time-sec')) : null,<br>  };<br>});<br>assert.notEqual(snap.simTimeSec, null, 'walker-results-rail must provide data-sim-time-sec');<br>assert.notEqual(snap.currentTimeSec, null, 'timeline-bar must provide data-current-time-sec');<br>assert.equal(<br>  (snap.simTimeSec as number) - (snap.currentTimeSec as number),<br>  450,<br>  'live timeline window start correctly applies demoStartOffset of 450s (simTimeSec - currentTimeSec === 450)'<br>);</code></pre> | 載入固定 450 秒 offset 的 profile，暫停播放 |
| 30 | `Y6:L665`<br>`simState.simTimeSec - liveTimelineWindowStartSec` | 專用 Live Timeline 驗證腳本（`validate-phase-y6-live-timeline.ts`） | <pre><code class="language-ts">// 一次讀取時間軸秒數、文字與 Walker rail 模擬秒數，避免 torn read<br>const snap = await page.evaluate(() => {<br>  const timeEl = document.querySelector('[data-testid="timeline-current-time"]');<br>  const timelineEl = document.querySelector('[data-testid="timeline-bar"]');<br>  const railEl = document.querySelector('[data-testid="walker-results-rail"]');<br>  return {<br>    displayedText: timeEl?.textContent?.trim(),<br>    currentTimeSec: timelineEl ? Number(timelineEl.getAttribute('data-current-time-sec')) : null,<br>    simTimeSec: railEl ? Number(railEl.getAttribute('data-sim-time-sec')) : null,<br>  };<br>});<br>assert.equal(snap.currentTimeSec, 120, 'timeline currentTimeSec resolves to 120s after seeking');<br>assert.equal(snap.simTimeSec, 570, 'simTimeSec resolves to 570s (120s seek + 450s demoStartOffset)');<br>assert.equal(snap.displayedText, '02:00', 'timeline UI displays elapsed time 02:00 (120s)');</code></pre> | 載入固定 450 秒 offset 的 profile，操作時間軸 seek 至 120 秒後暫停 |
| 31 | `Y6:L666`<br>`demoStartOffsetSec: demoStartOffset` | 專用 Live Timeline 驗證腳本（`validate-phase-y6-live-timeline.ts`） | <pre><code class="language-ts">// 操作 scrubber seek 至 60 秒後原子讀取 seek-target<br>const snap60 = await page.evaluate(() => {<br>  const shell = document.querySelector('.leo-app-shell');<br>  return {<br>    seekTarget: shell ? Number(shell.getAttribute('data-live-timeline-seek-target')) : null,<br>  };<br>});<br>assert.equal(snap60.seekTarget, 510, 'seek target includes demoStartOffsetSec when seeking to 60s (60 + 450 = 510)');<br><br>// 接續操作 scrubber seek 至 120 秒後原子讀取 seek-target<br>const snap120 = await page.evaluate(() => {<br>  const shell = document.querySelector('.leo-app-shell');<br>  return {<br>    seekTarget: shell ? Number(shell.getAttribute('data-live-timeline-seek-target')) : null,<br>  };<br>});<br>assert.equal(snap120.seekTarget, 570, 'seek target includes demoStartOffsetSec when seeking to 120s (120 + 450 = 570)');</code></pre> | 載入固定 450 秒 offset 的 profile，透過 `[data-testid="timeline-scrubber"]` 依序 seek 至 60 秒與 120 秒 |
| 32 | `Y6:L667`<br>`const absoluteTargetSec = Math.min(` | 專用 Live Timeline 驗證腳本（`validate-phase-y6-live-timeline.ts`） | <pre><code class="language-ts">// 點擊 jump-end 按鈕<br>await page.locator('[data-testid="timeline-jump-end"]').click();<br><br>// 原子讀取 seek-target 與 duration-sec 避免 torn read<br>const snap = await page.evaluate(() => {<br>  const shell = document.querySelector('.leo-app-shell');<br>  const timeline = document.querySelector('[data-testid="timeline-bar"]');<br>  return {<br>    seekTarget: shell ? Number(shell.getAttribute('data-live-timeline-seek-target')) : null,<br>    durationSec: timeline ? Number(timeline.getAttribute('data-duration-sec')) : null,<br>  };<br>});<br>assert.equal(snap.seekTarget, 7200, 'seek target is clamped to duration bound of 7200s by Math.min instead of overflowing to 7650s');</code></pre> | 載入固定 450 秒 offset 的 profile（總長度 7200 秒），點擊 `[data-testid="timeline-jump-end"]` |
