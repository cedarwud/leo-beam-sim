# Y3：前端原始碼斷言轉行為斷言提案

本報告以目前 checkout 的 `src/` 為準，只採用實際存在的 DOM selector、canvas `data-*` 屬性與可到達入口。`[data-testid="leo-main-scene"] canvas` 是真實 canvas 入口（`src/scene/MainScene.tsx:5896-5904`）；下表中的 camelCase dataset 名稱均換算為瀏覽器實際的 kebab-case HTML attribute。

## 提案表

| 行號 | 原斷言 | 進入點（URL/路由/前置操作） | 觀察點（真實存在的 selector 或屬性，附 src 檔案:行號） | 斷言內容 | 放哪個 browser validator |
|---|---|---|---|---|---|
| scene-lane L2309 | `visible={(focusBeamLoad?.load ?? 0) > 0}` | 以 `localStorage['leo-beam-sim.app-mode.v1']='modqn-demo'` 進入 `/`，開啟 Advanced setup，選 `explain-handover` 並以 `?modqnServiceAllocation=1` 啟用服務分配。 | 最近的真實訊號是 canvas 的 `data-beam-load-cylinder-rendered`，由實際 `mesh.visible` 寫入（`src/viz/BeamLoadCylinder.tsx:39-55`）；canvas 另有 `data-beam-load-contention-ue-count`（`src/scene/SceneTelemetry.tsx:164-165`）。 | **無法轉換。** 現有 UI 無法確定性地把「焦點 beam 的 load」設成 0／正值，且 DOM 沒有 `focusBeamLoad.load`；全域 contention 數不能證明焦點 beam load。只斷言 rendered flag 會缺少可核對的輸入前置條件。 | —（先補可控制 fixture 與 `data-focus-beam-load`／焦點 beam key，再放 `validate-phase-3-overlay-render-browser.ts`） |
| scene-lane L2385 | `modqnVisualLayerPreset === 'explain-handover'` | 同上，以 MODQN localStorage 進 `/`；開 `[data-testid="advanced-setup-trigger"]`，依序選 service-allocation 與 explain-handover。localStorage key 真實存在於 `src/app/appExperienceMode.ts:3-8,37-42`。 | `[data-testid="modqn-layer-preset-control"][data-modqn-layer-preset]`、`[data-testid="modqn-layer-preset-service-allocation"]`、`[data-testid="modqn-layer-preset-explain-handover"]`（`src/ui/modqn-controls/ModqnAdvancedDisplayControls.tsx:46-65`）；canvas `data-modqn-visual-layer-preset`（`src/scene/SceneTelemetry.tsx:212-216`）及 renderer-derived `data-upload-particle-rendered-count`（`src/viz/BeamLoadUploadParticles.tsx:104-143`）。 | **可轉換。** 在 service-allocation 時先斷言 control/canvas preset 都是 `service-allocation` 且 upload particle attr 不存在或為 0；切 explain-handover 後，斷言兩處 preset 都更新，並在 producer gate 開啟時等待 `Number(data-upload-particle-rendered-count) > 0`。必須保留負控制，才能證明粒子是由該 preset 啟動。 | 擴充 `scripts/validate-phase-3-overlay-render-browser.ts` |
| scene-lane L2396 | `beamConeScope: 'focus-satellite'` | 與 L2385 相同。 | 一般 CellBeamCones 的 canvas `data-cell-beam-cone-scope` 由 `SceneTelemetry` 寫入（`src/scene/SceneTelemetry.tsx:180-182`；傳值在 `src/scene/MainScene.tsx:4982-4984`）；upload particles 只發布總數 `data-upload-particle-rendered-count`（`src/viz/BeamLoadUploadParticles.tsx:133-143`）。 | **無法轉換。** `data-cell-beam-cone-scope` 描述一般 cone layer，不是 `uploadParticleFocusCones`；粒子沒有 satellite IDs／scope telemetry。用前者代替會在粒子錯誤擴散到多衛星時仍假綠。 | —（先由粒子元件發布實際有 instances 的 satellite IDs 或 scope，再放 `validate-phase-3-overlay-render-browser.ts`） |
| scene-lane L2401 | `<BeamLoadUploadParticles` | MODQN localStorage → `/?sceneSource=live-sim&modqnServiceAllocation=1` → Advanced setup → explain-handover。 | canvas `data-upload-particle-rendered-count` 是由實際 `InstancedMesh.visible/count` 加總後寫入（`src/viz/BeamLoadUploadParticles.tsx:104-143`）；掛載條件與元件在 `src/scene/MainScene.tsx:5148-5155`。 | **已被覆蓋。** 現有 browser gate 等待並斷言 renderer-derived particle count 大於 0，已非原始碼形狀檢查。原 validator 的這條字串斷言可直接刪除。 | 已在 `scripts/validate-phase-3-overlay-render-browser.ts` |
| scene-lane L2686 | `sceneFrame?.sceneSource === 'artifact-replay'` | 目前沒有穩定可到達的 App artifact 路由。`/`、`/legacy`、`/walker` 都強制初始化為 live（`src/App.tsx:393-409`）；catch-all 才讀 URL 不是可依賴的產品入口（`src/main.tsx:294-299`）。 | 若能進入，真實觀察點是 canvas `data-scene-source`（`src/scene/SceneTelemetry.tsx:168`）與 shell `data-artifact-source`（`src/App.tsx:3953-3955`）。 | **無法轉換。** selector 存在但前置入口不存在；既有 `/?sceneSource=artifact-replay` 不會令 `/` 進 artifact。建議維持 B 但標記「blocked by entry」，待 owner 提供命名路由或 dev-only bootstrap 後，斷言 canvas source=`artifact-replay` 且 shell artifact source 非 `pending`。 | —（入口恢復後放 `scripts/validate-phase-c-artifact-scene-real-data-browser.ts`） |
| scene-lane L2691 | `<ArtifactSceneContent` | 同 L2686：目前無穩定 artifact 入口。 | Artifact composer 若掛載會輸出 canvas `data-handover-story-layer="artifact-owned"`、`data-live-simulation-enabled="0"`、`data-scene-source="artifact-replay"`（`src/scene/MainScene.tsx:997-1085`）；分支本身在 `src/scene/MainScene.tsx:6008-6017`。 | **無法轉換。** 有非空 runtime signature，但不可到達；不得用目前無效的 query string 製造表面測試。入口恢復後應同時斷言三個 signature，避免只驗到共享 canvas。 | —（入口恢復後放 `scripts/validate-phase-c-artifact-scene-real-data-browser.ts`） |
| scene-lane L2696 | `liveSimulationEnabled="0"` | 同 L2686。 | canvas `data-live-simulation-enabled` 真實由 `SceneTelemetry` 寫入（`src/scene/SceneTelemetry.tsx:154`），Artifact composer 明確傳 `"0"`（`src/scene/MainScene.tsx:1013-1019`）。 | **無法轉換。** 觀察點存在但 artifact 前置狀態不可從目前公開 UI／命名路由建立。入口恢復後應與 `data-scene-source="artifact-replay"` 原子取樣並斷言為 `0`，不可只驗預設值。 | —（入口恢復後放 `scripts/validate-phase-c-artifact-scene-real-data-browser.ts`） |
| scene-lane L2701 | `liveSimulationEnabled={simSource === 'live' ? '1' : '0'}` | `/` 可測 Walker live；但 homepage source switch 被固定隱藏且 resolver 永遠回 Walker（`src/app/simulationSourceMode.ts:9-15,41-45`；掛載 gate 在 `src/App.tsx:4040-4044`），無法進 archived TLE 做另一半。 | canvas `data-live-simulation-enabled`（`src/scene/SceneTelemetry.tsx:154`；live 傳值在 `src/scene/MainScene.tsx:4937-4945`）；TLE 中心 `[data-testid="homepage-tle-center"][data-scene-source="archived-tle"]` 雖存在於 source（`src/scene/MainScene.tsx:5944-5965`）但目前不可到達。 | **無法轉換。** 只能斷言 live=`1` 會漏掉條件式的 `else` 行為。建議改判為「被產品入口凍結的 dead branch／V」或等 source switch 有 owner 授權重新開放後再做雙狀態斷言；不可自行選 Walker/TLE 權威。 | —（若入口恢復，放 `scripts/validate-tle-journey-browser.ts`） |
| scene-lane L2753 | `{presentationPlan.visible['serving-footprints'] && showSinrLiveCellBeams ...}` | `/`，等待候選場景 active；可用真實 `[data-testid="director-intra-focus"]`／`[data-testid="director-inter-focus"]` 觸發 handover navigation（`src/ui/SinrLiveQuickControls.tsx:167-192`）。 | canvas `data-scene-presentation-visible-layers`（`src/scene/MainScene.tsx:359-371`）及 mesh-derived `data-sinr-live-cell-footprint-ring-rendered-count`；後者由元件 traverse 實際 visible item 寫入（`src/viz/SinrLiveCellFootprintRings.tsx:103-125`），caller key 在 `src/scene/MainScene.tsx:5515-5540`。 | **可轉換。** 在同一個 active multi-candidate sample 中斷言 visible-layers 包含 `serving-footprints`，且 `Number(data-sinr-live-cell-footprint-ring-rendered-count) > 0`；跨一次 intra/inter handover 前後都成立。這會抓到 candidate authority 誤清掉 serving footprints。 | 擴充 `scripts/validate-homepage-multi-candidate-acceptance-browser.ts` |
| scene-lane L2775 | `<DecisionHandoverCue` | `/`，以 handover navigation 取得 decision transition。 | 最近的模型訊號是 canvas `data-multi-candidate-event-cue-count`（`src/scene/SceneTelemetry.tsx:188-194`）；元件本身只有 R3F `<group name=... userData=...>`，沒有 DOM/canvas renderer receipt（`src/viz/DecisionHandoverCue.tsx:93-140`）。 | **無法轉換。** DOM 不能 query R3F group；event cue count 在 caller 計算，不能證明 `<DecisionHandoverCue>` 畫出來。建議元件以 ref traverse 寫 renderer-derived `data-decision-handover-cue-rendered-count`，含 unmount cleanup，之後再斷言 transition 時 `>0`、非 transition 時 `0`／不存在。 | —（補訊號後放 `scripts/validate-homepage-multi-candidate-acceptance-browser.ts`） |
| scene-lane L2816 | `multiCandidateSceneRenderPlan?.telemetry.renderedPairCount` | `/`，等待 `data-multi-candidate-scene-render-status="active"` 的原子 sample。 | caller projection `data-multi-candidate-rendered-pair-count`（`src/scene/SceneTelemetry.tsx:188-194`）；renderer layer 的 `data-multi-candidate-scene-rendered-pair-count`、`data-multi-candidate-scene-central-rendered-pair-count`、`data-multi-candidate-scene-rendered-scene-join-keys`（key 定義 `src/viz/MultiCandidateBeamScene.tsx:273-288`，寫入 `src/viz/MultiCandidateBeamScene.tsx:2137-2170`）。 | **可轉換。** 在同一 active sample 解析 join-key JSON，斷言 caller pair count = scene pair count = join-key 數，並斷言 central rendered pair count `>0` 且 `<=` scene pair count。如此同時守 projection 與實際中央顯示密度 gate。 | 擴充 `scripts/validate-homepage-multi-candidate-acceptance-browser.ts` 的 `assertCandidateSceneRailJoin` |
| scene-lane L2821 | `multiCandidateSceneRenderPlan?.solidDataLinkCount` | `/`，等待 active multi-candidate comparison sample。 | canvas renderer receipt `data-multi-candidate-scene-solid-data-link-count`（`src/viz/MultiCandidateBeamScene.tsx:273-280,2154-2157`）；SceneTelemetry projection `data-multi-candidate-scene-global-solid-data-link-count`（`src/scene/SceneTelemetry.tsx:190-192`）。 | **已被覆蓋。** 現有 browser gate 已在同一 sample 斷言 canvas solid link 恰為 1，並與 rail 及中央 scene receipt 的 1 對齊。原字串斷言可直接刪除。 | 已在 `scripts/validate-homepage-multi-candidate-acceptance-browser.ts` |
| scene-lane L2826 | `multiCandidateCentralOverlayActive && !multiCandidateServingCarrierRenderable` | `/`，理論上需建立「central overlay active 且 serving instruction 的 cone/footprint/link 全不可見」狀態。 | 唯一直接訊號是 canvas `data-multi-candidate-carrier-fallback-active`（`src/scene/SceneTelemetry.tsx:188-194`），其值仍由同一條 caller 布林式傳入（`src/scene/MainScene.tsx:4990-5003`）；serving renderable 定義在 `src/scene/MainScene.tsx:2266-2273`。 | **無法轉換。** UI 沒有確定性操作可建立該負空間，且沒有 fallback mesh-derived receipt；斷言同一布林式投影出的 attr 只是把原始碼 pin 換位置。建議補可注入的 accepted snapshot fixture，以及 renderer-derived `data-serving-carrier-fallback-rendered-count`，再做正負控制。 | —（補 fixture/receipt 後放 `scripts/validate-homepage-multi-candidate-acceptance-browser.ts`） |
| scene-lane L2847 | `enabled: simSource === 'live' && sceneFrame.sceneSource !== 'artifact-replay'` | 需在同一 browser session 從 live 切 artifact；目前 artifact 無穩定入口。 | canvas `data-live-simulation-enabled` 存在（`src/scene/SceneTelemetry.tsx:154`），但 `useSimStatePublisher` 的 enabled gate 沒有 emission count／event／network receipt（hook call 在 `src/scene/MainScene.tsx:1817-1837`）。 | **無法轉換。** `data-live-simulation-enabled="0"` 只是 scene semantic flag，不能證明 publisher 沒有呼叫 `onSimUpdate`。建議 publisher 發布單調 sequence/count 或可監聽事件，測試先證明 live count 增加，再切 artifact 並證明 count 停止；也須先有穩定 artifact 入口。 | —（補入口與 publisher receipt 後放 `scripts/validate-phase-c-artifact-scene-real-data-browser.ts`） |
| scene-lane L3134 | `showDecisionPolicyControls={sceneLane === 'modqn-live-cell-preview'}` | 兩個獨立 context：預設 `/`（SINR）；另一個先設 `localStorage['leo-beam-sim.app-mode.v1']='modqn-demo'` 再進 `/`（MODQN live），各自打開 drawer。 | shell `[data-scene-lane]`（`src/App.tsx:3937-3940`）；drawer trigger/drawer（`src/ui/AdvancedDrawerShell.tsx:83-100,117-130`）；**實際 selector 是單數** `[data-testid="modqn-decision-policy-control"]`，並有 `data-modqn-decision-policy`（`src/ui/modqn-controls/ModqnAdvancedDisplayControls.tsx:74-105`）。 | **可轉換。** SINR context 斷言 policy control count=0；MODQN context 先斷言 shell lane=`modqn-live-cell-preview`，開 drawer 後斷言 count=1 且初值=`decision-overlay-on-live-sinr`。不可使用不存在的 `[data-testid="decision-policy-controls"]`。 | 修正並重用 `scripts/validate-phase-c-lane-experience-bar-browser.ts`（以 localStorage bootstrap 取代已失效 lane bar） |
| scene-lane L3154 | `handoverMode === 'omega-heuristic' && sceneLane === 'modqn-live-cell-preview' && <HeuristicNotPaperBanner />` | MODQN localStorage → `/` → Advanced setup → click `[data-testid="modqn-decision-policy-heuristic"]`；再 click overlay 作負控制。 | heuristic/overlay buttons 與 policy attr（`src/ui/modqn-controls/ModqnAdvancedDisplayControls.tsx:74-105`）；`[data-testid="heuristic-not-paper-banner"]`（`src/ui/HeuristicNotPaperBanner.tsx:92-111`）；App mount condition（`src/App.tsx:4334`）。 | **可轉換。** 點 heuristic 後斷言 policy attr=`omega-heuristic`、按鈕 `aria-pressed=true`、banner visible 且 role=alert；點 overlay 後斷言 policy 恢復 canonical、banner 消失。 | 修正並重用 `scripts/validate-phase-c-lane-experience-bar-browser.ts` |
| scene-lane L3159 | `if (targetLane === 'artifact-replay') { if (handoverMode === 'omega-heuristic') ... }` | 理論前置為 live MODQN 選 heuristic 後切 artifact；目前 lane bar／MODQN view toggle 未在 App 掛載，artifact 也無穩定入口。 | heuristic banner selector 真實存在（`src/ui/HeuristicNotPaperBanner.tsx:92-111`），artifact sidebar 亦存在（`src/App.tsx:4173-4187`）；但 artifact surface 沒有精確發布目前 `handoverMode`。 | **無法轉換。** 僅斷言 banner 消失是可空斷言，無法證明 policy 真被正規化；既有 lane browser 使用的切換 selector 現在不可到達。建議提供正式 lane transition 入口，並在 shell/control 發布精確 policy 後，斷言 artifact 進入前後由 `omega-heuristic` 變為 `decision-overlay-on-live-sinr`。 | —（入口恢復後放 `scripts/validate-phase-c-lane-experience-bar-browser.ts`） |
| scene-lane L3236 | `<AdvancedSetupDrawer` | 先設 MODQN localStorage，進 `/`，點 `[data-testid="advanced-setup-trigger"]`。 | `[data-testid="advanced-setup"]`、`[data-testid="advanced-setup-trigger"]`、`[data-testid="advanced-setup-drawer"]` 由 prefix 實際產生（`src/ui/AdvancedDrawerShell.tsx:83-100,117-130`；prefix 在 `src/ui/AdvancedSetupDrawer.tsx:57-69`）。 | **可轉換。** 修復該 gate 的進入方式後，點 trigger，斷言 drawer visible、role=`region`、bounding box 寬高皆正值，且 `.leo-advanced-setup-overlay` count=0；再點 trigger 關閉並斷言 detached。現有 gate 的 drawer assertions 可保留，但它目前先點不存在的 lane selector，故尚不能算覆蓋。 | 修正 `scripts/validate-frontend-advanced-drawer-modality-browser.ts` 的 MODQN 入口 |
| scene-lane L3244 | `showDecisionPolicyControls={sceneLane === 'modqn-live-cell-preview'}` | 與 L3134 相同；本條是 drawer 內相同 prop 的重複 source pin。 | `[data-testid="advanced-setup-drawer"]`（`src/ui/AdvancedDrawerShell.tsx:117-130`）內的 `[data-testid="modqn-decision-policy-control"]`（`src/ui/modqn-controls/ModqnAdvancedDisplayControls.tsx:74-105`）；App 傳值在 `src/App.tsx:4217-4226`。 | **可轉換。** 與 L3134 共用一個行為案例即可：live MODQN drawer 內存在且僅存在一個 policy control；SINR 不掛 drawer/policy。完成後兩條重複字串斷言都刪除，不需要複製兩個 browser case。 | 修正並重用 `scripts/validate-phase-c-lane-experience-bar-browser.ts` |
| phase-f L257 | `runtime.ueDistributionMode` | MODQN localStorage → `/` → Topology 的 advanced details → 切換 distribution radio。 | `[data-testid="topology-tab-advanced"]`、`[data-testid="topology-tab-ue-distribution-radio"]`、三個 option testid（`src/ui/signal-tuning/TopologyTab.tsx:657-714`）；canvas 唯一位置訊號是 `data-first-ue-position`（`src/scene/SceneTelemetry.tsx:158`）。 | **無法轉換。** UI 明示 distribution 只影響 secondary UEs、primary 固定（`src/ui/signal-tuning/TopologyTab.tsx:665-674`），而 canvas 只發布 first/primary UE；因此切 random/grid/clustered 後沒有可核對的 secondary distribution runtime 訊號。建議發布 effective mode 加 secondary UE position checksum／sample，並斷言 radio、mode、checksum 三者一起變化。 | —（補 telemetry 後新增或放入 phase-F browser gate） |
| phase-g L227 | `runtime.ueMobilityParams` | 沒有前置操作：TopologyTab 的 mobility controls 已移除。 | source 明確說 controls 不再讀寫 mobility，學生無法從此處觸及（`src/ui/signal-tuning/TopologyTab.tsx:331-335,555-577`）；canvas 只有 primary 的 `data-first-ue-position`（`src/scene/SceneTelemetry.tsx:158`），沒有 secondary UE trajectory。 | **無法轉換。** 目前既無 UI 控制也無足以辨別 speed/waypoint/grid spacing 的 runtime 訊號。建議此 integration source pin 改判 V 並刪除；mobility engine 行為保留現有單元測試。若未來重開 UI，再加入 secondary UE IDs/position samples 與 sim-time 的步進斷言。 | —（目前不應新增 browser gate） |

## 無法轉換的

- **scene-lane L2309**：renderer flag 存在，但焦點 beam load 沒有可控制輸入或獨立 telemetry；全域 contention 不是等價前置。建議仍判 B，但先補 fixture 與 focus-load telemetry。
- **scene-lane L2396**：現有 scope attr 屬於一般 CellBeamCones，不屬於 upload particles；用它會產生跨元件假綠。建議仍判 B，先補粒子實際 satellite IDs/scope receipt。
- **scene-lane L2686、L2691、L2696**：artifact runtime signatures 真實存在，但目前 App 的正式入口不可到達。建議三條維持 B/blocked；由 owner 提供命名路由或 dev bootstrap 後再轉。
- **scene-lane L2701**：只能測 live 分支，archived TLE 被 homepage authority 明確關閉。建議依 owner 決定改判 V/dead branch 或在入口恢復後測雙分支；本報告不選 cellId/beamId 或 Walker/TLE 權威。
- **scene-lane L2775**：R3F cue 沒有 renderer-derived DOM/canvas receipt，caller event count 不能證明 cue 畫出。建議維持 B，先補元件級 render count。
- **scene-lane L2826**：condition attr 是原布林式的直接投影，且 browser 無法穩定建立 serving carrier 不可渲染狀態；缺少 fallback mesh receipt。建議維持 B，補 fixture 與 renderer receipt。
- **scene-lane L2847**：沒有 publisher emission receipt，scene 的 live-enabled attr 不能證明沒有 `onSimUpdate`。建議維持 B，補 sequence/count 且先恢復 artifact transition。
- **scene-lane L3159**：artifact transition 無入口，artifact surface 也未發布精確 policy；只驗 banner 消失是可空斷言。建議維持 B/blocked。
- **phase-f L257**：真實 radio 存在，但只影響 secondary UEs；現有 canvas 只發布固定 primary UE 位置。建議維持 B，補 secondary distribution checksum/sample。
- **phase-g L227**：UI controls 已刻意移除且沒有 secondary trajectory telemetry。建議將 App integration source pin 改判 V；若未來重開 UI，再建立 browser 行為 gate。

## 已被覆蓋的

- **scene-lane L2401 `<BeamLoadUploadParticles`**：`scripts/validate-phase-3-overlay-render-browser.ts:155-180` 在真實 MODQN live scene 等待 `data-upload-particle-rendered-count > 0`，而該值來自實際 `InstancedMesh.visible/count`；這是非可空的 renderer 行為覆蓋。
- **scene-lane L2821 `solidDataLinkCount`**：`scripts/validate-homepage-multi-candidate-acceptance-browser.ts:389-442` 要求 active candidate scene、至少兩個 scene join keys，並同時斷言 canvas、rail、central scene 的 solid data link count 都恰為 1。

不列入「已被覆蓋」的既有 gate：

- `scripts/validate-phase-c-lane-experience-bar-browser.ts:31-67,77-95` 依賴 `[data-testid="lane-experience-bar"]`、`lane-experience-*`、`modqn-view-*`；這些元件目前未在 `src/App.tsx` 掛載，因此後續 policy/banner/artifact assertions 不構成目前覆蓋。
- `scripts/validate-frontend-advanced-drawer-modality-browser.ts:45-52` 同樣先點未掛載的 `lane-experience-modqn-live-cell-preview`；雖然後段 `:51-85` 的 drawer assertions 合理，現況會在到達它們以前失敗。
- Phase-F 現有單元 validator 守的是 distribution generator；Phase-G 現有單元 validator 守的是 runtime frame-step mobility。兩者都未守住本清單所釘的 App → runtime 整合線，因此不能把 L257/L227 算作已覆蓋。

## 統計

可轉換 **7** ／ 無法轉換 **12** ／ 已被覆蓋 **2**（總數 **21**）。
