已完成 FIX：

- 8 條紅測試改為行為斷言，未 re-pin 到新模組 source literal。
- `renderReceipt` 依審查刪除。
- 整合測試：12/12 pass。
- 兩個 `tsc` 設定：exit 0。
- `git diff --check`：exit 0。
- 未執行 browser、`check:ee`、`check:visual` 或 git 寫入指令。
- 保留其他 shared checkout WIP。

報告：[FIX.md](/home/u24/demo/leo-beam-sim/docs/sdd/worker-reports/FIX.md)

