# Y5 — App / Scene 文字 pin 的行為斷言提案

本報告只做當前 checkout 的靜態查證；依工作限制未執行任何 `*:browser` validator。

有一個會影響所有 artifact 提案的必要前提：目前 `src/App.tsx:398-409` 在 `/` 與 `/legacy` 強制把 `sceneSource` 初始化為 `live-sim`，因此既有 browser gate 使用的 `/?sceneSource=artifact-replay` 到不了 artifact lane。可執行的入口應改成 dev server 的 `/index.html?sceneSource=artifact-replay`：`/index.html` 不屬於 `src/main.tsx:16-68` 的特殊 route，會由 `src/main.tsx:294-299` fallback 掛載真實 `App`，而 `App` 會在非 legacy pathname 呼叫 `readSceneSourceFromUrl()`。以下所有 artifact 提案都使用這個已存在的 dev-server HTML 入口，不使用不存在的 lane switch，也不以 `/walker` 的 `AppWalkerSandbox` 冒充 `App` 覆蓋。

## 提案表

| 行號 | 原斷言 | 進入點（URL/路由/前置操作） | 觀察點（真實存在的 selector 或屬性，附 src 檔案:行號） | 斷言內容 | 放哪個 browser validator |
|---:|---|---|---|---|---|
| L935 | `artifactSource === SYNTHETIC_FIXTURE_SOURCE` | 不需新增 browser 情境；純決策函式已有完整輸入案例。 | `ArtifactSourceBadge` 的 `[data-testid="artifact-source-badge"]`、`[role="alert"]` 與 `data-artifact-source` 實際存在於 `src/ui/ArtifactSourceBadge.tsx:83-107`。 | **已被覆蓋**：synthetic、header-absent、任意未驗證來源的文案分流已由單元測試精確驗證；此 source pin 可直接刪除。 | 不新增；既有 `src/ui/ArtifactSourceBadge.test.ts:51-76`。 |
| L955 | `<ArtifactSourceBadge source={showcaseArtifactSource} />` | 在 `validate-phase-d-dashboard-browser.ts` 將既有 fixture route response 加上 `X-Showcase-Artifact-Source: synthetic-fixture-fallback`，再進 `/index.html?sceneSource=artifact-replay`。 | `[data-testid="artifact-source-badge"]`、`[role="alert"]`、`data-artifact-source`：`src/ui/ArtifactSourceBadge.tsx:87-107`。 | 等待 artifact sidebar 載入後，斷言 badge `count() === 1`、可見、`role=alert`，且 `data-artifact-source === 'synthetic-fixture-fallback'`。這會直接證明 artifact lane 有掛 honesty badge，而非只證明 decision helper 正確。 | `scripts/validate-phase-d-dashboard-browser.ts` |
| L960 | `data-artifact-source={` | 與 L955 同一個 mocked synthetic fixture 情境。 | `.leo-app-shell[data-artifact-source]`：屬性在 `src/App.tsx:3937-3955`，class 在 `src/App.tsx:4007`；badge 自身另有同名屬性於 `src/ui/ArtifactSourceBadge.tsx:91`。 | 斷言 shell 的 `data-artifact-source === 'synthetic-fixture-fallback'`，並與 badge 的同名屬性相等；不可只斷言非空字串。 | `scripts/validate-phase-d-dashboard-browser.ts` |
| L988 | `data-director-phase={camera.directorPhase}` | 不需新增；真實 live SINR Director 流程已有 browser gate。 | `.leo-app-shell[data-director-phase]`：`src/App.tsx:3937-3956,4007`；Director control 也有 `data-director-phase`：`src/ui/DirectorControls.tsx:66`。 | **已被覆蓋**：既有 gate 斷言初始 `idle`、操作後進入 `acquiring/focused/restoring`、退出後回 `idle`。source pin 可直接刪除。 | 不新增；`scripts/validate-phase-c-director-cinematic-live-browser.ts:91,141-148,187-202`（另有 `validate-phase-c-handover-cinema-browser.ts:111,163-170,232-260`）。 |
| L993 | `data-effective-speed={playback.effectiveSpeed...}` | 不需新增；首頁與 Director browser gate 都會操作實際控制。 | `.leo-app-shell[data-effective-speed]`：`src/App.tsx:3937-3957,4007`；速度按鈕 `[data-testid="timeline-speed-${preset}x"]`：`src/ui/TimelineBar.tsx:301-316`。 | **已被覆蓋**：首頁點 5x 後同時等待按鈕 active 與 shell effective speed；Director gate 另驗證進入 0.25x 及退出恢復。 | 不新增；`scripts/validate-homepage-authority-browser.ts:121-135`、`scripts/validate-phase-c-director-cinematic-live-browser.ts:93-97,150-157,187-202`。 |
| L1047 | `sceneLane === 'modqn-live-cell-preview' ? 'overlay-demo' : 'live-truth'` | **無法轉換**：目前真實 `App` 沒有可達的 `modqn-live-cell-preview` 入口。`/` 與 `/legacy` 強制 SINR；`LaneExperienceBar` / `ModqnViewToggle` 元件存在但 `src/App.tsx` 沒有 import 或 JSX 掛載。 | 候選訊號 `.leo-app-shell[data-live-director-focus-claim]` 真實存在於 `src/App.tsx:3958,4007`，但可達的 `App` 只能觀察 `live-truth`，不能觀察 ternary 的 `overlay-demo` 分支。 | 不能用只驗 `live-truth` 的測試冒充兩分支映射。需先由 owner 恢復合法 MODQN 入口，才可各進 SINR / MODQN 並斷言 `live-truth` / `overlay-demo`；目前建議改判 V（不可達 dormant branch），不是 B。 | 無 |
| L1091 | `data-live-director-focus-claim={directorFocusEnabled ? ...}` | 不需新增；真實 live SINR Director 流程已有 browser gate。 | `.leo-app-shell[data-live-director-focus-claim]`：`src/App.tsx:3958,4007`。 | **已被覆蓋**：既有 gate 在 Director 操作前即斷言值為 `live-truth`，會抓到 telemetry 被移除或錯標。 | 不新增；`scripts/validate-phase-c-director-cinematic-live-browser.ts:73-80`（另有 `validate-phase-c-handover-cinema-browser.ts:87-100`）。 |
| L1096 | `data-live-director-focus-event-sec={liveDirectorFocusEventS...}` | 不需新增；真實 live SINR Director focus 已會選到 source-backed event。 | `.leo-app-shell[data-live-director-focus-event-sec]`：`src/App.tsx:3961,4007`。 | **已被覆蓋**：既有 gate 讀出 finite event second，並斷言 seek target 等於 event second 減 lead-in；另一 gate還比對 explainer 的 `data-source-time-sec`。 | 不新增；`scripts/validate-phase-c-director-cinematic-live-browser.ts:116-139`、`scripts/validate-phase-c-handover-cinema-browser.ts:130-137`。 |
| L1390 | `proofViewportActive={sceneLane === 'modqn-replay-proof'}` | **無法轉換**：`ModqnReplayCuePanel` 只在 `appMode='modqn-demo'` 渲染，但目前真實 `App` 沒有進入該 app mode 的 runtime 入口。 | 候選 `[data-testid="modqn-replay-proof-viewport-toggle"][aria-pressed]` 真實存在於 `src/ui/ModqnReplayCuePanel.tsx:125-138`，但在目前 `App` 路徑不可達。 | 既有 `validate-live-walker-handover-event-focus-browser.ts:250-256` 雖寫了 click，但它從根 URL + localStorage 進場；`App` 於 `src/App.tsx:461-485` 將首頁初始 mode 重設為 SINR，因此不能算有效覆蓋。需先恢復合法 MODQN 入口，再斷言點擊後 `aria-pressed='true'` 且 shell lane 為 proof。建議暫改判 V。 | 無 |
| L1391 | `canToggleModqnReplayProof ? setModqnReplayProofRequested : undefined` | **無法轉換**：同 L1390；目前沒有一個真實 `App` 畫面能同時提供 eligible / ineligible 的 cue toggle 情境。 | 候選 toggle 的 `disabled`、`aria-label`、`aria-pressed` 真實存在於 `src/ui/ModqnReplayCuePanel.tsx:121-138`。 | 只有恢復入口後才能測：eligible 時按鈕 enabled 且 click 進 proof；ineligible 時 disabled 且 lane 不變。現況用獨立 `/walker` 複本測只會守到 `AppWalkerSandbox`，不能替代 `App`。建議暫改判 V。 | 無 |
| L1392 | `getLeftSidebarTabsForSceneLane(sceneLane, handoverMode)` | **無法完整轉換**：artifact 分支可由 `/index.html?sceneSource=artifact-replay` 觀察，但 MODQN live/proof 與 handover-mode 變體在真實 `App` 不可達。 | Sidebar shell 的 `[data-sidebar-side="left"]`、`data-tab-list-visible`、`data-tab-count`、`[role="tabpanel"]` 真實存在於 `src/ui/SidebarTabShell.tsx:59-68,93-101`；tab model 在 `src/app/appRuntimeModel.ts:148-162`。 | 不能只測 artifact 的單一 Evidence rail 就宣稱完整守住 lane/mode mapping。建議將純 mapping 改判 S，於 `src/app/appRuntimeModel.test.ts` 對所有 lane/mode table-drive；待 MODQN 入口恢復再補 browser smoke。 | 無 |
| L1393 | `getRightSidebarTabsForSceneLane(sceneLane, handoverMode)` | **無法完整轉換**：非根 `/index.html?sceneSource=live-sim` 可見 live 單一 rail，artifact 可見 artifact 單一 rail，但 MODQN 的雙 tab / proof override 在真實 `App` 不可達。 | `[data-sidebar-side="right"]`、`data-tab-list-visible`、`data-tab-count`、`[role="tab"]`：`src/ui/SidebarTabShell.tsx:59-100`；model 在 `src/app/appRuntimeModel.ts:190-227`。 | 兩個可達分支不足以守完整 lane/mode mapping。建議改判 S 並在 `src/app/appRuntimeModel.test.ts` 全列舉；恢復 MODQN runtime 入口後才加 browser tab 清單斷言。 | 無 |
| L1394 | `activeLeftSidebarTab === 'evidence'` | 在 `validate-phase-d-dashboard-browser.ts` 修正入口為 `/index.html?sceneSource=artifact-replay`，route-fulfill 合法 fixture。artifact lane 會自動落在唯一的 Evidence / Replay rail，不需點擊不存在的單 tab button。 | `[data-sidebar-side="left"] [role="tabpanel"]`：`src/ui/SidebarTabShell.tsx:60-68,93-101`；其內 `[data-testid="artifact-replay-sidebar"]`：`src/App.tsx:4173-4189`。 | 斷言 left shell `data-tab-count='1'`、`data-tab-list-visible='false'`、tabpanel `aria-label='Evidence / Replay'`，且其中恰有一個 artifact replay sidebar。 | `scripts/validate-phase-d-dashboard-browser.ts` |
| L1395 | `data-testid="artifact-replay-sidebar"` | 與 L1394 相同，使用修正後的 `/index.html?sceneSource=artifact-replay`。 | `[data-testid="artifact-replay-sidebar"]` 及 `data-artifact-loaded/loading/frame-index`：`src/App.tsx:4173-4188`。 | route-fulfill 後等待 `data-artifact-loaded='true'`；斷言 count 1、`data-artifact-loading='false'`、`data-artifact-frame-index` 是可解析的非負整數。現有 artifact browser gates 因錯誤根入口不可達，故不列「已被覆蓋」。 | `scripts/validate-phase-d-dashboard-browser.ts` |
| L1399 | `activeRightSidebarTab === 'artifact'` | 與 L1394 相同。 | `[data-testid="artifact-truth-sidebar"]`：`src/App.tsx:4477-4506`；外層 `[data-sidebar-side="right"] [role="tabpanel"]`：`src/ui/SidebarTabShell.tsx:60-68,93-101`。 | 斷言 right shell 的唯一 panel `aria-label='Artifact truth'`，其中 `[data-testid="artifact-truth-sidebar"]` count 1 且 `data-artifact-loaded='true'`；這直接守 artifact active branch。 | `scripts/validate-phase-d-dashboard-browser.ts` |
| L1403 | `!recordedReplayActive || activeSceneFrame !== undefined` | 在 `validate-phase-c-artifact-fail-closed-browser.ts` 保留 404 route mock，但把入口修正為 `/index.html?sceneSource=artifact-replay`。 | fail-closed `[data-testid="artifact-scene-fail-closed"]`：`src/App.tsx:4385-4393`；成功場景 `[data-testid="leo-main-scene"]`：`src/scene/MainScene.tsx:5896-5904`。 | 404 時 fail-closed count 1、main scene count 0；另在有效 fixture 情境反向斷言 fail-closed count 0、main scene count 1。必須有正反兩態，才守到 OR gate，而不是只守 placeholder。 | `scripts/validate-phase-c-artifact-fail-closed-browser.ts` |
| L1404 | `data-testid="artifact-scene-fail-closed"` | 與 L1403 的 404 情境相同。 | `[data-testid="artifact-scene-fail-closed"]`、`data-scene-lane`、`data-artifact-loading`：`src/App.tsx:4385-4393`。 | 斷言 placeholder 可見、count 1、`data-scene-lane='artifact-replay'`、inner text 非空；並斷言 `[data-testid="leo-main-scene"]` 不存在。現有 gate 的 assertions 可保留，只需修正入口。 | `scripts/validate-phase-c-artifact-fail-closed-browser.ts` |
| L1405 | `if (sceneSource === 'artifact-replay' \|\| appMode !== 'modqn-demo') return;` | 兩個 page/context：A. `/index.html?sceneSource=artifact-replay`；B. `/` 的預設 SINR。goto 前註冊 `page.on('request')`，等待 shell 與主要 lane surface 穩定後取樣。 | MODQN startup fetch 的實際 prefix `/modqn-bundles` 在 `src/modqn/replay-bundle/runtime-fetch.ts:32,64-76`，必需 surfaces 在 `src/modqn/replay-bundle/runtime-fetch.ts:140-148`；shell lane/mode 屬性在 `src/App.tsx:3937-3940`。 | 兩個情境都斷言 request pathname 不得以 `/modqn-bundles/` 開頭；A 同時斷言 shell lane `artifact-replay`，B 斷言 `data-app-mode='sinr-experiment'`。不要把合法 `/showcase-artifacts/visual-showcase-v1.json` 誤列為 MODQN startup fetch。 | A 放 `scripts/validate-phase-c-artifact-fail-closed-browser.ts`；B 放 `scripts/validate-homepage-authority-browser.ts`。 |
| L1413 | `sceneLane === 'modqn-live-cell-preview' && <ServiceStatusBanner appMode={appMode} />` | **無法轉換**：真正 banner 還要求 app mode 為 MODQN、localStorage 已配置 training URL、health probe 回 unreachable；第一個條件目前在真實 `App` 不可達。 | 候選 `[data-testid="service-status-banner-unreachable"]` 與 retry：`src/ui/modqn-training/ServiceStatusBanner.tsx:37-53`；服務 URL key 與 opt-in 判定：`src/modqn/training-trigger/baseUrl.ts:1-23`。 | 恢復合法 MODQN entry 後，才可 seed `leo-beam-sim.training-service.base-url.v1`、mock `/health` unreachable，斷言 MODQN live 出現 banner、artifact/SINR 不出現。現況不可用 `/walker` 複本宣稱守到 `App`；建議暫改判 V。 | 無 |
| L1469 | `axisPlaying={!playback.paused}` | **無法轉換**：目前 `App` 唯一會掛 `HandoverEventRail` 的可達 branch 是 artifact right rail（`src/App.tsx:4477-4490`），其 axis 固定為 `source-time`（`src/app/timelineRailAuthority.ts:231-256`）。 | 候選 `[data-testid="handover-event-rail"][data-axis-playing]` 真實存在於 `src/ui/HandoverEventRail.tsx:372-397`，但該屬性是 `axisKind === 'display-stretched' && axisPlaying` 的結果（`src/ui/HandoverEventRail.tsx:333-340`），artifact 情境不論播放/暫停都為 `false`。 | 因沒有 raw `axisPlaying` runtime 訊號，也沒有可達的 display-stretched proof rail，不能用永遠 `false` 的屬性製造假綠。需先恢復 proof rail runtime 入口或新增經 owner 核准的中性 input telemetry；目前建議改判 V。 | 無 |
| L1470 | `axisPlaybackRate={playback.effectiveSpeed}` | 在 `validate-phase-d-dashboard-browser.ts` 的有效 artifact fixture 情境；等待 artifact rail，依序點 `[data-testid="timeline-speed-5x"]` 與 `timeline-speed-1x`。 | `[data-testid="handover-event-rail"][data-axis-playback-rate]`：`src/ui/HandoverEventRail.tsx:372-397`；`.leo-app-shell[data-effective-speed]`：`src/App.tsx:3957,4007`；速度控制：`src/ui/TimelineBar.tsx:301-316`。 | 每次點擊後等待 shell effective speed 更新，斷言 rail 的 `data-axis-playback-rate` 與 shell `data-effective-speed` 完全相等，且 5x→1x 兩次讀值確實不同。用 cross-surface equality 避免 auto-slow 時硬寫錯誤常數。 | `scripts/validate-phase-d-dashboard-browser.ts` |
| L1589 | `parameterSection={` | 不需新增；目前產品的可達 SINR homepage 注入 Walker `SignalTuningPanel`，不是已停用的 archived-TLE branch。 | `[data-testid="sinr-live-display"]`：`src/ui/SinrLiveDisplayDrawer.tsx:51-54`；其內 `[data-testid="engineering-panel-dock"]`：`src/ui/SinrLiveDisplayDrawer.tsx:75-90`；`[data-testid="signal-tuning-drawer-content"]`：`src/ui/SignalTuningPanel.tsx:217-230`。 | **已被覆蓋**：首頁 browser gate 等待 drawer，接著實際點擊 parameter section 內的 power/throughput/energy tabs；移除 App 的 `parameterSection` 注入會使 gate 失敗。元件單元測試也驗證傳入內容落在 engineering dock。 | 不新增；`scripts/validate-homepage-authority-browser.ts:77-85,138-145`、`src/ui/TeachingPanelDock.test.tsx:43-56`。 |

## 無法轉換的

- **L1047**：`overlay-demo` 分支在真實 `App` 不可達；只有 `live-truth` 可觀察。只驗一邊不能取代 ternary 契約。建議現況改判 V；若 owner 恢復 MODQN 入口，再回到 B。
- **L1390**：proof cue/toggle 在真實 `App` 不可達。既有 browser 程式碼雖嘗試點擊，但其 root + localStorage 入口會被首頁初始狀態覆寫，不能算執行期證據。建議暫改判 V。
- **L1391**：同一不可達前提也使 eligible/ineligible callback 行為無法建構。建議暫改判 V。
- **L1392**：完整 left-tab lane/mode matrix 無 runtime 入口；這本質上是純 mapping，現階段更適合改判 S，table-drive `getLeftSidebarTabsForSceneLane()`，待入口恢復再補 browser smoke。
- **L1393**：完整 right-tab lane/mode matrix同樣不可達。建議改判 S，以純函式 matrix 為主；只測 live/artifact 兩個可達分支不能宣稱完整覆蓋。
- **L1413**：`ServiceStatusBanner` 的 MODQN live mount branch 不可達。`/walker` 是另一個 root component，測它不能保護 `App.tsx` 分解。建議暫改判 V。
- **L1469**：可達 rail 的 `data-axis-playing` 是經 `axisKind` gate 後的動畫狀態，artifact source-time 軸永遠為 `false`；沒有 raw prop telemetry，也沒有可達 display-stretched rail。建議暫改判 V，禁止用恆值斷言假裝覆蓋。

## 已被覆蓋的

- **L935**：`src/ui/ArtifactSourceBadge.test.ts:51-76` 已分別驗 synthetic、header-absent、任意 unknown source 的 label/detail 分流，包含「unknown 不可誤稱 synthetic」。
- **L988**：`scripts/validate-phase-c-director-cinematic-live-browser.ts:91,141-148,187-202` 已守 `data-director-phase` 的 `idle → active → idle`；`scripts/validate-phase-c-handover-cinema-browser.ts:111,163-170,232-260` 提供第二條 live 行為證據。
- **L993**：`scripts/validate-homepage-authority-browser.ts:121-135` 已操作 5x/1x 並讀 shell effective speed；Director gates 另守 cinematic 0.25x 與恢復。
- **L1091**：`scripts/validate-phase-c-director-cinematic-live-browser.ts:73-80` 與 `scripts/validate-phase-c-handover-cinema-browser.ts:87-100` 已讀取並斷言 live focus claim。
- **L1096**：`scripts/validate-phase-c-director-cinematic-live-browser.ts:116-139` 已把 event second 與 seek lead-in 關聯；`scripts/validate-phase-c-handover-cinema-browser.ts:130-137` 再與 explainer source time 交叉比對。
- **L1589**：`scripts/validate-homepage-authority-browser.ts:77-85,138-145` 已在真實首頁進入 injected parameter controls 並操作三個 tabs；`src/ui/TeachingPanelDock.test.tsx:43-56` 另守 drawer slot 投影。注意這守的是目前可達的 Walker parameter surface；archived-TLE homepage branch 依現行產品 gate 不可達。

## 統計

可轉換 **9** ／ 無法轉換 **7** ／ 已被覆蓋 **6**（總數 **22**）。
