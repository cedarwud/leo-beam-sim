已完成 `shape: 'hook'` 工具擴充；真實 archived TLE 案例由工具判定必須拒絕，且全程未修改 `src/App.tsx`。

- 實作與唯讀 `--plan`：[extract-region.ts](/home/u24/demo/leo-beam-sim/scripts/refactor/extract-region.ts:29)
- 新增 4 個 hook 測試，連同既有案例共 11/11 通過：[extract-region.test.ts](/home/u24/demo/leo-beam-sim/scripts/refactor/__tests__/extract-region.test.ts:159)
- 完整繁中報告：[M1.md](/home/u24/demo/leo-beam-sim/docs/sdd/worker-reports/M1.md:1)

真實案例的拒絕原因是循環依賴：

```text
CYCLIC_DEPENDENCY: Parameter timelineCurrentTimeSec depends on a selected hook return, so no legal call site can satisfy both declarations.
```

`timelineCurrentTimeSec` 是新 hook 的必要參數，但它又間接依賴 hook 應回傳的 `archivedTleTimelineDescriptor`，所以前後都不存在合法呼叫位置。

驗證結果：

- `extract-region`：11/11
- closure analyzer：3/3
- `tsconfig.json`：exit 0
- `tsconfig.scripts.json`：exit 0
- `src/App.tsx`：4,642 → 4,642 行，SHA-256 前後一致
- 目標模組未建立，沒有發生回滾，因為工具在寫入前即拒絕
- 未執行 git 寫入、browser validator、`check:ee` 或 `check:visual`

