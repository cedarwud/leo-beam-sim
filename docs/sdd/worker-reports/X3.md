# X3 原始碼文字斷言清理報告

## 我改了哪些檔案

以下次數均以 Node 直接讀取目標原始碼後，用 `source.split(literal).length - 1` 計算。倉庫的 `count-source-pins.ts` 因沙箱禁止其內部 `execSync('/bin/sh')` 而回報 `EPERM`，因此依工作單指示採用此 fallback。17 個候選皆確認為實作細節釘字；沒有任何一個候選因具真實保護力而保留。總計移除 17 個來源文字條件，其中 12 個是完整的獨立斷言，5 個只從 3 個複合斷言中移除指定子句。

### `scripts/validate-timeline-scrubbing.tsx`

刪除 3 個獨立斷言，英文替代註解位於目前第 151、158、159 行。

- `setLiveTimelineSeekRequest({`：`src/App.tsx` 內 1 次。
- `liveTimelineSeekTargetSec: liveTimelineSeekRequest?.targetSec`：`src/App.tsx` 內 1 次。
- `liveTimelineSeekRequestKey: liveTimelineSeekRequest?.requestKey`：`src/App.tsx` 內 1 次。

### `scripts/validate-phase-i-s5b-cell-beam-cones.tsx`

從 2 個複合斷言刪除 3 個指定子句，英文替代註解位於目前第 371、372、403 行；兩個複合斷言本身及其他條件均保留。

- `resolveCellBeamConeRenderCount,`：`src/scene/MainScene.tsx` 內 1 次。
- `resolveCellBeamConeSatelliteCount,`：`src/scene/MainScene.tsx` 內 1 次。
- `resolveCellBeamConeRenderCount({`：`src/scene/MainScene.tsx` 內 1 次。

### `scripts/validate-phase-h-s8-hud-camera-default.ts`

刪除 3 個獨立斷言，英文替代註解位於目前第 30、31、67 行。

- `appMode === 'modqn-demo' && !modqnDemoCameraAppliedRef.current`：`src/App.tsx` 內 1 次。
- 同一條離開模式／ref reset 複合條件含兩個字面：`appMode !== 'modqn-demo'` 為 5 次，`modqnDemoCameraAppliedRef.current = false` 為 1 次。
- `import { ModqnSceneHud } from './ui/modqn-controls/ModqnSceneHud'`：`src/App.tsx` 內 1 次。

### `scripts/validate-phase-b-artifact-picker.tsx`

刪除 2 個獨立斷言，英文替代註解位於目前第 211、214 行。

- `import { ArtifactPicker }`：`src/App.tsx` 內 1 次。
- `useState<'paper-faithful' | 'user-trained'>('paper-faithful')`：`src/App.tsx` 內 1 次。

### `scripts/validate-modqn-omega-s3-replay-mode-wiring.tsx`

從 1 個複合斷言刪除 2 個指定子句，英文替代註解位於目前第 434、435 行；禁止由 `appMode === 'modqn-demo'` 直接啟動 proof 的負向守衛仍保留。

- `resolveSceneLane({`：`src/App.tsx` 內 1 次。
- `modqnReplayProofRequested: modqnReplayProofRequestActive`：`src/App.tsx` 內 1 次。

### `scripts/validate-phase-b-training-form.tsx`

刪除 1 個獨立斷言，英文替代註解位於目前第 159 行。

- `import { AdvancedSetupDrawer } from './ui/AdvancedSetupDrawer';`：`src/App.tsx` 內 1 次。

### `scripts/validate-phase-b-service-client.tsx`

刪除 1 個獨立斷言，英文替代註解位於目前第 183 行。

- `import { ServiceStatusBanner } from './ui/modqn-training/ServiceStatusBanner';`：`src/App.tsx` 內 1 次。

### `scripts/validate-phase-d-reward-curve.tsx`

刪除 1 個獨立斷言，英文替代註解位於目前第 280 行。

- `import { RewardCurvePanel } from './ui/modqn-training/RewardCurvePanel';`：`src/App.tsx` 內 1 次。

### `scripts/validate-phase-b-jobs-panel.tsx`

刪除 1 個獨立斷言，英文替代註解位於目前第 200 行。

- `import { AdvancedSetupDrawer } from './ui/AdvancedSetupDrawer';`：`src/App.tsx` 內 1 次。

## 我沒動但有問題的

- `validate:phase-h:s8-hud-camera-default` 的既有 `assert.ok(PASSED.length >= 18)` 在刪除 3 條後得到 17，造成 after exit code 1。依工作單禁止調整門檻，留給 owner 決定。
- `scripts/validate-modqn-omega-s3-replay-mode-wiring.tsx` 在 `package.json` 沒有 npm key；檔頭只提供直接 Node 命令。本次用該命令完成前後驗證，是否補 npm key 留給 owner；本 worker 未修改 `package.json`。
- 基準時已紅、after 仍以相同標籤紅：timeline 的 `artifact replay seek wiring missing replayController?.seek(target);`；cell-beam-cones 的 `MainScene mounts CellBeamCones gated on showCellOverlay and the MODQN beam-cone visual layer scope`；replay wiring 的 2 個既有失敗（`data-handover-criterion` 切換與 `useSimulation.ts` reset）；service-client 的 right-aside placement；reward-curve 的 MODQN branch placement。本次未 re-pin、未修改這些非清單斷言。
- 倉庫 AST 計數工具在此沙箱因內部啟動 `/bin/sh` 而 `EPERM`；已用工作單允許的 Node `split` fallback 完成 17 個字面的計數，未修改工具。
- 共用 checkout 在工作開始前已有其他未提交修改；其中 timeline、HUD camera、artifact picker、replay wiring 也有既存 diff。本次只增添上述指定移除與英文註解，未回復或改寫其他 WIP。

## 驗證

所有命令均為非 `:browser`。前／後 exit code 如下：

| Validator | 實際指令 | Before | After |
|---|---|---:|---:|
| timeline scrubbing | `npm run --silent validate:timeline:scrubbing` | 1 | 1 |
| phase-i cell beam cones | `npm run --silent validate:phase-i:s5b-cell-beam-cones` | 1 | 1 |
| phase-h HUD camera default | `npm run --silent validate:phase-h:s8-hud-camera-default` | 0 | 1 |
| phase-b artifact picker | `npm run --silent validate:phase-b:artifact-picker` | 0 | 0 |
| MODQN omega S3 replay wiring | `node --import tsx/esm scripts/validate-modqn-omega-s3-replay-mode-wiring.tsx` | 1 | 1 |
| phase-b training form | `npm run --silent validate:phase-b:training-form` | 0 | 0 |
| phase-b service client | `npm run --silent validate:phase-b:service-client` | 1 | 1 |
| phase-d reward curve | `npm run --silent validate:phase-d:reward-curve` | 1 | 1 |
| phase-b jobs panel | `npm run --silent validate:phase-b:jobs-panel` | 0 | 0 |

補充結果：artifact picker 為 79 passed / 0 failed；replay wiring 為 46 passed / 2 failed；training form 為 49 passed / 0 failed；service client 為 16 passed / 1 failed；reward curve 為 88 passed / 1 failed；jobs panel 為 68 passed / 0 failed。HUD camera 唯一失敗為未調整的數量門檻 `expected >= 18 assertions; got 17`。`git diff --check` 對 9 個 validator 無 whitespace 錯誤。

計數工具嘗試命令：

```bash
node --import tsx/esm scripts/audit/count-source-pins.ts src/App.tsx
node --import tsx/esm scripts/audit/count-source-pins.ts src/scene/MainScene.tsx
```

兩者因工具內部 `execSync('/bin/sh')` 受沙箱阻擋而無法完成；fallback 的計數邏輯為：

```js
const source = fs.readFileSync(target, 'utf8');
const occurrences = source.split(literal).length - 1;
```

## 複合斷言與孤兒 pass 的處理

- 複合斷言共遇到 3 個：cell-beam-cones 2 個、replay wiring 1 個。只移除清單指定的 5 個子句；cell-beam-cones 保留掛載／scope 與 telemetry dataset bridge 條件，replay wiring 保留禁止從 `appMode` 直接驅動 proof 的負向條件。沒有整條刪除任何複合斷言。
- 孤兒 pass 共 0 個。timeline 的 `pass('App wires artifact seek, live seek, and app-shell timeline metadata')` 前仍有 8 個同組斷言，因此不是孤兒；cell-beam-cones 與 replay wiring 的複合斷言仍存在；其他 validator 的 `pass(...)` 位於 assertion helper 內，刪除 assertion 呼叫不會留下虛假的獨立 pass。
- 數量 ratchet 共遇到 2 個：cell-beam-cones 的複合斷言數量未減，既有 `>= 24` 門檻未因本次變更受影響；HUD camera 的完整斷言數量減 3 後跌破既有 `>= 18` 門檻，依規則未改數字並已列入 owner 決策事項。
