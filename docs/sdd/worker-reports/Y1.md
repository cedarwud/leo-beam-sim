# Y1 — B 類原始碼斷言的行為閘門提案

本報告只依目前 checkout 中實際存在的 DOM、ARIA 與 canvas dataset 擬案。`/` 與 `/legacy` 都是 Walker live 入口；需要 MODQN 的案例以 `localStorage['leo-beam-sim.app-mode.v1']='modqn-demo'` 預置，或經頂部 lane 按鈕切入。表中的新檔名是建議把同名靜態 validator 轉成 browser counterpart；不代表本 worker 已建立該 validator。

## 提案表

| 行號 | 原斷言 | 進入點（URL/路由/前置操作） | 觀察點（真實存在的 selector 或屬性，附 src 檔案:行號） | 斷言內容 | 放哪個 browser validator |
|---|---|---|---|---|---|
| `validate-phase-b-artifact-picker.tsx:L219` | `source.includes('<ArtifactPicker')` | `page.goto('/')`；點 `[data-testid="lane-experience-modqn-live-cell-preview"]`。目前沒有「開啟 Model Library」按鈕，切入 MODQN 後 picker 直接在 proof sidebar 掛載。 | `[data-testid="lane-experience-modqn-live-cell-preview"]`（`src/ui/LaneExperienceBar.tsx:81-90`）；`[data-testid="artifact-picker"][data-surface="model-library"]`（`src/ui/modqn-training/ArtifactPicker.tsx:339-345`） | **可轉換**：切 lane 後斷言 picker `toBeVisible()`、`aria-label="MODQN model library"`，且 paper-faithful、producer-official、synthetic 三個 section 各恰有一個。這比只找根節點多一個非空內容正控制。 | 新增 `scripts/validate-phase-b-artifact-picker-browser.ts` |
| `validate-phase-b-artifact-picker.tsx:L221` | `source.includes('onLoadEntry={handleLoadIntoScene}')` | 同上；用 `page.route()` mock training service 的完成 job 清單、job detail、artifact manifest 與 replay-bundle endpoints，回傳一個可載入的 `job-valid`；點 `[data-testid="artifact-picker-load"]`。fixture 可抽取/沿用 `validate-phase-d-user-trained-bundle-fetch.tsx` 的合法 bundle builder。 | load button `[data-testid="artifact-picker-load"]`（`src/ui/modqn-training/ArtifactPicker.tsx:545-552`）；選中 entry `[data-testid="artifact-picker-entry"][aria-current="true"]`（`src/ui/modqn-training/ArtifactPicker.tsx:505-515`）；`[data-testid="reward-curve-panel"]` 與其 `Reward curves · user-trained` header（`src/ui/modqn-training/RewardCurvePanel.tsx:190-195`） | **可轉換**：先確認 load button enabled，再 click；等待 `job-valid` 所在 entry 成為 `aria-current=true`，並斷言 reward panel 文字含 `user-trained`。同時斷言 `[data-testid="load-into-scene-error-banner"]` 數量為 0，避免只測到選取器自己的局部狀態。 | 新增 `scripts/validate-phase-b-artifact-picker-browser.ts` |
| `validate-phase-b-artifact-picker.tsx:L225` | `source.includes('onLoadPaperFaithful={handleRevertToPaperFaithful}')` | 延續上一列已載入的 user-trained 狀態；點 `[data-testid="revert-to-paper-faithful"]`。 | revert button 的 `disabled`、`aria-pressed` 與 click handler（`src/ui/modqn-training/ArtifactPicker.tsx:369-377`）；paper entry 的 `aria-current`（`src/ui/modqn-training/ArtifactPicker.tsx:346-357`）；reward panel mode header（`src/ui/modqn-training/RewardCurvePanel.tsx:190-195`） | **可轉換**：載入 user-trained 後先斷言 revert button enabled；click 後等待 paper entry `aria-current=true`、button `aria-pressed=true` 且 disabled，reward panel 文字改含 `paper-faithful`，user-trained entry 不再有 `aria-current=true`。 | 新增 `scripts/validate-phase-b-artifact-picker-browser.ts` |
| `validate-phase-b-artifact-picker.tsx:L229` | `source.includes('artifactReplaySource={showcaseArtifactSource}')` | MODQN live 先確認 picker 存在，再點 `[data-testid="modqn-view-modqn-replay-proof"]`；對 recorded replay window 回應加入已知 `X-Showcase-Artifact-Source: synthetic-fixture-fallback` header。 | proof view button（`src/ui/ModqnViewToggle.tsx:104-113`）；`[data-testid="artifact-picker-synthetic-entry"]` 的 `data-artifact-source`（`src/ui/modqn-training/ArtifactPicker.tsx:393-400`）；App 實際從 response header 更新來源（`src/App.tsx:2047-2060`） | **可轉換**：進 proof lane 前值應為 `pending`；mock replay 載入後等待 synthetic entry 的 `data-artifact-source="synthetic-fixture-fallback"`。這個值直接由 picker 的 `artifactReplaySource` prop 寫出；從 `pending` 轉成 mock header token 是非固定值正控制，不再加用只會出現在 `artifact-replay` sceneSource 的 shell attribute。 | 新增 `scripts/validate-phase-b-artifact-picker-browser.ts` |
| `validate-live-walker-handover-event-focus.tsx:L226` | `timelineRailDescriptor.rail.sourceOwner === 'live-walker'` | 以 `modqn-demo` storage 預置進 `/`，等待 `modqn-live-cell-preview` rail ready。 | `[data-testid="handover-event-rail"]` 的 `data-source-owner`、`data-focus-enabled`（`src/ui/HandoverEventRail.tsx:373-405`） | **已被覆蓋**：既有 MODQN probe 已斷言 `sourceOwner=live-walker` 與 `focusEnabled=true`；刪除原始碼文字斷言即可。注意同檔較早的 SINR probe 仍有舊期待，需由下一列一併修正，否則執行時會先行失敗。 | 已在 `scripts/validate-live-walker-handover-event-focus-browser.ts`；不新增 |
| `validate-live-walker-handover-event-focus.tsx:L231` | `timelineRailDescriptor.rail.sourceOwner === 'sinr-live-cell-truth'` | 以 `sinr-experiment` storage 預置進 `/`，等待 `sinr-live` rail 的 event count 大於 0；點第一個 `[data-testid^="handover-event-marker-"]`。 | rail `data-source-owner`（`src/ui/HandoverEventRail.tsx:373-405`）；marker 的 `data-source-time-sec`、`data-click-target-sec` 與 click seek（`src/ui/HandoverEventRail.tsx:463-481`）；focus panel 的 `data-source-owner`、`data-click-target-sec`（`src/ui/HandoverEventRail.tsx:497-511`）；shell 的 `data-live-timeline-seek-target`（`src/App.tsx:3975`） | **可轉換，且應修正既有 stale gate**：把目前 SINR probe 對 `live-walker`／`profile-derived-forecast` 的舊期待改為 `sinr-live-cell-truth`／`live-truth`；click marker 後斷言 focus panel 仍是 `sinr-live-cell-truth`，且 shell seek target 等於 marker `data-click-target-sec`。 | 修改 `scripts/validate-live-walker-handover-event-focus-browser.ts` 的 `validateLiveSinrFocus` |
| `validate-live-walker-handover-event-focus.tsx:L236` | `timelineRailDescriptor.rail.horizonKind === 'live-walker-window'` | 同 L226 的 MODQN live probe。 | rail 的 `data-horizon-kind`、`data-horizon-sec`（`src/ui/HandoverEventRail.tsx:373-405`） | **已被覆蓋**：既有 probe 斷言 `horizonKind=live-walker-window` 與 `horizonSec=7200.000`，並驗證 empty rail 不捏造 marker。刪除原始碼文字斷言即可。 | 已在 `scripts/validate-live-walker-handover-event-focus-browser.ts`；不新增 |
| `validate-phase-h-s5-orbit-trail-modqn-demo.ts:L27` | `appMode !== 'modqn-demo' \|\| reducedMotion` | `modqn-demo`，分別建立 `reducedMotion: 'no-preference'` 與 `'reduce'` context。 | **無**。App 只在記憶體內產生設定（`src/App.tsx:850-864`）；實體效果是 R3F primitive（`src/scene/MainScene.tsx:5185-5195`），canvas 沒有 orbit/spine enabled dataset。 | **無法轉換**：`canvas` 存在不代表這兩個效果的 gate 正確，截圖差異也無法可靠分離 orbit、spine 與其他動畫。建議改判 **S**，以匯出的 pure resolver／render-plan 單元測試守 `appMode × reducedMotion`；若 owner 要 browser gate，先新增明確 runtime telemetry 後再轉。 | — |
| `validate-phase-h-s5-orbit-trail-modqn-demo.ts:L31` | `orbitTrail: false` | `modqn-demo` live lane。 | **無**。`showOrbitTrail` 只控制 `<OrbitTrail>` R3F 掛載（`src/scene/MainScene.tsx:5185-5187`）；`OrbitTrail` 建立 Three.js geometry/material，沒有 DOM/testid/dataset（例：`src/viz/OrbitTrail.tsx:105-133`）。 | **無法轉換**：目前 Playwright 能定位的 canvas 無法回答 orbit trail 是否關閉。建議改判 **S**，對 App 的 runtime visual resolver 做單元斷言；不可拿其他 prototype 的 reduced-motion dataset 冒充主 App 訊號。 | — |
| `validate-phase-h-s5-orbit-trail-modqn-demo.ts:L35` | `spineParticles: true` | `modqn-demo` live lane。 | **無**。雖然 App override 設為 true（`src/App.tsx:854-860`），render plan 又要求 `showSinrLiveViewport`（`src/scene/sceneLaneRenderPlan.ts:242-255`），MODQN proof lane 實際不會掛載；primitive 也只有 R3F `<group>/<mesh>`（`src/viz/SpineParticles.tsx:133-155`）。 | **無法轉換**：先前提議的「MODQN spine 應為 enabled」與目前 render plan 的實際行為矛盾，且沒有 runtime 訊號。建議改判 **S** 並先由 owner 釐清這條是在守「設定值」還是「可見效果」；不能寫必然假紅或假綠的 browser assertion。 | — |
| `validate-modqn-omega-s3-replay-mode-wiring.tsx:L262` | `appSrc.includes('data-handover-criterion')` | `/` 切入 MODQN live；開 `[data-testid="advanced-setup-trigger"]`，依序選 heuristic 與 paper overlay。 | policy control 與兩按鈕（`src/ui/modqn-controls/ModqnAdvancedDisplayControls.tsx:74-103`）；`[data-testid="leo-shell-canvas"]` 的 `data-handover-criterion`（`src/App.tsx:4305-4315`） | **可轉換**：選 heuristic 後等待 control `data-modqn-decision-policy="omega-heuristic"` 且 canvas criterion 為 `sinr-offset`；選 overlay 後分別變為 `decision-overlay-on-live-sinr`。兩方向切換避免只斷言固定初值。 | 擴充 `scripts/validate-phase-c-lane-experience-bar-browser.ts` |
| `validate-modqn-omega-s3-replay-mode-wiring.tsx:L271` | `appSrc.includes("if (handoverMode !== 'decision-overlay-on-live-sinr')")` | 同上；先選 heuristic，再選 overlay。 | `[data-testid="modqn-replay-cue-panel"]` 的 `data-replay-ready`（`src/ui/ModqnReplayCuePanel.tsx:288-315`）；policy buttons（`src/ui/modqn-controls/ModqnAdvancedDisplayControls.tsx:84-103`） | **可轉換**：heuristic 時斷言 cue panel `data-replay-ready="0"`；切 overlay 後等待 `data-replay-ready="1"`。必須包含後半正控制，否則 backend/replay 永遠沒載入也會造成假綠。 | 擴充 `scripts/validate-phase-c-lane-experience-bar-browser.ts` |
| `validate-modqn-phase7k-replay-scene-layer.ts:L419` | `assertContains(appSource, "if (handoverMode !== 'decision-overlay-on-live-sinr')", ...)` | 同上一列。 | 同一個真實 cue panel dataset（`src/ui/ModqnReplayCuePanel.tsx:288-315`）與 policy controls（`src/ui/modqn-controls/ModqnAdvancedDisplayControls.tsx:74-103`） | **可轉換（與 L271 合併成一個行為案例）**：非 overlay 為 ready 0、overlay 為 ready 1。兩個靜態 validator 都可刪，但 browser 中只需一份斷言，不要複製兩次。 | 擴充 `scripts/validate-phase-c-lane-experience-bar-browser.ts`；與 L271 共用案例 |
| `validate-modqn-phase7k-replay-scene-layer.ts:L440` | `assertContains(appSource, '<ModqnReplayCuePanel', ...)` | `modqn-demo` 進 live preview，等待 baseline replay ready。 | `[data-testid="modqn-replay-cue-panel"][data-replay-ready="1"]`（`src/ui/ModqnReplayCuePanel.tsx:304-315`） | **已被覆蓋**：既有 browser probe 先等待 ready=1，之後才允許進 replay proof；不是只有查 selector 字面。刪除原始碼文字斷言即可。 | 已在 `scripts/validate-live-walker-handover-event-focus-browser.ts`；不新增 |
| `validate-phase-h-s3-live-sim-callouts.ts:L79` | `<SinrLiveCellBeamCallouts` | `/` 的 Walker homepage，等待 live frame。 | `[data-testid="beam-callout"][data-beam-primary="1"]`；callout 另帶真實 cell/beam/SINR/EE attrs（`src/viz/SinrLiveCellBeamCallouts.tsx:172-189`） | **已被覆蓋**：homepage authority gate 等待 primary callout，後續把其 cell/beam/frame 值與 rail/canvas snapshot 對齊；已比「元件有掛載」更強。刪除原始碼文字斷言即可。 | 已在 `scripts/validate-homepage-authority-browser.ts`；不新增 |
| `validate-phase-h-s3-live-sim-callouts.ts:L98` | `beamCalloutsEnabled={showBeamCallouts ? '1' : '0'}` | 用 `/legacy`（避開 `/` 的 homepage handover 強制 beam-info 狀態），等待 live canvas；點 `[data-testid="beam-info-toggle"]` off/on。 | checkbox（`src/ui/SinrLiveQuickControls.tsx:94-109`）；canvas 的 `data-beam-callouts-enabled` 寫入點（`src/scene/SceneTelemetry.tsx:143-151`）；callout DOM（`src/viz/SinrLiveCellBeamCallouts.tsx:172-189`） | **可轉換**：初始 checked 且 canvas attr=1；click off 後 checkbox unchecked、canvas attr=0、callout count=0；click on 後 attr=1 且至少一個 callout 回來。off/on 正控制同時守 toggle→scene telemetry→實際 callout 三段 wiring。 | 擴充 `scripts/validate-phase-c-sinr-live-cell-beams-browser.ts` |
| `validate-modqn-omega-s4-heuristic-not-paper.tsx:L230` | `handoverMode === 'omega-heuristic' && sceneLane === 'modqn-live-cell-preview' && <HeuristicNotPaperBanner ...>` | `/` 切 MODQN live、開 Advanced、選 heuristic；再選 overlay與離開 live lane。 | `[data-testid="heuristic-not-paper-banner"]`、`role="alert"`（`src/ui/HeuristicNotPaperBanner.tsx:92-109`） | **已被覆蓋**：既有 lane browser 已斷言 heuristic 選取後 banner 出現、overlay 後消失，並再次確認離開 live lane會卸載。刪除原始碼文字斷言即可。 | 已在 `scripts/validate-phase-c-lane-experience-bar-browser.ts`；不新增 |
| `validate-phase-b-jobs-panel.tsx:L205` | `appSource.includes('<AdvancedSetupDrawer')` | `/` 切 MODQN live，點 Advanced trigger。 | `[data-testid="advanced-setup-trigger"]` 的 `aria-expanded`；`[data-testid="advanced-setup-drawer"]` region（由 prefix 組出，`src/ui/AdvancedDrawerShell.tsx:83-100,117-130`） | **已被覆蓋**：既有 modality browser 實際 click trigger、等待 drawer、要求非零 bounding box，並驗證 drawer 不遮 timeline。刪除原始碼文字斷言即可。 | 已在 `scripts/validate-frontend-advanced-drawer-modality-browser.ts`；不新增 |
| `validate-phase-c-camera-preset.tsx:L260` | `controls.enabled = false` | 新增 `reducedMotion:'reduce'` context，進 `/?sceneSource=artifact-replay`，等待 Director intra enabled；點 `[data-testid="director-intra-focus"]`，等待 focused，再在 canvas 中央 drag。 | Director phase/button（`src/ui/DirectorControls.tsx:63-80`）；canvas 每 frame 的 `data-camera-position`、`data-camera-target`（`src/scene/SceneTelemetry.tsx:315-322`）；實際 acquiring 時關閉 controls（`src/scene/MainScene.tsx:714-743`） | **可轉換**：reduced-motion 可避免 focused 自動繞行造成假差異。focused 穩定後記錄 position+target，drag 並跨兩個 `requestAnimationFrame`，兩值都必須不變；click exit 回 idle 後做同樣 drag，至少一值必須改變，證明手勢確實打到 OrbitControls，而不是測到無效座標。 | 擴充 `scripts/validate-phase-c-director-cinematic-browser.ts` |
| `validate-phase-c-scene-scale-override.tsx:L195` | `visualScaleMultipliers.beamFootprintMultiplier` | 理論上可預置 `localStorage['leo-beam-sim.scene-visual-scale.v1']`，但 UI 已無可操作入口。 | **沒有能證明 footprint 幾何有採用 multiplier 的 runtime 觀察點**。shell 只有 `data-visual-scale-key`（`src/App.tsx:3998-4000`）；TopologyTab 明載 sceneScale 控制已移除，且 `sinr-live` footprint 不讀 multiplier（`src/ui/signal-tuning/TopologyTab.tsx:555-577`）。 | **無法轉換**：`data-visual-scale-key` 只能證明 persistence/state 改變，不能證明 MainScene forwarding 或幾何縮放；把它當替代會是假綠。建議改判 **S**（以 pure geometry/integration unit 守真正 consumer），若需求只剩已移除的 live-lane knob，則改判 **V**。 | — |
| `validate-phase-i-s7b-serving-count-selector.tsx:L241` | `runtime.cellServingCount ?? DEFAULT_SERVING_COUNT` | 以 `modqn-demo` storage 預置，並把 `leo-beam-sim.scene-topology.v1` 的合法 topology fixture 設為 `cellServingCount:null`；進 MODQN live，選 service-allocation preset 使 cell overlay 可觀察。 | default L=8 radio `[data-testid="topology-tab-serving-count-option-8"]` 與 clear button（`src/ui/signal-tuning/TopologyTab.tsx:76-83,361-399`）；canvas `data-cell-serving-count` 寫入（`src/scene/SceneTelemetry.tsx:171-176`；傳值 `src/scene/MainScene.tsx:4973-4978`）；HUD `data-service-serving-count`（`src/ui/modqn-controls/ModqnSceneHud.tsx:69-95`） | **可轉換**：null 初始時 L=8 checked，且 canvas/HUD serving count=8；選 L=4 後兩者變 4；點 clear 後回 8。現有同名單元 validator 已守 UI default、runtime undefined 與 schedule resolver，但沒有守 App→MainScene 的真實 wiring，因此不標 `已被覆蓋`。 | 新增 `scripts/validate-phase-i-s7b-serving-count-selector-browser.ts` |
| `validate-phase6b-handover-policy-controls.tsx:L148` | `assertContains(appSource, 'parameterSection={');` | `page.goto('/')`，等待 SINR formula panel。 | `[data-testid="sinr-live-display"]`（`src/ui/SinrLiveDisplayDrawer.tsx:51-54`）；`[data-testid="sinr-live-advanced-formula"]` 內的 `engineeringContent={parameterSection}`（`src/ui/SinrLiveDisplayDrawer.tsx:75-89`）；實際 parameter UI `[data-testid="sinr-formula-page"]` 與 `[data-testid="sinr-formula-tabs"]`（`src/ui/SignalTuningPanel.tsx:288-296,398-403`；`src/ui/signal-tuning/FormulaTabList.tsx:73-78`） | **已被覆蓋**：既有 formula-layout browser 在兩個 viewport 都等待 formula tabs 可見，且逐 tab 量測內容；這已證明 parameter section 穿過 drawer 實際渲染。刪除原始碼文字斷言即可。 | 已在 `scripts/validate-homepage-sinr-formula-layout-browser.ts`；不新增 |

## 無法轉換的

1. `validate-phase-h-s5-orbit-trail-modqn-demo.ts:L27`：沒有 `runtimeVisualSettings` 或個別 effect 的 DOM/canvas telemetry；只能看到共用 canvas。建議改判 **S**，把 `appMode × reducedMotion` 決策抽成 pure resolver 後測。
2. `validate-phase-h-s5-orbit-trail-modqn-demo.ts:L31`：`OrbitTrail` 是 Three.js primitive，沒有 selector/dataset；主 App 也沒有對應計數。建議改判 **S**。若未來新增例如由真實 render plan 寫出的 `data-orbit-trail-enabled`，才能再做 browser 轉換。
3. `validate-phase-h-s5-orbit-trail-modqn-demo.ts:L35`：除了沒有 runtime 訊號外，`spineParticles:true` 還會被 MODQN lane 的 `showSinrLiveViewport` gate 擋掉；「設定為 true」與「畫面存在粒子」不是同一件事。建議改判 **S**，並先由 owner 定義要守哪一層。
4. `validate-phase-c-scene-scale-override.tsx:L195`：可見控制已移除，`sinr-live` footprint 明確不消費 multiplier；shell reset key 不是幾何證據。若仍需守其他 lane 的 forwarding，改判 **S**；若需求只針對已撤下的 live knob，改判 **V**。

## 已被覆蓋的

1. `validate-live-walker-handover-event-focus.tsx:L226`：`scripts/validate-live-walker-handover-event-focus-browser.ts:220-244` 的 MODQN probe 斷言 rail `sourceOwner=live-walker`、focus enabled 及 empty-event 邊界。提醒：同檔 `:170-214` 的 SINR 舊期待需依 L231 修正，才不會在抵達此 probe 前失敗。
2. `validate-live-walker-handover-event-focus.tsx:L236`：同一 probe `scripts/validate-live-walker-handover-event-focus-browser.ts:224-237` 斷言 `horizonKind=live-walker-window`、`horizonSec=7200.000` 與 marker count。
3. `validate-modqn-phase7k-replay-scene-layer.ts:L440`：`scripts/validate-live-walker-handover-event-focus-browser.ts:250-256` 等待真實 cue panel `data-replay-ready=1` 後才切 proof viewport。
4. `validate-phase-h-s3-live-sim-callouts.ts:L79`：`scripts/validate-homepage-authority-browser.ts:221-235` 等待 primary beam callout；同檔後續把 callout 的 frame/cell/beam telemetry 與 homepage rail、canvas 對齊。
5. `validate-modqn-omega-s4-heuristic-not-paper.tsx:L230`：`scripts/validate-phase-c-lane-experience-bar-browser.ts:91-113` 驗證 heuristic 時 banner 出現、切 overlay 後消失；`:165-180` 再驗證離開 live lane 時卸載。
6. `validate-phase-b-jobs-panel.tsx:L205`：`scripts/validate-frontend-advanced-drawer-modality-browser.ts:45-85` 實際開 drawer、確認非零 box，且驗證它不遮 timeline。
7. `validate-phase6b-handover-policy-controls.tsx:L148`：`scripts/validate-homepage-sinr-formula-layout-browser.ts:15-30` 在真實 homepage 等待 `[data-testid="sinr-formula-tabs"] [role="tab"]` 可見並量測。

## 統計

可轉換 **11** ／ 無法轉換 **4** ／ 已被覆蓋 **7**（總數 **22**）。

> 執行限制：依工作單要求，本 worker 沒有執行任何 `*:browser` validator；以上是以目前 `src/` 與既有 validator 的靜態查核所得的實作提案，不宣稱 Chromium 驗證已通過。
