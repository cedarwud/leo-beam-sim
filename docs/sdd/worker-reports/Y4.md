# Y4：B 類原始碼斷言的行為化提案

以下行號以目前共用 checkout 的 `src/` 為準；表中的 L 編號則沿用待處理 validator 的原始清單。`已被覆蓋` 表示既有測試中已存在相應的行為斷言程式碼，不代表本輪曾執行該測試。本輪依工作限制不執行任何 `*:browser` validator。

## 提案表

| 行號 | 原斷言 | 進入點（URL/路由/前置操作） | 觀察點（真實存在的 selector 或屬性，附 src 檔案:行號） | 斷言內容 | 放哪個 browser validator |
|---|---|---|---|---|---|
| L1594 | `teachingPolicySection={isWalkerSceneActive ? (` | 無可達的應用程式進入點：首頁常數目前把 teaching auxiliary UI 固定關閉。 | 候選內容本身確實有 `[data-testid="handover-policy-controls"]`（`src/ui/HandoverPolicyControls.tsx:449-452`）與 `[data-testid="archived-tle-handover-policy-boundary"]`（`src/ui/ArchivedTleBoundaryNote.tsx:7-10`），但 `teachingPolicySection` 只有在 `showTeachingAuxiliaryUi` 為真時才掛載（`src/ui/SinrLiveDisplayDrawer.tsx:77-89`），而呼叫端傳入固定為假的常數（`src/App.tsx:388`, `src/App.tsx:4231-4251`）。 | **無法轉換**。目前 runtime 不可達；任何對上述兩個 selector 的缺席斷言都只會得到恆真結果，無法守住 Walker 條件分支。 | 不新增；建議改判 **V（目前不可達）**。若日後重新開啟 teaching auxiliary UI，再加 Drawer 的 Walker／非 Walker 雙向測試。 |
| L1739 | `if (!showSinrServingMosaic) return null;` | 理論上先進入 SINR live，再切到非 SINR lane 或關閉 mosaic；但現有 runtime 訊號不足以驗證這個 guard。 | Canvas 上確有 `data-sinr-serving-mosaic-color-count`，由 `src/viz/GroundScene.tsx:67-86` 寫入，呼叫端在 `src/scene/MainScene.tsx:5104` 傳入；然而它受另一個 telemetry gate 控制（`src/scene/MainScene.tsx:1734-1741`），不是 mosaic mesh 是否掛載的直接訊號。guard 本體在 `src/scene/MainScene.tsx:1724-1732`。 | **無法轉換**。即使刪掉或接錯 guard，既有 color-count telemetry 仍可能保持正確，會形成假綠；缺少實際 mosaic mesh 的掛載／卸載訊號。 | 不新增；建議改判 **S（目前只有組裝形狀）**，或先由元件提供 mesh-derived runtime telemetry，再寫 browser 斷言。 |
| L1744 | `buildSinrServingUeColorMapFromCells(cellFrame.ues)` | SINR live，等待 cell frame 更新。 | 現有 Canvas 只暴露 `data-sinr-serving-mosaic-color-count`（`src/viz/GroundScene.tsx:67-86`）；色彩映射函式在 `src/scene/sinrServingMosaic.ts:157-171`，呼叫點在 `src/scene/MainScene.tsx:1724-1732`。沒有每個 UE 的 ID、serving cell/satellite 與實際渲染色彩的 runtime 對照。 | **無法轉換**。distinct-color count 無法證明色圖是從 `cellFrame.ues` 建立，也無法判斷每個 UE 是否對應正確；換成另一個同樣多色的 map 仍會假綠。 | 不新增；建議改判 **S**。模型行為已有 `src/scene/sinrServingMosaic.test.ts:82-100` 與 `scripts/validate-s4-serving-equivalence.tsx:281-315` 的部分保障，但它們不守 MainScene 的接線。 |
| L1943 | `recentHandoverEvents: (sim.sinrLiveCells?.recentHandoverEvents ?? [])` | `/?sceneSource=live-sim&appMode=sinr-experiment`；把模擬調到 20x，等待 handover pulse 後暫停。 | Canvas 的 `data-sinr-live-handover-pulse-cone-count` 由 `src/scene/SceneTelemetry.tsx:183-187` 寫入；實際 cone mesh count 為 `data-sinr-live-handover-pulse-cone-rendered-count`，key 由 `src/scene/MainScene.tsx:5597-5609` 傳入、元件在 `src/viz/SinrLiveCellBeamCones.tsx:1372-1385` 動態寫入。事件解析接線見 `src/scene/MainScene.tsx:4035-4079`。 | **已被覆蓋**：等待 resolved count 大於 0，再斷言 rendered count 大於 0 且兩者相等；若 recent events 未接入，這個流程不會出現有效 pulse。 | 已在 `scripts/validate-phase-c-handover-pulse-render-browser.ts:66-115`；不新增。 |
| L1950 | `telemetryCountDatasetKey="sinrLiveHandoverPulseConeRenderedCount"` | 同 L1943。 | `data-sinr-live-handover-pulse-cone-rendered-count` 的 key 接線在 `src/scene/MainScene.tsx:5597-5609`，實際 dataset 寫入與清除在 `src/viz/SinrLiveCellBeamCones.tsx:1372-1385`。 | **已被覆蓋**：既有 browser gate 直接讀該 Canvas 屬性，要求為有限正數且等於 resolved pulse count；key 遺失或拼錯會令讀值失敗。 | 已在 `scripts/validate-phase-c-handover-pulse-render-browser.ts:99-115`；不新增。 |
| L1960 | `telemetryCountDatasetKey="sinrLiveTriggeredIntraConeRenderedCount"` | `/?sceneSource=live-sim&appMode=sinr-experiment`；點擊 `[data-testid="director-intra-focus"]`，等待導演流程進入 switching beat。 | 觸發按鈕為 `[data-testid="director-intra-focus"]`（`src/ui/SinrLiveQuickControls.tsx:167-185`）；Canvas 實際渲染計數 `data-sinr-live-triggered-intra-cone-rendered-count` 的 key 在 `src/scene/MainScene.tsx:5616-5628`，動態寫入／清除在 `src/viz/SinrLiveCellBeamCones.tsx:1372-1385`。 | **可轉換**：在既有 intra 流程中輪詢 Canvas，顯示視窗內至少一個樣本須為有限正數；流程結束且圖層卸載後該屬性須消失。這會直接守住 dataset key，而不是只檢查 resolver。注意目前視窗常數是 5000 ms（`src/constants/sinrLiveConeStyle.ts:207`），先前「2.5 秒」敘述已過時，不應硬編進測試。 | 擴充 `scripts/validate-homepage-multi-candidate-acceptance-browser.ts` 的既有 intra flow。 |
| L1965 | `resolveTriggeredIntraConeItems({` | 同 L1960；點擊 intra focus 後等待 switching beat，而非假設點擊當下立即出現 cone。 | 同一實際 mesh telemetry：Canvas `data-sinr-live-triggered-intra-cone-rendered-count`（key：`src/scene/MainScene.tsx:5616-5628`；寫入：`src/viz/SinrLiveCellBeamCones.tsx:1372-1385`）。resolver 呼叫點在 `src/scene/MainScene.tsx:4119-4186`；流程狀態另有 `[data-testid="sinr-live-manual-status"]`（`src/ui/SinrLiveQuickControls.tsx:232-239`）。 | **可轉換**：在 manual status 進入 switching／presentation 時，要求 rendered count 曾大於 0；結束後要求屬性清除。既有純函式測試 `src/viz/SinrLiveCellBeamCones.test.ts:770-823` 可繼續守解析細節，browser 斷言負責守 MainScene 接線與實際掛載。 | 與 L1960 一起擴充 `scripts/validate-homepage-multi-candidate-acceptance-browser.ts`，共用一次導演流程。 |
| L2117 | `const showUav = sceneLane === 'sinr-live';` | 首頁預設 SINR；點擊 `[data-testid="lane-experience-modqn-live-cell-preview"]`，再點 `[data-testid="lane-experience-sinr-live"]` 返回。這兩個值列在 `src/ui/LaneExperienceBar.tsx:23-26`，testid 由 `src/ui/LaneExperienceBar.tsx:78-86` 組成。 | `[data-testid="render-isolation-probe"]` 同時暴露 `data-scene-lane` 與 `data-uav-visible`（`src/scene/MainScene.tsx:5916-5922`）；Canvas 也有 `data-uav-visible`（`src/scene/SceneTelemetry.tsx:152-156`）。 | **可轉換**：預設 `sinr-live` 時 probe 與 Canvas 的 `data-uav-visible` 都須為 `1`；切到 `modqn-live-cell-preview` 時兩者都須為 `0`；切回 SINR 再恢復為 `1`。同時核對 probe 的 `data-scene-lane`，避免只測到固定值。 | 擴充 `scripts/validate-phase-c-lane-experience-bar-browser.ts`；不依賴目前被隱藏的 artifact 子頁。 |
| L2123 | `data-scene-lane={sceneLane}` | 首頁；使用 Lane Experience Bar 在 SINR、MODQN 與可達的 proof lane 間切換。 | App shell 的 `.leo-app-shell[data-scene-lane]` 在 `src/App.tsx:3937-3940`；按鈕為 `[data-testid="lane-experience-${option.lane}"]`（`src/ui/LaneExperienceBar.tsx:75-86`）。 | **已被覆蓋**：逐次切換 lane，等待 shell 的 `data-scene-lane` 等於目標 lane，並檢查按鈕狀態。 | 已在 `scripts/validate-phase-c-lane-experience-bar-browser.ts:77-85`, `:91-159`, `:205-217`；不新增。 |
| L2158 | `MODQN_SERVICE_ALLOCATION_PRODUCER_READY \|\| readModqnServiceAllocationOverrideFromUrl(...)` | 先進入 MODQN lane；比較無參數首頁與 `/?modqnServiceAllocation=1`。若使用目前首頁流程，應由 `[data-testid="lane-experience-modqn-live-cell-preview"]` 明確切 lane，不只依賴舊 localStorage。 | URL reader 在 `src/app/appPersistence.ts:77-87`；OR 接線在 `src/App.tsx:593-597`。Canvas 的 `data-modqn-service-map-enabled` 由 `src/scene/SceneTelemetry.tsx:213` 寫入，呼叫端在 `src/scene/MainScene.tsx:5029`。 | **已被覆蓋**：無 override 時要求 service map parked（`0`），有 `modqnServiceAllocation=1` 時要求解除 parked（`1`）。 | 已在 `scripts/validate-phase-3-overlay-render-browser.ts:122-169`；不新增。該 validator 的 lane 前置若仍只靠 localStorage，主 session 應先改成明確點擊 MODQN，因目前 root 初始化會強制 SINR（`src/app/appRuntimeModel.ts:120-133`）。 |
| L2174 | `showUeCounts={modqnVisualLayers.ueCountBadges && showModqnServiceAllocation}` | 進入 MODQN lane。先用無 override 的 parked 狀態，再用 `/?modqnServiceAllocation=1`；點 `[data-testid="advanced-setup-trigger"]` 開 Drawer，依序點 `[data-testid="modqn-layer-preset-baseline-faithful"]` 與 `[data-testid="modqn-layer-preset-minimal"]`。Drawer prefix 接線在 `src/ui/AdvancedSetupDrawer.tsx:57-58`、trigger 生成於 `src/ui/AdvancedDrawerShell.tsx:84-100`。 | 實際 badge DOM 是 `.leo-cell-ue-count-badge`，掛載 gate 與文字分別在 `src/viz/CellOverlay.tsx:100-110`。preset 按鈕由 `[data-testid="modqn-layer-preset-${preset}"]` 產生（`src/ui/modqn-controls/ModqnAdvancedDisplayControls.tsx:50-64`），合法 preset 與 flags 在 `src/scene/modqnVisualLayers.ts:35-58`, `:60-70`；MainScene 傳入 gate 在 `src/scene/MainScene.tsx:5106-5114`。 | **可轉換**：parked + baseline 時 badge 數須為 0；override + baseline 後 badge 數須大於 0、每個文字符合 `/^\d+ UE$/`；切到 minimal 後數量回到 0。不要只讀設定 telemetry，必須檢查實際 badge DOM。 | 擴充 `scripts/validate-phase-3-overlay-render-browser.ts`。 |
| L2179 | `modqnServedUeCount={showModqnServiceAllocation ? modqnServiceMap.servedUeCount : 0}` | MODQN lane；比較 parked 首頁與 `/?modqnServiceAllocation=1`。 | Canvas `data-modqn-served-ue-count` 由 `src/scene/SceneTelemetry.tsx:214` 寫入，MainScene 傳值在 `src/scene/MainScene.tsx:5030`。 | **已被覆蓋**：parked 時嚴格等於 0；解除 parked 後須為有限正數。 | 已在 `scripts/validate-phase-3-overlay-render-browser.ts:122-179`；不新增。 |
| L2184 | `modqnIdleUeCount={showModqnServiceAllocation ? modqnServiceMap.idleUeCount : 0}` | MODQN lane，無 `modqnServiceAllocation=1`，保持 parked。 | Canvas `data-modqn-idle-ue-count` 由 `src/scene/SceneTelemetry.tsx:215` 寫入，MainScene 傳值在 `src/scene/MainScene.tsx:5031`。 | **已被覆蓋**：parked 時嚴格等於 0；這正是本條要求的對外行為，不依賴 cell frame 當下有多少 idle UE。 | 已在 `scripts/validate-phase-3-overlay-render-browser.ts:122-147`；不新增。 |
| L2189 | `modqnServiceMapEnabled={showModqnServiceAllocation && modqnVisualLayers.serviceMap ? '1' : '0'}` | MODQN lane；比較 parked／override。override 頁面開 `[data-testid="advanced-setup-trigger"]`，依序點 `[data-testid="modqn-layer-preset-minimal"]` 與 `[data-testid="modqn-layer-preset-explain-handover"]`。selector 的 prefix／preset 生成分別在 `src/ui/AdvancedSetupDrawer.tsx:57-58`、`src/ui/AdvancedDrawerShell.tsx:84-100`、`src/ui/modqn-controls/ModqnAdvancedDisplayControls.tsx:50-64`。 | Canvas `data-modqn-service-map-enabled` 由 `src/scene/SceneTelemetry.tsx:213` 寫入，MainScene 傳值在 `src/scene/MainScene.tsx:5029`；minimal 的 `serviceMap: false` 與 explain 的 `serviceMap: true` 在 `src/scene/modqnVisualLayers.ts:48-58`, `:84-94`。 | **可轉換**：parked + Explain 須為 `0`；override + minimal 仍須為 `0`；override + Explain 須變成 `1`。既有 gate 已有第一與第三點，但缺少第二點；補上後才真正守住 AND 的兩個輸入。 | 擴充 `scripts/validate-phase-3-overlay-render-browser.ts`。 |
| L2235 | `ueCountByCellId={modqnServiceMap.ueCountByCellId}` | `/?modqnServiceAllocation=1`，明確切到 MODQN lane，選擇會顯示 UE count badges 的 preset。 | `.leo-cell-ue-count-badge` 與 `{ueCount} UE` 在 `src/viz/CellOverlay.tsx:100-110`；map 與 show gate 的 props 在 `src/scene/MainScene.tsx:5106-5114`。Canvas 總數 `data-modqn-served-ue-count` 在 `src/scene/SceneTelemetry.tsx:214`。 | **可轉換**：要求至少一個 badge；逐一解析所有 `/^\d+ UE$/`，總和須等於 Canvas 的 `data-modqn-served-ue-count`。這同時驗證 map 已傳到 overlay，且各 cell 呈現合計與服務分佈一致；僅斷言「有 badge」不足。 | 與 L2174 一起擴充 `scripts/validate-phase-3-overlay-render-browser.ts`。 |
| L2246 | `visible={showCellReassignmentEventArcs}` | MODQN lane，切換 handover-cues 的設定／preset。 | 元件依 `visible` 決定回傳 `null`（`src/viz/CellHandoverArcs.tsx:42-47`），MainScene prop 接線在 `src/scene/MainScene.tsx:5122-5127`。現有 Canvas 只有設定態 `data-modqn-handover-cues-visible`（`src/scene/SceneTelemetry.tsx:216`；傳入 `src/scene/MainScene.tsx:5032`），沒有實際 arc mesh count。 | **無法轉換**。若 `visible` prop 被刪掉而元件採預設可見，設定態 telemetry 仍可正確顯示 `0`，但 arc 實際仍在，browser 斷言會假綠。 | 不新增；建議改判 **S**，或先讓 `CellHandoverArcs` 發布由實際 mesh 數量導出的 Canvas telemetry。 |
| L2251 | `<HandoverStoryLayer` | `/?modqnServiceAllocation=1`，切到 MODQN lane並選 Explain preset。 | 元件實際 mesh count 是 Canvas `data-handover-story-rendered-mesh-count`，由 `src/viz/HandoverStoryLayer.tsx:306-332` 寫入／清除；掛載點在 `src/scene/MainScene.tsx:5116-5120`。 | **已被覆蓋**：解除 parked 且 Explain 狀態下要求 rendered mesh count 大於 0；若整個 component mount 被刪掉，屬性缺失會使斷言失敗。 | 已在 `scripts/validate-phase-3-overlay-render-browser.ts:181-190`；不新增。 |
| L2256 | `{presentationPlan.visible['event-effects'] && showProfileHandoverStoryLayer && modqnVisualLayers.handoverStory && (` | `/?scenePresenter=1&modqnServiceAllocation=1`；切到 MODQN lane，選 Explain preset；在 presentation toolbar 切 full／ground，再切換 minimal preset，最後回 Explain + full 並切到 SINR lane。 | 實際 mesh 屬性 `data-handover-story-rendered-mesh-count` 來自 `src/viz/HandoverStoryLayer.tsx:306-332`。presentation 按鈕為 `[data-testid="scene-presentation-stage-${stage}"]`（`src/scene/presentation/ScenePresentationToolbar.tsx:27-50`），Canvas `data-scene-presentation-stage` 寫入於 `src/scene/MainScene.tsx:359-370`；full／ground 的 visible layer 定義在 `src/scene/presentation/scenePresentation.ts:61-92`；preset selector 在 `src/ui/modqn-controls/ModqnAdvancedDisplayControls.tsx:50-64`；`showProfileHandoverStoryLayer = showCellOverlay` 在 `src/scene/sceneLaneRenderPlan.ts:168-169`。 | **可轉換**：full + Explain 時 count 須大於 0；切 ground 後 count 屬性須消失；回 full 後切 minimal，屬性也須消失；恢復 Explain 後切 SINR，屬性仍須消失。三個負向案例分別守 event-effects、layer preset 與 profile/lane gate。 | 擴充 `scripts/validate-phase-3-overlay-render-browser.ts`。 |
| L2261 | `{presentationPlan.visible['serving-beams'] && showCellOverlay && modqnVisualLayers.beamCones && (` | MODQN lane；切換 presentation serving-beams、Cell Overlay 與 beamCones。 | MainScene conditional 在 `src/scene/MainScene.tsx:5129-5137`。Canvas 雖有 `data-cell-beam-cone-count`、`data-cell-beam-cone-scope`、`data-cell-beam-cone-satellite-count`（`src/scene/SceneTelemetry.tsx:180-182`），但這些值由呼叫前的 resolver 狀態傳入（`src/scene/MainScene.tsx:4982-4984`），不是 `CellBeamCones` 實際 mesh 的回報。 | **無法轉換**。刪掉整個 JSX gate 時三個現有屬性仍可能維持原值；用它們斷言掛載／卸載會是假綠。 | 不新增；建議改判 **S**。先在 `CellBeamCones` 增加實際 mesh-derived count／cleanup 訊號，才可做 browser gate。 |
| L2272 | `beamConeScope={modqnVisualLayers.beamConeScope}` | MODQN lane；切換具有不同 `beamConeScope` 的 preset。 | prop 與 mesh `userData` 在 `src/viz/CellBeamCones.tsx:42-83`，resolver 在 `src/viz/CellBeamCones.tsx:133-187`；Canvas 設定態 `data-cell-beam-cone-scope` 在 `src/scene/SceneTelemetry.tsx:180-182`。沒有 DOM selector 能讀 WebGL mesh 的 `userData`，也沒有元件回報的實際 scope。 | **無法轉換**。現有 Canvas scope 與傳入 prop 同源；即使 prop 接線遺失、元件採預設 scope，telemetry 仍可能顯示預期字串。`scripts/validate-phase-i-s5b-cell-beam-cones.tsx:300-355` 只守 resolver 模型，未守 MainScene 接線。 | 不新增；建議改判 **S**，待元件提供實際 rendered scope／satellite set telemetry。 |
| L2283 | `deriveBeamLoadContention([...modqnServiceMap.ueById.values()].map(projection => ({` | `/?modqnServiceAllocation=1`，切到 MODQN lane，選 baseline preset，等待 contention 情境。 | Canvas `data-beam-load-contention-ue-count` 由 `src/scene/SceneTelemetry.tsx:165` 寫入，MainScene 傳值在 `src/scene/MainScene.tsx:4952`；service-map derivation 接線在 `src/scene/MainScene.tsx:1759-1765`。 | **已被覆蓋**：要求 contention count 為有限正數、served count 大於 0，且 contention 不得大於 served；若誤回舊的空／不相干 UE 來源，測試會失敗。 | 已在 `scripts/validate-phase-3-contention-render-browser.ts:84-130`；不新增。 |
| L2304 | `{presentationPlan.visible['load-overlays'] && showCellOverlay && modqnVisualLayers.handoverStory && showModqnServiceAllocation && (` | `/?scenePresenter=1&modqnServiceAllocation=1`；切 MODQN、選 Explain；再切 presentation full／ground、minimal preset與 SINR lane。另保留無 override 的 parked 對照。 | `BeamLoadCylinder` 以實際 `shouldShow` 寫入／清除 Canvas `data-beam-load-cylinder-rendered`（`src/viz/BeamLoadCylinder.tsx:39-60`）；MainScene conditional 在 `src/scene/MainScene.tsx:5139-5146`。presentation 按鈕 selector 在 `src/scene/presentation/ScenePresentationToolbar.tsx:27-50`，Canvas `data-scene-presentation-stage` 寫入於 `src/scene/MainScene.tsx:359-370`；preset selector 在 `src/ui/modqn-controls/ModqnAdvancedDisplayControls.tsx:50-64`，lane selector 由 `src/ui/LaneExperienceBar.tsx:23-26`, `:78-86` 組成。 | **可轉換**：full + Explain + override 時屬性須為 `true`；切 ground 後屬性須消失；回 full 再切 minimal 後也須消失；恢復 Explain 並切 SINR 後仍須消失；parked 對照不得為 `true`。四個負向案例分別守 load-overlays、handoverStory、showCellOverlay 與 service gate。 | 擴充 `scripts/validate-phase-3-overlay-render-browser.ts`；既有測試已有 parked negative／unparked positive，可在同一流程補三個負向 gate。 |

## 無法轉換的

- **L1594**：`teachingPolicySection` 在目前產品設定下不可達。雖然兩種內容各有真實 testid，但呼叫端固定關閉承載該 slot 的 teaching auxiliary UI；缺席斷言只會恆真。建議改判 V，待功能重新可達才建立雙向行為測試。
- **L1739**：現有 mosaic color-count 是獨立 telemetry gate 的輸出，不是實際 mosaic mesh mount 訊號；無法守 `return null` guard。建議改判 S，或先補 mesh-derived telemetry。
- **L1744**：runtime 只提供 distinct-color count，沒有逐 UE 的 serving 關係與 rendered color 對照，無法證明資料來源是 `cellFrame.ues`。建議改判 S；保留既有模型測試。
- **L2246**：`data-modqn-handover-cues-visible` 是期望設定值，不是實際 event arc 數。`visible` prop 接線損壞時仍可假綠。建議改判 S，待 `CellHandoverArcs` 回報實際 mesh count。
- **L2261**：三個 cell beam cone Canvas 屬性都在元件掛載前由 resolver 計算，無法證明 JSX gate 有掛載／卸載元件。建議改判 S，待實際 mesh telemetry。
- **L2272**：DOM 無法讀 WebGL mesh 的 `userData`；現有 scope 屬性與 prop 同源，無法偵測 prop 未傳入。建議改判 S，待元件回報實際 rendered scope 或 satellite set。

## 已被覆蓋的

- **L1943、L1950**：`scripts/validate-phase-c-handover-pulse-render-browser.ts:66-115` 會等待 live handover pulse，讀取 resolved 與實際 rendered Canvas count，並要求兩者皆為正數且相等。
- **L2123**：`scripts/validate-phase-c-lane-experience-bar-browser.ts:77-85`, `:91-159`, `:205-217` 已檢查 shell 的 `data-scene-lane` 隨 lane 操作改變。當前 proof-artifact 選項在 `src/ui/ModqnViewToggle.tsx:39-46` 被隱藏，故主 session 執行時仍應確認該 validator 的可達路徑沒有過時。
- **L2158**：`scripts/validate-phase-3-overlay-render-browser.ts:122-169` 已比較 parked 與 `?modqnServiceAllocation=1` 的 `data-modqn-service-map-enabled`。目前 root 初始化在 `src/app/appRuntimeModel.ts:120-133` 強制 SINR，若測試仍只以 localStorage 選 MODQN，應先修正前置操作；這是入口可靠性問題，不是缺少本條斷言。
- **L2179、L2184**：`scripts/validate-phase-3-overlay-render-browser.ts:122-179` 已驗證 parked 的 served／idle 均為 0，以及 override 後 served 為正數。L2189 不列入本節，因既有測試尚未守 `serviceMap: false` 的 preset 分支。
- **L2251**：`scripts/validate-phase-3-overlay-render-browser.ts:181-190` 已要求 `data-handover-story-rendered-mesh-count` 大於 0；此訊號由實際元件寫入，整個 mount 遺失時不會假綠。
- **L2283**：`scripts/validate-phase-3-contention-render-browser.ts:84-130` 已要求 contention 為正、served 為正且 contention 不超過 served，守住 service-map UE 投影進入 contention derivation 的行為。

以上是對既有測試原始碼的靜態確認；依任務限制，本輪沒有執行 browser gate，因此沒有宣稱當前 checkout 已取得 runtime green。

## 統計

可轉換 **8** ／ 無法轉換 **6** ／ 已被覆蓋 **8**（總計 **22**）
