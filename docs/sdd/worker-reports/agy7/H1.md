我是資深測試維護者 **H1**。針對 `validate:homepage:authority:browser` 逾時卡死問題，以下是基於所提供材料的完整診斷與處置判定：

---

### 1. 點擊步驟原本想達成的狀態

從材料 A（第 795–812 行）的狀態變化與斷言上下文可以明確判斷：

* **原始場景意圖**：在此步驟前，測試先將候選衛星波束佈局切換為 19 束（`beamBudgetCandidate === '19'`），而服務衛星波束為 7 束（`beamBudgetServing === '7'`）。
* **想達成的狀態**：點擊 `scenario-data-candidate-follow-serving` 原本是為了觸發**「候選衛星波束預算自動跟隨／同步服務衛星」**的連動機制，使 3D 場景遙測（`leo-main-scene canvas`）中的 `beamBudgetCandidate` 被自動拉回並鎖定為與 `beamBudgetServing` 相同的 `'7'`。

---

### 2. 現存控制項中是否有等價的受支援途徑？

**結論：沒有。**

* **依據**：
  1. 檢視材料 B 的現有清單，只存在獨立的波束配置容器（`scenario-data-candidate-beam-configuration` 與 `scenario-data-serving-beam-configuration`），沒有任何「連動／跟隨（follow-serving）」功能的控制項。
  2. 材料 A 的註解指出 `Candidate beam count is a real candidate-fan budget, not a selected-link scalar`，說明架構上候選衛星波束預算已被重構成獨立實體扇區預算，不再支援或提供「跟隨服務衛星」的連動切換。
  3. 根據紀律不發明未出現的 selector，且單純手動切換候選波束本質上也無法等價替代「透過 follow-serving 觸發兩者自動同步」的原互動語意。

---

### 3. 正確處置方式與論證

**正確處置：(a) 刪除這個互動步驟與其後續斷言（第 805–812 行）**

#### 判定證據與分析：

1. **為什麼不是 (c) 產品回歸？**
   * 材料 C 的單元測試 `SignalTuningPanel.formula.test.tsx` 明確使用 `assert.doesNotMatch(roleControlledMarkup, /scenario-data-candidate-follow-serving/)` **主動鎖定該控制項不得存在**，並輸出 `without retired symbols`。
   * 這證明移除該控制項是**經過設計的刻意架構演進**（已將連動符號 retired），並由單元測試建立了負向契約（negative contract）。若選 (c) 強行補回控制項，將直接破壞單元測試所保護的架構邊界。

2. **為什麼不是 (b) 整支閘門標記 QUARANTINED？**
   * 此處不是非決定性（flaky）失敗或基礎設施異常，而是**測試腳本在重構後殘留了已退休符號（stale test artifact）**。故障根因完全確定且可控，不應對整支健康且具權威性的閘門進行隔離。

3. **處置 (a) 的覆蓋率得失評估**：
   * **失去的覆蓋**：僅失去「候選波束跟隨服務波束連動」這項**已被產品與架構廢棄的舊機制**之 UI 操作覆蓋（無效覆蓋）。
   * **保留的覆蓋**：第 793–804 行已完整覆蓋獨立的候選波束預算配置（19 束）與場景畫布遙測斷言（`beamBudgetServing === '7'`, `beamBudgetCandidate === '19'`），第 814 行之後亦完整保留焦點單元（focus cell）與後續 Walker Snapshot 斷言。

---

### 可貼片段（第 793 行起的修正建議）

移除第 805–812 行已廢棄的點擊與等價斷言，使流程無縫銜接至預設焦點 frame 與後續驗證：

```ts
  // Candidate beam count is a real candidate-fan budget, not a selected-link
  // scalar. Assert scene telemetry while retaining the shared formula frame.
  await page.locator('#scenario-data-candidate-beam-layout-19').check();
  await page.waitForFunction(
    () => {
      const scene = document.querySelector<HTMLCanvasElement>('[data-testid="leo-main-scene"] canvas');
      return scene?.dataset.beamBudgetServing === '7' && scene.dataset.beamBudgetCandidate === '19';
    },
    undefined,
    { timeout: 30_000 },
  );
  await assertSharedWalkerSnapshot(page);

  const defaultFocusFrame = await assertSharedWalkerSnapshot(page);
  await page.locator('label[for="scenario-data-focus-cell-1"]').click();
  await page.waitForFunction(
    ({ previousBeam, previousSatellite, previousSinr }) => {
```
