# D1：browser 閘門治理決策審查

查證基準：目前 checkout 為 `wip/ee-handover-authority-2026-09-05`，HEAD `456381997577399e388f17918e37b251068d127b`。查證時 `git status --short` 有 45 筆既有／共用 WIP，並非題述的乾淨工作區；以下檔案行號與靜態結論因此都以**目前工作樹**為準，不能反推先前乾淨快照的 runtime timeout。D1 沒有執行任何 `*:browser` validator，也沒有修改任何既有檔案。

## 逐項建議

### 1. `validate:phase-c:lane-experience-bar:browser` — 保留並標記 `QUARANTINED`

**建議。** 不退休。現行公開首頁確實刻意不 mount `LaneExperienceBar`，所以目前 gate 必然停在等待 bar；但證據只證明「公開入口已隱藏」，沒有證明「功能永久移除、不打算回來」。相反地，現行 handoff 明載元件與 transition path 保留給 internal proof tooling，且 unreferenced allowlist 仍刻意保留元件。依「park／日後恢復則保留腳本」的既有分界，應在 gate 檔頭標示 `QUARANTINED 2026-09-07`，保留腳本與 npm key；待受支援的 MODQN/proof 入口恢復後修 gate 並解除 quarantine。不得以 localStorage、隱藏 state 或 URL 注入讓它假綠。

**重新查證後的證據。** Gate 在 `scripts/validate-phase-c-lane-experience-bar-browser.ts:31-36,77-81` 明確等待 `[data-testid="lane-experience-bar"]`；`scripts/validate-frontend-scene-lane-governance.ts:2790-2800` 正向要求 App 不 import／不 mount；全 `src` 除元件本身外沒有 import 或 JSX mount。但 `docs/handoff/HANDOFF-ee-presentation-2026-08-03.md:51-57` 明載 public switch 從 launch surface 移除、元件與 transition path 保留給 internal proof tooling；`scripts/fixtures/known-unreferenced-components.json:12` 也把它列為已知保留的 unreferenced component。`docs/decisions/ADR-002-nav-surface-vs-lane-authority.md:3-9` 只說舊 3-story selector 被 2-tab consolidation supersede，並沒有核准永久刪除 proof path。

```text
$ rg -n "<LaneExperienceBar|from ['\"].*LaneExperienceBar" src --glob '!src/ui/LaneExperienceBar.tsx'
[no matches]

$ rg -n 'retired top-level LaneExperienceBar|LaneExperienceBar is not mounted|public.*LaneExperienceBar|LaneExperienceBar.*transition|path 保留' scripts/validate-frontend-scene-lane-governance.ts docs/handoff/HANDOFF-ee-presentation-2026-08-03.md
scripts/validate-frontend-scene-lane-governance.ts:2794:  'App does not retain the retired top-level LaneExperienceBar import',
scripts/validate-frontend-scene-lane-governance.ts:2799:  'LaneExperienceBar is not mounted after the MODQN sub-nav consolidation',
docs/handoff/HANDOFF-ee-presentation-2026-08-03.md:53:左上角 `SINR / MODQN` 的 public `LaneExperienceBar` 已從 `App.tsx` 的 launch surface
docs/handoff/HANDOFF-ee-presentation-2026-08-03.md:56:`LaneExperienceBar` 元件本身、`handleExperienceChange` 與 MODQN/replay proof transition
docs/handoff/HANDOFF-ee-presentation-2026-08-03.md:57:path 保留給內部 proof tooling，沒有刪 MODQN truth、資料或 frame contract。
```

**可逆性。** 加 quarantine 註記、保留 npm key，完全可逆且不移除覆蓋資產。它目前不在 `validate:live-render` 或 `.github/workflows/governance.yml`，所以無 aggregate 項需移出。退休則會構成題定義的不可逆覆蓋移除；現有證據未達此門檻。

### 2. `validate:frontend:advanced-drawer-modality:browser` — 保留並標記 `QUARANTINED`

**建議。** 不退休，也不能在現況直接修到綠；標記 `QUARANTINED 2026-09-07`。Gate 的唯一進入操作確實是點已消失的 LaneExperienceBar segment，但它真正保護的「drawer 開啟時非 modal、無 scrim、timeline 仍可操作」並未被產品決定廢除：drawer 仍在 MODQN lane 條件式 mount，governance 還反向要求 npm key 存在，既有 program 也把 modality gate 稱為 behavior lock。這是「功能／入口 parked，保護契約仍活著」，符合 quarantine，不符合永久退休。恢復受支援入口後，應修 gate 的 entry step；在此之前不能繞過公開操作直接改 React state。

**重新查證後的證據。** 過期入口在 `scripts/validate-frontend-advanced-drawer-modality-browser.ts:45-52`；真正 modality 斷言在 `:56-85`。`src/App.tsx:3533-3537` 說 transition path 保留給 internal MODQN/replay proof surfaces，只是不 mount public switch；`src/App.tsx:4214-4226` 仍在 `sceneLane !== 'sinr-live'` 時 mount `AdvancedSetupDrawer`。`scripts/validate-frontend-scene-lane-governance.ts:3227-3233` 把遺失此 npm key 定義為會 void retirement contract；`docs/frontend-consolidation-program.md:77-81` 仍稱此行為已被該 gate 鎖住。

```text
$ rg -n 'lane-experience-modqn-live-cell-preview|intentionally does not mount|sceneLane !== .sinr-live.|advanced-drawer-modality' scripts/validate-frontend-advanced-drawer-modality-browser.ts src/App.tsx scripts/validate-frontend-scene-lane-governance.ts
scripts/validate-frontend-advanced-drawer-modality-browser.ts:46:    await page.click('[data-testid="lane-experience-modqn-live-cell-preview"]');
src/App.tsx:3534:  // proof surfaces, but the SINR launch surface intentionally does not mount the
src/App.tsx:4217:          {sceneLane !== 'sinr-live' && (
scripts/validate-frontend-scene-lane-governance.ts:3233:assertContains(packageJson, '"validate:frontend:advanced-drawer-modality:browser"', 'package exposes the drawer modality behavior gate (non-modal inline: no scrim, timeline stays clickable)');
```

**可逆性。** Quarantine 只加註記並保留腳本／npm key，可逆且不移除 coverage asset；此 key 本來就不在 aggregate 或 workflow。退休會同時要求刪 `scripts/validate-frontend-scene-lane-governance.ts:3233` 的 replacement-gate pin，造成兩層覆蓋一起消失，證據不足以承擔這個不可逆後果。

### 3. `validate:phase-c:handover-pulse:render:browser` — 修，不退休

**建議。** 把 gate 的入口從 `/` 改為 `/legacy`，並先斷言 `[data-testid="leo-main-scene"]@data-homepage-visual-identity="default"`，其餘 live-fire 與 paused `count === rendered > 0` 契約保留。Y7 正確證明首頁 `/` 已由 normalized accepted pair 取代 natural pulse，卻漏查到 natural-pulse renderer 明確仍保留在非首頁 lane，而 `/legacy` 仍由同一個 `App` 與 `MainScene` 執行。這支 gate 的名稱與檔頭保護的是 pulse renderer，不是 homepage-only product contract；把它移到仍受支援的 legacy carrier 比刪除覆蓋更符合證據。修後須由可啟動 Chromium 的 session 實跑；若 `/legacy` 沒有產生 pulse，保留紅燈並收集兩個 pulse dataset 與 recent-event telemetry，不可退回退休結論。

**重新查證後的證據。** 原 gate 在 `scripts/validate-phase-c-handover-pulse-render-browser.ts:66-68,78-115` 開 root、等待 pulse 並要求 `everRendered`。首頁抑制在 `src/scene/MainScene.tsx:3434-3437,4042-4049`，policy steady state 在 `src/homepage/controller/homepageSceneGeometryPolicy.ts:129-137` 與 test `:19-26` 鎖成 `renderNaturalPulse: false`；但註解同時明載「legacy pulse fallback remains available on non-homepage lanes」。`src/main.tsx:31-43,283-300` 顯示 `/legacy` 不走 `/walker` sandbox，而是落到同一個 `App`；`src/App.tsx:614,4343` 又只在 pathname `/` 啟用 homepage identity，因此 `/legacy` 會走 `LEGACY_POLICY`（`homepageSceneGeometryPolicy.ts:49-57,73`）並保留 pulse。

```text
$ rg -n 'legacy pulse fallback|naturalPulseAvailable|homepageVisualIdentity=|isLegacyWalkerRoute|everRendered' src/scene/MainScene.tsx src/App.tsx src/main.tsx scripts/validate-phase-c-handover-pulse-render-browser.ts
src/scene/MainScene.tsx:3436:    // that legacy pulse fallback remains available on non-homepage lanes.
src/scene/MainScene.tsx:3437:    naturalPulseAvailable: !homepageVisualIdentity && recentPrimaryHandoverEvent !== null,
src/App.tsx:4343:              homepageVisualIdentity={typeof window !== 'undefined' && window.location.pathname === '/'}
src/main.tsx:37:const isLegacyWalkerRoute = isHomepageRoute
scripts/validate-phase-c-handover-pulse-render-browser.ts:78:    let everRendered = false; // B: the pulse draws meshes on the live lane
scripts/validate-phase-c-handover-pulse-render-browser.ts:108:    assert.ok(everRendered, 'the ambient pulse rendered >= 1 cone on the live lane (handovers draw, mesh-derived)');
```

**可逆性。** 改 gate route／加 route guard 是可逆的測試修正，不移除覆蓋。保留 `package.json:187` 的 npm key、`package.json:210` 的 `validate:live-render` chain 項，以及 `.github/workflows/governance.yml:95` 的 workflow 清單項。故本審查沒有任何 npm key 或 workflow 項目應因第 1–3 項被刪除。若仍照 Y7 退休，才會是不可逆的覆蓋移除；D1 明確不建議。

### 4. `validate:golden-flow:browser` 之 16px 違規 — 修 CSS，不改契約

**建議。** 在 `@media (max-width: 1179px)` 內，緊鄰既有 `.golden-flow-angle-power-ee span { font-size: 1rem; }`，新增 `.golden-flow-angle-power-ee__note { font-size: 1rem; }`。根節點 `1rem` 明確是 16px；這是把唯一漏掉的長註解納入既有 mobile typography 收斂，不是修改科學內容。不要把 gate 的 `<15.9px` 判斷放寬：它是專案自行選定的「所有可見文字至少 16 CSS px」產品契約，現有證據沒有批准例外。題述「沒有 mobile 提高字級慣例」與目前工作樹不符：同一 media block 已把 event copy、angle-power spans、handover chain spans 提到 `1rem`。

**重新查證後的證據。** `.golden-flow` 根字級在 `src/prototype/golden-flow/GoldenFlowPrototype.scss:20-33` 是 16px；違規 note 在 `:1507-1512` 是 `.92rem`，即 14.72px。Mobile block 在 `:2497-2573` 已把 dock copy 與 angle-power spans 設為 `1rem`，並把五欄值卡改成兩欄、隱藏箭頭；dock 本身 `:2525-2537` 有 `max-height: 36svh` 與 `overflow: auto`，所以字級增加最多造成內部換行／捲動，不會讓 dock 超出既有高度上限。Validator 在 `scripts/validate-golden-flow-browser.ts:234-277` 只量可見文字，並於 `:503-654` 對 390／320 mobile 各 beat 同時檢查 collision 與中央區域至少 70%。

```text
$ rg -n 'golden-flow-angle-power-ee__note|golden-flow-angle-power-ee span|font-size: 16px|max-height: 36svh|overflow: auto' src/prototype/golden-flow/GoldenFlowPrototype.scss
src/prototype/golden-flow/GoldenFlowPrototype.scss:32:  font-size: 16px;
src/prototype/golden-flow/GoldenFlowPrototype.scss:1507:.golden-flow-angle-power-ee__note {
src/prototype/golden-flow/GoldenFlowPrototype.scss:1510:  font-size: .92rem;
src/prototype/golden-flow/GoldenFlowPrototype.scss:2534:    max-height: 36svh;
src/prototype/golden-flow/GoldenFlowPrototype.scss:2536:    overflow: auto;
src/prototype/golden-flow/GoldenFlowPrototype.scss:2571:  .golden-flow-angle-power-ee span { font-size: 1rem; }
```

**可逆性。** 單一 mobile CSS override 可逆；不改 gate contract。是否無碰撞仍屬 browser acceptance，必須以修後 390×844 與 320×720 實跑確認，靜態「有 overflow」不是視覺通過證明。

### 5. `validate:homepage:authority:browser` — 保留契約；現有證據只能縮到兩階段／九組新 conjunct

**建議。** 不退休、不弱化，也暫不指定產品修復行。現有摘要沒有 stack 的 predicate 行號或 timeout 當下 DOM，因此仍無法誠實選出單一 conjunct；但讀完呼叫順序後，可以比 Y7 再縮小：`assertWalkerHomepage` 已先讓 rail／host／scene 的 snapshot、source-frame、phase 三方 join 成立（`scripts/validate-homepage-authority-browser.ts:87-103`），且 timeline 已暫停，主流程之後才第一次呼叫 `assertSharedWalkerSnapshot`（`:893-900`）。所以若 timeout 在第二個 wait，真正新增、優先懷疑的不是那組已通過的三方 join，而是：(1) canvas，(2) serving row，(3) serving row snapshot/source-frame join，(4) rail active-link=1，(5) scene active-link=1，(6) active candidate receipt join，(7) serving row sat/beam 對 callout formula-frame sat/beam，(8) callout 五個 formula 欄非空，(9) serving row 四個 metric text 非空（`:232-268`）。然而若 timeout 發生在第一個 wait，唯一失敗 conjunct 就是 primary callout 不存在（`:222-226`）；目前摘要仍無法分辨這兩支。

**重新查證後的證據。** Accepted ADR 在 `docs/decisions/ADR-013-walker-only-homepage-source.md:69-78` 要求 right rail 與 central callouts 消費同一 Walker frame。Callout 預設開啟於 `src/scene/beamDisplaySpec.ts:92-97,284-288`，掛載條件在 `src/scene/MainScene.tsx:5560-5591`，datasets 由 `src/viz/SinrLiveCellBeamCallouts.tsx:124-186` 產生；rail row datasets 在 `src/ui/homepage/HomepageBeamRail.tsx:533-560`，rail snapshot datasets 在 `:1368-1397`。因此 surface 與契約都活著。靜態原始碼也揭示第一 wait 若失敗的具體上游集合：`beamInfoItems.length === 0`、`showBeamCallouts || homepageHandoverBeamInfoActive` 為 false、lecture gate 非 null，或 measuredItems 因沒有 finite SINR/EE 被濾空；沒有 runtime dump 不能在其中猜一個。

```text
$ rg -n 'data-testid="beam-callout"|data-beam-primary|showBeamCallouts \|\| homepageHandoverBeamInfoActive|data-snapshot-id|same accepted Walker' src/scene/MainScene.tsx src/viz/SinrLiveCellBeamCallouts.tsx src/ui/homepage/HomepageBeamRail.tsx docs/decisions/ADR-013-walker-only-homepage-source.md
src/scene/MainScene.tsx:5567:      {(showBeamCallouts || homepageHandoverBeamInfoActive)
src/viz/SinrLiveCellBeamCallouts.tsx:173:              data-testid="beam-callout"
src/viz/SinrLiveCellBeamCallouts.tsx:179:              data-beam-primary={isPrimary ? '1' : '0'}
src/ui/homepage/HomepageBeamRail.tsx:1376:      data-snapshot-id={snapshotId}
docs/decisions/ADR-013-walker-only-homepage-source.md:75:- The right rail and central callouts must consume the same accepted Walker
```

**需要的額外資訊。** 先把兩個 waits 各自 catch 成 `primary-callout-wait`／`shared-snapshot-predicate`，但不改 pass predicate。第二個 wait 每次 poll 應寫一份同時刻的 named-check object（上述九組再加 rail/host/scene/center existence 與舊三方 join）及完整相關 datasets；timeout 時輸出最後一份 object、所有 callout 的 sat/beam/cell/primary identity，以及 canvas `sinrLiveCellBeamCalloutRenderedCount`。取得第一個 false named check 後才修 owner code；cellId vs beamId 僅原樣輸出，不在 D1 選邊。

**可逆性。** 加分段遙測可逆且不改 acceptance；刪 conjunct、延長 timeout 或改 identity key 都會弱化／改變契約，D1 不建議。

## 我推翻前一輪的地方

1. Y7 說 `validate:phase-c:lane-experience-bar:browser` 應退休；D1 認為證據只支持 quarantine。Y7 使用的「不 mount」證據成立，但漏納 `docs/handoff/HANDOFF-ee-presentation-2026-08-03.md:53-57` 對元件／transition path「保留給 internal proof tooling」的明文，因此不能推到永久移除。
2. Y7 說 `validate:frontend:advanced-drawer-modality:browser` 應退休；D1 改為 quarantine。Drawer 本體、非 modal 行為契約與 governance replacement-gate pin 都仍在（`src/App.tsx:4214-4226`、`scripts/validate-frontend-scene-lane-governance.ts:3227-3233`）；消失的是受支援入口，不是被保護行為。
3. Y7 說 `validate:phase-c:handover-pulse:render:browser` 應退休；D1 改為修到 `/legacy`。首頁 carrier 已永久換掉這點成立，但 `src/scene/MainScene.tsx:3434-3437` 明載 legacy fallback 保留於非首頁 lane，且 `src/main.tsx:31-43,294-300`、`src/App.tsx:4343` 證明 `/legacy` 是同 App 的可用非首頁路徑。刪 gate 會誤刪仍活著的 renderer coverage。
4. Y7 對 homepage authority 說完全無法縮到第二 predicate 的子集合；D1 利用 `assertWalkerHomepage` 先行通過的三方 join 與呼叫順序，把第二 wait 的新增嫌疑縮為九組。但沒有 stack 行號／同刻 dump，仍不能誠實指定其中唯一一組，故不推翻其「不得猜修復行」結論。

## 執行清單

1. 在 `scripts/validate-phase-c-lane-experience-bar-browser.ts` 與 `scripts/validate-frontend-advanced-drawer-modality-browser.ts` 加 `QUARANTINED 2026-09-07` 檔頭，寫明：public MODQN entry parked、腳本與 npm key 保留、禁止 hidden-state bypass、合法入口恢復後解除。兩者本來都不在 aggregate／workflow，不需移項。
2. 修 `scripts/validate-phase-c-handover-pulse-render-browser.ts:66`，把入口改為 `/legacy?sceneSource=live-sim&appMode=sinr-experiment`，增加 `data-homepage-visual-identity="default"` route guard，保留 `everRendered` 與 paused count/render equality。由可啟動 Chromium 的主 session 跑 `npm run validate:phase-c:handover-pulse:render:browser`。
3. **npm／workflow 移除清單：無。** 保留三個 npm keys：`validate:phase-c:lane-experience-bar:browser`、`validate:frontend:advanced-drawer-modality:browser`、`validate:phase-c:handover-pulse:render:browser`；保留 `package.json` 的 `validate:live-render` pulse 項與 `.github/workflows/governance.yml` 的 `npm run validate:phase-c:handover-pulse:render:browser`。D1 不批准任何不可逆覆蓋移除。
4. 在 `GoldenFlowPrototype.scss` 的 `@media (max-width: 1179px)` 加 note 的 `font-size: 1rem`；由主 session 跑 `npm run validate:golden-flow:browser`，特別確認 390×844、320×720 的字級、collision、70% central area 與 scrollability。
5. 先只替 `assertSharedWalkerSnapshot` 加兩段 named diagnostics；再跑 `npm run validate:homepage:authority:browser`。以第一個 false check 決定修復 owner；保留所有原 conjunct，不以 timeout 增長或 identity 改名掩蓋失敗。
6. 完成後跑非 browser 的 `npm run validate:frontend:scene-lane-governance`，並以 `rg` 確認 quarantine keys 仍存在、pulse key 仍在 aggregate/workflow。Browser aggregate 最後再跑 `npm run validate:live-render`；其範圍外既有紅燈須分開報告，不能拿 aggregate exit code 單獨判定本清單。

## 仍需 owner 本人拍板的

1. 是否把 public SINR／MODQN switch 與 internal proof path 從「保留／parked」改成「永久不回來」。目前權威只足以支持 quarantine；若 owner 明示永久移除，才能另開不可逆退休決定，並同步處理元件、舊文件與 coverage replacement，而不是只刪 browser gate。
2. 是否撤銷 Golden Flow「可見文字至少 16 CSS px」這項產品契約。D1 的明確建議是維持並修 CSS；若要容許 14.72px，那是新的產品／無障礙方向決定，不能由既有紅燈倒推。
3. Homepage identity 的 `cellId` vs `beamId` 顏色鍵仍由 owner 保留；D1 只要求遙測兩者及 join 結果，不選邊、不藉此弱化 gate。
