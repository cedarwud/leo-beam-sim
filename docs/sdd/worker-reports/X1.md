# X1 原始碼文字斷言清理報告

## 我改了哪些檔案

### `scripts/validate-frontend-scene-lane-governance.ts`

刪除 38 條 `assertContains` 原始碼文字斷言，並在各原處留下單行英文理由註解。逐條檢視後，清單內沒有我認為應保留的真守衛。下表行號是修改後的理由註解行；「次數」由 Node 直接讀取指定來源後，以 `source.split(literal).length - 1` 計得。

| 註解行 | 來源 | 被刪字面 | 次數 |
|---:|---|---|---:|
| 623 | `src/App.tsx` | `const [modqnVisualLayerPreset, setModqnVisualLayerPreset]` | 1 |
| 637 | `src/App.tsx` | `from './app/sceneLane'` | 1 |
| 647 | `src/App.tsx` | `from './app/timelineRailAuthority'` | 1 |
| 648 | `src/App.tsx` | `from './app/liveWalkerHandoverRailAdapter'` | 1 |
| 649 | `src/App.tsx` | `from './scene/liveWalkerHandoverEventIndex'` | 1 |
| 820 | `src/App.tsx` | `from './showcase/dashboard/AlgorithmDashboard'` | 1 |
| 946 | `src/App.tsx` | `from './ui/ArtifactSourceBadge'` | 1 |
| 1023 | `src/App.tsx` | `useDirectorOrchestration({` | 1 |
| 1046 | `src/App.tsx` | `indexWorker.build(buildInput` | 1 |
| 1193 | `src/App.tsx` | `from './ui/ArtifactSatelliteCompass'` | 1 |
| 1367 | `src/App.tsx` | `const [modqnReplayProofRequested, setModqnReplayProofRequested] = useState(false);` | 1 |
| 1483 | `src/App.tsx` | `from './ui/SinrLiveDisplayDrawer'` | 1 |
| 2144 | `src/scene/MainScene.tsx` | `modqnServiceAllocationEnabled: runtime.modqnServiceAllocationEnabled ?? false` | 1 |
| 2171 | `src/scene/MainScene.tsx` | `deriveModqnServiceMap({` | 1 |
| 2172 | `src/scene/MainScene.tsx` | `buildModqnCellServiceReadout({` | 1 |
| 2174 | `src/scene/MainScene.tsx` | `modqnCellServiceReadout,` | 1 |
| 2175 | `src/scene/MainScene.tsx` | `markerColor: isOtherHandover` | 1 |
| 2176 | `src/scene/MainScene.tsx` | `mosaic?.markerColor ?? service?.markerColor` | 1 |
| 2192 | `src/scene/MainScene.tsx` | `const showCellReassignmentEventArcs = modqnVisualLayers.handoverCues` | 1 |
| 2214 | `src/scene/MainScene.tsx` | `beamConeScope: renderedCellBeamConeScope` | 1 |
| 2237 | `src/scene/MainScene.tsx` | `const focusBeamLoad = beamLoadContentionEnabled` | 1 |
| 2238 | `src/scene/MainScene.tsx` | `beamLoadContention.byUeId.get(focusedCellBeamConeUe?.id ?? '')` | 1 |
| 2280 | `src/scene/MainScene.tsx` | `import { BeamLoadUploadParticles }` | 1 |
| 2312 | `src/scene/MainScene.tsx` | `const uploadParticlesEnabled =` | 1 |
| 2319 | `src/scene/MainScene.tsx` | `resolveCellBeamConeItems({` | 1 |
| 2330 | `src/scene/MainScene.tsx` | `focusCones={uploadParticleFocusCones}` | 1 |
| 2331 | `src/scene/MainScene.tsx` | `beamLoadContention={beamLoadContention}` | 1 |
| 2597 | `src/scene/MainScene.tsx` | `function ArtifactSceneContent` | 1 |
| 2706 | `src/scene/MainScene.tsx` | `acceptedHandoverPresentation?.commit,` | 1 |
| 2707 | `src/scene/MainScene.tsx` | `const visibleSatelliteId = authorityPresentationCommitObserved` | 1 |
| 2811 | `src/App.tsx` | `const [sceneSource, setSceneSource] = useState<SceneSourceMode>` | 1 |
| 2812 | `src/App.tsx` | `const handleExperienceChange = useCallback((targetLane: SceneLane) => {` | 1 |
| 2953 | `src/App.tsx` | `from './ui/AdvancedSetupDrawer'` | 1 |
| 3041 | `src/App.tsx` | `from './ui/HeuristicNotPaperBanner'` | 1 |
| 3052 | `src/App.tsx` | `onModqnDecisionPolicyChange={handleModqnDecisionPolicyChange}` | 1 |
| 3133 | `src/App.tsx` | `onModqnVisualLayerPresetChange={setModqnVisualLayerPreset}` | 1 |
| 3134 | `src/App.tsx` | `onModqnDecisionPolicyChange={handleModqnDecisionPolicyChange}` | 1 |
| 3135 | `src/App.tsx` | `onLoadEntry={handleLoadIntoScene}` | 1 |

`onModqnDecisionPolicyChange={handleModqnDecisionPolicyChange}` 在來源中出現 1 次，但 validator 原有兩條相同斷言；兩條均依清單刪除。

### `docs/sdd/worker-reports/X1.md`

新建本報告；沒有修改其他報告或來源檔。

## 我沒動但有問題的

- validator 在本輪前已是紅燈：清單外的 `liveTimelineWindowStartSec + target` 在 `src/App.tsx` 為 0 次，導致 `App converts bottom timeline elapsed seek to absolute Walker time` 失敗。本輪前後都是同一失敗；依範圍與禁止 re-pin 規則留給 owner 決定。
- `scripts/audit/count-source-pins.ts` 兩個目標都因沙箱禁止其內部 `execSync('/bin/sh')` 而 exit 1；因此依工作指示改用 Node 直接讀檔計數，fallback 計數命令 exit 0。
- 目標 validator 在本輪開始前已有其他未提交修改；本輪沒有回退、覆寫或整理那些既有 WIP。
- 清單內沒有保留項目，也沒有發現需 owner 在這 38 條中另作判定的真守衛。

## 驗證

| 階段 | 指令 | Exit code | 結果 |
|---|---|---:|---|
| 修改前 | `npm run validate:frontend:scene-lane-governance` | 1 | 既有 stale pin：缺少 `liveTimelineWindowStartSec + target` |
| 修改後 | `npm run validate:frontend:scene-lane-governance` | 1 | 同一既有 stale pin；未出現新的失敗 |
| App 計數工具 | `node --import tsx/esm scripts/audit/count-source-pins.ts src/App.tsx` | 1 | 沙箱阻擋工具內部 `/bin/sh` |
| MainScene 計數工具 | `node --import tsx/esm scripts/audit/count-source-pins.ts src/scene/MainScene.tsx` | 1 | 沙箱阻擋工具內部 `/bin/sh` |
| fallback 計數 | Node 直接 `readFileSync` 後逐字面執行 `source.split(literal).length - 1` | 0 | 38 條來源字面皆為 1 次 |
| diff 格式 | `git diff --check -- scripts/validate-frontend-scene-lane-governance.ts` | 0 | 無 whitespace error |

未執行任何 `*:browser` validator，也未執行 `check:ee` / `check:visual`。

## 複合斷言與孤兒 pass 的處理

- 複合斷言：0 個。38 條都是獨立 `assertContains`，沒有從 `a.includes(X) && b.includes(Y)` 移除局部條件的情況。
- 配對 pass／孤兒 pass：0 個。檔案中沒有 `recordPass(...)`、`pass(...)`、`PASSED.push(...)` 或同類配對記錄，因此沒有留下虛假的 pass。
- 數量 ratchet：0 個。檔案中沒有 `PASSED.length >= N` 或同類計數門檻，未調整任何數字。

---

# X1 VisualLab 純 I/O 下沉報告（2026-09-07）

## 每個標的的結果

本輪把資料合約、ZIP 組裝、解析驗證、上傳 transport，以及 replay provenance/archive 下沉到不依賴 React、DOM 或 session closure 的 TypeScript 模組。元件保留瀏覽器 canvas 合成、MediaRecorder、播放重啟與狀態提示；這些是瀏覽器生命週期 adapter，不是本輪要下沉的純 I/O。

| 標的 | 純模組簽章與測試 | 驗證指令與輸出 | `tsc` | 行數變化（僅追蹤，不作成功指標） |
|---|---|---|---|---|
| `uploadFigureBundle`（原 L1194-1209） | `parseFigureBundle(raw: FigureBundleRaw): Promise<VisualLabCaptureBundle>`；`uploadVisualLabFigureBundle(input: FigureBundleUploadInput): Promise<Phase1UploadResult>`；`figureBundle.test.ts` | `node --import tsx/esm --test src/prototype/visual-lab-g0/figureBundle.test.ts`：4 tests、4 pass、0 fail | `npx tsc --noEmit -p tsconfig.json --pretty false`：exit 0、無輸出 | 元件現 L1155-1170（16 行）；upload 的 endpoint/schema/fetcher 與 bundle validation 改由純模組接收明確輸入 |
| `downloadFigureBundle`（原 L903-990） | `buildVisualLabFigureBundle(input: FigureBundleBuildInput): VisualLabCaptureBundle`；`buildVisualLabFigureBundleArchive(input): FigureBundleArchive`；`serializeVisualLabFigureBundle(bundle): Blob`；`downloadVisualLabFigureBundle(input: FigureBundleDownloadInput): Promise<FigureBundleDownloadResult>`；同一 `figureBundle.test.ts` | 同上：4 pass；包含 Blob round-trip 與 tampered digest boundary | 同一 figure checkpoint：exit 0、無輸出 | 元件現 L904-991（88 行）；canvas 合成仍在元件，bundle build/ZIP/write 已改成一個明確輸入的呼叫 |
| `downloadActiveReplay`（原 L992-1189） | `buildVisualLabReplayProvenance(input: VisualLabReplayProvenanceInput): CanvasWebmProvenanceIdentity`；`prepareVisualLabReplayBundle(input: VisualLabReplayBundleInput): VisualLabClipArchive`；`serializeReplayBundle(input): Promise<Blob>`；`downloadVisualLabReplayBundle(input): Promise<ClipArchiveWriteResult>`；`replayBundleExport.test.ts` | `node --import tsx/esm --test src/prototype/visual-lab-g0/replayBundleExport.test.ts`：3 tests、3 pass、0 fail | replay checkpoint：`npx tsc --noEmit -p tsconfig.json --pretty false` exit 0、無輸出 | 元件現 L993-1150（158 行，從 198 行減 40）；canvas/MediaRecorder capture 仍留在元件，provenance 與 archive schema 已可離開瀏覽器測試 |

兩個新純模組及測試檔目前為：`figureBundle.ts` 287 行、`figureBundle.test.ts` 101 行；`replayBundleExport.ts` 206 行、`replayBundleExport.test.ts` 137 行。元件總行數由前置複核的 1,725 行變為 1,686 行；這些數字只用來定位變更，驗收依可測性與指令結果。

另外補跑既有 export/adapter 契約：

```text
node --import tsx/esm --test \
  src/visualLab/export/captureBundle.test.ts \
  src/visualLab/export/artifactWriter.test.ts \
  src/visualLab/export/phase1Upload.test.ts \
  src/visualLab/export/clipArchive.test.ts \
  src/prototype/visual-lab-g0/figureBundle.test.ts \
  src/prototype/visual-lab-g0/replayBundleExport.test.ts
```

輸出為 `tests 11`、`pass 11`、`fail 0`、exit 0。全程未執行任何 `*:browser` validator，也未執行 `check:ee` 或 `check:visual`。

## round-trip 測試

`figureBundle.test.ts` 以 `createTestSnapshot()`、`TEST_PROFILE` 和合法 PNG signature 建立 figure archive，取得 `buildVisualLabFigureBundleArchive(...).blob`，再直接呼叫 `parseFigureBundle(archive.blob)`。實際斷言：

- parser 回傳的是不同物件，但整個 parsed bundle 與原始 bundle `deepEqual`，包含 manifest、JSON/CSV supporting data、artifact path、media type、size、SHA-256 與 bytes。
- `figureId` 還原為 `round-trip-figure`，artifact roles 完整包含 manifest、JSON、CSV、composed PNG。
- composed PNG bytes 逐 byte 還原，未只檢查檔案存在。
- 另外把 ZIP 中 PNG 最後一個 byte 改掉後重新封裝；parser 以 manifest digest mismatch 拒絕，確認不是只做 JSON/schema 形狀驗證。

## 寫不出測試因此退回的

無。
