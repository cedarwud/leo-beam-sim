## 提案表

> 本報告以目前 checkout 的 `src/` 為準，僅做靜態查核，未執行任何 `*:browser` validator。表中的 `/runtime-probe` 指 Vite dev server 會落到 `src/main.tsx` 最後 App fallback 的同源非保留 pathname；它不是新增的 UI testid。現在 `/`、`/legacy`、`/walker` 會略過 `sceneSource` query，因此 artifact 案例不能沿用舊腳本的 `/?sceneSource=artifact-replay` 前置條件。

| 行號 | 原斷言 | 進入點（URL/路由/前置操作） | 觀察點（真實存在的 selector 或屬性，附 src 檔案:行號） | 斷言內容 | 放哪個 browser validator |
|---:|---|---|---|---|---|
| 638 | `modqnReplayProofRequested: modqnReplayProofRequestActive` | **無法轉換**：目前 App 啟動一律經 `resolveHomepageInitialRuntimeState` 強制為 `sinr-experiment`，公開 lane 切換也未掛載；因此 proof toggle 不會在任何可達 App 路由出現。 | selector 本身確實存在：`[data-testid="modqn-replay-proof-viewport-toggle"]`（`src/ui/ModqnReplayCuePanel.tsx:130`）；若可掛載，結果可看 `.leo-app-shell[data-scene-lane="modqn-replay-proof"]`（`src/App.tsx:3937-3939,4007`）。 | 目前沒有可執行的使用者前置操作；不能把舊 validator 僅設定 localStorage 的做法當成覆蓋。 | 無法轉換；先恢復受支援的 MODQN App 入口，之後才放 `scripts/validate-live-walker-handover-event-focus-browser.ts`。 |
| 652 | `buildLiveWalkerHandoverEventIndex({` | **無法轉換**：此特定 builder 只走 MODQN 分支；目前可達的 SINR 分支使用另一個 cell-truth builder，不能拿 SINR 畫面冒充此斷言。 | 可見的事件 marker selector 是 `[data-testid^="timeline-event-marker-"]`（`src/ui/TimelineBar.tsx:330-342`），但目前可達頁面上的 marker 不是由這個特定 builder 路徑建立。 | 不可用「有 marker」作假綠；那只能證明 SINR cell-truth index，不證明 `buildLiveWalkerHandoverEventIndex`。 | 無法轉換；建議改判 S（builder/分支契約），或在恢復 MODQN 入口後再新增真正的 runtime 斷言。 |
| 653 | `liveWalkerHandoverEventIndexToRailEvents(liveWalkerHandoverEventIndex)` | **已被覆蓋**：開 `/`，等待自然換手時間軸完成。 | `[data-testid="timeline-bar"] [data-testid^="timeline-event-marker-"]`，marker 帶 `data-source-time-sec`、`data-click-target-sec`、`data-marker-kind`（`src/ui/TimelineBar.tsx:330-342`）。 | 既有 browser gate 已要求恰有 intra/inter 兩種 marker，且每個 source/click time 都有限並位於 duration 內；adapter 單元測試另核對 cell/beam label 映射。 | 已被覆蓋，可直接刪原文字 pin；`scripts/validate-homepage-authority-browser.ts:160-218`，另見 `src/app/liveWalkerHandoverRailAdapter.test.ts:53-82`。 |
| 656 | `producerTraceDisplayDurationSec` | **無法轉換**：只有 `modqn-replay-proof` 才會讓 producer source horizon 與 display-stretched axis 同時出現在 rail；該 lane 目前不可達。 | 若 lane 可達，真實觀察點為 `[data-testid="handover-event-rail"]` 的 `data-horizon-sec`、`data-axis-sec`、`data-axis-kind`（`src/ui/HandoverEventRail.tsx:376-393`）。 | 應斷言 `data-axis-kind="display-stretched"`、`data-axis-sec > data-horizon-sec`；但現況無合法入口，不能實作成會跑到該 DOM 的測試。 | 無法轉換；先恢復 proof 入口，再放 `scripts/validate-live-walker-handover-event-focus-browser.ts`。 |
| 661 | `horizonSec={activeTimelineDescriptor.horizonSec}` | **無法轉換**：目前所有可達 timeline descriptor 的 `horizonSec` 都等於 `durationSec`；即使移除此 prop，`TimelineBar` 也會 fallback 到 duration，DOM 完全相同。 | `[data-testid="timeline-bar"]` 的 `data-duration-sec`、`data-horizon-sec`（`src/ui/TimelineBar.tsx:217-225`）；fallback 明載於 `src/ui/TimelineBar.tsx:156-159`。 | 任何現況 browser 斷言都無法區分「prop 正確傳入」與「prop 被刪後 fallback」，所以會是假綠。 | 無法轉換；建議改判 V。若未來出現 horizon 與 display duration 不同的可達 timeline，再補行為測試。 |
| 664 | `const liveTimelineWindowStartSec = demoStartOffset;` | 開 `/runtime-probe?sceneSource=live-sim`；先把 `leo-beam-sim.profile-by-app-mode.v1` 設為 `{"sinr-experiment":"hobs-2024-mobile-demo-aircraft"}`。該 profile 的固定 offset 是 450 秒（`src/profiles/hobs-2024-mobile-demo-aircraft.json:119`）。 | `[data-testid="walker-results-rail"]@data-sim-time-sec`（`src/ui/signal-tuning/WalkerResultsRail.tsx:176-189`）與 `[data-testid="timeline-bar"]@data-current-time-sec`（`src/ui/TimelineBar.tsx:217-221`）。 | 暫停後等待兩者穩定，斷言 `Number(simTimeSec) - Number(currentTimeSec) === 450`（容許顯示精度誤差），而非只斷言 timeline 從 0 開始。 | 新增 `scripts/validate-live-timeline-window-offset-browser.ts`，集中承接 L664-L667。 |
| 665 | `simState.simTimeSec - liveTimelineWindowStartSec` | 同 L664；以 `[data-testid="timeline-scrubber"]` seek 到 120 秒，等待 source frame 落地。 | `[data-testid="timeline-current-time"]`（`src/ui/TimelineBar.tsx:287-289`）、timeline 的 `data-current-time-sec`（`src/ui/TimelineBar.tsx:220`）、Walker rail 的 `data-sim-time-sec`（`src/ui/signal-tuning/WalkerResultsRail.tsx:189`）。 | 斷言文字為 `2:00`，數值約 120，source sim time 約 570；也就是 UI 顯示 elapsed 120，不是 absolute 570。 | 新增 `scripts/validate-live-timeline-window-offset-browser.ts`。 |
| 666 | `demoStartOffsetSec: demoStartOffset` | 同 L664；依序 seek elapsed 60 與 120 秒。 | `[data-testid="timeline-scrubber"]`（`src/ui/TimelineBar.tsx:353-365`）、`.leo-app-shell@data-live-timeline-seek-target`（`src/App.tsx:3975`）、`[data-testid="walker-results-rail"]@data-sim-time-sec`（`src/ui/signal-tuning/WalkerResultsRail.tsx:189`）。 | 第一次 target 應為 510、第二次為 570，且每次 `sourceSimTime - timelineElapsed` 都維持 450；證明 seek 沒有重設/漂移 window start。 | 新增 `scripts/validate-live-timeline-window-offset-browser.ts`。 |
| 667 | `const absoluteTargetSec = Math.min(` | 同 L664；點 `[data-testid="timeline-jump-end"]`。這比直接把 range 填到超過 max 更能真的走 App 的 absolute-target clamp。 | jump 按鈕（`src/ui/TimelineBar.tsx:273-280`）、`.leo-app-shell@data-live-timeline-seek-target`（`src/App.tsx:3975`）、timeline 的 `data-duration-sec` 與 `data-current-time-sec`（`src/ui/TimelineBar.tsx:220-221`）。 | offset=450 時仍斷言 seek target 為 `7200.000`，不得是 7650；落地後 current 不超過 duration，且沒有 page error。現有 jump-end 腳本用 offset=0，無法分辨這個 bug。 | 新增 `scripts/validate-live-timeline-window-offset-browser.ts`；可重用 `scripts/validate-timeline-jump-end-browser.ts` 的 transport helper，但不能把其 offset=0 案例算成覆蓋。 |
| 670 | `if (sceneLane === 'sinr-live' \|\| sceneLane === 'modqn-live-cell-preview') return liveWalkerHandoverRailEvents;` | **無法轉換**：目前 `modqn-live-cell-preview` 沒有可達入口，而且 live lane 上 `HandoverEventRail` 也未掛載；只測 SINR timeline marker 不能證明兩個 lane 的 routing。 | rail selector 確實存在：`[data-testid="handover-event-rail"]`（`src/ui/HandoverEventRail.tsx:372-385`），但目前只在 artifact sidebar 的 `{handoverEventRail}` 掛載（`src/App.tsx:4477-4490`）。 | 必須同一個 gate 真的進入兩個 lane 並比較 rail 的 owner/event telemetry；現況做不到。 | 無法轉換；建議改判 S，或先恢復 MODQN/live rail 的 runtime 入口後再放 `scripts/validate-live-walker-handover-event-focus-browser.ts`。 |
| 671 | `if (sceneLane === 'modqn-replay-proof') return modqnHandoverRailEvents;` | **無法轉換**：`modqn-replay-proof` 目前不可達。舊 browser validator 的 proof 點擊前置條件本身不會掛載。 | 若可達，應看 `[data-testid="handover-event-rail"]@data-source-owner="modqn-producer-trace"` 及 `[data-testid^="handover-event-marker-"]` 的 `data-source-time-sec`（`src/ui/HandoverEventRail.tsx:376-385,463-476`）。 | 應要求 `data-event-count > 0`、marker 數 > 0，且 owner/claim 為 producer；現有腳本對 marker 使用條件式 `if (count > 0)`，不構成此項覆蓋。 | 無法轉換；恢復 proof 入口後補到 `scripts/validate-live-walker-handover-event-focus-browser.ts`。 |
| 676 | `durationSec={timelineRailDescriptor.rail.durationSec}` | 開 `/runtime-probe?sceneSource=artifact-replay`，等待 `[data-testid="artifact-replay-sidebar"][data-artifact-loaded="true"]`（`src/App.tsx:4173-4178`）。 | `[data-testid="handover-event-rail"]@data-horizon-sec`（`src/ui/HandoverEventRail.tsx:376-380`）與 `[data-testid="timeline-bar"]@data-duration-sec`（`src/ui/TimelineBar.tsx:217-221`）。 | 斷言兩個數值有限、正值且相等；另與 artifact playback status 顯示的 scenario duration 相符。 | 補進 `scripts/validate-phase-c-artifact-scene-real-data-browser.ts`；先把舊的 root query 入口改為可達的 App fallback path。 |
| 677 | `sourceLabel={timelineRailDescriptor.rail.sourceLabel}` | 同 L676；使用含 handover event 的 artifact。 | `[data-testid="handover-event-rail"]@data-source`（`src/ui/HandoverEventRail.tsx:376-378`）；同一值也顯示於 `.leo-handover-event-rail__source`（`src/ui/HandoverEventRail.tsx:412`）。 | 斷言 `data-source === "artifact event index"`，且可見 header 文字相同；不要使用不存在的 `[data-testid="rail-source-label"]`。 | 補進 `scripts/validate-phase-c-artifact-scene-real-data-browser.ts`。 |
| 678 | `sourceOwner={timelineRailDescriptor.rail.sourceOwner}` | 同 L676。 | `[data-testid="handover-event-rail"]@data-source-owner`（`src/ui/HandoverEventRail.tsx:376-379`）與 `[data-testid="timeline-bar"]@data-source-owner`（`src/ui/TimelineBar.tsx:217-223`）。 | 兩者皆應為 `artifact-replay`；同時 shell 的 `data-scene-lane` 應為 `artifact-replay`（`src/App.tsx:3939`）。 | 補進 `scripts/validate-phase-c-artifact-scene-real-data-browser.ts`。 |
| 679 | `sourceGapReasons={timelineRailDescriptor.rail.sourceGapReasons}` | `page.route` 取回有效 artifact JSON 後只把頂層 `events` 改成 `[]`，以 200 回傳；開 `/runtime-probe?sceneSource=artifact-replay`。 | `[data-testid="handover-event-rail"]@data-source-gap-count`（`src/ui/HandoverEventRail.tsx:376-385`）、`[data-testid="handover-event-rail-source-gap"]`（`src/ui/HandoverEventRail.tsx:419-422`）。 | 斷言 gap count 為 `1`，警示可見且文字精確為 `Source gap: loaded artifact has no validated handover event index.`；不要使用不存在的 `[data-testid="source-gap-warning"]`。 | 補進 `scripts/validate-phase-c-artifact-fail-closed-browser.ts`，作為 route-mocked 200/no-event 子案例。 |
| 680 | `const timelineDurationSec = activeTimelineDescriptor.durationSec;` | 同一 browser context 先開 `/runtime-probe?sceneSource=live-sim` 取 Walker timeline，再開 `/runtime-probe?sceneSource=artifact-replay` 取 artifact timeline。 | `[data-testid="timeline-bar"]` 的 `data-duration-sec`、`data-source-owner` 及 `[data-testid="timeline-duration"]`（`src/ui/TimelineBar.tsx:217-225,293-295`）。 | Walker 應為 owner `live-walker`、duration `7200.000`、文字 `2:00:00`；artifact 應為 owner `artifact-replay` 且 duration 等於 artifact scenario（現有基線約 60–120 秒），兩者不得相同。 | 補進 `scripts/validate-phase-c-artifact-scene-real-data-browser.ts`，以兩個隔離 page/context 比較。 |
| 684 | `const activeTimelineDescriptor = isArchivedTleSceneActive` | **無法轉換**：首頁 source switch 明確設為不可見，且 `readHomepageSimulationSourceMode()` 在此狀態永遠回 Walker；localStorage 也無法強制 TLE。 | selector 雖存在於未掛載元件：`[data-testid="simulation-source-toggle-archived-tle"]`（`src/ui/SimulationSourceToggle.tsx:42-49`）；若可達，timeline owner 在 `[data-testid="timeline-bar"]@data-source-owner`（`src/ui/TimelineBar.tsx:217-223`）。關閉旗標在 `src/app/simulationSourceMode.ts:11-15,41-45`。 | 目前沒有合法操作能讓 App 產生 `data-source-owner="archived-tle-run"`，因此 browser 提案必然卡在前置條件。 | 無法轉換；建議改判 S（parked integration contract）或 V（若首頁確定只保留 Walker）。純 descriptor 已有單元測試，但不等於 App runtime 覆蓋。 |
| 686 | `stepSec={isArchivedTleSceneActive` | **無法轉換**：同 L684，Archived TLE 分支不可達。 | 可讀的真實 range 是 `[data-testid="timeline-scrubber"]`，其 native `step` 由 `scrubStepSec` 決定（`src/ui/TimelineBar.tsx:353-363`）；Archived TLE 未掛載時只能看到一般 Walker 的 step。 | 只有同一 App 能切換 TLE/Walker 時，才可斷言 TLE range `step="1"`、transport aria-label 為 30 秒，而 Walker 為預設 10 秒。現況只能測到後者，會是假綠。 | 無法轉換；建議改判 S，待 source switch 重開後放 `scripts/validate-homepage-authority-browser.ts`。元件步進已有 `src/ui/TimelineBar.test.tsx:67-80`，但未覆蓋 App 條件分支。 |
| 907 | `<TrainingTelemetryFeed enabled={appMode === 'modqn-demo'}` | **無法轉換**：Feed 是 headless，唯一可觀察結果需先進入 `modqn-demo`；目前 App 無可達 MODQN 入口。 | 下游真實觀察點為 `[data-testid="live-telemetry-panel"][data-job-id]` 與 `[data-testid="live-telemetry-status-badge"]@data-telemetry-status`（`src/showcase/dashboard/LiveTelemetryPanel.tsx:182-194`）。 | 舊 `validate-phase-d-live-telemetry-browser.ts` 只在 init script 寫 localStorage；目前 App 仍強制 SINR，所以不能算現行覆蓋。要測此項，必須先有受支援的 MODQN 入口，再 route-mock `/jobs` 與 SSE，並以 SINR context 做「零請求」負控制。 | 無法轉換；建議先恢復 MODQN test/runtime entry，再沿用 `scripts/validate-phase-d-live-telemetry-browser.ts`。 |
| 920 | `r.headers.get('X-Showcase-Artifact-Source')` | `page.route('**/showcase-artifacts/visual-showcase-v1.json')` 以原有效 JSON 回傳，但覆寫 response header 為 `X-Showcase-Artifact-Source: external-artifact-path`；開 `/runtime-probe?sceneSource=artifact-replay`。 | `.leo-app-shell@data-artifact-source`（`src/App.tsx:3937,3953-3955,4007`）與 `[data-testid="artifact-source-badge"]@data-artifact-source`（`src/ui/ArtifactSourceBadge.tsx:87-91`）。 | 兩個屬性都必須精確等於 `external-artifact-path`，且 alert 文字包含該 token；這能抓到把 header 讀取改成硬編碼的回歸。 | 補進 `scripts/validate-phase-c-artifact-fail-closed-browser.ts`，新增 route-mocked 200/custom-header 子案例。 |
| 925 | `artifactSource !== PRODUCER_PINNED_SOURCE` | 同 L920，但 header 設為 `synthetic-fixture-fallback`。 | `[data-testid="artifact-source-badge"]`，`role="alert"` 與 `data-artifact-source`（`src/ui/ArtifactSourceBadge.tsx:83-105`）；shell source attr 在 `src/App.tsx:3953-3955`。 | 斷言 badge 可見、source 精確相符，標題含 `Synthetic fixture — not producer data`；另用 `producer-pinned` 子案例斷言 badge 數量為 0。既有 pure unit 只守 decision function，未守 App fetch→state→mount 整合。 | 補進 `scripts/validate-phase-c-artifact-fail-closed-browser.ts`。 |
| 930 | `r.headers.get('X-Showcase-Artifact-Source') ?? HEADER_ABSENT_SOURCE` | 同 L920，但回傳 200 有效 JSON時移除所有大小寫形式的 `X-Showcase-Artifact-Source` header。 | `.leo-app-shell@data-artifact-source`（`src/App.tsx:3953-3955`）、`[data-testid="artifact-source-badge"]@data-artifact-source`（`src/ui/ArtifactSourceBadge.tsx:90-91`）、artifact loaded 狀態（`src/App.tsx:4177-4178`）。 | 等待 loaded=true 後斷言 shell 與 badge 都是 `header-absent`，badge 可見且文字含 `Unknown artifact source — no source header`；不得停在 `pending` 或被誤標成 synthetic。 | 補進 `scripts/validate-phase-c-artifact-fail-closed-browser.ts`。 |

## 無法轉換的

- **L638**：proof toggle selector 存在，但 App 現況沒有可達的 MODQN live/proof 入口。舊腳本的 localStorage 前置已被 `resolveHomepageInitialRuntimeState` 覆蓋，建議暫改 S；若產品確定移除該入口則改 V。
- **L652**：可達 SINR marker 來自 cell-truth builder，不是此行的 MODQN `buildLiveWalkerHandoverEventIndex`；拿它代替會是假綠。建議改 S。
- **L656**：source horizon 與 display axis 的差異只在不可達 proof rail 可見。入口恢復前建議改 S。
- **L661**：現況 `horizonSec === durationSec`，且元件缺 prop 時會自動 fallback 到 duration；DOM 行為無法辨認此 wiring 是否存在。建議改 V。
- **L670**：要求同時守兩個 live lane，但 MODQN lane 不可達，且 live rail 沒有掛載。建議改 S。
- **L671**：proof lane 不可達；此外舊 browser gate 對 marker 是「有才測」，沒有 fail-loud event-count 要求。入口恢復前建議改 S。
- **L684**：`HOMEPAGE_SIMULATION_SOURCE_SWITCH_VISIBLE = false`，首頁只允許 Walker，localStorage 不能進入 archived TLE。建議依 owner 決策改 S 或 V。
- **L686**：與 L684 相同；只能測一般 Walker step，無法測條件切換。建議改 S。
- **L907**：headless feed 必須藉由 MODQN telemetry 下游觀察，但 MODQN App 入口不可達。現有 phase-d browser 腳本的前置已失效，建議先恢復入口再沿用，暫改 S。

## 已被覆蓋的

- **L653**：`scripts/validate-homepage-authority-browser.ts:160-218` 在真實首頁等待並檢查 adapter 產出的 intra/inter timeline markers、source time、click target、可操作狀態；`src/app/liveWalkerHandoverRailAdapter.test.ts:53-82` 另直接守住 cell/beam label 轉換。兩者合起來已守住「index 經 adapter 成為可見事件」，原 App 文字 pin 可直接刪除。

## 統計

可轉換 **12** ／ 無法轉換 **9** ／ 已被覆蓋 **1**（總數 **22**）。
