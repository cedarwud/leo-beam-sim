本批共 10 列

| 來源 | 原斷言（截 60 字） | 歸屬 | 理由與具體做法（繁中） |
| :--- | :--- | :---: | :--- |
| L935 | `artifactSource === SYNTHETIC_FIXTURE_SOURCE` | **BLOCKED** | **卡在缺乏可信的 App 整合接線測試或綠色 E2E 閘門。**<br>子元件單元測試（`ArtifactSourceBadge.test.ts`）僅能驗證 Badge 收到 prop 後的文案分流，無法證明 `App` 有正確計算並傳入該值。在 20/26 browser 閘門皆紅且無 App 容器測試的情況下，無法保證接線不被破壞。需待建立 App 層級整合測試或確認對應 E2E 閘門轉綠後方可放行。 |
| L1047 | `sceneLane === 'modqn-live-cell-preview' ? 'overlay-demo' : 'live-truth'` | **BLOCKED** | **卡在 MODQN 入口不可達與缺乏產品生命週期決策。**<br>提案因不可達而企圖改判 V 屬於「以測試套件殘缺掩蓋業務邏輯防線」。若此分支為有效功能，重構寫死會破壞分流；需先由 Owner 確認 MODQN 是否為廢棄死碼（若是方可判 V），或修復 MODQN 路由與紅閘門後以動態行為測試接手。 |
| L1091 | `data-live-director-focus-claim={directorFocusEnabled ? ...}` | **BLOCKED** | **卡在依賴之 validator 為紅閘門，且既有測試為靜態取樣假綠。**<br>現有測試僅在操作前讀取靜態初始值，若重構將三元式寫死為 `'live-truth'` 測試依然全綠，完全無法守護 `directorFocusEnabled` 開關邏輯。必須先修復該紅閘門，並在測試中補上切換開關前後的 DOM 屬性變化斷言。 |
| L1390 | `proofViewportActive={sceneLane === 'modqn-replay-proof'}` | **BLOCKED** | **卡在 MODQN replay-proof 分支不可達。**<br>原斷言守護 Viewport Active 狀態與 `sceneLane` 的綁定。不能因路徑不可達就判定刪除（V）。需先修復 MODQN 相關測試路徑，並補上切換至該 lane 時驗證 Viewport Active 行為的測試。 |
| L1391 | `canToggleModqnReplayProof ? setModqnReplayProofRequested : undefined` | **BLOCKED** | **卡在安全權限控制的行為驗證缺失與路徑不可達。**<br>原斷言守護的是無權限時傳遞 `undefined` 禁制回呼的安全防線。若直接刪除，權限失效將無人察覺。需在測試環境中建立「無權限狀態下點擊不觸發行為」的整合測試後才能解除文字 pin。 |
| L1392 | `getLeftSidebarTabsForSceneLane(sceneLane, handoverMode)` | **BLOCKED** | **卡在缺乏 App/Layout 整合接線測試及 Browser 閘門多數處於紅燈。**<br>`appRuntimeModel.test.ts` 僅能證明純函式映射正確，無法證明 `App.tsx` 有呼叫並將 tabs 正確注入 Left Sidebar。在缺乏綠色閘門驗證 UI 實際渲染 tabs 的情況下，純單元測試不能算作 U。 |
| L1393 | `getRightSidebarTabsForSceneLane(sceneLane, handoverMode)` | **BLOCKED** | **卡在缺乏 Right Sidebar 的整合渲染接線測試。**<br>同 L1392，純函式單元測試無法守護 App 端的參數傳遞與元件接線。需待建立針對 Sidebar 實際渲染 tabs 的整合測試或修復對應閘門後才能替換。 |
| L1413 | `sceneLane === 'modqn-live-cell-preview' && <ServiceStatusBanner appMode={appMode} />` | **BLOCKED** | **卡在 MODQN live cell preview 入口不可達。**<br>此處守護條件掛載防線。若改判 V，元件被誤刪或漏掛將完全失守。需待 MODQN 路由打通並具備驗證 Banner 掛載/卸載的行為測試後方能解封。 |
| L1469 | `axisPlaying={!playback.paused}` | **BLOCKED** | **卡在現有可達測試路徑未涵蓋播放/暫停互動。**<br>不可因現有測試路徑中狀態恆為 false 就放棄狀態同步契約。需補寫或修復包含 Play/Pause 互動操作的測試，動態驗證播放狀態與 `axisPlaying` 同步後，才能移除此 pin。 |
| Y6:L653 | `liveWalkerHandoverEventIndexToRailEvents(liveWalkerHandoverEventIndex)` | **BLOCKED** | **卡在目標閘門 `validate-homepage-authority-browser.ts` 為紅閘門且首頁測試走錯分支（假綠）。**<br>首頁測試走 SINR cell-truth 分支，完全覆蓋不到 Walker Handover 分支。必須先修復該紅閘門並新增針對 Walker Handover 分支的 Timeline Marker 渲染測試；單純將資料轉換抽出為 U 無法保證接線。 |

小計: S=0 U=0 V=0 BLOCKED=10

---

## 我最不確定的 3 條

1. **L1047 / L1413（MODQN 分流與 ServiceStatusBanner 條件渲染）**
   * **不確定點**：目前無法確認 MODQN 是否屬於已放棄的實驗性死碼（Dead Code）。若是產品已決定廢棄的死碼，其最終歸屬應為 **V（刪除）**；但若是正在開發中或暫時被關閉入口的正式功能，直接判 V 會造成實質回歸。在缺乏 Product Owner 明確裁決前，只能從嚴判為 **BLOCKED**。
2. **L1392 / L1393（Sidebar Tabs 決策函式呼叫與接線）**
   * **不確定點**：底層 mapping 函式已有純函式測試覆蓋，爭議點在於「App 呼叫端接線」。如果專案架構允許將這段接線重構成自定義 Hook（如 `useSidebarTabs`）並以 Node 環境的 `renderHook` 進行模組測試，它有機會升格為 **U**；但在目前 20/26 browser 閘門皆紅且缺乏 Hook 層級測試約定前，無法單靠現有純函式測試放行，故暫列 **BLOCKED**。
3. **Y6:L653（`liveWalkerHandoverEventIndexToRailEvents` 轉換）**
   * **不確定點**：此轉換本質上是標準的純資料處理函式，極適合以 **U（單元測試）** 完整驗證各種 Index 輸入與 RailEvents 輸出。然而提案試圖以「首頁既有測試已覆蓋」為由刪除 pin，卻撞上「走錯分支（假綠）」與「依賴紅閘門」的雙重違規。若治理團隊同意將其拆解為「純函式以 U 接手 + 接線另立 issue 追蹤」，此條可轉 U；但依嚴格治理紀律（接線未明前不假定綠燈），目前必須判 **BLOCKED**。
