本批共 16 列

| 來源 | 原斷言（截 60 字） | 歸屬 | 理由與具體做法（繁中） |
| :--- | :--- | :---: | :--- |
| `validate-phase-b-artifact-picker.tsx:L219` | `source.includes('<ArtifactPicker')` | **BLOCKED** | 卡在進入點 `lane-experience-modqn-live-cell-preview` 屬未掛載死碼，執行期永遠無法定位。需待產品 owner 釐清 ArtifactPicker 與該 lane 是待接線功能還是應清理之死碼，並提供有效進入點後方可建立行為斷言。 |
| `validate-phase-b-artifact-picker.tsx:L221` | `source.includes('onLoadEntry={handleLoadIntoScene}')` | **BLOCKED** | 卡在（1）進入點為死碼 selector；（2）提案僅驗證局部 UI 屬性與文字，無法驗證 `handleLoadIntoScene` 是否真正將資料注入 3D 場景（實質已壞但測試仍綠）。需待有效進入點修復，並建立能觀測 3D 場景狀態注入的綠色整合/browser 閘門。 |
| `validate-phase-b-artifact-picker.tsx:L225` | `source.includes('onLoadPaperFaithful={handleRevertToPaperFaithful}')` | **BLOCKED** | 卡在提案僅斷言按鈕屬性與面板文字改為 `paper-faithful`，無法保證 3D 模擬場景的物理/模型參數被真實重置。在缺乏可檢驗 3D 物理參數重置的觀察點與綠色 browser 閘門前不可替代。 |
| `validate-phase-b-artifact-picker.tsx:L229` | `source.includes('artifactReplaySource={showcaseArtifactSource}')` | **BLOCKED** | 卡在進入點與觀察點 `modqn-view-modqn-replay-proof` 皆為未掛載死碼，執行期不可見。需 owner 釐清回放證明視圖之產品狀態與生命週期，目前無可執行的行為觀察路徑。 |
| `validate-modqn-omega-s3-replay-mode-wiring.tsx:L262` | `appSrc.includes('data-handover-criterion')` | **BLOCKED** | 卡在提案依賴的進入點 `advanced-setup-trigger` 完全不存在於 `src/`。缺乏真實 UI 觸發入口導致行為測試無法執行，需開發端修復或指出正確之操作路徑。 |
| `validate-modqn-omega-s3-replay-mode-wiring.tsx:L271` | `appSrc.includes("if (handoverMode !== 'decision-overlay-on-live-sinr')")` | **BLOCKED** | 卡在進入點 `advanced-setup-trigger` 不存在於 `src/`，無法透過 UI 操作路徑驗證換手模式切換時 cue panel 的條件渲染與抑制行為。 |
| `validate-modqn-phase7k-replay-scene-layer.ts:L419` | `assertContains(appSource, "if (handoverMode !== 'decision-overlay-on-live-sinr')", ...)` | **BLOCKED** | 卡在進入點 `advanced-setup-trigger` 在 `src/` 中完全不存在，缺乏可運行的 runtime 驗證路徑與對應通過的 browser 閘門。 |
| `validate-modqn-omega-s4-heuristic-not-paper.tsx:L230` | `handoverMode === 'omega-heuristic' && sceneLane === 'modqn-live-cell-preview' && <HeuristicNotPaperBanner ...>` | **BLOCKED** | 卡在雙重無效 selector：依賴未掛載之死碼分頁（`lane-experience-modqn-live-cell-preview`）與不存在之 trigger（`advanced-setup-trigger`），執行期無法觸發並驗證警示 Banner 條件渲染。 |
| `validate-phase-b-jobs-panel.tsx:L205` | `appSource.includes('<AdvancedSetupDrawer')` | **BLOCKED** | 卡在觀察點與觸發點使用的 `advanced-setup-drawer` 及 `advanced-setup-trigger` 在 `src/` 中皆完全不存在。需產品 owner 確認該抽屜元件是否已廢棄或處於未實作狀態。 |
| `validate-phase6b-handover-policy-controls.tsx:L148` | `assertContains(appSource, 'parameterSection={');` | **BLOCKED** | 卡在現有 formula-layout 提案嚴重削弱（僅量測 tabs 排版可見度，未驗證策略控制項是否注入與可用），且 26 個 browser 閘門中僅 6 個綠，缺乏可信賴的整合測試接手此 wiring 守衛。 |
| `validate-modqn-handover-story-layer.ts:L555` | `ueCountByCellId={modqnServiceMap.ueCountByCellId}` | **BLOCKED** | 卡在雙重障礙：（1）進入點包含不存在的 `advanced-setup-trigger`；（2）提案僅驗證 badge 數值總和屬嚴重削弱（各 cell 間分配錯誤時測試仍會呈綠）。需修復路徑並提供針對各 cell 分布的精確行為斷言。 |
| `validate-phase-h-s8-hud-camera-default.ts:L27` | `source.includes("camera.selectCameraPreset('oblique')")` | **BLOCKED** | 卡在進入點使用未掛載死碼 `lane-experience-modqn-live-cell-preview`，執行期無法定位點擊以觸發預設相機視角（oblique）切換並觀測 canvas 屬性。 |
| `validate-phase-h-s8-hud-camera-default.ts:L77` | `source.includes('<ModqnSceneHud')` | **BLOCKED** | 卡在進入點依賴未掛載之死碼 selector，執行期無法切換模式以觀測 `[data-testid="modqn-scene-hud"]` 的真實渲染與掛載。 |
| `validate-timeline-scrubbing.tsx:L147` | `const target = clampTimelineTime(targetSec, timelineDurationSec);` | **BLOCKED** | 卡在純函式單元測試（`timelineRailAuthority.test.ts`）僅能保證函式本身計算正確，無法證明 App 拖曳事件處理器有確實呼叫 clamp；現有 `timeline-jump-end` 僅測按鈕點擊。在缺乏綠色時間軸拖曳行為/整合測試前不可直接刪除或標為覆蓋。 |
| `validate-phase-i-s5b-cell-beam-cones.tsx:L413` | `cellBeamConeCount={showCellOverlay ? String(renderedCellBeamConeCount) : ''}` | **BLOCKED** | 卡在操作路徑依賴死碼 selector（`lane-experience-modqn-live-cell-preview`）與不存在之 trigger（`advanced-setup-trigger`），無法於執行期觸發並觀測 canvas 之 `data-cell-beam-cone-count`。 |
| `validate-phase-b-training-form.tsx:L164` | `<AdvancedSetupDrawer` | **BLOCKED** | 卡在依賴的 3 個 selector 皆無效（trigger 與 drawer 不存在、live-cell 為死碼）。在產品 owner 釐清進階設定抽屜與訓練表單之真實進入路徑前無法建立有效斷言。 |

小計: S=0 U=0 V=0 BLOCKED=16

---

## 我最不確定的 3 條

1. **`validate-timeline-scrubbing.tsx:L147` (`const target = clampTimelineTime(...)`)**
   - **不確定原因**：此項為全批中最接近 **U（單元測試接手）** 的案例。純運算邏輯已有 `timelineRailAuthority.test.ts` 覆蓋。若專案將拖曳狀態機或 reducer 抽離為純模組，即可透過單元測試直接測「輸入越界 targetSec $\rightarrow$ 輸出 clamped targetSec」。
   - **為何仍判 BLOCKED**：原文字斷言守的是「App 拖曳事件是否有接上此 clamp 呼叫」。在目前無法確認是否有綠色時間軸拖曳整合/browser 閘門守衛接線的情況下，直接判 U 會違反「單元測試無法證明呼叫端接上即不算 U」的鐵律，因此必須誠實卡在缺乏拖曳事件接線驗證閘門上。

2. **`validate-phase-b-jobs-panel.tsx:L205` / `validate-phase-b-training-form.tsx:L164` (`<AdvancedSetupDrawer`)**
   - **不確定原因**：`advanced-setup-drawer` 與 `advanced-setup-trigger` 在 `src/` 中完全不存在。若該抽屜功能早已在過去重構中被產品端徹底放棄，此斷言實質上已是守護幽靈程式碼的無效 pin，可直接判 **V（刪除）**。
   - **為何仍判 BLOCKED**：在無法檢視完整 AST 與 git 歷史的前提下，無法排除該元件只是更名、或作為未完成的 WIP 功能等待掛載。貿然判 V 可能誤刪未來的關鍵守衛，必須由產品 owner 明確下達廢棄裁決。

3. **`validate-phase6b-handover-policy-controls.tsx:L148` (`parameterSection={`)**
   - **不確定原因**：此斷言守的是換手策略控制項注入 Drawer 的 props 接線。現有提案聲稱由 `sinr-formula-page` 相關測試覆蓋，存在「可能已有現成 layout 測試」的模糊空間。
   - **為何仍判 BLOCKED**：複核已明確指出 formula-layout 僅量測 tabs 排版可見度，若傳入假元件該測試依然全綠，屬於典型的「假保護／削弱」。加上目前 26 個 browser 閘門只有 6 個是綠的，無法指派任何未經驗證為綠色的 browser 閘門接手，故判定卡在缺乏有效的整合驗證閘門。
