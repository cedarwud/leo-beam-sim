本批共 11 列

| 來源 | 原斷言（截 60 字） | 歸屬 | 理由與具體做法（繁中） |
| :--- | :--- | :---: | :--- |
| scene-lane L2385 | `modqnVisualLayerPreset === 'explain-handover'` | BLOCKED | **卡在無效 selector 與紅閘門**：(1) 前置操作依賴的 `[data-testid="advanced-setup-trigger"]` 不存在於 `src/`，缺乏進入 preset 控制項的有效途徑；(2) 目標落點 `validate-phase-3-overlay-render-browser.ts` 目前為紅閘門（全庫僅 6/26 綠燈），無法作為接手守衛。需待 UI 補齊 testid 並修復 Phase 3 閘門後方能轉為 E2E 行為斷言。 |
| scene-lane L2396 | `beamConeScope: 'focus-satellite'` | BLOCKED | **卡在 Three.js/WebGL 粒子狀態無 DOM 觀察點**：一般 cone scope 屬性不屬於上傳粒子元件，且場景內部 WebGL 粒子擴散在 DOM 無 runtime 訊號（若強行用 DOM 替代會造成粒子錯誤擴散時仍假綠）。需待建立 Three.js 狀態檢驗機制或場景視覺回歸綠閘門後才能驗證接線。 |
| scene-lane L3236 | `<AdvancedSetupDrawer` | BLOCKED | **卡在 UI 元件掛載狀態與 testid 缺失**：提案依賴的 `advanced-setup-trigger` 與 `advanced-setup-drawer` 在 `src/` 均不存在。需由 Owner 確認該 Drawer 是「有效功能但缺 data-testid」（補齊後由組件/整合測試接手）或是「未掛載的遺留死碼」（確認後連同組件清理並判 V）。 |
| scene-lane L3244 | `showDecisionPolicyControls={sceneLane === 'modqn-live-cell-...` | BLOCKED | **卡在容器 selector 缺失致使接線無法驗證**：觀察點容器 `advanced-setup-drawer` 在 `src/` 不存在，無法在 DOM 驗證條件式是否正確控制子面板渲染。需待 Drawer 容器與觸發入口 testid 補齊後，由組件測試驗證「當 `sceneLane === 'modqn-live-cell-preview'` 時渲染 Policy 控制項，其餘 lane 隱藏」之接線。 |
| L1965 | `resolveTriggeredIntraConeItems({` | U | **由純函式單元測試接手，解耦無效 DOM 監聽**：<br>1. **測試函式**：`resolveTriggeredIntraConeItems(params)`。<br>2. **輸入與斷言**：輸入包含衛星波束角度、UE 座標與觸發閾值的參數物件；斷言回傳精確落入錐體內的觸發項目陣列。<br>3. **接線守護**：原提案依賴不存在的 `sinr-live-manual-status`；呼叫端接線改由 `useSinrLiveSimulation` Hook 的單元測試（傳入 mock 事件並斷言 state 更新）守護。 |
| L2117 | `const showUav = sceneLane === 'sinr-live';` | U | **由場景配置解析單元測試接手**：<br>1. **測試函式**：提取 `resolveShowUav(sceneLane: string): boolean`。<br>2. **輸入與斷言**：輸入 `'sinr-live'` 斷言回傳 `true`；輸入 `'modqn-live-cell-preview'` 或其他 lane 斷言回傳 `false`。<br>3. **接線守護**：在場景容器（`SceneContainer`）單元測試中注入 `sceneLane="sinr-live"` 並斷言 UAV 渲染群組為 active，避開未掛載的 `lane-experience-*` 死碼按鈕。 |
| L2158 | `MODQN_SERVICE_ALLOCATION_PRODUCER_READY \|\| readModqnServiceAllocationOverrideFromUrl(...)` | U | **補齊真值表單元測試，修復削弱並守護常數**：<br>1. **測試函式**：`resolveServiceAllocationActive(producerReady, urlOverride)`。<br>2. **輸入與斷言**：<br>&emsp;• `(true, null)` $\to$ `true`（**修復削弱**：驗證常數接線，常數被刪立即變紅）；<br>&emsp;• `(false, null)` $\to$ `false`；<br>&emsp;• `(false, true)` $\to$ `true`（URL 正向覆寫）；<br>&emsp;• `(true, false)` $\to$ `false`（URL 負向覆寫）。<br>3. **接線守護**：常數集中於 config 模組，由 App 根狀態初始化單元測試驗證傳入該常數。 |
| L2174 | `showUeCounts={modqnVisualLayers.ueCountBadges && showModqnServiceAllocation}` | U | **由圖層邏輯與組件單元測試接手**：<br>1. **測試函式**：`resolveShowUeCounts(badgesEnabled: boolean, allocationEnabled: boolean): boolean`。<br>2. **輸入與斷言**：驗證 `(true, true) => true`，其餘 3 種組合皆為 `false`。<br>3. **接線守護**：在 `ModqnOverlay` 組件測試中直接注入 `showUeCounts={true/false}` 斷言 Badge DOM 渲染與否，無需經由不存在的 Drawer trigger 操作。 |
| L2179 | `modqnServedUeCount={showModqnServiceAllocation ? modqnServiceMap.servedUeCount : 0}` | U | **改以精確值斷言單元測試，修復削弱**：<br>1. **測試函式**：`resolveServedUeCountProp(showAllocation: boolean, servedCount: number): number`。<br>2. **輸入與斷言**：<br>&emsp;• `(true, 42)` $\to$ 精確斷言輸出為 `42`（**修復削弱**：禁止僅斷言 `>0`，防範誤傳 `1` 或總 UE 數）；<br>&emsp;• `(false, 42)` $\to$ 斷言輸出為 `0`。<br>3. **接線守護**：在 Canvas 圖層單元測試中傳入 mock 數值 `42`，斷言 Canvas 屬性 `data-modqn-served-ue-count="42"`。 |
| L2184 | `modqnIdleUeCount={showModqnServiceAllocation ? modqnServiceMap.idleUeCount : 0}` | U | **補足雙分支單元測試，修復削弱**：<br>1. **測試函式**：`resolveIdleUeCountProp(showAllocation: boolean, idleCount: number): number`。<br>2. **輸入與斷言**：<br>&emsp;• `(true, 17)` $\to$ 精確斷言輸出為 `17`（**修復削弱**：補回原提案放棄的正向分支，防範寫死 `0`）；<br>&emsp;• `(false, 17)` $\to$ 斷言輸出為 `0`。<br>3. **接線守護**：在圖層單元測試中注入 mock 數值 `17`，斷言 Canvas 屬性 `data-modqn-idle-ue-count="17"`，停用時斷言為 `"0"`。 |
| L2189 | `modqnServiceMapEnabled={showModqnServiceAllocation && modqnVisualLayers.serviceMap ? '1' : '0'}` | U | **由屬性轉換函式單元測試接手**：<br>1. **測試函式**：`resolveServiceMapEnabledFlag(allocation: boolean, layer: boolean): '1' \| '0'`。<br>2. **輸入與斷言**：`(true, true) => '1'`，其餘 `(true, false)`、`(false, true)`、`(false, false)` 皆為 `'0'`。<br>3. **接線守護**：在場景屬性映射單元測試中驗證輸出注入 Canvas 的 `data-modqn-service-map-enabled` 屬性值，徹底擺脫不存在的 Drawer trigger。 |

小計: S=0 U=7 V=0 BLOCKED=4

---

## 我最不確定的 3 條

1. **`scene-lane L2396` (`beamConeScope: 'focus-satellite'`)**
   - **不確定原因**：這是 Three.js/WebGL 內部的粒子專屬設定，在 DOM 完全沒有可對應的屬性或事件。若僅以單元測試保護「參數工廠函式」，在缺乏 WebGL 狀態檢查或視覺快照綠閘門的情況下，無法真正證明粒子系統呼叫端已正確接上；但若長期掛在 BLOCKED 又會阻礙場景元件拆分。
2. **`scene-lane L3236` (`<AdvancedSetupDrawer`)**
   - **不確定原因**：`advanced-setup-trigger` 與 `advanced-setup-drawer` 在 `src/` 中完全不存在。目前無法斷定該抽屜是「開發中/遺失 testid 的核心功能」（應維持 BLOCKED 等待補齊）還是「已被重構遺棄的死碼」（應連同 JSX 刪除並歸為 V）。
3. **`L2117` (`const showUav = sceneLane === 'sinr-live';`)**
   - **不確定原因**：提案點擊的按鈕是死碼，雖然邏輯本身可輕易轉為單元測試（U），但若 `sinr-live` 場景本身在產品中已不再透過任何入口對外暴露，該判斷式可能已淪為無效邏輯。目前依「保留行為防護」先判 U，但後續若確認該 lane 已廢棄，應改判 V 予以清理。
