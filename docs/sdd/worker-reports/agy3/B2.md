本批共 18 列

| 來源 | 原斷言（截 60 字） | 歸屬 | 理由與具體做法（繁中） |
| :--- | :--- | :---: | :--- |
| **Y3** | `scene-lane L2309`：renderer flag 存在，但焦點 beam load 無可控輸入或獨立 telemetry | **BLOCKED** | **卡在缺少輸入 fixture 與獨立 telemetry**。全域 contention 不是等價前置，現階段無可觀察訊號可轉行為斷言；且 26 個 browser 閘門僅 6 個為綠，無法直接委託現有閘門。需待補齊 focus-load telemetry 後再轉行為測試。 |
| **Y3** | `scene-lane L2396`：現有 scope attr 屬 CellBeamCones，非 upload particles | **BLOCKED** | **卡在粒子元件未回報獨立 receipt**。現有 DOM scope 屬性屬於一般 Cones，拿來驗證粒子會產生跨元件假綠。需待 particle 元件補齊實際 rendered satellite IDs/scope receipt 後方可建立行為斷言。 |
| **Y3** | `scene-lane L2686`：artifact runtime signatures 存在，但目前 App 正式入口不可達 | **BLOCKED** | **卡在產品缺少可達入口**。功能簽章真實存在但端到端無法到達，屬於可達性阻擋。需待 Owner 提供命名路由或 dev bootstrap 入口後，方可建立端到端行為斷言。 |
| **Y3** | `scene-lane L2691`：artifact runtime signatures 存在，但目前 App 正式入口不可達 | **BLOCKED** | **卡在產品缺少可達入口**。同 L2686，簽章存在但正式入口封閉。需待 Owner 決策路由開放或提供 dev harness 後轉行為斷言。 |
| **Y3** | `scene-lane L2696`：artifact runtime signatures 存在，但目前 App 正式入口不可達 | **BLOCKED** | **卡在產品缺少可達入口**。同 L2686，需待 Owner 決策產品入口規範後轉行為斷言。 |
| **Y3** | `scene-lane L2701`：只能測 live 分支，archived TLE 被 homepage authority 明確關閉 | **BLOCKED** | **卡在 Owner 產品決策**。Homepage authority 已明確關閉 archived TLE，目前無法判定此分支是已廢棄死碼（應判 V）還是暫時關閉且需恢復雙分支之功能（應轉 B）。需待 Owner 定調後處置。 |
| **Y3** | `scene-lane L2775`：R3F cue 無 renderer-derived DOM/canvas receipt | **BLOCKED** | **卡在 R3F 缺少渲染回執訊號**。呼叫端 event count 只能證明發起呼叫，無法證明 WebGL/R3F cue 真實畫出。需待補齊元件級 render count 或 mesh receipt 後轉行為斷言。 |
| **Y3** | `scene-lane L2826`：condition attr 為布林投影，無法穩定建立 serving carrier 不可渲染狀態 | **BLOCKED** | **卡在異常狀態 fixture 與 fallback mesh receipt 缺失**。Browser 無法穩定構造不可渲染情境，且缺乏 fallback mesh 觀測點，直接斷言布林屬性會假綠。需補齊測試環境 fixture 與渲染回執。 |
| **Y3** | `scene-lane L2847`：無 publisher emission receipt，scene 屬性無法證明無 onSimUpdate | **BLOCKED** | **卡在缺乏 event sequence/count 回執且 transition 未恢復**。現有 live-enabled 屬性無法證明回呼被抑制。需補齊 sequence/count telemetry 並修復 transition 入口。 |
| **Y3** | `scene-lane L3159`：artifact transition 無入口，surface 未發布精確 policy | **BLOCKED** | **卡在 transition 入口未開且 policy 未暴露**。僅驗證 banner 消失為可空斷言（vacuous assertion）。需待 transition 入口與 policy telemetry 就緒後再行轉換。 |
| **Y3** | `phase-f L257`：真實 radio 存在但僅影響 secondary UEs；canvas 僅發布 primary 位置 | **BLOCKED** | **卡在缺少 secondary UEs 之 telemetry**。現有 canvas 僅發布固定 primary UE 位置，無法觀測 secondary radio 的行為變化。需補齊 secondary distribution checksum/sample。 |
| **Y3** | `phase-g L227`：UI controls 已刻意移除且無 secondary trajectory telemetry | **V** | **刪除**。產品已刻意移除 UI 控制項，底層亦無 telemetry，防護的是已廢棄的互動路徑。就算該 source pin 被違反，使用者在 UI 上亦無從操作或察覺差異，無回歸風險。 |
| **Y4** | `L1594`：teachingPolicySection 在目前產品設定下不可達，缺席斷言恆真 | **V** | **刪除**。呼叫端已在產品設定中固定關閉承載該 slot 的教學輔助 UI，導致該斷言在不可達路徑下恆真。使用者在畫面上完全不可見此 section，刪除無任何功能或品質損失。 |
| **Y4** | `L1739`：現有 mosaic color-count 為獨立 gate 輸出，無法守 return null guard | **S** | **真結構約束（去檔名化）**。因 telemetry 無法反映實際 mesh mount，改採 AST 靜態規則：掃描 `src/**` 內所有包含 mosaic 渲染的模組，約束其在存取 frame 資料前必須包含 `return null` 的 guard clause 分支結構。 |
| **Y4** | `L1744`：runtime 僅提供 distinct-color count，無法證明資料源為 cellFrame.ues | **S** | **真結構約束（去檔名化）**。Runtime 無法逐 UE 比對資料來源，改採 AST/依賴分析規則：掃描 `src/**` 中負責計算 mosaic color 之函式/模組，約束其資料輸入管線必須明確傳入並引用 `cellFrame.ues`。 |
| **Y4** | `L2246`：data-modqn-handover-cues-visible 為期望值，prop 斷線仍假綠 | **S** | **真結構約束（去檔名化）**。為防止 JSX 接線脫落，改採 AST 靜態規則：掃描 `src/**` 內所有 `<CellHandoverArcs />` JSX 節點，強制檢查其必須宣告並綁定 `visible` prop。 |
| **Y4** | `L2261`：三個 cone Canvas 屬性在掛載前已由 resolver 計算，無法證明 JSX gate | **S** | **真結構約束（去檔名化）**。Resolver 無法證明元件掛載狀態，改採 AST 靜態規則：掃描 `src/**` 內所有 `<CellBeamCones />` 呼叫點，強制約束其必須包覆於條件渲染閘門（如 `{condition && <CellBeamCones />}`）之下。 |
| **Y4** | `L2272`：DOM 無法讀取 WebGL mesh 之 userData，無法偵測 prop 未傳入 | **S** | **真結構約束（去檔名化）**。DOM 屬性與傳入 prop 同源無法測斷線，改採 AST 靜態規則：掃描 `src/**` 中對應 WebGL beam/cone 元件之調用點，強制檢查 `scope` 與 `satellite` props 必須存在於 JSX 屬性列表中。 |

小計: S=5 U=0 V=2 BLOCKED=11

---

## 我最不確定的 3 條

1. **`scene-lane L2701`（Archived TLE 分支）**
   - **不確定點**：Homepage authority 已明確關閉 archived TLE。若該功能在架構上已被永遠廢棄，技術上應判 `V`（刪除死碼 pin）；但若此關閉僅是暫時性的首頁設定、未來仍需支援 Walker/TLE 雙分支，則應轉為行為閘門。在沒有 Owner 的產品決策前無法擅自判定死活，故恪守紀律判 `BLOCKED`。
2. **`Y4 L1739`（Mosaic `return null` guard）**
   - **不確定點**：此條目在「等待 mesh telemetry 以轉行為斷言（`BLOCKED`）」與「改立 AST 靜態守衛（`S`）」之間存在取捨。若判 S，去檔名化的 AST 檢查能立即解鎖神元件拆檔且確保 guard clause 不被手滑刪除，但它依然不是真正的 runtime 行為驗證；若團隊堅持不留任何靜態 pin，則此項需轉為 `BLOCKED`。
3. **`phase-f L257`（Secondary UEs radio 影響）**
   - **不確定點**：材料指出 secondary radio 運算真實存在，但 canvas 僅發布 primary UE 位置。理論上若 radio 演算法為純運算，可將計算核心抽離並改判 `U`（單元測試）；然而單元測試無法證明 Scene/Canvas 呼叫端確實將計算結果接線傳遞至 secondary UEs（無法保證接線），且現有 telemetry 缺失，故最終維持誠實的 `BLOCKED`。
