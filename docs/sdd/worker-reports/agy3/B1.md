本批共 9 列

| 來源 | 原斷言（截 60 字） | 歸屬 | 理由與具體做法（繁中） |
| :--- | :--- | :---: | :--- |
| Y1: `validate-phase-h-s5-orbit-trail-modqn-demo.ts:L27` | `無 runtimeVisualSettings 或 effect DOM/canvas telemetry` | **U** | **理由**：`appMode × reducedMotion` 是無副作用的狀態決策矩陣，應抽離為純函式測試。<br>**具體做法**：<br>1. **受測函式**：`resolveOrbitTrailVisibility({ appMode, reducedMotion })`<br>2. **輸入與斷言**：輸入 `{ appMode: 'modqn-demo', reducedMotion: true }` 斷言 `false`；輸入 `{ appMode: 'modqn-demo', reducedMotion: false }` 斷言 `true`。<br>3. **接線防護**：呼叫端透過 TypeScript 嚴格介面（如 `SceneVisualConfig`）進行編譯期型別約束，確保場景渲染層必須消費此 resolver 產出。 |
| Y1: `validate-phase-h-s5-orbit-trail-modqn-demo.ts:L31` | `OrbitTrail 為 Three.js primitive，無 selector/dataset` | **S** | **理由**：Three.js 幾何原語無法自 DOM 讀取，但「場景樹中必須掛載 OrbitTrail」屬於架構組裝事實。<br>**具體做法（去檔名化）**：改用 AST 分析工具（如 ts-morph 或 ESLint 規則），對 `src/scene/**/*.{ts,tsx}` 的渲染樹進行結構掃描，斷言「存在 `<OrbitTrail ... />` JSX 節點且其屬性受 visual config 控制」，不鎖死在 `MainScene.tsx`，分解至子模組亦能維持綠燈。 |
| Y1: `validate-phase-h-s5-orbit-trail-modqn-demo.ts:L35` | `spineParticles:true 被 showSinrLiveViewport gate 擋掉` | **BLOCKED** | **卡在產品/Owner 規格定義**：設定為 `true` 與畫面實際粒子渲染存在邏輯衝突（被 viewport 閘門攔截）。需由 Owner 先定義此處防護意圖：究竟是守「靜態配置預設值」（轉純資料測試），還是守「MODQN 模式下的可見性行為」（需先釐清該閘門的預期業務邏輯）。定義明確前無法決定歸屬。 |
| Y1: `validate-phase-c-scene-scale-override.tsx:L195` | `可見控制已移除，sinr-live footprint 不消費 multiplier` | **BLOCKED** | **卡在產品/Owner 需求確認**：UI 控制項已撤除且 `sinr-live` 不使用該參數。需由 Owner 確認該 scale override 是否已全面廢棄（若是則直接判 **V** 刪除無損），抑或仍有其他 active lane 依賴其轉發契約（若是則轉 **S** 進行去檔名化結構防護）。 |
| Y2: `validate-modqn-handover-story-layer.ts:L558` | `data-modqn-handover-cues-visible 與上游旗標同源無 subtree 檢驗` | **BLOCKED** | **卡在缺少真實 mesh-derived telemetry**：現有 dataset 僅鏡像上游狀態，若 `CellHandoverArcs` 在組件分解時遺失 JSX prop 仍會報綠（假綠漏洞）。在 Three.js 場景層補齊實際讀取 handover arc mesh 的 render telemetry 之前，無法轉為有效行為閘門。 |
| Y2: `validate-modqn-handover-story-layer.ts:L562` | `cell-beam-cone 屬 MainScene 事先計算 model telemetry 非掛載` | **BLOCKED** | **卡在缺少組件掛載觀察點**：現有數據是父層預先計算的 model telemetry，若 `CellBeamCones` 內部 wiring 斷裂仍會呈現假綠。需待研發於場景層建立基於真實 cone mesh 的 telemetry 後，方能轉為行為斷言。 |
| Y2: `validate-phase-h-s8-hud-camera-default.ts:L41` | `paper-faithful-closeup 僅型別與 pose，無 UI/命令觸發` | **BLOCKED** | **卡在架構/Product Owner 裁決**：該 preset 在 UI 無任何入口。需由 Owner 確認此 preset 是保留給無頭/論文重現腳本的程式化合約（若是則改由單元測試 **U** 守 pose 字典映射），抑或為無用死碼（若是則判 **V** 刪除）。 |
| Y2: `validate-phase-i-s5b-cell-beam-cones.tsx:L379` | `與 L562 相同 wiring，缺少獨立 mesh-derived 訊號` | **BLOCKED** | **卡在缺少獨立 cone mesh telemetry（與 L562 同源）**：禁止以父層 model telemetry 偽裝為兩份獨立行為證據。必須等待真實 cone mesh 觀察點建立後，由單一端到端行為閘門統一收斂。 |
| Y2: `validate-phase-g-ue-mobility-step.tsx:L262` | `canvas 僅曝露 primary UE，未提供 secondary UE 位移/軌跡數據` | **BLOCKED** | **卡在缺少 secondary UE 的 render telemetry**：primary UE 靜止且 handover rail index 為平行計算，無法作為 secondary UE 移動的代理訊號（有假綠風險）。需先在渲染管線為 secondary UE 建立專屬位移與軌跡觀察點。 |

小計: S=1 U=1 V=0 BLOCKED=7

---

## 我最不確定的 3 條

1. **`validate-phase-h-s5-orbit-trail-modqn-demo.ts:L27`（判 U）**
   - **不確定點**：此項介於 **U（純邏輯單元測試）** 與 **S（AST 結構約束）** 的邊界。將決策矩陣抽為 pure resolver 並以單元測試覆蓋是最佳實踐，但純單元測試無法獨立驗證 React JSX 層是否有確實「接上」該設定。目前依賴 TypeScript 嚴格介面作接線防護；若團隊短期內無重構業務代碼抽出 resolver 的計畫，則需退回 **S**（以 AST 掃描整體模組表達式）。
2. **`validate-phase-h-s8-hud-camera-default.ts:L41`（判 BLOCKED）**
   - **不確定點**：此項高度接近 **V（刪除死碼）**。UI 上完全無調用路徑，極可能是過期遺留；但考量 `leo-beam-sim` 具備論文圖表重現屬性，部分 pose preset 可能專門保留給無頭自動化腳本或非 UI 導出流程。在未取得 Owner 證實其為死碼前，不可貿然判 V 刪除，亦不能在無消費者的情況下盲目補單元測試。
3. **`validate-phase-c-scene-scale-override.tsx:L195`（判 BLOCKED）**
   - **不確定點**：此項介於 **V（清理無效 pin）** 與 **S（保留跨 lane 轉發）** 之間。雖然 `sinr-live` 明確不消費且 UI 已移除（具備強烈的廢棄特徵），但材料保留了「若仍需守其他 lane 的 forwarding」之假設。在缺乏完整跨 lane 調用鏈證據的情況下，必須由 Owner 明確宣示該 override 的生命週期狀態。
