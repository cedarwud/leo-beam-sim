本批共 16 列

| 來源 | 原斷言（截 60 字） | 歸屬 | 理由與具體做法（繁中） |
| :--- | :--- | :---: | :--- |
| Y5-L1047 | `overlay-demo 分支在真實 App 不可達；只有 live-truth 可觀察...` | **BLOCKED** | **卡在產品決策**：`overlay-demo` / MODQN 入口在 `App` 內不可達，只驗單邊會破壞三元契約完整性。需由 owner 明確裁決該分支是「已廢棄需清理」還是「暫時不可達需修復入口」；在決策前貿然判 V 會誤刪真守衛，修復入口後應轉為真行為斷言 (B)。 |
| Y5-L1390 | `proof cue/toggle 在真實 App 不可達。既有 browser 程式碼雖嘗試...` | **BLOCKED** | **卡在產品決策與首頁狀態修復**：proof toggle 被首頁初始化狀態覆寫導致不可達，且既有 browser 測試已失效。需 owner 判定 proof 模式是否保留；若保留需先修復 `resolveHomepageInitialRuntimeState` 覆寫缺陷，才能建立真行為測試。 |
| Y5-L1391 | `同一不可達前提也使 eligible/ineligible callback 行為無法...` | **BLOCKED** | **卡在 L1390 proof 入口修復與決策**：與 L1390 綁定，因 proof 模式不可達導致 callback 觸發鏈路中斷。在 proof 模式入口定位明確且可達前，無法建構有效的行為或單元接線測試。 |
| Y5-L1392 | `完整 left-tab lane/mode matrix 無 runtime 入口；這本質...` | **U** | **單元測試接手**：此邏輯為純 mapping。<br>1. **測試標的**：純函式 `getLeftSidebarTabsForSceneLane(lane, mode)`。<br>2. **輸入**：列舉所有 `lane`（live/artifact/proof）與 `mode` 的矩陣組合。<br>3. **輸出斷言**：回傳之 tab 識別碼陣列與順序完全符合規格。<br>4. **接線說明**：可達之 live/artifact 分支由現有 UI 煙霧測試守護，不可達分支由純函式矩陣保證 mapping 正確性，解除神元件原始碼文字綁定。 |
| Y5-L1393 | `完整 right-tab lane/mode matrix同樣不可達。建議改判 S...` | **U** | **單元測試接手**：此邏輯同為純 mapping。<br>1. **測試標的**：純函式 `getRightSidebarTabsForSceneLane(lane, mode)`。<br>2. **輸入**：完整 `(lane, mode)` 矩陣參數。<br>3. **輸出斷言**：回傳對應的右側面板 tabs 配置清單與屬性。<br>4. **接線說明**：可達分支由現有 UI 測試覆蓋，不可達矩陣由純函式單元測試接管，分解時移出神元件。 |
| Y5-L1413 | `ServiceStatusBanner 的 MODQN live mount branch 不可...` | **V** | **刪除**：`ServiceStatusBanner` 的 MODQN live 分支在 `App` 執行路徑上不可達，而 `/walker` 為獨立 root 元件。此文字 pin 就算被違反（甚至分支被移除），使用者訪問 `App` 或 `/walker` 在畫面與行為上完全看不出任何差別，屬無效防護，刪除無損失。 |
| Y5-L1469 | `可達 rail 的 data-axis-playing 是經 axisKind gate 後...` | **V** | **刪除**：在可達的 artifact rail 下，source-time 軸永遠為靜態 `false`，無動態 telemetry 也無可達 display-stretched rail。此斷言本質為恆值檢查，就算被違反，使用者在時間軸動畫與介面上完全看不出任何差別，禁止以恆假斷言充數，予以刪除。 |
| Y6-L638 | `proof toggle selector 存在，但 App 現況沒有可達的 MODQN...` | **BLOCKED** | **卡在產品決策**：proof toggle 存在但入口被 `resolveHomepageInitialRuntimeState` 覆寫。卡在 owner 需拍板「首頁是否保留 MODQN live/proof 入口」；若保留則需修復初始化覆蓋後轉 B，若確認廢棄則清代碼判 V。 |
| Y6-L652 | `可達 SINR marker 來自 cell-truth builder，不是此行的...` | **S** | **真結構約束（去檔名化）**：可達 marker 來自 cell-truth builder，拿它測 MODQN builder 會造成假綠，但架構上需確保 scene 仍管線化組裝此 builder。<br>**去檔名化做法**：改為 AST / 模組依賴掃描，斷言 `src/scene/**/*.{ts,tsx}` 集合中必須 import 並呼叫 `buildLiveWalkerHandoverEventIndex`，且其回傳值需傳入 scene 渲染狀態管線，不綁死 `MainScene.tsx`。 |
| Y6-L656 | `source horizon 與 display axis 的差異只在不可達 proof...` | **BLOCKED** | **卡在 proof rail 入口恢復**：此差異本質為行為契約，但因 proof rail 不可達而無法在 runtime 驗證。卡在 proof rail 執行期入口尚未修復，修復前無法進行真實行為斷言，不應降級為無意義的靜態文字 pin。 |
| Y6-L661 | `現況 horizonSec === durationSec，且元件缺 prop 時會自動...` | **V** | **刪除**：元件缺 prop 時自帶 fallback 到 `duration`，且目前業務情境 `horizonSec === durationSec`。就算被違反（漏傳 prop），渲染結果完全等價，使用者在畫面與時間軸行為上完全看不出差別，文字 pin 守護的是冗餘 wiring，刪除無損失。 |
| Y6-L670 | `要求同時守兩個 live lane，但 MODQN lane 不可達，且 live...` | **BLOCKED** | **卡在 MODQN live rail 掛載修復**：斷言要求同時覆蓋雙 live lane，但 MODQN lane 不可達且 live rail 未掛載。卡在需修復 MODQN live rail 掛載與可達性，才能建立真實的多 lane 行為驗證。 |
| Y6-L671 | `proof lane 不可達；此外舊 browser gate 對 marker 是...` | **BLOCKED** | **卡在 proof lane 入口與 browser 閘門重構**：卡在兩項前提：1. proof lane 目前不可達；2. 既有 browser 閘門採「有才測」無 fail-loud 機制（存在假綠缺陷，不符通過標準）。需修復入口並重寫閘門斷言後才能轉移。 |
| Y6-L684 | `HOMEPAGE_SIMULATION_SOURCE_SWITCH_VISIBLE = false...` | **BLOCKED** | **卡在產品決策**：首頁強制關閉切換開關且鎖定 Walker，導致 archived TLE 分支不可達。卡在 owner 需明確裁決 TLE 模擬來源切換功能是否已永久廢棄；廢棄則連同死碼判 V，若為長期保留旗標則轉 S。 |
| Y6-L686 | `與 L684 相同；只能測一般 Walker step，無法測條件切換...` | **BLOCKED** | **卡在產品決策**：同 L684，因開關關閉無法測試條件切換分支。卡在 owner 對 TLE 模擬切換功能的存廢決策，在入口開放前無法決定歸屬。 |
| Y6-L907 | `headless feed 必須藉由 MODQN telemetry 下游觀察，但 MOD...` | **BLOCKED** | **卡在紅閘門修復與入口恢復**：現有 phase-d browser 腳本前置已失效（紅閘門），且 MODQN App 入口不可達。依據治理原則，不得依賴失效閘門，必須先修復前置腳本並打通入口後才能恢復行為守衛。 |

小計: S=1 U=2 V=3 BLOCKED=10

---

## 我最不確定的 3 條

1. **Y6-L652（判 S 還是 U/BLOCKED）**
   - **分歧點**：`buildLiveWalkerHandoverEventIndex` 運算本體是純函式，可用單元測試 (U) 驗證；但原 pin 的意圖是防範「Scene 元件層有沒有接上這個 builder」。由於可達 marker 來自 cell-truth builder，使得 DOM 行為測試會產生假綠，因此我判為去檔名化的結構約束 (S)。然而，如果 MODQN 在 Scene 中的呼叫鏈路本身也是待清理的不可達死碼，則此條實質上也卡在 MODQN 的產品存廢決策（應為 BLOCKED）。

2. **Y5-L1413（判 V 還是 BLOCKED）**
   - **分歧點**：`ServiceStatusBanner` 的 MODQN live mount branch 在 `App.tsx` 內不可達，而 `/walker` 已是獨立 root。如果系統架構已定案將 MODQN 徹底隔離在 `/walker` 頁面，則 `App.tsx` 中的掛載純屬無效死碼，判 V 完全合理；但若架構設計預期 `App.tsx` 應維持全功能聚合（未來需修復 MODQN 入口），則逕行判 V 會丟失掛載守衛，需 owner 明確確認架構意圖。

3. **Y5-L1047（判 BLOCKED 還是 V）**
   - **分歧點**：`overlay-demo` 在真實 App 不可達，提案者建議現況改判 V、若恢復入口再轉 B。但「不可達」究竟是「已廢棄的功能（應刪除 V）」還是「暫時壞掉或未開放的入口（需修復）」？在缺乏 owner 產品決策前，依據「嚴禁為了數字好看而猜測、避免誤刪真守衛」的治理紀律，我選擇判 BLOCKED，但這取決於團隊對 dead branch 的容忍度。
