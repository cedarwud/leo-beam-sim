我是測試治理決策者 **I2**。以下是針對 `validate:phase-c:sinr-live-cells:render:browser` 的最終定案與執行規範。

---

### 一、決策定案與覆蓋率得失分析

**【最終決策】：採納「方案 A（移除 pulse 子段，純化首頁 Serving-Cell 閘門職責）」**

#### 覆蓋率得失分析（非實作難易度導向）：
1. **首頁 Serving-Cell 核心行為：100% 完整保留（零損失）**
   - 首頁 `sinr-live` 上的 cell-truth cones 渲染、fixed cell centres、UE off-axis 幾何拓撲、跨 frame 動態恢復力（`conesLater > 0`）、泳道防洩漏隔離（Lane Isolation）以及 Console Error 清潔度，全部繼續留在首頁（`pathname === '/'`）執行完整嚴格驗證。
2. **Pulse 行為覆蓋率：無任何防禦真空（Zero Coverage Gap）**
   - 由於首頁視覺識別規範（`homepageVisualIdentity === true`）刻意抑制 legacy natural-pulse carrier，pulse 渲染本來就不屬於首頁的有效行為。
   - `/legacy` 泳道上的 pulse 渲染已由獨立專屬閘門 `validate:phase-c:handover-pulse:render:browser` 進行 100% 的端到端斷言與守護，並已實跑驗證通過。
3. **否決方案 B 與方案 C 的治理理由**：
   - **否決方案 B（整支移至 `/legacy`）**：會直接撤除首頁對 live cell cones 的所有回歸保護，造成核心業務首頁防線完全真空，代價不可接受。
   - **否決方案 C（分裂為兩支）**：分裂出的 `/legacy` pulse 閘門與既有的 `validate:phase-c:handover-pulse:render:browser` 斷言標的完全重疊，屬於無效冗餘（Duplicate Coverage），徒增 CI 耗時與維護負擔。

---

### 二、職責切分、命名規範與 CI 治理同步說明

雖然最終定案為方案 A（直接移除無效子段，不另行分裂新檔案），但為明確兩類行為的邊界責任，規範如下：

#### 1. 斷言歸屬切分明細
* **首頁閘門（本腳本保留之斷言）**：
  - Cell-truth cones 渲染與拓撲檢查（served cells, serving satellites, fixed cell centres, UE off-axis）。
  - 動態跨 frame 恢復驗證（`conesLater > 0`）。
  - 泳道防洩漏隔離斷言（`[data-testid="artifact-satellite-compass"]` 與 `[data-testid="artifact-source-badge"]` count 為 0）。
  - Runtime Console Error 檢驗。
* **Legacy 閘門（已由獨立閘門承擔之斷言）**：
  - 20x 播放加速與時間軸推進。
  - `pulse > 0` 與 `pulseRendered > 0` 之 pulse 渲染判定。

#### 2. npm key 命名
- **首頁 Serving-Cell 閘門**：維持原 npm script key `validate:phase-c:sinr-live-cells:render:browser`（其語意本就是 sinr-live-cells，移除不屬於該語意的 pulse 雜質後名實相符）。
- **Legacy Pulse 閘門**：沿用既有 npm script key `validate:phase-c:handover-pulse:render:browser`，無需新增多餘 key。

#### 3. `.github/workflows/governance.yml` 同步狀態
- **無需變更 CI Workflow 結構**。
- 原有的 `validate:phase-c:sinr-live-cells:render:browser` 呼叫保留在工作流中，修復後將直接消除 90 秒假性逾時，順暢通過；既有的 `validate:phase-c:handover-pulse:render:browser` 亦繼續在 CI 中守護 pulse。

---

### 三、關鍵防線確認

**問：處置後首頁的 serving-cell 行為是否仍有人守？**

**答：是，100% 由本閘門在首頁現場嚴密守護。**
- 本次處置「未放寬、未刪除」任何一條 serving-cell 相關的幾何拓撲、動態恢復、泳道隔離與 runtime hygiene 斷言。
- 移除的僅為「在首頁上依規格被抑制、在數學上永遠無法觸發」的過期 pulse 子段。
- 首頁 live SINR cell cones 的渲染與健康度依然擁有最高規格的自動化測試覆蓋。

---

### 四、工程執行步驟清單（不含 diff）

1. **清理檔頭說明**：移除檔案頂部註解中關於 fast-forward 與 handover pulse 驗證的說明點。
2. **剔除 Pulse 子段**：完整移除包含點擊 `[data-testid="timeline-speed-20x"]`、`waitForFunction` 輪詢 pulse 屬性、`pulse > 0` 與 `pulseRendered > 0` 斷言及 pulse console 輸出的整個代碼區塊。
3. **校正 PASS 日誌宣告**：將腳本結尾 PASS 日誌字串中的 `+ decoupled live-handover pulse` 移除，確保終端輸出與實際執行的斷言集精確吻合。
4. **驗證與驗收**：
   - 執行 `npm run validate:phase-c:sinr-live-cells:render:browser`，確認在首頁無 90 秒逾時並迅速亮綠燈。
   - 確認 `npm run validate:phase-c:handover-pulse:render:browser` 亦獨立維持綠燈。
