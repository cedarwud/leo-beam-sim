完成 X4 任務。

- 刪除 27 個經重數確認 `>1` 的字串斷言項目。
- S3 指定字面實際為 0 次，因此保留未動。
- 7 個複合斷言均保留清單外條件；沒有留下孤兒 pass。
- 10 個 validator 改前／改後：cell-overlay `0→0`，其餘皆 `1→1`。
- Phase H ratchet 將成為 `22 < 24`，依規則未調整，交 owner 處理。
- 未執行 git 寫入、browser validator，也未修改 `src/**`。
- 同期另一 session 修改了 camera validator 的另一處，我未覆寫。

完整報告：[X4.md](/home/u24/demo/leo-beam-sim/docs/sdd/worker-reports/X4.md)

