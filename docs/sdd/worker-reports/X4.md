# X4：刪除可證明抓不到東西的斷言

## 我改了哪些檔案

以下行號均為任務清單／修改前行號。計數以 Node 直接讀取 `src/scene/MainScene.tsx`，逐字執行 `source.split(lit).length - 1`；只有結果大於 1 的項目才刪除。共刪除 27 個指定的 source-text predicate／斷言項目，另有 1 個不符計數而保留。

- `scripts/validate-phase-i-s4-cell-overlay.tsx`：刪 5 項。
  - 原 L228：`resolveSceneLaneRenderPlan({`，2 次。
  - 原 L252：`cellOverlaySlotIndex=`，2 次。
  - 原 L253：`cellOverlayActiveCount=`，2 次。
  - 原 L254：`cellOverlayIdleCount=`，2 次。
  - 原 L255：`cellOverlayCellCount=`，2 次。
- `scripts/validate-phase-i-s5a-footprint-handover.tsx`：刪 3 項。
  - 原 L323：`cellHoReassignmentCount=`，2 次。
  - 原 L324：`cellHoInterCount=`，2 次。
  - 原 L325：`cellHoIntraCount=`，2 次。
- `scripts/validate-phase-i-s5b-cell-beam-cones.tsx`：刪 8 項。
  - 原 L372：`CellBeamCones,`，3 次。
  - 原 L376：`schedule={cellSchedule}`，2 次。
  - 原 L377：`satelliteWorldById={satelliteWorldById}`，3 次。
  - 原 L378：`satelliteTintById={satelliteTintById}`，3 次。
  - 原 L384：`items={sinrLiveCellBeamConeItems}`，2 次。
  - 原 L385：`dimShallowCones`，3 次。
  - 原 L396：`<SinrLiveCellFootprintRings`，2 次。
  - 原 L397：`items={sinrLiveCellBeamConeItems}`，2 次；這是同一字面在另一個斷言中的第二個待刪項。
- `scripts/validate-phase-g-ue-trail-viz.tsx`：刪 1 項。
  - 原 L183：`useUeTrailHistory`，3 次。
- `scripts/validate-phase-3-overlays.ts`：刪 1 項。
  - 原 L88：`<BeamLoadCylinder`，2 次。
- `scripts/validate-phase-h-s1-live-sim-beams.ts`：刪 5 條獨立 `expect(...)`。
  - 原 L111：`SinrLiveCellBeamCones`，11 次。
  - 原 L116：`beamConeCount`，2 次。
  - 原 L121：`viz.satBeams`，10 次。
  - 原 L126：`resolveSinrLiveCellBeamConeItems`，3 次。
  - 原 L131：`footprintRadius`，2 次。
- `scripts/validate-phase-c-ue-marker-size.tsx`：刪 1 條獨立 `check(...)`，並移除因此成為空殼的 section 與只供該斷言使用的區域變數。
  - 原 L164：`ueMarkerMultiplier={visualScaleMultipliers.ueMarkerMultiplier}`，2 次。
- `scripts/validate-phase-c-scene-scale-override.tsx`：刪 2 條獨立 `check(...)`。
  - 原 L194：`visualScaleMultipliers: SceneVisualScaleMultipliers`，3 次。
  - 原 L196：`visualScaleMultipliers,`，5 次。
- `scripts/validate-phase-c-camera-preset.tsx`：刪 1 條獨立 `check(...)`。
  - 原 L257：`if (effectiveCinematicMode !== 'director')`，2 次。
- `docs/sdd/worker-reports/X4.md`：新增本報告。

## 我沒動但有問題的

- `scripts/validate-s3-one-reset.tsx` 原 L330 沒有修改。任務所列精確字面 `const useEarthFixedCellTruth = sceneLane === 'sinr-live' || sceneLane === 'modqn-live'` 在 `MainScene.tsx` 是 **0 次**，不符合 `>1`。附近實際斷言多了 `-cell-preview`：`const useEarthFixedCellTruth = sceneLane === 'sinr-live' || sceneLane === 'modqn-live-cell-preview'`，此不同字面是 2 次；依禁止 re-pin 與「對不上就不刪」規則保留原斷言，交 owner 決定。
- `scripts/validate-phase-h-s1-live-sim-beams.ts` 的 `assert.ok(PASSED.length >= 24)` 沒有調整。刪除 5 條目標 `expect(...)` 後，依目前剩餘的 `expect`／`expectEqual`／對應 `pass` 靜態計數，完整跑到底最多會記錄 **22** 條，低於 24。現在 validator 先在原 L87 的既有 `showSinrLiveCellBeams` source pin 失敗，因此尚未執行到 ratchet；前置失敗日後修復時，owner 需要另行決定 ratchet 的治理方式。
- 改前已有 9 個目標 validator 為 exit 1；本次不 re-pin、不新增斷言，也不處理清單外失敗，所以改後仍為 exit 1。唯一改前為綠的 cell-overlay 保持 exit 0。
- 執行期間，另一個 session 在 `scripts/validate-phase-c-camera-preset.tsx` 約修改前 L332 將 `from './directorFocusPose'` source-text check 換成註解。那不是 X4 變更；X4 沒有覆寫、回復或宣稱該段成果。owner 整合時需把該並行變更與 X4 的原 L257 刪除分開核對。
- 沒有執行任何 git 寫入指令、任何 `*:browser` validator，也沒有修改 `src/**`、`package.json`、`.github/**`、`check:ee` 或 `check:visual`。

## 驗證

字面計數實際使用 Node here-doc，核心為：

```js
const source = fs.readFileSync('src/scene/MainScene.tsx', 'utf8');
for (const lit of literals) console.log(`${source.split(lit).length - 1}\t${lit}`);
```

每個 validator 改前與改後都各跑一次相同的非 browser npm key：

| validator | 實際指令 | 改前 exit | 改後 exit |
|---|---|---:|---:|
| cell overlay | `npm run validate:phase-i:s4-cell-overlay` | 0 | 0 |
| S3 one reset | `npm run validate:s3:one-reset` | 1 | 1 |
| footprint handover | `npm run validate:phase-i:s5a-footprint-handover` | 1 | 1 |
| cell beam cones | `npm run validate:phase-i:s5b-cell-beam-cones` | 1 | 1 |
| UE trail viz | `npm run validate:phase-g:ue-trail-viz` | 1 | 1 |
| Phase 3 overlays | `npm run validate:phase-3:overlays` | 1 | 1 |
| live sim beams | `npm run validate:phase-h:s1-live-sim-beams` | 1 | 1 |
| UE marker size | `npm run validate:phase-c:ue-marker-size` | 1 | 1 |
| scene scale override | `npm run validate:phase-c:scene-scale-override` | 1 | 1 |
| camera preset | `npm run validate:phase-c:camera-preset` | 1 | 1 |

改後重點輸出：cell-overlay 仍為 `PASS (41/0 assertions)`；UE trail 由 54 pass 降為 53 pass；UE marker 由 31 pass 降為 30 pass；scene scale 由 50 pass 降為 48 pass；camera preset 由 81 pass 降為 79 pass，其中 camera preset 另含上述另一 session 同時移除的一條 pass。其餘在目標區塊之前失敗的 validator，exit code 維持 1。

## 複合斷言與孤兒 pass 的處理

- 共遇到 7 個含目標字面的 `&&` 複合斷言：cell-overlay 2 個、footprint handover 1 個、cell-beam-cones 3 個、Phase 3 overlays 1 個。
- 其中 6 個仍含清單外條件，只移除指定 predicate 並保留其他條件：cell-overlay 1 個、footprint handover 1 個、cell-beam-cones 3 個、Phase 3 overlays 1 個。
- cell-overlay 的第二個複合斷言只由 4 個待刪 predicate 組成，因此整個 `expect(...)` 刪除。
- 獨立 orphan `recordPass(...)`／`pass(...)`／`PASSED.push(...)`：遇到 0 個、留下 0 個。`expect(...)` 與 `check(...)` helper 只會在 helper 呼叫仍存在且條件成功時內部記 pass；刪除整個 helper 呼叫不會留下虛假 pass。Phase H 的 5 個目標是獨立 `expect(...)`，其 pass 由 helper 內部產生，沒有額外的相鄰 `pass(...)` 可留成孤兒。
- 所有 assertion-count ratchet 數字均保持原值；Phase H 的 `22 < 24` 風險已列在上一節，沒有為變綠而改門檻。
