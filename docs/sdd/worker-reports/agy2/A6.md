本批共 22 列提案

| 來源 | 原斷言（截 60 字） | 提案觀察點 | 判定 | 說明（繁中，具體） |
|---|---|---|---|---|
| Y6:L638 | `modqnReplayProofRequested: modqnReplayProofRequestActive` | `[data-testid="modqn-replay-proof-viewport-toggle"]` / `.leo-app-shell[data-scene-lane="modqn-replay-proof"]` | 落在紅閘門 | 報告已指出 MODQN 入口不可達故無法轉換；其預定關聯之 `validate-live-walker-handover-event-focus-browser.ts` 目前為紅閘門（非 6 個綠閘門之一），在入口恢復與閘門修好前無法生效。 |
| Y6:L652 | `buildLiveWalkerHandoverEventIndex({` | `[data-testid^="timeline-event-marker-"]`（報告已指出可達頁面 marker 來自 SINR cell-truth） | 落在紅閘門 | 報告已正確識別不可用 SINR marker 假綠並標註無法轉換；預定掛載之 MODQN 相關 validator 目前為紅閘門。 |
| Y6:L653 | `liveWalkerHandoverEventIndexToRailEvents(liveWalkerHandoverEventIndex)` | `[data-testid="timeline-bar"] [data-testid^="timeline-event-marker-"]` | 削弱 | **典型假綠**。提案宣稱首頁 `/` 既有 timeline marker 已覆蓋此行故可刪除 pin；但首頁走的是 SINR cell-truth 分支，即使 `liveWalkerHandoverEventIndexToRailEvents` 被刪除或損壞，首頁仍會因 cell-truth 而渲染 marker 保持綠燈。此外目標 `validate-homepage-authority-browser.ts` 亦為紅閘門。 |
| Y6:L656 | `producerTraceDisplayDurationSec` | `[data-testid="handover-event-rail"]` 的 `data-horizon-sec`、`data-axis-sec`、`data-axis-kind` | 落在紅閘門 | proof lane 目前不可達；預定放入的 `validate-live-walker-handover-event-focus-browser.ts` 目前為紅閘門。 |
| Y6:L661 | `horizonSec={activeTimelineDescriptor.horizonSec}` | `[data-testid="timeline-bar"]` 的 `data-duration-sec`、`data-horizon-sec` | 落在紅閘門 | 報告已說明現況 `horizonSec === durationSec` 且元件有 fallback，DOM 斷言無法區分 prop 傳遞或 fallback 故無法轉換；目前亦無綠閘門可承接。 |
| Y6:L664 | `const liveTimelineWindowStartSec = demoStartOffset;` | `[data-testid="walker-results-rail"]@data-sim-time-sec` 與 `[data-testid="timeline-bar"]@data-current-time-sec` | OK | 載入固定 450 秒 offset 的 profile，暫停後驗證 `simTimeSec - currentTimeSec === 450`，能精確驗證 demoStartOffset 正確套用於時間軸起點，且獨立新增專用 validator 承接。 |
| Y6:L665 | `simState.simTimeSec - liveTimelineWindowStartSec` | `[data-testid="timeline-current-time"]`、timeline `data-current-time-sec`、Walker rail `data-sim-time-sec` | OK | seek 至 120 秒後驗證 UI 顯示 elapsed 120 (2:00) 且 simTime 為 570，能有效證明時間軸目前時間確實由 simTime 減去 windowStart 計算而來。 |
| Y6:L666 | `demoStartOffsetSec: demoStartOffset` | `[data-testid="timeline-scrubber"]`、`.leo-app-shell@data-live-timeline-seek-target`、`[data-testid="walker-results-rail"]@data-sim-time-sec` | OK | 依序 seek 60 與 120 秒並驗證 seek-target 分別為 510 與 570（差值維持 450），精確證明 seek target 計算包含 demoStartOffsetSec。 |
| Y6:L667 | `const absoluteTargetSec = Math.min(` | `[data-testid="timeline-jump-end"]`、`.leo-app-shell@data-live-timeline-seek-target`、timeline `data-duration-sec` 與 `data-current-time-sec` | OK | 在 offset=450 下觸發 jump-end，驗證 seek target 被 clamp 在 7200.000 而非 7650，成功以行為斷言守護 `Math.min` 的邊界保護。 |
| Y6:L670 | `if (sceneLane === 'sinr-live' \|\| sceneLane === 'modqn-live-cell-preview') return liveWalkerHandoverRailEvents;` | `[data-testid="handover-event-rail"]`（artifact sidebar 以外未掛載） | 落在紅閘門 | 報告正確標記無法轉換；預定放入的 `validate-live-walker-handover-event-focus-browser.ts` 目前為紅閘門。 |
| Y6:L671 | `if (sceneLane === 'modqn-replay-proof') return modqnHandoverRailEvents;` | `[data-testid="handover-event-rail"]@data-source-owner="modqn-producer-trace"` | 落在紅閘門 | proof lane 目前不可達；預定放入之 `validate-live-walker-handover-event-focus-browser.ts` 目前為紅閘門。 |
| Y6:L676 | `durationSec={timelineRailDescriptor.rail.durationSec}` | `[data-testid="handover-event-rail"]@data-horizon-sec` 與 `[data-testid="timeline-bar"]@data-duration-sec` | 落在紅閘門 | 預定放進的 `validate-phase-c-artifact-scene-real-data-browser.ts` 目前為紅閘門（非 6 個綠閘門之一），在該閘門修復前無法發揮保護效力。 |
| Y6:L677 | `sourceLabel={timelineRailDescriptor.rail.sourceLabel}` | `[data-testid="handover-event-rail"]@data-source` 與 `.leo-handover-event-rail__source` | 落在紅閘門 | 雖正確避開無效 selector，但目標 `validate-phase-c-artifact-scene-real-data-browser.ts` 目前為紅閘門。 |
| Y6:L678 | `sourceOwner={timelineRailDescriptor.rail.sourceOwner}` | `[data-testid="handover-event-rail"]@data-source-owner` 與 `[data-testid="timeline-bar"]@data-source-owner` | 落在紅閘門 | 目標 `validate-phase-c-artifact-scene-real-data-browser.ts` 目前為紅閘門。 |
| Y6:L679 | `sourceGapReasons={timelineRailDescriptor.rail.sourceGapReasons}` | `[data-testid="handover-event-rail"]@data-source-gap-count`、`[data-testid="handover-event-rail-source-gap"]` | 落在紅閘門 | 雖避開無效 selector 並設計 mock 200/no-event，但預定放進的 `validate-phase-c-artifact-fail-closed-browser.ts` 目前為紅閘門。 |
| Y6:L680 | `const timelineDurationSec = activeTimelineDescriptor.durationSec;` | `[data-testid="timeline-bar"]` 的 `data-duration-sec`、`data-source-owner` 及 `[data-testid="timeline-duration"]` | 落在紅閘門 | 跨 Walker 與 Artifact 雙場景比對時長，邏輯合理，但目標 `validate-phase-c-artifact-scene-real-data-browser.ts` 目前為紅閘門。 |
| Y6:L684 | `const activeTimelineDescriptor = isArchivedTleSceneActive` | `[data-testid="simulation-source-toggle-archived-tle"]`（未掛載） | 落在紅閘門 | Archived TLE 路由被鎖定無法轉換；預定放入的 `validate-homepage-authority-browser.ts` 目前為紅閘門。 |
| Y6:L686 | `stepSec={isArchivedTleSceneActive` | `[data-testid="timeline-scrubber"]` | 落在紅閘門 | Archived TLE 分支不可達，現況只能測到 Walker step 故標記無法轉換；目標閘門 `validate-homepage-authority-browser.ts` 目前為紅閘門。 |
| Y6:L907 | `<TrainingTelemetryFeed enabled={appMode === 'modqn-demo'}` | `[data-testid="live-telemetry-panel"][data-job-id]` 與 `[data-testid="live-telemetry-status-badge"]` | 落在紅閘門 | MODQN 入口不可達，報告標記無法轉換；預定放進的 `validate-phase-d-live-telemetry-browser.ts` 目前為紅閘門。 |
| Y6:L920 | `r.headers.get('X-Showcase-Artifact-Source')` | `.leo-app-shell@data-artifact-source` 與 `[data-testid="artifact-source-badge"]@data-artifact-source` | 落在紅閘門 | 設計了 route mock 自訂 header 觀察 badge，但目標 `validate-phase-c-artifact-fail-closed-browser.ts` 目前為紅閘門。 |
| Y6:L925 | `artifactSource !== PRODUCER_PINNED_SOURCE` | `[data-testid="artifact-source-badge"]`（`role="alert"` 與 `data-artifact-source`） | 落在紅閘門 | 驗證 synthetic-fixture-fallback 與 producer-pinned alert 狀態，但目標 `validate-phase-c-artifact-fail-closed-browser.ts` 目前為紅閘門。 |
| Y6:L930 | `r.headers.get('X-Showcase-Artifact-Source') ?? HEADER_ABSENT_SOURCE` | `.leo-app-shell@data-artifact-source`、`[data-testid="artifact-source-badge"]@data-artifact-source` | 落在紅閘門 | 驗證缺少 header 時 fallback 到 `header-absent` 警示，但目標 `validate-phase-c-artifact-fail-closed-browser.ts` 目前為紅閘門。 |

小計: 削弱=1 selector無效=0 落在紅閘門=17 OK=4

---

## 最該退回的前 3 條

1. **Y6:L653 (`liveWalkerHandoverEventIndexToRailEvents`)**
   - **退回原因**：**嚴重假綠（削弱）**。提案宣稱「已被 `validate-homepage-authority-browser.ts` 覆蓋，可直接刪除原文字 pin」。但首頁時間軸上的 marker 是由 SINR cell-truth builder 產生；即便 `liveWalkerHandoverEventIndexToRailEvents` 被完全註解或破壞，首頁仍會因為 cell-truth 而正常產生 marker，導致測試持續綠燈，完全喪失守護作用。且該 validator 本身亦為紅閘門。
2. **Y6:L679 (`sourceGapReasons`)**
   - **退回原因**：**掛載於紅閘門且無保護效力**。雖然提案透過 `page.route` mock 200 空 event 陣列並正確避開了不存在的 `source-gap-warning` selector，但目標檔案 `validate-phase-c-artifact-fail-closed-browser.ts` 現狀為紅燈。在該 validator 被修復並納入綠閘門前，此項行為斷言無法執行也無法阻擋回歸。
3. **Y6:L676–L678、L680（Phase C Artifact 系列：`durationSec`、`sourceOwner` 等）**
   - **退回原因**：**批次依賴未修復的 Phase C 紅閘門**。此批提案設計了透過 `/runtime-probe?sceneSource=artifact-replay` 進行雙場景與屬性對齊的行為斷言，但全部指定放入目前為紅燈的 `validate-phase-c-artifact-scene-real-data-browser.ts`。在未修好 Phase C 基礎載入流程前，這些變更無法提供任何防護，應先修閘門再行掛載。
