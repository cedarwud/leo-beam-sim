本批共 21 列提案

| 來源 | 原斷言（截 60 字） | 提案觀察點 | 判定 | 說明（繁中，具體） |
|---|---|---|---|---|
| scene-lane L2309 | `visible={(focusBeamLoad?.load ?? 0) > 0}` | `data-beam-load-cylinder-rendered`、`data-beam-load-contention-ue-count` | OK | 提案正確判定「無法轉換」：分析指出全域 contention 數無法等價證明焦點 beam load，且現有 UI 無法受控設定輸入條件，未提出弱化斷言。 |
| scene-lane L2385 | `modqnVisualLayerPreset === 'explain-handover'` | `[data-testid="advanced-setup-trigger"]`、`[data-testid="modqn-layer-preset-control"]` 等 | selector 無效 | 前置操作依賴已知不存在於 `src/` 的 `advanced-setup-trigger`；且落點 `validate-phase-3-overlay-render-browser.ts` 為紅閘門。依優先級判為 selector 無效。 |
| scene-lane L2396 | `beamConeScope: 'focus-satellite'` | `data-cell-beam-cone-scope`、`data-upload-particle-rendered-count` | OK | 提案正確判定「無法轉換」：明確指出一般 cone scope 屬性不屬於上傳粒子元件，若強行用其替代會造成粒子錯誤擴散時仍假綠（削弱防護），分析精確。 |
| scene-lane L2401 | `<BeamLoadUploadParticles` | canvas `data-upload-particle-rendered-count` | 落在紅閘門 | 提案主張行為「已被覆蓋」並建議刪除原字串斷言，但所指的 `validate-phase-3-overlay-render-browser.ts` 目前為紅閘門，在閘門修復前刪除原防線會失去保護。 |
| scene-lane L2686 | `sceneFrame?.sceneSource === 'artifact-replay'` | canvas `data-scene-source`、shell `data-artifact-source` | OK | 提案正確判定「無法轉換」：指出目前 App 缺乏可到達的 artifact 命名路由，拒絕以無效的 query string 建立表面測試。 |
| scene-lane L2691 | `<ArtifactSceneContent` | canvas `data-handover-story-layer` 等簽章 | OK | 提案正確判定「無法轉換」：指出目前無穩定 artifact 入口，維持 blocked 待入口提供。 |
| scene-lane L2696 | `liveSimulationEnabled="0"` | canvas `data-live-simulation-enabled` | OK | 提案正確判定「無法轉換」：指出 artifact 前置狀態無法自公開 UI 建立，避免只測預設值。 |
| scene-lane L2701 | `liveSimulationEnabled={simSource === 'live' ? '1' : '0'}` | canvas `data-live-simulation-enabled`、`homepage-tle-center` | OK | 提案正確判定「無法轉換」：指出 homepage 凍結 TLE 入口導致 else 分支不可達，拒絕只斷言單邊行為。 |
| scene-lane L2753 | `{presentationPlan.visible['serving-footprints'] && showSinr...` | `data-scene-presentation-visible-layers`、`data-sinr-live-cell-footprint-ring-rendered-count` | 落在紅閘門 | 提案預計擴充之 `validate-homepage-multi-candidate-acceptance-browser.ts` 目前為紅閘門，在修復前無法發揮行為驗證效力。 |
| scene-lane L2775 | `<DecisionHandoverCue` | canvas `data-multi-candidate-event-cue-count` | OK | 提案正確判定「無法轉換」：指出 R3F group 無法由 DOM 查詢，且呼叫端的 event count 無法證明元件實際渲染。 |
| scene-lane L2816 | `multiCandidateSceneRenderPlan?.telemetry.renderedPairCount` | `data-multi-candidate-scene-rendered-pair-count` 等 | 落在紅閘門 | 提案設計的多重 receipt 與 join-key 校驗合理，但目標落點 `validate-homepage-multi-candidate-acceptance-browser.ts` 目前為紅閘門。 |
| scene-lane L2821 | `multiCandidateSceneRenderPlan?.solidDataLinkCount` | `data-multi-candidate-scene-solid-data-link-count` 等 | 落在紅閘門 | 提案主張原斷言「已被覆蓋」可刪除，但其依託的 `validate-homepage-multi-candidate-acceptance-browser.ts` 當前為紅閘門。 |
| scene-lane L2826 | `multiCandidateCentralOverlayActive && !multiCandidateServing...` | canvas `data-multi-candidate-carrier-fallback-active` | OK | 提案正確判定「無法轉換」：指出該 canvas 屬性僅為呼叫端布林式的直接投影，未能證明 fallback mesh 實質渲染，換位置 pin 無實質效益。 |
| scene-lane L2847 | `enabled: simSource === 'live' && sceneFrame.sceneSource ...` | canvas `data-live-simulation-enabled` | OK | 提案正確判定「無法轉換」：指出場景語意旗標不能證明 publisher 停止呼叫 `onSimUpdate`，需補 publisher sequence receipt。 |
| scene-lane L3134 | `showDecisionPolicyControls={sceneLane === 'modqn-live-cell-...` | `[data-testid="modqn-decision-policy-control"]` | 落在紅閘門 | 提案擬修正重用之 `validate-phase-c-lane-experience-bar-browser.ts` 目前為紅閘門。 |
| scene-lane L3154 | `handoverMode === 'omega-heuristic' && sceneLane === 'modqn-...` | `[data-testid="heuristic-not-paper-banner"]`、policy attr | 落在紅閘門 | 提案設計的正負控制邏輯完整，但擬修正重用之 `validate-phase-c-lane-experience-bar-browser.ts` 目前為紅閘門。 |
| scene-lane L3159 | `if (targetLane === 'artifact-replay') { if (handoverMode...` | heuristic banner、artifact sidebar | OK | 提案正確判定「無法轉換」：指出切換路徑不可達，且單純斷言 banner 消失為可空斷言（無法證明 policy 被正確正規化）。 |
| scene-lane L3236 | `<AdvancedSetupDrawer` | `[data-testid="advanced-setup-trigger"]`、`[data-testid="advanced-setup-drawer"]` | selector 無效 | 提案明確依賴 `advanced-setup-trigger` 與 `advanced-setup-drawer`，此二者已查證完全不存在於 `src/`（報告聲稱可由 prefix 產生存有誤判）。 |
| scene-lane L3244 | `showDecisionPolicyControls={sceneLane === 'modqn-live-cell-...` | `[data-testid="advanced-setup-drawer"]` 內的 policy control | selector 無效 | 提案之觀察點容器依賴不存在於 `src/` 的 `advanced-setup-drawer`。 |
| phase-f L257 | `runtime.ueDistributionMode` | `topology-tab-ue-distribution-radio`、`data-first-ue-position` | OK | 提案正確判定「無法轉換」：指出 distribution 只影響 secondary UEs，現有 canvas 僅發布固定 primary UE 位置，無有效遙測可核對。 |
| phase-g L227 | `runtime.ueMobilityParams` | canvas `data-first-ue-position` | OK | 提案正確判定「無法轉換」：指出 UI 控制項已刻意移除且無 secondary 軌跡 telemetry。 |

小計: 削弱=0 selector無效=3 落在紅閘門=6 OK=12

## 最該退回的前 3 條

1. **scene-lane L3236 (`<AdvancedSetupDrawer`)** — **【判定：selector 無效】**
   - **退回原因**：提案報告聲稱 `[data-testid="advanced-setup-trigger"]` 與 `[data-testid="advanced-setup-drawer"]` 是由 `AdvancedDrawerShell.tsx` prefix 實際產生，但已知事實已確認此二 selector **完全不存在於 `src/`**。提案以此非真實存在的 trigger 作為點擊進入點並斷言 drawer，執行期必在第一步 timeout 失敗。
2. **scene-lane L2385 (`modqnVisualLayerPreset === 'explain-handover'`)** — **【判定：selector 無效】**
   - **退回原因**：進入點步驟指名操作 `[data-testid="advanced-setup-trigger"]` 來開啟 drawer 與切換 preset，命中 9 個無效 selector 之一；且目標擴充檔案 `validate-phase-3-overlay-render-browser.ts` 現為紅閘門，雙重不可行。
3. **scene-lane L3244 (`showDecisionPolicyControls`)** — **【判定：selector 無效】**
   - **退回原因**：觀察點路徑明定依賴不存在的父層容器 `[data-testid="advanced-setup-drawer"]` 來尋找 policy control，且放置目標 `validate-phase-c-lane-experience-bar-browser.ts` 當前為紅閘門。
