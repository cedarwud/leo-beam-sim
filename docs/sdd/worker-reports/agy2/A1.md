本批共 22 列提案

| 來源 | 原斷言（截 60 字） | 提案觀察點 | 判定 | 說明（繁中，具體） |
|---|---|---|---|---|
| `validate-phase-b-artifact-picker.tsx:L219` | `source.includes('<ArtifactPicker')` | `[data-testid="lane-experience-modqn-live-cell-preview"]`；`[data-testid="artifact-picker"]` | selector 無效 | 進入點使用 `lane-experience-modqn-live-cell-preview`，已查證為未掛載的死碼元件，執行期永遠無法定位。 |
| `validate-phase-b-artifact-picker.tsx:L221` | `source.includes('onLoadEntry={handleLoadIntoScene}')` | load 按鈕、entry `aria-current`、reward panel 文字 | 削弱 | 原斷言守場景載入回呼 `handleLoadIntoScene` 的接線。提案僅斷言 picker entry `aria-current=true` 與 reward panel 文字為 `user-trained`，這只驗證了 UI 局部狀態更新；若 `handleLoadIntoScene` 本身斷線或未將資料注入 3D 場景，提案仍會呈綠色，但場景載入實質已壞（且進入點依賴死碼 selector）。 |
| `validate-phase-b-artifact-picker.tsx:L225` | `source.includes('onLoadPaperFaithful={handleRevertToPaperFaithful}')` | revert 按鈕屬性、paper entry `aria-current`、reward panel 文字 | 削弱 | 原斷言守場景還原回呼 `handleRevertToPaperFaithful`。提案僅斷言 UI 按鈕狀態與面板文字改為 `paper-faithful`；若 `handleRevertToPaperFaithful` 未真正重置 3D 模擬場景的物理/模型參數，提案仍會呈綠色，但場景還原實質已壞。 |
| `validate-phase-b-artifact-picker.tsx:L229` | `source.includes('artifactReplaySource={showcaseArtifactSource}')` | `[data-testid="modqn-view-modqn-replay-proof"]`；synthetic entry 的 `data-artifact-source` | selector 無效 | 進入點與觀察點使用 `modqn-view-modqn-replay-proof`，已查證為死碼未掛載元件，執行期不可見。 |
| `validate-live-walker-handover-event-focus.tsx:L226` | `timelineRailDescriptor.rail.sourceOwner === 'live-walker'` | `[data-testid="handover-event-rail"]` 的 `data-source-owner`、`data-focus-enabled` | 落在紅閘門 | 宣稱已被既有 probe 覆蓋，但落點 `scripts/validate-live-walker-handover-event-focus-browser.ts` 目前為紅閘門（非 6 個綠色閘門之一），在該驗證器修復前此斷言無效。 |
| `validate-live-walker-handover-event-focus.tsx:L231` | `timelineRailDescriptor.rail.sourceOwner === 'sinr-live-cell-truth'` | rail `data-source-owner`、marker seek、focus panel、shell seek target | 落在紅閘門 | 提案修改之目標檔案 `scripts/validate-live-walker-handover-event-focus-browser.ts` 目前為紅閘門。 |
| `validate-live-walker-handover-event-focus.tsx:L236` | `timelineRailDescriptor.rail.horizonKind === 'live-walker-window'` | rail 的 `data-horizon-kind`、`data-horizon-sec` | 落在紅閘門 | 宣稱已被覆蓋，但落點 `scripts/validate-live-walker-handover-event-focus-browser.ts` 目前為紅閘門。 |
| `validate-phase-h-s5-orbit-trail-modqn-demo.ts:L27` | `appMode !== 'modqn-demo' \|\| reducedMotion` | 無（提案結論為無法轉換，建議改判 S） | OK | 準確指出純記憶體狀態與 R3F primitive 無 DOM telemetry，正確拒絕在 browser 硬轉假斷言，建議改由 pure resolver 單元測試守護。 |
| `validate-phase-h-s5-orbit-trail-modqn-demo.ts:L31` | `orbitTrail: false` | 無（提案結論為無法轉換，建議改判 S） | OK | 準確指出 `OrbitTrail` 為 Three.js 幾何且無 DOM testid，正確建議改為單元測試守護 resolver。 |
| `validate-phase-h-s5-orbit-trail-modqn-demo.ts:L35` | `spineParticles: true` | 無（提案結論為無法轉換，建議改判 S） | OK | 準確指出設定值與 render plan 實際掛載邏輯存在矛盾且無 runtime 訊號，正確建議改判 S 單元測試。 |
| `validate-modqn-omega-s3-replay-mode-wiring.tsx:L262` | `appSrc.includes('data-handover-criterion')` | `[data-testid="advanced-setup-trigger"]`；`[data-testid="leo-shell-canvas"]` | selector 無效 | 進入點使用 `advanced-setup-trigger`，已查證完全不存在於 `src/` 中。 |
| `validate-modqn-omega-s3-replay-mode-wiring.tsx:L271` | `appSrc.includes("if (handoverMode !== 'decision-overlay-on-live-sinr')")` | `[data-testid="advanced-setup-trigger"]`；`[data-testid="modqn-replay-cue-panel"]` | selector 無效 | 進入點使用 `advanced-setup-trigger`，完全不存在於 `src/` 中。 |
| `validate-modqn-phase7k-replay-scene-layer.ts:L419` | `assertContains(appSource, "if (handoverMode !== 'decision-overlay-on-live-sinr')", ...)` | 同上一列（使用 `advanced-setup-trigger` 與 cue panel） | selector 無效 | 進入點使用 `advanced-setup-trigger`，完全不存在於 `src/` 中。 |
| `validate-modqn-phase7k-replay-scene-layer.ts:L440` | `assertContains(appSource, '<ModqnReplayCuePanel', ...)` | `[data-testid="modqn-replay-cue-panel"][data-replay-ready="1"]` | 落在紅閘門 | 宣稱覆蓋於 `scripts/validate-live-walker-handover-event-focus-browser.ts`，但該驗證器為紅閘門。 |
| `validate-phase-h-s3-live-sim-callouts.ts:L79` | `<SinrLiveCellBeamCallouts` | `[data-testid="beam-callout"][data-beam-primary="1"]` | 落在紅閘門 | 宣稱覆蓋於 `scripts/validate-homepage-authority-browser.ts`，但該驗證器目前為紅閘門。 |
| `validate-phase-h-s3-live-sim-callouts.ts:L98` | `beamCalloutsEnabled={showBeamCallouts ? '1' : '0'}` | `[data-testid="beam-info-toggle"]`、canvas telemetry、callout DOM | 落在紅閘門 | 提案落點 `scripts/validate-phase-c-sinr-live-cell-beams-browser.ts` 目前為紅閘門。 |
| `validate-modqn-omega-s4-heuristic-not-paper.tsx:L230` | `handoverMode === 'omega-heuristic' && sceneLane === 'modqn-live-cell-preview' && <HeuristicNotPaperBanner ...>` | `[data-testid="heuristic-not-paper-banner"]`、`role="alert"` | selector 無效 | 進入點依賴 `lane-experience-modqn-live-cell-preview`（死碼無掛載）與 `advanced-setup-trigger`（不存在於 `src/`）。 |
| `validate-phase-b-jobs-panel.tsx:L205` | `appSource.includes('<AdvancedSetupDrawer')` | `[data-testid="advanced-setup-trigger"]`、`[data-testid="advanced-setup-drawer"]` | selector 無效 | 觀察點與觸發點使用的 `advanced-setup-trigger` 與 `advanced-setup-drawer` 皆完全不存在於 `src/`。 |
| `validate-phase-c-camera-preset.tsx:L260` | `controls.enabled = false` | canvas 拖曳前後 `data-camera-position`、`data-camera-target` | 落在紅閘門 | 行為驗證邏輯健全，但落點 `scripts/validate-phase-c-director-cinematic-browser.ts` 目前為紅閘門。 |
| `validate-phase-c-scene-scale-override.tsx:L195` | `visualScaleMultipliers.beamFootprintMultiplier` | 無（提案結論為無法轉換，建議改判 S 或 V） | OK | 準確指出 footprint 幾何無 runtime 觀察點且已不讀 multiplier，拒絕以 shell key 製造假綠，建議改判 S 或 V 正確。 |
| `validate-phase-i-s7b-serving-count-selector.tsx:L241` | `runtime.cellServingCount ?? DEFAULT_SERVING_COUNT` | radio L=8、clear 按鈕、canvas 與 HUD serving count | OK | 新增獨立驗證腳本，透過 storage 預置與清空操作完整驗證 null fallback 預設值與雙向連動，無無效 selector 或紅閘門依賴。 |
| `validate-phase6b-handover-policy-controls.tsx:L148` | `assertContains(appSource, 'parameterSection={');` | `[data-testid="sinr-formula-page"]`、`[data-testid="sinr-formula-tabs"]` | 削弱 | 原斷言守換手策略控制項（handover policy controls）傳入 drawer 的 wiring。提案宣稱由 formula-layout 覆蓋，但該 layout 測試僅量測數學公式 tabs 的排版可見度，未驗證策略控制元件是否存在與可用；若 parameterSection 傳入假元件，layout 測試仍會全綠，實質已壞。 |

小計: 削弱=3 selector無效=7 落在紅閘門=7 OK=5

## 最該退回的前 3 條

1. **`validate-phase6b-handover-policy-controls.tsx:L148`（原斷言：`parameterSection={`）**
   - **退回原因**：這是最嚴重的偽覆蓋。提案宣稱既有 `validate-homepage-sinr-formula-layout-browser.ts` 已能證明此接線，但該 layout 測試只驗證了公式分頁的容器排版尺寸。若工程師重構時將 `parameterSection` 替換為空節點或破壞其中的換手控制元件，公式版面依然量測通過（呈綠燈），但原本要守護的「換手策略控制項正確穿透渲染」已完全壞死。

2. **`validate-phase-b-artifact-picker.tsx:L221`（原斷言：`onLoadEntry={handleLoadIntoScene}`）**
   - **退回原因**：原斷言守的是「點擊載入後將 Artifact 模型資料注入 3D 模擬場景（MainScene）」。提案僅檢查了 Picker 元件自身的 `aria-current` 與側邊 Panel 標題文字是否顯示 `user-trained`。若回呼函式 `handleLoadIntoScene` 斷線或未更新 3D 場景狀態，純 UI 標籤仍會更新成功（呈綠燈），但 3D 模擬場景實質未載入模型。

3. **`validate-phase-b-artifact-picker.tsx:L225`（原斷言：`onLoadPaperFaithful={handleRevertToPaperFaithful}`）**
   - **退回原因**：與上一條相同，原斷言守的是「還原 3D 模擬場景至 Paper-faithful 基準」。提案僅斷言按鈕自身 `aria-pressed` 與 Panel 標題改為 `paper-faithful`，完全沒有驗證 3D 場景是否真正還原參數。若還原回呼失效，測試依舊假綠，造成假安全感。
