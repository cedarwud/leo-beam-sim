本批共 23 條，我會輸出 23 列

## scripts/validate-frontend-scene-lane-governance.ts

| 行號 | 斷言字面（原文照抄，可截到 60 字） | 類別 | 分解衝擊 | 處置建議（繁中，一到兩句，具體） |
|---|---|---|---|---|
| 2826 | `multiCandidateCentralOverlayActive && !multiCandidateServingCarrierRenderable` | B | RED | 改為 Playwright 測試：在 Central Overlay 啟用且 serving carrier 不可渲染時，斷言 overlay fallback DOM 節點或 canvas dataset 狀態呈現；搬遷至 Overlay 子元件會導致原檔字串檢查失敗。 |
| 2847 | `enabled: simSource === 'live' && sceneFrame.sceneSource !== 'artifact-replay'` | B | RED | 改為 Playwright 測試：切換至 `artifact-replay` 模式時，驗證 canvas 遙測屬性或網路層無 live SimState 推播；抽出 publisher hook 至子模組後原檔字串會變紅。 |
| 2907 | `const [sceneSource, setSceneSource] = useState<SceneSourceMode>` | V | RED | 刪除此斷言；釘住內部 state 宣告語法，若重構成 custom hook（如 `useSceneLaneState`），使用者在 UI 模式切換上毫無感知，純屬內部實作細節。 |
| 2912 | `const handleExperienceChange = useCallback((targetLane: SceneLane) => {` | V | RED | 刪除此斷言；釘住單一回呼函式名稱與宣告方式，切換 experience 時的實際狀態重置與 URL 同步應由既有行為測試保護，更名或抽離對使用者無差別。 |
| 3057 | `from './ui/AdvancedSetupDrawer'` | V | RED | 刪除此斷言；釘死 App.tsx 的單一 import 路徑，抽離至 Shell/Layout 容器時必然假紅，且 TypeScript 編譯器與 Drawer 掛載行為已有充分防護。 |
| 3134 | `showDecisionPolicyControls={sceneLane === 'modqn-live-cell-preview'}` | B | RED | 改為 Playwright 測試：在 `modqn-live-cell-preview` 斷言 `[data-testid="decision-policy-controls"]` 可見，切換到其他 lane 斷言節點隱藏；此 JSX prop 移至子元件會變紅。 |
| 3149 | `from './ui/HeuristicNotPaperBanner'` | V | RED | 刪除此斷言；純粹釘住 App.tsx 的 import 語法，Banner 搬至橫幅容器時會假紅，而使用者只在乎 Banner 是否正確依條件渲染在畫面上。 |
| 3154 | `handoverMode === 'omega-heuristic' && sceneLane === 'modqn-live-cell-preview' && <HeuristicNotPaperBanner />` | B | RED | 改為 Playwright 測試：在 `modqn-live-cell-preview` 下選擇 `omega-heuristic` 模式，斷言 `[data-testid="heuristic-not-paper-banner"]` 出現，切換模式或 lane 斷言消失；JSX 條件移出原檔會變紅。 |
| 3159 | `if (targetLane === 'artifact-replay') {\n      if (handoverMode === 'omega-heuristic')` | B | RED | 改為 Playwright 測試：在 live 下啟用 `omega-heuristic` 並切換至 `artifact-replay`，斷言 policy 下拉選單自動重置離開 heuristic；多行字串比對極度脆弱且抽離邏輯後必變紅。 |
| 3164 | `onModqnDecisionPolicyChange={handleModqnDecisionPolicyChange}` | V | RED | 刪除此斷言；釘住內部 prop 命名與接線，若改用 Context 或內聯處理，行為完全不變但字串斷言即假紅，點擊切換效果應由 E2E 互動測試覆蓋。 |
| 3236 | `<AdvancedSetupDrawer` | B | RED | 改為 Playwright 測試：操作觸發按鈕，斷言 `[data-testid="advanced-setup-drawer"]` 出現於 DOM 中；抽至 Layout/Shell 模組會導致 App.tsx 遺失此標籤。 |
| 3244 | `showDecisionPolicyControls={sceneLane === 'modqn-live-cell-preview'}` | B | RED | （與 3134 重疊）改為 Playwright 測試：在 Drawer 內檢查 `[data-testid="decision-policy-controls"]` 僅在 `modqn-live-cell-preview` lane 出現；字面搬入抽離的 Drawer 容器會導致 App.tsx 變紅。 |
| 3249 | `onModqnVisualLayerPresetChange={setModqnVisualLayerPreset}` | V | RED | 刪除此斷言；釘住內部 state setter 與 prop 接線名稱，狀態重構至 Store/Context 時使用者毫無感知，但靜態字串檢查會立刻變紅。 |
| 3254 | `onModqnDecisionPolicyChange={handleModqnDecisionPolicyChange}` | V | RED | （與 3164 重複）刪除此斷言；純內部 prop 命名綁定，無 runtime 語意保證，重構搬遷後使用者視覺與操作皆無差異。 |
| 3259 | `onLoadEntry={handleLoadIntoScene}` | V | RED | 刪除此斷言；釘住 Model Library 的 callback 屬性名稱，真實行為已由點擊載入後場景狀態改變之測試覆蓋，內部換名或搬移不影響使用者。 |

## scripts/validate-phase-d-app-wire.tsx

| 行號 | 斷言字面（原文照抄，可截到 60 字） | 類別 | 分解衝擊 | 處置建議（繁中，一到兩句，具體） |
|---|---|---|---|---|
| 100 | `import { fetchUserTrainedBundleEnvelope } from './modqn/training-trigger/userTrainedBundleFetch';` | S | RED | 改為去檔名化架構約束：透過 ESLint 或 AST/glob 規則檢查全專案 `src/**/*.{ts,tsx}` 對 envelope fetch 的引用必須來自 training-trigger 模組而非舊 barrel；App 分解移出此 import 會變紅。 |
| 233 | `data-testid="load-into-scene-error-banner"` | B | RED | 改為 Playwright 測試：觸發模型載入錯誤情境，斷言 `page.getByTestId('load-into-scene-error-banner')` 於畫面上可見且文字正確；錯誤 Banner 抽取為獨立元件後 App.tsx 字串檢查會變紅。 |
| 248 | `onLoadPaperFaithful={handleRevertToPaperFaithful}` | V | RED | 刪除此斷言；釘住內部回呼 prop 名稱，真正功能（點擊 revert 按鈕還原預設）由 252 行 testid 搭配的瀏覽器測試保護，內部接線語法改變對使用者無影響。 |

## scripts/validate-phase-b-service-client.tsx

| 行號 | 斷言字面（原文照抄，可截到 60 字） | 類別 | 分解衝擊 | 處置建議（繁中，一到兩句，具體） |
|---|---|---|---|---|
| 184 | `import { ServiceStatusBanner } from './ui/modqn-training/ServiceStatusBanner';` | V | RED | 刪除此斷言；單檔 import 語句釘死，側邊欄抽離至 `RightSidebar.tsx` 時 App.tsx 將不再包含此 import，TypeScript 編譯器已能確保型別與模組存在。 |
| 188 | `<ServiceStatusBanner appMode={appMode} />` | B | RED | 改為 Playwright 測試：定位右側欄 `page.locator('aside.leo-shell-right [data-testid="service-status-banner"]')` 並驗證在不同 `appMode` 下橫幅渲染與狀態正確；側邊欄抽離後 App.tsx 會變紅。 |

## scripts/validate-phase-h-s3-live-sim-callouts.ts

| 行號 | 斷言字面（原文照抄，可截到 60 字） | 類別 | 分解衝擊 | 處置建議（繁中，一到兩句，具體） |
|---|---|---|---|---|
| 79 | `<SinrLiveCellBeamCallouts` | B | RED | 改為 Playwright 測試：操作 UI 開啟「Beam Info」開關，斷言畫面上出現對應的 Beam Callout DOM 節點（`[data-testid="beam-callout"]` 或圖層標籤）；MainScene 拆分子圖層後原檔字串檢查會變紅。 |
| 98 | `beamCalloutsEnabled={showBeamCallouts ? '1' : '0'}` | B | RED | 改為 Playwright 測試：切換 Beam Info 開關，直接讀取 canvas/scene 容器的 dataset `expect(await page.locator('#scene-root').getAttribute('data-beam-callouts-enabled')).toBe('1')`；遙測傳遞改至子圖層時原檔字串會變紅。 |

## scripts/validate-modqn-omega-s4-heuristic-not-paper.tsx

| 行號 | 斷言字面（原文照抄，可截到 60 字） | 類別 | 分解衝擊 | 處置建議（繁中，一到兩句，具體） |
|---|---|---|---|---|
| 230 | `handoverMode === 'omega-heuristic' && sceneLane === 'modqn-live-cell-preview' && <HeuristicNotPaperBanner />` | B | RED | 改為 Playwright 測試：切換至 `modqn-live-cell-preview` 並選取 `omega-heuristic` 模式，斷言 `[data-testid="heuristic-not-paper-banner"]` 顯示，切換至其他 lane 或模式斷言隱藏；條件渲染抽離後 App.tsx 檢查會變紅。 |

實際輸出 23 列
