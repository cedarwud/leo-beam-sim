本批共 4 個檔案的 diff

## scripts/validate-modqn-handover-story-layer.ts

| 判定 | 被刪/被改的斷言（原文照抄，可截到 70 字） | 問題說明（繁中，具體） | 建議動作 |
| :--- | :--- | :--- | :--- |
| `OK` | `assertContains(mainScene, 'deriveProfileHandoverStoryModel', 'MainScene derives story model');` | 字面在目標檔出現 2 次，單純字串搜尋無法精確鎖定推導邏輯位置；且下方仍保留 Story Layer 渲染與掛載條件檢查。符合計數授權。 | 維持刪除。 |
| `失去保護` | `assertContains(mainScene, 'selectProfileDerivedHandoverCues', 'MainScene caps profile-derived handover cue density');` | 出現 2 次符合授權，但「MainScene 對 Handover Cues 進行密度上限過濾（density cap）」是重要的防畫面過載行為。刪除後僅剩開關旗標檢查，密度過濾完全無人守護。 | 改以單元測試覆蓋密度過濾演算法，或以 AST 檢查 MainScene 有調用該過濾函式。 |
| `失去保護` | `assertContains(mainScene, 'resolveCellBeamConeSatelliteCount', 'MainScene computes MODQN beam-cone satellite telemetry');` | 出現 2 次符合授權，但刪除後「MODQN 波束圓錐衛星數量遙測計算」在靜態契約中失去保護，後續的 telemetry 斷言未涵蓋此衛星計數計算。 | 應在 `SceneTelemetry` 補上對應的 dataset 遙測屬性檢查，或增加行為單元測試。 |
| `OK` | `assertContains(mainScene, 'replayBackedHandoverStoryVisible', 'MainScene keeps replay proof story telemetry source-backed');` | 出現 3 次符合授權。且下方已保留 `telemetry` 對 `el.dataset.handoverStoryNotBaselineProof` 的實質輸出檢查，無實質盲區。 | 維持刪除。 |

小計: 越權=0 理由不成立=0 失去保護=2 OK=2

---

## scripts/validate-phase-h-s8-hud-camera-default.ts

| 判定 | 被刪/被改的斷言（原文照抄，可截到 70 字） | 問題說明（繁中，具體） | 建議動作 |
| :--- | :--- | :--- | :--- |
| `OK` | `expect(source.includes('modqnDemoCameraAppliedRef'), 'App.tsx declares modqnDemoCameraAppliedRef');` | 字面在 `App.tsx` 出現 4 次，無法證明特定宣告行為；下方仍保留相機預設角度排程斷言 `camera.selectCameraPreset('oblique')`。 | 維持刪除。 |
| `失去保護` | `expect(source.includes('sceneSource={sceneSource}'), 'App.tsx threads sceneSource into HUD truth chip');` | 字面在 `App.tsx` 出現 2 次符合刪除授權，但刪除後 `validateHudMount` 僅剩 `<ModqnSceneHud` 標籤掛載檢查，HUD Truth Chip 資料管線失去保護。 | 改以 AST 解析 `<ModqnSceneHud>` 的 JSX Props 是否帶有 `sceneSource`，或建立 HUD 整合測試。 |
| `失去保護` | `expect(source.includes('bundleProvenanceKind={bundleProvenanceKind}'), 'App.tsx threads bundleProvenanceKind into HUD truth chip');` | 字面出現 5 次符合授權，但與上一條同屬一組，刪除後 HUD 來源屬性注入完全無斷言守護。 | 改以 AST 解析 `<ModqnSceneHud>` 標籤屬性，避免全檔純文字搜尋。 |
| `失去保護` | `expect(source.includes('simState={simState}'), 'App.tsx threads simState into HUD');` | 字面出現 2 次符合授權，但刪除後 HUD 接收模擬狀態（`simState`）的連線無任何靜態斷言保護。 | 改以 AST 解析 `<ModqnSceneHud>` 標籤屬性或以渲染測試接手。 |

小計: 越權=0 理由不成立=0 失去保護=3 OK=1

---

## scripts/validate-phase-b-artifact-picker.tsx

| 判定 | 被刪/被改的斷言（原文照抄，可截到 70 字） | 問題說明（繁中，具體） | 建議動作 |
| :--- | :--- | :--- | :--- |
| `OK` | `assert(source.includes('fetchTrainingServiceManifest'), 'App.tsx imports fetchTrainingServiceManifest');` | 字面出現 2 次（import 與使用處），屬於無效的假綠字串 pin，刪除符合授權。 | 維持刪除；服務清單載入由 API / service 測試守護。 |
| `OK` | `assert(source.includes('useState<string | null>(null)'), 'App.tsx tracks selectedUserTrainedJobId');` | 通用型別 Hook 簽名在 `App.tsx` 出現 6 次，完全無法證明特定狀態追蹤，是典型的假綠斷言，刪除合理。 | 維持刪除。 |
| `OK` | `assert(source.includes('handleLoadIntoScene'), 'App.tsx defines handleLoadIntoScene callback');` | 出現 2 次符合授權，且下方緊接著保留了更精準的接線斷言 `onLoadEntry={handleLoadIntoScene}`，核心功能保護未中斷。 | 維持刪除。 |

小計: 越權=0 理由不成立=0 失去保護=0 OK=3

---

## scripts/validate-phase9b-power-noise-separation.tsx

| 判定 | 被刪/被改的斷言（原文照抄，可截到 70 字） | 問題說明（繁中，具體） | 建議動作 |
| :--- | :--- | :--- | :--- |
| `失去保護` | `assertContains(appSource, 'setStaleFormulaEvidenceKey(getSignalTuningEvidenceKey(next))');` | 出現 2 次符合授權，但這是驗證「訊號調諧參數變更時觸發公式證據失效機制」的唯一防線。刪除後該檔案對此失效路徑完全零檢查，且造成變數 `appSource` 閒置殘留。 | 暫緩刪除或改用 AST 檢查 onChange 回呼函式內部之失效呼叫；並處理 `appSource` 殘留。 |
| `失去保護` | `assertContains(appSource, 'isFormulaEvidenceStale={staleFormulaEvidenceKey !== null}');` | 出現 4 次符合授權，但刪除後整個 `assertTuningWiringAndEvidencePath()` 對 `App.tsx` 傳遞 stale 狀態完全無檢查，加劇殘留變數問題。 | 改以 AST 檢查接收該 Prop 的面板元件 JSX 屬性，並清理未使用的 `appSource`。 |

小計: 越權=0 理由不成立=0 失去保護=2 OK=0

---

## 總結

### 專項審查發現

1. **複合斷言連坐（首要檢查項）**：經逐條比對，本批 4 個檔案被刪除的 11 處斷言均為針對單一檔案變數（`mainScene` / `source` / `appSource`）的單一表達式，**本批無複合斷言（如 `&&` 連坐）遭到誤刪的情況**，未發生越權刪除。
2. **假 re-pin 檢查**：無任何就地修改字面值以強行變綠的行為，全數皆為註解刪除。
3. **殘留物（Residue）**：在 [`scripts/validate-phase9b-power-noise-separation.tsx`](file:///scripts/validate-phase9b-power-noise-separation.tsx) 中，刪除兩條斷言後，上方的 `const appSource = readFileSync(...)` 變成完全無人讀取的**閒置變數（unused local variable）**，若啟用 `noUnusedLocals` 將導致編譯/Lint 報錯。

---

### 最該退回或優先補救的前 3 條

1. **[`scripts/validate-phase9b-power-noise-separation.tsx`](file:///scripts/validate-phase9b-power-noise-separation.tsx) 的 `setStaleFormulaEvidenceKey(...)`**
   - **原因**：這是調諧參數連動公式證據失效（Stale Evidence Invalidation）的核心更新路徑。刪除後整個函式對 `App.tsx` 形成 0 斷言覆蓋，且留下 `appSource` 殘留變數。
2. **[`scripts/validate-phase-h-s8-hud-camera-default.ts`](file:///scripts/validate-phase-h-s8-hud-camera-default.ts) 的 `<ModqnSceneHud />` 屬性組（`sceneSource` / `bundleProvenanceKind` / `simState`）**
   - **原因**：一口氣將 `<ModqnSceneHud />` 的三大關鍵資料注入（Provenance Truth Chip 與 Simulation State）全部拔除，只剩下空的標籤存在檢查 `<ModqnSceneHud`，使 HUD 呈現空殼化風險。
3. **[`scripts/validate-modqn-handover-story-layer.ts`](file:///scripts/validate-modqn-handover-story-layer.ts) 的 `selectProfileDerivedHandoverCues`**
   - **原因**：守護 MainScene 對切換提示（Handover Cues）進行密度上限過濾（Density Cap）以防止視覺與渲染效能崩潰，刪除後無任何靜態契約防線。

---

### 沒有把握的地方

1. **資訊不足（原始檔分佈）**：僅憑 diff 無法得知 `App.tsx` 與 `MainScene.tsx` 內部多次出現這些字面的確切上下文（例如 `sceneSource={sceneSource}` 出現 2 次是否分別位於 `ArtifactSceneContent` 與 `ModqnSceneHud`，或 `setStaleFormulaEvidenceKey` 出現 2 次是否屬於兩個不同的 Tab/Slider）。
2. **未知的運行期測試覆蓋率**：資訊不足以確認專案是否有平行的 Playwright / Vitest 運行期測試正在守護這些被刪除的 Prop 傳遞與資料流。若無運行期測試，上述判定為「失去保護」的項目風險將大幅提升。
