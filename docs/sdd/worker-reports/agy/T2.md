本批共 22 條，我會輸出 22 列

## scripts/validate-frontend-scene-lane-governance.ts

| 行號 | 斷言字面（原文照抄，可截到 60 字） | 類別 | 分解衝擊 | 處置建議（繁中，一到兩句，具體） |
| :--- | :--- | :---: | :---: | :--- |
| 623 | `const [modqnVisualLayerPreset, setModqnVisualLayerPreset]` | V | RED | 刪除。此斷言僅鎖定內部 React state 宣告字面；狀態抽至 custom hook 時必紅但對使用者零影響，應由現有 preset UI 控制項測試覆蓋。 |
| 637 | `from './app/sceneLane'` | V | RED | 刪除。純粹釘死特定相對路徑 import 字串，若將 scene lane 封裝至子元件或容器模組將引發假紅，無實質保護力。 |
| 638 | `modqnReplayProofRequested: modqnReplayProofRequestActive` | B | RED | 轉為 Playwright 行為測試。於 UI 觸發重播證明請求（點選 proof toggle/按鈕），驗證容器 `[data-scene-lane="modqn-replay-proof"]` 狀態啟用與證明面板之顯示。 |
| 647 | `from './app/timelineRailAuthority'` | V | RED | 刪除。釘死 App.tsx 的內部 import 語法，抽取時間軸容器或 Hook 時會假紅，對使用者無任何可觀察差異。 |
| 648 | `from './app/liveWalkerHandoverRailAdapter'` | V | RED | 刪除。為內部適配器之引用字面，重構至 timeline 領域模組時無效中斷建置，應予刪除。 |
| 649 | `from './scene/liveWalkerHandoverEventIndex'` | V | RED | 刪除。斷言 App 直接依賴特定 scene helper 檔名與路徑，違反模組封裝且對執行期行為毫無保障。 |
| 652 | `buildLiveWalkerHandoverEventIndex({` | B | RED | 轉為 Playwright 行為測試。載入即時 Walker 場景，定位 `[data-testid="timeline-rail"]` 並斷言渲染出對應數量與位置的換手事件標記 `[data-testid^="handover-event-"]`。 |
| 653 | `liveWalkerHandoverEventIndexToRailEvents(liveWalkerHandoverEventIndex)` | B | RED | 轉為 Playwright 行為測試。於即時模擬模式下檢查時間軸軌道（`[data-testid="timeline-rail"]`）是否成功將事件轉換並呈現為軌道事件節點。 |
| 656 | `producerTraceDisplayDurationSec` | B | RED | 轉為 Playwright 行為測試。在 MODQN 重播模式下讀取時間軸與軌道屬性（如 `[data-testid="timeline-bar"]` 之 duration 標籤），斷言資料源視窗秒數與顯示拉伸軌道長度已正確分離呈現。 |
| 661 | `horizonSec={activeTimelineDescriptor.horizonSec}` | B | RED | 轉為 Playwright 行為測試。透過 Selector `[data-testid="timeline-bar"]` 檢查 DOM 屬性 `data-horizon-sec`，斷言其數值與當前場景描述子之資料源 horizon 一致。 |
| 664 | `const liveTimelineWindowStartSec = demoStartOffset;` | B | RED | 轉為 Playwright 行為測試。設定指定之 `demoStartOffset` 載入即時場景，選取 `[data-testid="timeline-start-time"]` 斷言時間軸起點刻度精確對齊選定之視窗偏移。 |
| 665 | `simState.simTimeSec - liveTimelineWindowStartSec` | B | RED | 轉為 Playwright 行為測試。推進模擬進度，選取 `[data-testid="timeline-current-time"]` 斷言顯示值為視窗經過時間（elapsed time）而非絕對模擬時間。 |
| 666 | `demoStartOffsetSec: demoStartOffset` | B | RED | 轉為 Playwright 行為測試。在時間軸進行拖曳/seek 互動，斷言時間軸視窗起點標籤與 `[data-testid="timeline-bar"]` 的 window offset 屬性維持不變。 |
| 667 | `const absoluteTargetSec = Math.min(` | B | RED | 轉為 Playwright 行為測試。模擬將時間軸 scrubber 拖曳至超出邊界的位置，斷言目標時間被限制在最大允許秒數內，未發生越界異常。 |
| 670 | `if (sceneLane === 'sinr-live' \|\| sceneLane === 'modqn-live-cell-preview') return liveWalkerHandoverRailEvents;` | B | RED | 轉為 Playwright 行為測試。分別切換 sceneLane 至 `sinr-live` 與 `modqn-live-cell-preview`，斷言 `[data-testid="handover-rail"]` 均載入並呈現 Live Walker 換手事件。 |
| 671 | `if (sceneLane === 'modqn-replay-proof') return modqnHandoverRailEvents;` | B | RED | 轉為 Playwright 行為測試。切換 sceneLane 至 `modqn-replay-proof`，斷言 `[data-testid="handover-rail"]` 呈現之軌道事件節點來自 MODQN 生產者重播資料。 |
| 676 | `durationSec={timelineRailDescriptor.rail.durationSec}` | B | RED | 轉為 Playwright 行為測試。檢查 `[data-testid="handover-rail"]` 之 `data-duration-sec` 屬性或寬度標記，驗證其數值與當前場景描述子之宣告長度一致。 |
| 677 | `sourceLabel={timelineRailDescriptor.rail.sourceLabel}` | B | RED | 轉為 Playwright 行為測試。讀取 `[data-testid="rail-source-label"]` 之文字內容，斷言顯示之資料源標籤與目前啟用之 descriptor 定義相符。 |
| 678 | `sourceOwner={timelineRailDescriptor.rail.sourceOwner}` | B | RED | 轉為 Playwright 行為測試。定位 `[data-testid="handover-rail"]` 並檢查 DOM 屬性 `data-source-owner`，斷言其值符合當前描述子設定。 |
| 679 | `sourceGapReasons={timelineRailDescriptor.rail.sourceGapReasons}` | B | RED | 轉為 Playwright 行為測試。在具有資料間隙之情境下，斷言 `[data-testid="handover-rail"]` 出現警告標記 `[data-testid="source-gap-warning"]` 且文字正確。 |
| 680 | `const timelineDurationSec = activeTimelineDescriptor.durationSec;` | B | RED | 轉為 Playwright 行為測試。切換不同場景模式，選取 `[data-testid="timeline-bar"]` 並斷言顯示與屬性中的總時長隨 active descriptor 動態更新。 |
| 684 | `const activeTimelineDescriptor = isArchivedTleSceneActive` | B | RED | 轉為 Playwright 行為測試。於首頁切換存檔 TLE 場景，斷言 `[data-testid="timeline-bar"]` 之 `data-source-owner` 變更為 `archived-tle-run` 且時長為兩小時視窗。 |

實際輸出 22 列
