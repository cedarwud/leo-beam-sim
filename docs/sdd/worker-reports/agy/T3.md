本批共 23 條，我會輸出 23 列

## scripts/validate-frontend-scene-lane-governance.ts

| 行號 | 斷言字面（原文照抄，可截到 60 字） | 類別 | 分解衝擊 | 處置建議（繁中，一到兩句，具體） |
| :--- | :--- | :--- | :--- | :--- |
| 1391 | `canToggleModqnReplayProof ? setModqnReplayProofRequested : undefined` | B | RED | 轉為 Playwright 行為測試：在具備與不具備切換資格的情境下，點擊 Proof 切換按鈕並斷言 Proof Viewport 是否正確展開/切換，或按鈕是否處於 disabled 狀態。 |
| 1392 | `getLeftSidebarTabsForSceneLane(sceneLane, handoverMode)` | B | RED | 轉為 Playwright 行為測試：切換不同的 `sceneLane` 與 `handoverMode`，檢查左側側邊欄渲染出的 Tab 標籤項目清單是否符合預期。 |
| 1393 | `getRightSidebarTabsForSceneLane(sceneLane, handoverMode)` | B | RED | 轉為 Playwright 行為測試：切換不同的 `sceneLane` 與 `handoverMode`，檢查右側側邊欄渲染出的 Tab 標籤項目清單是否正確。 |
| 1394 | `activeLeftSidebarTab === 'evidence'` | B | RED | 轉為 Playwright 行為測試：點擊或切換至 `evidence` 分頁，斷言 Evidence/Replay 面板內容 DOM（如 `[data-testid="artifact-replay-sidebar"]`）是否正確可見。 |
| 1395 | `data-testid="artifact-replay-sidebar"` | B | RED | 轉為 Playwright 行為測試：在對應模式下開啟側邊欄，直接以 `page.locator('[data-testid="artifact-replay-sidebar"]')` 斷言其存在於 DOM 中。 |
| 1399 | `activeRightSidebarTab === 'artifact'` | B | RED | 轉為 Playwright 行為測試：切換右側側邊欄至 `artifact` 分頁，斷言 Artifact 相關面板與屬性是否正確渲染於畫面。 |
| 1403 | `!recordedReplayActive \|\| activeSceneFrame !== undefined` | B | RED | 轉為 Playwright 行為測試：模擬錄製回放幀尚未就緒時斷言出現 fail-closed placeholder，就緒後斷言該 placeholder 消失並正常渲染場景。 |
| 1404 | `data-testid="artifact-scene-fail-closed"` | B | RED | 轉為 Playwright 行為測試：在回放幀未載入時，直接以 `page.locator('[data-testid="artifact-scene-fail-closed"]')` 斷言該 placeholder 於 DOM 中可見。 |
| 1405 | `if (sceneSource === 'artifact-replay' \|\| appMode !== 'modqn-demo') return;` | B | RED | 轉為 Playwright 網路行為測試：在 `artifact-replay` 或非 `modqn-demo` 模式啟動時，監聽網路請求並斷言未發送 MODQN replay bundle 的 fetch 請求。 |
| 1413 | `sceneLane === 'modqn-live-cell-preview' && <ServiceStatusBanner appMode={appMode} />` | B | RED | 轉為 Playwright 行為測試：切換至 `modqn-live-cell-preview` 時斷言 ServiceStatusBanner 出現，切換至其他 lane 則斷言該元素不存在於 DOM。 |
| 1469 | `axisPlaying={!playback.paused}` | B | RED | 轉為 Playwright 行為測試：操作播放/暫停控制項，觀察 HandoverEventRail 軌道上的播放狀態標記或動畫屬性是否同步切換。 |
| 1470 | `axisPlaybackRate={playback.effectiveSpeed}` | B | RED | 轉為 Playwright 行為測試：調整播放速率（如 1x 切換為 5x），斷言 HandoverEventRail 上的播放速率相關屬性或 CSS 變數是否相應更新。 |
| 1503 | `from './ui/SinrLiveDisplayDrawer'` | V | RED | 刪除。這是對單一檔案 import 路徑字面的死釘；只要抽出的 Drawer 元件能正常在 DOM 渲染，App 本身是否直接 import 該路徑對使用者與架構毫無差別。 |
| 1589 | `parameterSection={` | B | RED | 轉為 Playwright 行為測試：打開 SINR-live Drawer，斷言其內部是否包含 TLE 參數設定區塊之 DOM 元素或 testid。 |
| 1594 | `teachingPolicySection={isWalkerSceneActive ? (` | B | RED | 轉為 Playwright 行為測試：在 Walker 場景啟用與停用狀態下分別打開 Drawer，斷言 Walker 教學策略區塊是否正確渲染或隱藏。 |
| 1739 | `if (!showSinrServingMosaic) return null;` | B | RED | 轉為 Playwright 行為測試：在非 SINR 模式（或關閉 mosaic 時），檢查 Canvas 遙測屬性，斷言 SINR mosaic 渲染計數為 0 且 UE 標記顏色維持 MODQN 來源。 |
| 1744 | `buildSinrServingUeColorMapFromCells(cellFrame.ues)` | B | RED | 轉為 Playwright 行為測試：在 SINR-live 模式下隨 cell frame 變更，斷言 Canvas 遙測中 UE 標記的著色狀態正確對應 cell serving truth。 |
| 1943 | `recentHandoverEvents: (sim.sinrLiveCells?.recentHandoverEvents ?? [])` | B | RED | 轉為 Playwright 行為測試：模擬換手事件發生時，讀取 Canvas 上 `data-sinr-live-handover-pulse-cone-rendered-count` 遙測屬性，斷言脈衝光錐計數大於 0。 |
| 1950 | `telemetryCountDatasetKey="sinrLiveHandoverPulseConeRenderedCount"` | B | RED | 轉為 Playwright 行為測試：在瀏覽器中直接斷言 Canvas DOM 節點上存在 `data-sinr-live-handover-pulse-cone-rendered-count` 屬性。 |
| 1960 | `telemetryCountDatasetKey="sinrLiveTriggeredIntraConeRenderedCount"` | B | RED | 轉為 Playwright 行為測試：在瀏覽器中直接斷言 Canvas DOM 節點上存在 `data-sinr-live-triggered-intra-cone-rendered-count` 屬性。 |
| 1965 | `resolveTriggeredIntraConeItems({` | B | RED | 轉為 Playwright 行為測試：觸發 intra 換手事件後，在 2.5 秒顯示視窗內驗證 Canvas 遙測 `data-sinr-live-triggered-intra-cone-rendered-count` 數值大於 0。 |
| 2117 | `const showUav = sceneLane === 'sinr-live';` | B | RED | 轉為 Playwright 行為測試：切換 `sceneLane` 至 `sinr-live` 時斷言 UAV 遙測/渲染存在，切換至其他 lane 則斷言 UAV 隱藏且計數為 0。 |

## scripts/validate-phase-c-scene-scale-override.tsx

| 行號 | 斷言字面（原文照抄，可截到 60 字） | 類別 | 分解衝擊 | 處置建議（繁中，一到兩句，具體） |
| :--- | :--- | :--- | :--- | :--- |
| 195 | `visualScaleMultipliers.beamFootprintMultiplier` | B | RED | 轉為 Playwright 行為測試：在 SignalTuningPanel 調整 beam scale 數值，斷言 Canvas 上光束涵蓋面積遙測值或視覺縮放倍率產生相應變更。 |

實際輸出 23 列
