本批共 23 條，我會輸出 23 列

## scripts/validate-phase-h-s8-hud-camera-default.ts

| 行號 | 斷言字面（原文照抄，可截到 60 字） | 類別 | 分解衝擊 | 處置建議（繁中，一到兩句，具體） |
| :--- | :--- | :--- | :--- | :--- |
| 27 | `source.includes("camera.selectCameraPreset('oblique')")` | B | RED | 轉為 Playwright 測試：切換進入 `modqn-demo` 模式，操作 UI 模式切換按鈕，斷言相機預設下拉選單或 canvas 遙測屬性（如 `[data-camera-preset="oblique"]`）變更為 `oblique`。 |
| 31 | `source.includes("appMode === 'modqn-demo' && !modqnDemoCameraAppliedRef.current")` | V | RED | 刪除。釘死內部 `useRef` 命名與條件運算式語法；業務意圖（單次套用預設）應由 Playwright 測試（進入模式後手動調整鏡頭，驗證 re-render 時不會強制重置相機）保證。 |
| 35 | `source.includes("appMode !== 'modqn-demo'") && source.includes('modqnDemoCameraAppliedRef.current = false')` | V | RED | 刪除。釘死內部 ref 重設賦值語法，使用者完全無法感知；重入行為應由 Playwright 測試（離開 `modqn-demo` 再重新進入，確認相機再次切回預設視角）覆蓋。 |
| 41 | `mainScene.includes("'paper-faithful-closeup'")` | B | RED | 轉為 Playwright 測試：檢查相機預設選單控制項中存在 `paper-faithful-closeup`（如 `option[value="paper-faithful-closeup"]`），選取後觀察 canvas 鏡頭位移與視角變化。 |
| 74 | `source.includes("import { ModqnSceneHud } from './ui/modqn-controls/ModqnSceneHud'")` | V | RED | 刪除。釘死特定檔案的 import 路徑寫法，阻礙 barrel 匯入或元件重構；只要 HUD 正常渲染，使用者對 import 語法無感。 |
| 77 | `source.includes('<ModqnSceneHud')` | B | RED | 轉為 Playwright 測試：在對應場景與模式下，斷言 HUD 容器（如 `[data-testid="modqn-scene-hud"]`）在 DOM 中可見且正常渲染。 |

## scripts/validate-phase-b-artifact-picker.tsx

| 行號 | 斷言字面（原文照抄，可截到 60 字） | 類別 | 分解衝擊 | 處置建議（繁中，一到兩句，具體） |
| :--- | :--- | :--- | :--- | :--- |
| 211 | `source.includes('import { ArtifactPicker }')` | V | RED | 刪除。釘死 import 識別字字面；若將選取器封裝進 Modal 或側邊欄子模組會假紅，且執行期由誰直接 import 對使用者無差別。 |
| 215 | `source.includes("useState<'paper-faithful' \| 'user-trained'>('paper-faithful')")` | V | RED | 刪除。釘死內部 `useState` 泛型簽名與初值字面；初值狀態應由 UI 預設渲染的 provenance 標籤或徽章（如 `[data-testid="provenance-badge"]` 顯示 `paper-faithful`）由行為測試覆蓋。 |
| 219 | `source.includes('<ArtifactPicker')` | B | RED | 轉為 Playwright 測試：點擊開啟 Model Library / Artifact Picker 按鈕，斷言選取器對話框 `[data-testid="artifact-picker-modal"]` 正確掛載並可見。 |
| 221 | `source.includes('onLoadEntry={handleLoadIntoScene}')` | B | RED | 轉為 Playwright 測試：在 ArtifactPicker 中選取模型條目並點擊載入，斷言場景狀態與模型名稱更新為選定的模型資料。 |
| 225 | `source.includes('onLoadPaperFaithful={handleRevertToPaperFaithful}')` | B | RED | 轉為 Playwright 測試：在 Model Library 介面點擊「恢復為論文基準 (Paper-Faithful)」，斷言系統標籤與場景參數切換回論文預設值。 |
| 229 | `source.includes('artifactReplaySource={showcaseArtifactSource}')` | B | RED | 轉為 Playwright 測試：開啟 ArtifactPicker 時，斷言來源選擇/顯示區塊（如 `[data-testid="artifact-replay-source"]`）正確呈現目前來源標籤內容。 |

## scripts/validate-modqn-omega-s3-replay-mode-wiring.tsx

| 行號 | 斷言字面（原文照抄，可截到 60 字） | 類別 | 分解衝擊 | 處置建議（繁中，一到兩句，具體） |
| :--- | :--- | :--- | :--- | :--- |
| 262 | `appSrc.includes('data-handover-criterion')` | B | RED | 轉為 Playwright 測試：定位 DOM 容器（如 `page.locator('[data-handover-criterion]')`），斷言該 DOM 屬性存在且隨交接模式切換即時更新。 |
| 271 | `appSrc.includes("if (handoverMode !== 'decision-overlay-on-live-sinr')")` | B | RED | 轉為 Playwright 測試：切換交接模式至非 overlay 模式（如 `sinr-offset`），斷言 MODQN 重播疊加層 `[data-testid="modqn-replay-overlay"]` 從 DOM 移除。 |
| 435 | `appSrc.includes('resolveSceneLane({')` | V | RED | 刪除。釘死 App 內特定輔助函式的呼叫語法；若抽換為 custom hook 或狀態機派生會假紅，場景閘門行為應由行為測試覆蓋。 |
| 436 | `appSrc.includes('modqnReplayProofRequested: modqnReplayProofRequestActive')` | V | RED | 刪除。釘死內部參數傳遞物件的鍵值字面；重播證明圖層是否受 proof request 開關控制應由 Playwright 行為測試覆蓋。 |

## scripts/validate-modqn-phase7k-replay-scene-layer.ts

| 行號 | 斷言字面（原文照抄，可截到 60 字） | 類別 | 分解衝擊 | 處置建議（繁中，一到兩句，具體） |
| :--- | :--- | :--- | :--- | :--- |
| 419 | `assertContains(appSource, "if (handoverMode !== 'decision-overlay-on-live-sinr')", 'App replay-to-scene mode gate')` | B | RED | 轉為 Playwright 測試：切換 `handoverMode` 至非 overlay 模式時，驗證場景 canvas/DOM 中的 MODQN 重播層標記（如 `[data-layer="modqn-replay"]`）處於卸載/隱藏狀態。 |
| 440 | `assertContains(appSource, '<ModqnReplayCuePanel', 'App replay sidebar cue panel')` | B | RED | 轉為 Playwright 測試：在重播模式下展開側邊欄，斷言 `[data-testid="modqn-replay-cue-panel"]` 於 DOM 中正常掛載並顯示 cue 列表。 |
| 445 | `assertContains(appSource, 'modqnReplayProofRequested: modqnReplayProofRequestActive', 'App must resolve the scene lane before mounting replay scene proof')` | V | RED | 刪除。釘死內部參數傳遞鍵值字面；其實質保護的閘門條件應轉為 Playwright 測試（切換 Proof Request 開關驗證重播證明層渲染）。 |

## scripts/validate-phase-b-jobs-panel.tsx

| 行號 | 斷言字面（原文照抄，可截到 60 字） | 類別 | 分解衝擊 | 處置建議（繁中，一到兩句，具體） |
| :--- | :--- | :--- | :--- | :--- |
| 201 | `appSource.includes("import { AdvancedSetupDrawer } from './ui/AdvancedSetupDrawer';")` | V | RED | 刪除。釘死特定元件的 import 路徑與寫法，阻礙 barrel 匯入或版面容器重構，對使用者與執行期毫無防護價值。 |
| 205 | `appSource.includes('<AdvancedSetupDrawer')` | B | RED | 轉為 Playwright 測試：點擊開啟進階設定按鈕，斷言 `[data-testid="advanced-setup-drawer"]` 抽屜展開且內部配置項目正常呈現。 |

## scripts/validate-phase6b-handover-policy-controls.tsx

| 行號 | 斷言字面（原文照抄，可截到 60 字） | 類別 | 分解衝擊 | 處置建議（繁中，一到兩句，具體） |
| :--- | :--- | :--- | :--- | :--- |
| 148 | `assertContains(appSource, 'parameterSection={');` | B | RED | 轉為 Playwright 測試：開啟 SINR Live 抽屜，斷言抽屜內存在參數控制區塊（如 `[data-testid="drawer-parameter-section"]`）且可正常操作。 |

## scripts/validate-phase-d-decision-viz.tsx

| 行號 | 斷言字面（原文照抄，可截到 60 字） | 類別 | 分解衝擊 | 處置建議（繁中，一到兩句，具體） |
| :--- | :--- | :--- | :--- | :--- |
| 265 | `appSource.includes("import { DecisionVizPanel } from './ui/modqn-training/DecisionVizPanel';")` | V | RED | 刪除。釘死 import 語法與路徑，阻礙側邊欄面板重構；只要 DecisionVizPanel 在側邊欄正常掛載即可，使用者對 import 語法無感。 |

實際輸出 23 列
