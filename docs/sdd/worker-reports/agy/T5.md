本批共 23 條，我會輸出 23 列

## scripts/validate-frontend-scene-lane-governance.ts

| 行號 | 斷言字面（原文照抄，可截到 60 字） | 類別 | 分解衝擊 | 處置建議（繁中，一到兩句，具體） |
| :--- | :--- | :---: | :---: | :--- |
| 2298 | `beamLoadContention.byUeId.get(focusedCellBeamConeUe?.id ?? '')` | V | RED | 刪除。此處釘死內部 Map 存取語法，重構為 helper 函式或抽離 hook 即假紅；使用者無感，波束負載邏輯應由渲染與單元測試保障。 |
| 2304 | `{presentationPlan.visible['load-overlays'] && showCellOverla...` | B | RED | 轉為行為斷言。在 Playwright 中切換圖層顯示設定，斷言 BeamLoadCylinder 3D 物件或 canvas 遙測屬性的掛載/卸載狀態。 |
| 2309 | `visible={(focusBeamLoad?.load ?? 0) > 0}` | B | RED | 轉為行為斷言。在 Playwright 中模擬 UE 負載為 0 與大於 0，透過 canvas 遙測或物件屬性斷言圓柱體幾何體的 visible 行為。 |
| 2344 | `import { BeamLoadUploadParticles }` | V | RED | 刪除。釘死單一檔案的 import 語法；若抽成子圖層元件或透過 barrel 匯入會引發無意義假紅，對使用者無任何行為差異。 |
| 2380 | `const uploadParticlesEnabled =` | V | RED | 刪除。釘死元件內部區域變數宣告；重構改名或 inline 完全不影響執行期行為，屬無效約束。 |
| 2385 | `modqnVisualLayerPreset === 'explain-handover'` | B | RED | 轉為行為斷言。在 Playwright 中切換視覺 Preset 選項，斷言粒子系統是否在 `explain-handover` 模式下於畫面中正確啟動。 |
| 2391 | `resolveCellBeamConeItems({` | V | RED | 刪除。釘死內部輔助函式呼叫字串；邏輯搬移至子圖層即假紅，波束錐體計算正確性應由場景渲染與演算法單元測試驗證。 |
| 2396 | `beamConeScope: 'focus-satellite'` | B | RED | 轉為行為斷言。在 Playwright 中選定聚焦衛星，斷言粒子效果僅限定在該聚焦波束錐體範圍內渲染，而非全域發散。 |
| 2401 | `<BeamLoadUploadParticles` | B | RED | 轉為行為斷言。在 Playwright 中觸發上傳粒子圖層開關，驗證場景 canvas telemetry 或 DOM 遙測節點掛載狀態。 |
| 2406 | `focusCones={uploadParticleFocusCones}` | V | RED | 刪除。釘死 JSX prop 傳遞字面與內部變數名；重構改名或在子元件內部存取對使用者毫無差別，屬過度約束。 |
| 2411 | `beamLoadContention={beamLoadContention}` | V | RED | 刪除。釘死 prop 傳遞寫法；改用 Context 或模組狀態管理即假紅，應由粒子碰撞/競爭的視覺遙測測試替代。 |
| 2681 | `function ArtifactSceneContent` | V | RED | 刪除。強行限制 composer 必須存在於同檔，阻礙拆分獨立檔案；其存在價值應由回放路由行為測試保證。 |
| 2686 | `sceneFrame?.sceneSource === 'artifact-replay'` | B | RED | 轉為行為斷言。在 Playwright 中載入回放資料幀，斷言畫面切換至 Artifact 場景且即時模擬遙測標記為停用。 |
| 2691 | `<ArtifactSceneContent` | B | RED | 轉為行為斷言。在 Playwright 中切換回放幀，斷言場景掛載了 Artifact 回放專屬的 `[data-testid]` 或遙測標記。 |
| 2696 | `liveSimulationEnabled="0"` | B | RED | 轉為行為斷言。在 Playwright 進入回放模式時，檢查 DOM/Canvas 節點上的 `[data-live-simulation-enabled="0"]` 屬性。 |
| 2701 | `liveSimulationEnabled={simSource === 'live' ? '1' : '0'}` | B | RED | 轉為行為斷言。在 Playwright 中分別切換即時模擬與 TLE 模式，斷言 DOM 遙測屬性分別輸出 `"1"` 與 `"0"`。 |
| 2753 | `{presentationPlan.visible['serving-footprints']\n && showS...` | B | RED | 轉為行為斷言。在 Playwright 中開啟服務覆蓋開關並切換候選模式，斷言覆蓋腳印 3D 圖層持續存在且未被多候選權威覆蓋清除。 |
| 2775 | `<DecisionHandoverCue` | B | RED | 轉為行為斷言。在 Playwright 中觸發換手決策幀，斷言畫面出現換手提示（DecisionHandoverCue）對應的 DOM 節點或 Canvas 特效。 |
| 2794 | `acceptedHandoverPresentation?.commit,` | V | RED | 刪除。釘死內部屬性存取與依賴項字串片段；重構為 hook 或解構即假紅，對使用者無可觀察影響。 |
| 2799 | `const visibleSatelliteId = authorityPresentationCommitObserv...` | V | RED | 刪除。釘死內部變數宣告與賦值字串；換手過渡期的波束錐體唯一性應由 Canvas 遙測資料斷言，而非約束區域變數命名。 |
| 2816 | `multiCandidateSceneRenderPlan?.telemetry.renderedPairCount` | B | RED | 轉為行為斷言。在 Playwright 中啟用多候選模式，讀取場景遙測屬性 `[data-rendered-pair-count]` 驗證數值與配對數相符。 |
| 2821 | `multiCandidateSceneRenderPlan?.solidDataLinkCount` | B | RED | 轉為行為斷言。在 Playwright 中配置連線場景，讀取 DOM 上的 `[data-solid-data-link-count]` 驗證其正確發布全域實體鏈路數。 |

## scripts/validate-phase-g-ue-mobility-params.tsx

| 行號 | 斷言字面（原文照抄，可截到 60 字） | 類別 | 分解衝擊 | 處置建議（繁中，一到兩句，具體） |
| :--- | :--- | :---: | :---: | :--- |
| 227 | `runtime.ueMobilityParams` | B | RED | 轉為行為斷言。在 Playwright 中透過 TopologyTab 變更移動參數，驗證模擬迴圈步進時 UE 座標更新與遙測資料符合該設定。 |

實際輸出 23 列
