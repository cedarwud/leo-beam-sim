本批共 23 條，我會輸出 23 列

## scripts/validate-frontend-scene-lane-governance.ts

| 行號 | 斷言字面（原文照抄，可截到 60 字） | 類別 | 分解衝擊 | 處置建議（繁中，一到兩句，具體） |
| :--- | :--- | :---: | :---: | :--- |
| 686 | `stepSec={isArchivedTleSceneActive` | B | RED | 改為 Playwright 行為測試：切換至 Archived TLE 場景與一般場景，定位時間軸滑桿元素（如 `[data-testid="timeline-slider"]`），斷言其 `step` 屬性或步進解析度數值是否依場景模式切換。 |
| 820 | `from './showcase/dashboard/AlgorithmDashboard'` | V | RED | 刪除（V）。純屬頂層 import 路徑字面釘住；重構將指標面板拆至 Sidebar 子元件後即假紅，指標是否正常渲染由 Sidebar DOM 測試保護即可。 |
| 907 | `<TrainingTelemetryFeed enabled={appMode === 'modqn-demo'}` | B | RED | 改為 Playwright 行為測試：啟動頁面並帶入 `appMode=modqn-demo` 參數，驗證頁面遙測資料流（WebSocket/網路請求或 DOM 遙測節點）處於啟用狀態，其他模式下則處於停用狀態。 |
| 920 | `r.headers.get('X-Showcase-Artifact-Source')` | B | RED | 改為 Playwright 行為測試：使用 `page.route` 攔截 Replay fetch 請求並注入自定義 `X-Showcase-Artifact-Source` 回應標頭，斷言 DOM 上 `[data-artifact-source]` 屬性或 Badge 內容精確反映該標頭值。 |
| 925 | `artifactSource !== PRODUCER_PINNED_SOURCE` | B | RED | 改為 Playwright 行為測試：載入非 producer 釘選之產物（如 synthetic fixture），操作進入重播模式，檢查畫面是否出現非 producer 警示標籤（如 `[data-testid="artifact-source-warning"]`）。 |
| 930 | `r.headers.get('X-Showcase-Artifact-Source') ?? HEADER_ABSEN...` | B | RED | 改為 Playwright 行為測試：模擬 200 成功回應但不帶來源標頭，斷言 DOM 上 `[data-artifact-source]` 屬性或 Badge 即刻轉為 header-absent 標記，而非卡在 loading 狀態。 |
| 935 | `artifactSource === SYNTHETIC_FIXTURE_SOURCE` | B | RED | 改為 Playwright 行為測試：分別傳入合成 fixture 與未驗證來源之產物，定位 Badge/Alert 元素，斷言畫面警示文案精確呈現 "synthetic" 與 "unverified" 之區分。 |
| 950 | `from './ui/ArtifactSourceBadge'` | V | RED | 刪除（V）。純屬 import 語法釘住；重構拆分至 Replay 工具列或側邊欄子元件後即便在 `App.tsx` 消失，只要畫面正常渲染 Badge 即無損使用者體驗。 |
| 955 | `<ArtifactSourceBadge source={showcaseArtifactSource} />` | B | RED | 改為 Playwright 行為測試：切換至 `artifact-replay` lane，使用 `page.locator('[data-testid="artifact-source-badge"]')` 斷言該 Badge 節點在畫面中可見且屬性值正確。 |
| 960 | `data-artifact-source={` | B | RED | 改為 Playwright 行為測試：載入頁面後直接透過 `page.locator('[data-artifact-source]')` 斷言該 DOM 遙測屬性存在且值與當前解析的產物來源一致。 |
| 988 | `data-director-phase={camera.directorPhase}` | B | RED | 改為 Playwright 行為測試：在 Director 模式下操作運鏡流程，讀取並斷言 Shell 節點上的 `[data-director-phase]` 屬性值隨狀態機階段即時更新。 |
| 993 | `data-effective-speed={playback.effectiveSpeed` | B | RED | 改為 Playwright 行為測試：操作播放速率控制器（如 1x、2x），斷言外層節點上的 `[data-effective-speed]` 屬性值同步變更為對應數值。 |
| 1031 | `useDirectorOrchestration({` | V | RED | 刪除（V）。釘住內部 Hook 呼叫字串；重構將 Director 編排下移至專屬 Provider/Controller 必假紅，外層運鏡行為由既有 Director 遙測與鏡頭測試保護即可。 |
| 1047 | `sceneLane === 'modqn-live-cell-preview' ? 'overlay-demo' : '...` | B | RED | 改為 Playwright 行為測試：在啟用 Director Focus 下切換 `modqn-live-cell-preview` 與一般 live 模式，斷言 DOM 上 `data-live-director-focus-claim` 屬性分別為 `'overlay-demo'` 與 `'live-truth'`。 |
| 1053 | `createSinrLiveCellHandoverEventIndexWorkerTransport()` | S | RED | 保留但去檔名化（S）。改為掃描 `src/**/*.{ts,tsx}` 或模組依賴圖，確保事件索引協調模組引用 `createSinrLiveCellHandoverEventIndexWorkerTransport`，不再限定於 `App.tsx`。 |
| 1058 | `indexWorker.build(buildInput` | V | RED | 刪除（V）。釘死內部局部變數與呼叫語法；Worker 通訊與索引建立細節應由 Worker 模組之單元/整合測試保護，而非原始碼字串匹配。 |
| 1063 | `builder = createSinrLiveCellHandoverEventIndexBuilder(buildI...` | S | RED | 保留但去檔名化（S）。改為掃描 `src/engine/**` 或索引協調模組，驗證保留 `createSinrLiveCellHandoverEventIndexBuilder` 降級引用；或由 Mock Worker 失敗的整合測試驗證 Fallback 行為。 |
| 1091 | `data-live-director-focus-claim={directorFocusEnabled ? live...` | B | RED | 改為 Playwright 行為測試：操作 Director Focus 開關，斷言 DOM 上 `[data-live-director-focus-claim]` 屬性在啟用時呈現正確 claim 字串，未啟用時為 undefined。 |
| 1096 | `data-live-director-focus-event-sec={liveDirectorFocusEventS...` | B | RED | 改為 Playwright 行為測試：觸發 Director Focus 跳轉事件，定位 `page.locator('[data-live-director-focus-event-sec]')`，斷言其屬性值精確呈現 3 位小數之事件秒數。 |
| 1209 | `from './ui/ArtifactSatelliteCompass'` | V | RED | 刪除（V）。純屬頂層 import 路徑字面釘住；指南針元件搬移至 Replay 子元件後此行必紅，其掛載與方位角呈現應由其 DOM 節點或子元件測試保護。 |
| 1387 | `const [modqnReplayProofRequested, setModqnReplayProofRequest...` | V | RED | 刪除（V）。直接釘死 React 內部 `useState` 宣告字串；重構抽取 Hook 必假紅，狀態切換應由點擊切換按鈕並觀察 Viewport 渲染的行為測試取代。 |
| 1390 | `proofViewportActive={sceneLane === 'modqn-replay-proof'}` | B | RED | 改為 Playwright 行為測試：切換 `sceneLane` 至 `'modqn-replay-proof'`，定位 Cue Panel 元素（如 `[data-testid="cue-panel"]`），斷言其包含 active 狀態屬性或 Proof Viewport 節點可見。 |

## scripts/validate-phase-g-ue-mobility-step.tsx

| 行號 | 斷言字面（原文照抄，可截到 60 字） | 類別 | 分解衝擊 | 處置建議（繁中，一到兩句，具體） |
| :--- | :--- | :---: | :---: | :--- |
| 262 | `runtime.ueMobilityMode` | B | RED | 改為 Playwright/模擬整合測試：在場景中設定不同 `ueMobilityMode`（如 `random-walk` 與 `static`），推進模擬時間步，斷言 Canvas 遙測或 UE 狀態中座標是否如期位移或保持靜止。 |

實際輸出 23 列
