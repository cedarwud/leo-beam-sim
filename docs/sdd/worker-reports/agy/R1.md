本批共 6 個檔案的 diff

## scripts/validate-phase-e-sat-count-override.tsx

| 判定 | 被刪/被改的斷言（原文照抄，可截到 70 字） | 問題說明（繁中，具體） | 建議動作 |
| :--- | :--- | :--- | :--- |
| `OK` | `check(appSource.includes('applySceneTopology(trainingProfile, activeSceneTopology)') && appSource.includes('applyTrainingEnvAxesToProfile'), 'App.tsx applies live scene topology...')` | 兩條件皆僅針對目標檔 `src/App.tsx`。其中舊字面已出現 0 次（已紅/過期），另一個字面出現 2 次（>1 次無法定位），符合授權刪除範圍。 | 維持刪除。管線套用順序後續應由行為或整合測試接手。 |
| `OK` | `check(appSource.includes('getSceneTopologyResetKey('), 'App.tsx joins getSceneTopologyResetKey into reset chain');` | 字面在 `src/App.tsx` 出現 2 次（>1 次），無法唯一識別 reset chain 連接，符合授權。 | 維持刪除。 |
| `OK` | `check(appSource.includes('appMode={appMode}'), 'App.tsx passes appMode into SignalTuningPanel');` | 字面在 `src/App.tsx` 出現 7 次，完全無法證明 `SignalTuningPanel` 之特定屬性傳遞，為典型無效字串斷言，符合授權。 | 維持刪除。 |
| `OK` | `check(appSource.includes('liveSceneTopologyControlsEnabled') && appSource.includes("sceneLane === 'modqn-live-cell-preview'"), 'App.tsx enables topology overrides on both live scene lanes');` | 兩條件皆針對 `src/App.tsx`，分別出現 3 次與 12 次（皆 >1 次），複合條件無法唯一鎖定 lane gate，符合授權。 | 維持刪除。 |

> **殘留物注意**：`section('(c)')` 內第 125 行的 `const appSource = source('src/App.tsx');` 於刪除後已無任何斷言使用，成為未清理的殘留變數。

小計: 越權=0 理由不成立=0 失去保護=0 OK=4

---

## scripts/validate-modqn-omega-s2-runtime-fetch.tsx

| 判定 | 被刪/被改的斷言（原文照抄，可截到 70 字） | 問題說明（繁中，具體） | 建議動作 |
| :--- | :--- | :--- | :--- |
| `OK` | `assert.ok(appSource.includes('fetchModqnReplayBundleEnvelope'), 'S2.1a: App.tsx must wire fetchModqnReplayBundleEnvelope');` | 字面在 `src/App.tsx` 出現 4 次（>1 次），無法唯一識別接線行為，符合刪除授權。 | 維持刪除。（注意其後 `recordPass` 變成無條件執行） |
| `OK` | `assert.ok(appSource.includes('getModqnReplayPlaybackFallbackShellModel'), 'S2.1b: App.tsx must call getModqnReplayPlaybackFallbackShellModel...');` | 字面在 `src/App.tsx` 出現 2 次（>1 次），無法唯一識別 fallback 呼叫，符合刪除授權。 | 維持刪除。 |
| `越權` | `assert.equal(appSource.includes('data-testid="modqn-bundle-fetch-banner"'), false, 'S2.1d: App.tsx must not mount the removed MODQN bundle fetch warning banner');` | 此為**負向斷言**（驗證為 `false`）。目的是防禦「已移除的 warning banner 不被重新掛載」。當前在 `src/` 中出現 0 次正是該斷言**處於綠燈、有效守護回歸**的狀態。清理 agent 誤將「0 次」當成過期 pin 刪除，嚴重違反「負向斷言一律不准動」的授權限制。 | **退回並恢復此斷言**。 |
| `越權` | `assert.equal(appSource.includes('leo-modqn-bundle-fetch-banner'), false, 'S2.1d: App.tsx must not mount the removed MODQN bundle fetch warning banner');` | 同為**負向斷言**，用於防禦已移除的 class 名稱不被重新引進。當前為有效綠燈斷言，刪除違反「負向斷言一律不准動」授權。 | **退回並恢復此斷言**。 |

小計: 越權=2 理由不成立=0 失去保護=0 OK=2

---

## scripts/validate-phase-h-s5-orbit-trail-modqn-demo.ts

| 判定 | 被刪/被改的斷言（原文照抄，可截到 70 字） | 問題說明（繁中，具體） | 建議動作 |
| :--- | :--- | :--- | :--- |
| `OK` | `expect(source.includes('showOrbitTrail'), 'MainScene threads showOrbitTrail through effectsEnabled.orbitTrail');` | 字面在 `src/scene/MainScene.tsx` 出現 2 次（>1 次），無法識別唯一穿透，符合授權。 | 維持刪除。 |
| `OK` | `expect(source.includes('showSpineParticles'), 'MainScene threads showSpineParticles through effectsEnabled.spineParticles');` | 字面在 `src/scene/MainScene.tsx` 出現 2 次（>1 次），無法識別唯一穿透，符合授權。 | 維持刪除。 |
| `OK` | `expect(source.includes('effectsEnabled: runtime.effectsEnabled') && renderPlan.includes('input.effectsEnabled.orbitTrail'), ...)` | 此為跨檔複合斷言。清理 agent 做法正確：僅移除 `MainScene.tsx` 端（出現 2 次）的無效 pin，**保留了非目標檔 `sceneLaneRenderPlan.ts`（出現 1 次）的有效斷言**，未犯連坐錯誤。 | 維持修改。 |
| `OK` | `expect(source.includes('effectsEnabled: runtime.effectsEnabled') && renderPlan.includes('input.effectsEnabled.spineParticles'), ...)` | 同上，正確拆分複合斷言，僅移除目標檔重複字面，保留非目標檔的單次有效斷言。 | 維持修改。 |

> **規範表率**：該檔未使用的 `const source = readSource(...)` 亦同步乾淨移除，無殘留物。

小計: 越權=0 理由不成立=0 失去保護=0 OK=4

---

## scripts/validate-phase-g-ue-mobility-step.tsx

| 判定 | 被刪/被改的斷言（原文照抄，可截到 70 字） | 問題說明（繁中，具體） | 建議動作 |
| :--- | :--- | :--- | :--- |
| `OK` | `check(appSource.includes('buildAppRuntimeConfig'), 'App delegates runtime config construction');` | 字面在 `src/App.tsx` 出現 3 次（>1 次），無法唯一識別委派建構，符合授權刪除範圍。 | 維持刪除。 |

小計: 越權=0 理由不成立=0 失去保護=0 OK=1

---

## scripts/validate-phase-f-ue-distribution-mode.tsx

| 判定 | 被刪/被改的斷言（原文照抄，可截到 70 字） | 問題說明（繁中，具體） | 建議動作 |
| :--- | :--- | :--- | :--- |
| `OK` | `check(appSource.includes('buildAppRuntimeConfig'), 'App.tsx delegates runtime config construction');` | 字面在 `src/App.tsx` 出現 3 次（>1 次），無法唯一識別委派建構，符合授權刪除範圍。 | 維持刪除。 |

小計: 越權=0 理由不成立=0 失去保護=0 OK=1

---

## scripts/validate-modqn-omega-s1-sidebar-truth-up.tsx

| 判定 | 被刪/被改的斷言（原文照抄，可截到 70 字） | 問題說明（繁中，具體） | 建議動作 |
| :--- | :--- | :--- | :--- |
| `越權` | `assert.equal(appSource.includes('useModqnDemoStub'), false, 'S1.1a: src/App.tsx must not import useModqnDemoStub');` | 此為標準的**負向防禦斷言**（`=== false`），專門防止已刪除的 stub 被誤 import 回 `App.tsx`。經確認 `src/` 中為 0 次，代表此斷言**現正綠燈發揮防禦效果**。清理 agent 誤認其為過期 pin 而刪除，踩中「負向斷言一律不准動」紅線。 | **退回並恢復此負向斷言**。 |

> **殘留物注意**：第 131 行 `const appSource = readFileSync(APP_PATH, 'utf8');` 於刪除後變成未使用的殘留變數，且其後的 `recordPass('S1.1a', ...)` 變成在無任何實質檢查下空轉放行。

小計: 越權=1 理由不成立=0 失去保護=0 OK=0

---

## 總結

### 最該退回的前 3 條

1. **`scripts/validate-modqn-omega-s1-sidebar-truth-up.tsx`**
   `assert.equal(appSource.includes('useModqnDemoStub'), false, ...)`
   * **退回原因**：這是防禦已被移除的 stub 不被重新引入的**負向斷言**。該斷言當前為綠燈且具備真實防護力，清理 agent 誤將「0 次出現」當成過期 pin 刪除，屬於重大越權。
2. **`scripts/validate-modqn-omega-s2-runtime-fetch.tsx`**
   `assert.equal(appSource.includes('data-testid="modqn-bundle-fetch-banner"'), false, ...)`
   * **退回原因**：標準負向斷言，確保 fallback 靜默且不再掛載舊 warning banner，當前為綠燈，受授權條款明確保護（負向斷言一律不准動）。
3. **`scripts/validate-modqn-omega-s2-runtime-fetch.tsx`**
   `assert.equal(appSource.includes('leo-modqn-bundle-fetch-banner'), false, ...)`
   * **退回原因**：同為 S2.1d 負向斷言防護網的一半，禁止以「檔案內已無此字面」為由刪除有效的負向斷言。

---

### 沒有把握的地方

1. **`recordPass` 假綠燈風險**：在 `validate-modqn-omega-s2-runtime-fetch.tsx`（S2.1a, S2.1b）與 `validate-modqn-omega-s1-sidebar-truth-up.tsx`（S1.1a）中，前置 `assert` 被移除後，後續的 `recordPass` 仍被保留並無條件執行。由於無法看到 validator 外部 reporter 的計分機制，無法確定這是否會對測試套件的覆蓋率報告產生誤導性通過。
2. **殘留變數引發 Lint 報警**：`validate-phase-e-sat-count-override.tsx` 中的 `const appSource = source(...)` 與 `validate-modqn-omega-s1-sidebar-truth-up.tsx` 中的 `const appSource = readFileSync(...)` 在刪除斷言後均變成未引用變數。若 CI 啟用了 `no-unused-vars`，可能導致 validator 本身在型別檢查或 lint 階段失敗。
3. **Phase E 的順序行為覆蓋度**：`validate-phase-e-sat-count-override.tsx` 中刪除的 `applySceneTopology` 與 `applyTrainingEnvAxesToProfile`，其標籤意圖守護「套用層級順序」。在不透視 `src/` 程式碼的情況下，無法確定目前是否有其他 end-to-end 或 reducer 測試接手此順序邏輯的行為驗證。
