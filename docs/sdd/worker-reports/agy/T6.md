本批共 23 條，我會輸出 23 列

## scripts/validate-timeline-scrubbing.tsx

| 行號 | 斷言字面（原文照抄，可截到 60 字） | 類別 | 分解衝擊 | 處置建議（繁中，一到兩句，具體） |
| --- | --- | --- | --- | --- |
| 147 | `const target = clampTimelineTime(targetSec, timelineDurationSec);` | B | RED | 在 Playwright 操作時間軸拖曳至超出邊界（< 0 或 > duration），斷言 DOM `[data-timeline-current-time-sec]` 正確 clamp 在合法區間內。 |
| 151 | `setLiveTimelineSeekRequest({` | V | RED | 斷言內部 React 狀態更新語法，若重構成 Hook 或 Reducer 使用者完全無感；即時跳轉行為應由 E2E 測試覆蓋，此條應刪除。 |
| 153 | `data-timeline-current-time-sec={timelineCurrentTimeSec.toFixed(3)}` | B | RED | 在 Playwright 透過 `page.locator('[data-timeline-current-time-sec]')` 讀取屬性，斷言時間軸播放與跳轉時數值即時更新且格式化為 3 位小數。 |
| 154 | `data-timeline-duration-sec={timelineDurationSec.toFixed(3)}` | B | RED | 在 Playwright 讀取 `[data-timeline-duration-sec]` 屬性，斷言其數值正確反映當前場景之總時長。 |
| 158 | `liveTimelineSeekTargetSec: liveTimelineSeekRequest?.targetSec` | V | RED | 斷言內部 config 物件傳參細節；若透過 Context 或狀態庫傳遞使用者無法察覺差異，屬於無效釘字應刪除。 |
| 159 | `liveTimelineSeekRequestKey: liveTimelineSeekRequest?.requestKey` | V | RED | 斷言內部 Request Key 之組裝賦值，為純內部管道實作細節；實質功能由跳轉行為保證，應刪除。 |

## scripts/validate-phase-i-s5b-cell-beam-cones.tsx

| 行號 | 斷言字面（原文照抄，可截到 60 字） | 類別 | 分解衝擊 | 處置建議（繁中，一到兩句，具體） |
| --- | --- | --- | --- | --- |
| 373 | `resolveCellBeamConeRenderCount,` | V | RED | 斷言特定 Helper 函式之識別字存在；若將計算內聯或重構抽取，渲染結果不受影響，應刪除。 |
| 374 | `resolveCellBeamConeSatelliteCount,` | V | RED | 斷言內部計算函式名稱，為實作細節；就算函式改名或由上層傳入，畫面與行為皆無變化，應刪除。 |
| 379 | `beamConeScope={modqnVisualLayers.beamConeScope}` | B | RED | 在 Playwright 操作圖層選單切換 beam cone scope，斷言 3D 場景 telemetry（`canvas[data-cell-beam-cone-count]`）對應改變數量。 |
| 413 | `cellBeamConeCount={showCellOverlay ? String(renderedCellBeamConeCount) : ''}` | B | RED | 在 Playwright 切換 `showCellOverlay` 開關，斷言 `canvas` 上的 `data-cell-beam-cone-count` 屬性相應呈現計數或被清空。 |
| 414 | `resolveCellBeamConeRenderCount({` | V | RED | 斷言內部函式呼叫語法；只要 Telemetry dataset 輸出正確，呼叫形式不影響任何執行期行為，應刪除。 |

## scripts/validate-live-walker-handover-event-focus.tsx

| 行號 | 斷言字面（原文照抄，可截到 60 字） | 類別 | 分解衝擊 | 處置建議（繁中，一到兩句，具體） |
| --- | --- | --- | --- | --- |
| 226 | `timelineRailDescriptor.rail.sourceOwner === 'live-walker'` | B | RED | 在 Playwright 點擊 `live-walker` rail 上的事件節點，斷言時間軸觸發 live seek 並更新 `[data-timeline-current-time-sec]`。 |
| 231 | `timelineRailDescriptor.rail.sourceOwner === 'sinr-live-cell-truth'` | B | RED | 在 Playwright 切換至 SINR cell-truth 模式並點擊 rail 標記，斷言觸發對應的 cell truth seek 與狀態更新。 |
| 236 | `timelineRailDescriptor.rail.horizonKind === 'live-walker-window'` | B | RED | 在 Playwright 點擊視窗時間軸刻度，斷言視窗時間與絕對時間轉換正確反應於時間軸與模擬播放狀態。 |
| 241 | `setLiveTimelineSeekRequest({` | V | RED | 斷言內部 React 狀態 setter 呼叫；若改由自訂 Hook 或事件中心驅動，外部行為不變，應刪除。 |

## scripts/validate-phase-h-s5-orbit-trail-modqn-demo.ts

| 行號 | 斷言字面（原文照抄，可截到 60 字） | 類別 | 分解衝擊 | 處置建議（繁中，一到兩句，具體） |
| --- | --- | --- | --- | --- |
| 27 | `appMode !== 'modqn-demo' || reducedMotion` | B | RED | 在 Playwright 啟動 `appMode=modqn-demo` 並模擬 `prefers-reduced-motion`，斷言特效設定遙測屬性正確響應覆寫邏輯。 |
| 31 | `orbitTrail: false` | B | RED | 在 Playwright 切換至 `modqn-demo` 模式，讀取設定面板或 DOM/Canvas 遙測屬性，斷言 `orbitTrail` 處於停用狀態。 |
| 35 | `spineParticles: true` | B | RED | 在 Playwright 切換至 `modqn-demo` 模式，斷言 spine particles 對應之遙測屬性或渲染節點處於啟用狀態。 |

## scripts/validate-phase-b-training-form.tsx

| 行號 | 斷言字面（原文照抄，可截到 60 字） | 類別 | 分解衝擊 | 處置建議（繁中，一到兩句，具體） |
| --- | --- | --- | --- | --- |
| 160 | `import { AdvancedSetupDrawer } from './ui/AdvancedSetupDrawer';` | V | RED | 斷言特定檔案之靜態 import 路徑字面；若改用 barrel export 或 dynamic import 使用者無感，應刪除。 |
| 164 | `<AdvancedSetupDrawer` | B | RED | 在 Playwright 點擊進階設定按鈕，斷言抽屜 DOM 節點（如 `[data-testid="advanced-setup-drawer"]`）成功掛載並可見。 |

## scripts/validate-phase-3-overlays.ts

| 行號 | 斷言字面（原文照抄，可截到 60 字） | 類別 | 分解衝擊 | 處置建議（繁中，一到兩句，具體） |
| --- | --- | --- | --- | --- |
| 84 | `const beamLoadContentionEnabled = showModqnServiceAllocation && modqnVisualLayers.serviceMap;` | B | RED | 在 Playwright 分別切換服務分配與服務地圖圖層開關，斷言只有兩者皆開啟時 contention glow 遙測標記才為 active。 |
| 97 | `<BeamLoadUploadParticles` | B | RED | 在 Playwright 滿足圖層啟用條件（如開啟 cell overlay 與 handover story），斷言粒子圖層節點掛載且遙測粒子數 > 0。 |

## scripts/validate-phase-i-s7b-serving-count-selector.tsx

| 行號 | 斷言字面（原文照抄，可截到 60 字） | 類別 | 分解衝擊 | 處置建議（繁中，一到兩句，具體） |
| --- | --- | --- | --- | --- |
| 241 | `runtime.cellServingCount ?? DEFAULT_SERVING_COUNT` | B | RED | 在 Playwright 初始化 `cellServingCount` 為 null 的場景，斷言場景 Telemetry（`canvas[data-cell-serving-count]`）回退顯示預設值。 |

實際輸出 23 列
