本批共 23 條，我會輸出 23 列

## scripts/validate-frontend-scene-lane-governance.ts

| 行號 | 斷言字面（原文照抄，可截到 60 字） | 類別 | 分解衝擊 | 處置建議（繁中，一到兩句，具體） |
| :--- | :--- | :---: | :---: | :--- |
| 2123 | `data-scene-lane={sceneLane}` | B | RED | 轉為 Playwright 瀏覽器測試，使用 `page.locator('[data-scene-lane]')` 檢查 DOM 屬性是否隨場景切換為當前的 `sceneLane`。 |
| 2158 | `MODQN_SERVICE_ALLOCATION_PRODUCER_READY \|\| readModqnServiceAllocationOverrideFromUrl()` | B | RED | 轉為 Playwright 瀏覽器測試，開啟帶有 `?modqnServiceAllocation=1` 的網址，驗證服務配置遙測與 UI 是否解除 parked 狀態並正確啟用。 |
| 2168 | `modqnServiceAllocationEnabled: runtime.modqnServiceAllocationEnabled ?? false` | V | RED | 刪除此字串斷言。此為內部物件屬性組裝細節，若被違反使用者無感，真行為已被下游圖層渲染與遙測斷言覆蓋。 |
| 2174 | `showUeCounts={modqnVisualLayers.ueCountBadges && showModqnServiceAllocation}` | B | RED | 轉為 Playwright 瀏覽器測試，切換 `ueCountBadges` 或 service allocation 閘門，檢查 Cell Overlay 上的 UE 計數標籤 DOM 節點之可見性變化。 |
| 2179 | `modqnServedUeCount={showModqnServiceAllocation ? modqnServiceMap.servedUeCount : 0}` | B | RED | 轉為 Playwright 瀏覽器測試，在 parked 狀態下讀取 DOM/Canvas 遙測屬性 `data-modqn-served-ue-count`，驗證輸出值為 `0`。 |
| 2184 | `modqnIdleUeCount={showModqnServiceAllocation ? modqnServiceMap.idleUeCount : 0}` | B | RED | 轉為 Playwright 瀏覽器測試，在 parked 狀態下驗證遙測屬性 `data-modqn-idle-ue-count` 為 `0`，且不受 cell overlay 開關干擾。 |
| 2189 | `modqnServiceMapEnabled={showModqnServiceAllocation && modqnVisualLayers.serviceMap ? '1' : '0'}` | B | RED | 轉為 Playwright 瀏覽器測試，在不同圖層開關與 URL 參數下，驗證 DOM 遙測屬性 `data-modqn-service-map-enabled` 正確切換為 `'1'` 或 `'0'`。 |
| 2199 | `deriveModqnServiceMap({` | V | RED | 刪除此字串斷言。此為內部資料推導函式呼叫，改寫或抽取至 hook 不影響外部行為，其正確性應由消費其產物的 UI 行為測試保障。 |
| 2204 | `buildModqnCellServiceReadout({` | V | RED | 刪除此字串斷言。釘死內部 helper 函式名稱對外部黑盒行為無約束力，其產生的服務讀數應由 SimState/Telemetry 測試覆蓋。 |
| 2210 | `modqnCellServiceReadout,` | V | RED | 刪除此字串斷言。此處僅為物件傳遞內部變數之簡寫語法，即使移除或更名，只要資料流傳遞正常，使用者完全無法察覺差異。 |
| 2215 | `markerColor: isOtherHandover` | V | RED | 刪除此字串斷言。釘住局部三元運算子指派為脆弱內部實作，色彩回退邏輯應在獨立的 marker 樣式單元測試中驗證。 |
| 2220 | `mosaic?.markerColor ?? service?.markerColor` | V | RED | 刪除此字串斷言。純運算式細節應抽取至純函式並以單元測試保護優先級，不應在元件 JSX/主場景檔案內進行字面鎖定。 |
| 2235 | `ueCountByCellId={modqnServiceMap.ueCountByCellId}` | B | RED | 轉為 Playwright 瀏覽器測試，進入 MODQN 場景並檢查各 Cell Overlay 節點呈現的 UE 數量文字是否與當前服務分佈吻合。 |
| 2240 | `const showCellReassignmentEventArcs = modqnVisualLayers.handoverCues` | V | RED | 刪除此字串斷言。宣告局部中繼變數為內部語法糖，改寫為 inline 或自定義 hook 不影響功能，不應限制重構自由度。 |
| 2246 | `visible={showCellReassignmentEventArcs}` | B | RED | 轉為 Playwright 瀏覽器測試，切換 `handoverCues` 視覺選項，驗證重分配事件弧線（Event Arcs）圖層的可見性狀態或遙測標記。 |
| 2251 | `<HandoverStoryLayer` | B | RED | 轉為 Playwright 瀏覽器測試，在滿足條件時透過 `page.locator('[data-testid="handover-story-layer"]')` 驗證故事圖層是否正確掛載於場景 DOM。 |
| 2256 | `{presentationPlan.visible['event-effects'] && showProfileHandoverStoryLayer && modqnVisualLayers.handoverStory && (` | B | RED | 轉為 Playwright 瀏覽器測試，分別關閉 `event-effects` 或 `handoverStory` 圖層開關，驗證 HandoverStoryLayer 是否如預期停止渲染。 |
| 2261 | `{presentationPlan.visible['serving-beams'] && showCellOverlay && modqnVisualLayers.beamCones && (` | B | RED | 轉為 Playwright 瀏覽器測試，切換 `serving-beams` 或 `beamCones` 開關，驗證波束錐體（Beam Cones）圖層節點是否正確顯隱。 |
| 2266 | `beamConeScope: renderedCellBeamConeScope` | V | RED | 刪除此字串斷言。此為傳入 render-count helper 的參數命名寫法，無 runtime 直接訊號，對使用者行為無可觀察影響。 |
| 2272 | `beamConeScope={modqnVisualLayers.beamConeScope}` | B | RED | 轉為 Playwright 瀏覽器測試，變更 `beamConeScope` 控制項設定，驗證場景中波束錐體渲染數量與遙測屬性是否相應更新。 |
| 2283 | `deriveBeamLoadContention([...modqnServiceMap.ueById.values()].map(projection => ({` | B | RED | 轉為 Playwright 瀏覽器測試，在 MODQN 爭端情境下驗證爭用波束光暈（contention glow）之遙測屬性或視覺節點具備非零爭用狀態。 |
| 2293 | `const focusBeamLoad = beamLoadContentionEnabled` | V | RED | 刪除此字串斷言。內部變數宣告與計算為私有實作，焦點 UE 負載顯示應由 UI 面板數值或焦點遙測屬性測試直接驗證。 |

## scripts/validate-phase-f-ue-distribution-mode.tsx

| 行號 | 斷言字面（原文照抄，可截到 60 字） | 類別 | 分解衝擊 | 處置建議（繁中，一到兩句，具體） |
| :--- | :--- | :---: | :---: | :--- |
| 257 | `runtime.ueDistributionMode` | B | RED | 轉為 Playwright 瀏覽器測試，在 TopologyTab 切換 UE 分布模式單選鈕，驗證模擬核心產生之 UE 位置分佈狀態或相關遙測產生相應變更。 |

實際輸出 23 列
