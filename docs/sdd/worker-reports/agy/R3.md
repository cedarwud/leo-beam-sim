本批共 4 個檔案的 diff

## scripts/validate-modqn-omega-s3-replay-mode-wiring.tsx

| 判定 | 被刪/被改的斷言（原文照抄，可截到 70 字） | 問題說明（繁中，具體） | 建議動作 |
| :--- | :--- | :--- | :--- |
| `OK` | `appSrc.includes('return null;')` | `return null;` 在 `src/App.tsx` 出現 11 次，無法識別具體分支。清理時精確只刪除該項，保留了對 `if` 條件的斷言，無連坐破壞。 | 保留此修改。 |
| `OK` | `assert(appSrc.includes('<LaneExperienceBar value={sceneLane} onChange={handleExperienceChange} />'), 'App wires LaneExperienceBar -> handleExperienceChange (which drives appMode)')` | 註解附帶具體查證依據（`rg '<LaneExperienceBar' src` 確認無 JSX 掛載，SINR 啟動介面已刻意省略），符合「今天已紅且守護對象已自 src 移除」授權。 | 保留此刪除。 |
| `OK` | `appSrc.includes('readInitialRuntimeState')` | `readInitialRuntimeState` 在 `src/App.tsx` 出現 2 次。清理時僅剔除該條件，完整保留了 `appRuntimeModelSrc` 與負向斷言，無連坐越權。 | 保留此修改。 |
| `OK` | `appSrc.includes('getLeftSidebarTabsForSceneLane')` | `getLeftSidebarTabsForSceneLane` 在 `src/App.tsx` 出現 2 次。清理時僅移除該條件，完整保留 `appRuntimeModelSrc` 對左側欄多個 sub-lane 規則的斷言。 | 保留此修改。 |
| `OK` | `appSrc.includes('getRightSidebarTabsForSceneLane')` | `getRightSidebarTabsForSceneLane` 在 `src/App.tsx` 出現 2 次。清理時僅移除該條件，完整保留 `appRuntimeModelSrc` 對右側欄隔離規則的斷言。 | 保留此修改。 |
| `失去保護` | `assert(appSrc.includes('handoverMode={handoverMode}'), 'App.tsx passes handoverMode into live status and MODQN evidence panels')` | 該字面出現 3 次符合 >1 次授權，但刪除後「App.tsx 需將 handoverMode 傳入 LiveStatus 與 MODQN evidence 面板」的 Props 綁定完全無人守護。 | 標記待補行為測試；應由元件渲染測試（驗證 Panels 接收到的 props 或 DOM `data-handover-mode`）接手。 |
| `OK` | `appSrc.includes('appMode,') && appSrc.includes('sceneSource,')` | `appMode,`（13次）與 `sceneSource,`（17次）出現頻率過高。清理時僅移除這兩項次要參數，保留了 `resolveSceneLane({`、proof 請求與負向斷言。 | 保留此修改。 |

小計: 越權=0 理由不成立=0 失去保護=1 OK=6

---

## scripts/validate-timeline-scrubbing.tsx

| 判定 | 被刪/被改的斷言（原文照抄，可截到 70 字） | 問題說明（繁中，具體） | 建議動作 |
| :--- | :--- | :--- | :--- |
| `OK` | `assertContains(appSource, 'LIVE_SIM_TIMELINE_DURATION_SEC', 'live seek clamps to the source horizon')` | `LIVE_SIM_TIMELINE_DURATION_SEC` 常數在 `src/App.tsx` 出現 6 次，單純檢查常數名字串無法證明 seek 範圍限制；上方已有 `clampTimelineTime` 斷言守護。 | 保留此刪除。 |
| `失去保護` | `assertContains(appSource, 'setLiveObservedHandoverRailEvents([]);', 'live seek clears observed handover rail events')` | 雖然該呼叫在 `src/App.tsx` 出現 4 次符合授權，但「在 Timeline Seek/Scrub 時需清空觀察到的切換事件」是關鍵狀態重設行為，刪除後此處無其他斷言覆蓋。 | 標記待補行為測試；應由 Timeline Controller / Hook 單元測試驗證 `seek()` 操作觸發 events 重設。 |

小計: 越權=0 理由不成立=0 失去保護=1 OK=1

---

## scripts/validate-phase8b-path-loss-controls.tsx

| 判定 | 被刪/被改的斷言（原文照抄，可截到 70 字） | 問題說明（繁中，具體） | 建議動作 |
| :--- | :--- | :--- | :--- |
| `失去保護` | `assertContains(appSource, 'setStaleFormulaEvidenceKey(getSignalTuningEvidenceKey(next))')` | 該字串出現 2 次符合授權，但其原意在守護「信號調諧變更時將公式 Evidence 標記為 Stale」的快取失效路徑，刪除後此失效邏輯失去靜態保護。 | 標記待補行為測試；應由信號調諧 State/Hook 整合測試驗證參數更新時 Stale Key 的計算與更新。 |
| `失去保護` | `assertContains(appSource, 'isFormulaEvidenceStale={staleFormulaEvidenceKey !== null}')` | 該屬性傳遞出現 4 次（傳給多個面板）符合授權，但刪除後「App 是否將過期狀態旗標傳入子面板」完全失去校驗。 | 標記待補行為測試；應由 UI 元件層驗證子元件是否收到 `isFormulaEvidenceStale` 屬性。 |

小計: 越權=0 理由不成立=0 失去保護=2 OK=0

---

## scripts/validate-modqn-phase7k-replay-scene-layer.ts

| 判定 | 被刪/被改的斷言（原文照抄，可截到 70 字） | 問題說明（繁中，具體） | 建議動作 |
| :--- | :--- | :--- | :--- |
| `失去保護` | `assertContains(appSource, 'createOmegaRescalarizedModqnReplayPlaybackDisplayState', 'App replay-to-scene bridge')` | 該高特異性識別字長達 54 字元，在 `src/App.tsx` 出現 2 次極可能為 1 次 import 加上 1 次呼叫點。形式上雖符合 >1 次，但刪除後使「MODQN 回放狀態橋接至 3D 場景」的核心管線完全失去守護。 | 建議退回或立即補上 Scene Bridge 整合測試（驗證 App 是否正確調用橋接函式生成 DisplayState）。 |

小計: 越權=0 理由不成立=0 失去保護=1 OK=0

---

## 總結

### 最該退回（或優先補建行為測試）的前 3 條

1. **`scripts/validate-modqn-phase7k-replay-scene-layer.ts` 中的 `createOmegaRescalarizedModqnReplayPlaybackDisplayState`**
   - **原因**：這是 MODQN 回放狀態進入 3D 場景的關鍵橋接函式。出現 2 次通常僅是 `import` 與唯一呼叫點；直接刪除導致核心場景橋接完全失守。
2. **`scripts/validate-modqn-omega-s3-replay-mode-wiring.tsx` 中的 `handoverMode={handoverMode}`**
   - **原因**：守護切換模式參數是否傳遞進 LiveStatus 與 MODQN 面板。重構分解 App 時極易漏傳 Props，此處完全刪除將失去介面聯動保護。
3. **`scripts/validate-phase8b-path-loss-controls.tsx` 中的 `setStaleFormulaEvidenceKey(...)`**
   - **原因**：信號調諧（Carrier Freq / Path Loss）引發公式失效的狀態更新路徑，若重構漏掉呼叫，使用者介面將持續顯示過期的公式證據而無法察覺。

---

### 沒有把握的地方（資訊不足）

1. **`src/App.tsx` 實際語境**：僅憑 diff 無法確認 `createOmegaRescalarizedModqnReplayPlaybackDisplayState` 出現 2 次是否確為「1 次 import + 1 次呼叫」，抑或是重構過程中遺留的多餘呼叫點。
2. **外部行為測試覆蓋狀況**：無法得知專案內是否已有 Vitest/Playwright 等其他動態測試覆蓋了 `setLiveObservedHandoverRailEvents([])` 或 `isFormulaEvidenceStale`，若已有動態測試覆蓋，則「失去保護」的風險程度可降低。
3. **`InfoPanel.tsx` 殘留檢查**：在 `validate-modqn-omega-s3-replay-mode-wiring.tsx` 第 393 行附近讀取了 `InfoPanel.tsx`，但因 diff 截斷，無法完全確認其宣告的變數在檔案後續是否仍有被使用。
