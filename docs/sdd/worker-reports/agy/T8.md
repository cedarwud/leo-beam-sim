本批共 23 條，我會輸出 23 列

## scripts/validate-modqn-handover-story-layer.ts

| 行號 | 斷言字面（原文照抄，可截到 60 字） | 類別 | 分解衝擊 | 處置建議（繁中，一到兩句，具體） |
| :--- | :--- | :---: | :---: | :--- |
| 466 | `const [modqnVisualLayerPreset, setModqnVisualLayerPreset]` | V | RED | 刪除。釘死內部 `useState` 變數名稱；重構抽成 custom hook 或 state store 時會假紅，且 preset 功能已有 UI 控制項與 telemetry 驗證，對使用者零實質差異。 |
| 489 | `from './app/timelineRailAuthority'` | V | RED | 刪除。釘死 App.tsx 的 import 路徑字面；時間軸邏輯下放至子元件後 App 無需直接引用即會假紅，對執行期功能無保護力。 |
| 490 | `from './app/liveWalkerHandoverRailAdapter'` | V | RED | 刪除。釘死 import 語法字面；重構將 rail adapter 移至專屬時間軸子模組時會假紅，使用者無任何感知差別。 |
| 491 | `from './scene/liveWalkerHandoverEventIndex'` | V | RED | 刪除。釘死 import 語法字面；將 event index 建構移入自訂 hook 或協調模組時會假紅，缺乏實質防護價值。 |
| 498 | `producerTraceDisplayDurationSec` | V | RED | 刪除。釘死內部區域變數名稱；時長計算與分離已有 timelineAuthority 單元測試與 UI 渲染保證，變數改名或抽離不影響功能。 |
| 502 | `buildLiveWalkerHandoverEventIndex({` | V | RED | 刪除。釘死 App.tsx 內的函式呼叫字面；邏輯抽至自訂 hook 時會假紅，事件索引的正確性應由軌道渲染與遙測驗證。 |
| 503 | `liveWalkerHandoverEventIndexToRailEvents(liveWalkerHandover` | V | RED | 刪除。釘死內部轉換函式呼叫與變數名稱；重構管線時會假紅，事件映射結果應透過軌道渲染驗證。 |
| 504 | `if (sceneLane === 'sinr-live' \|\| sceneLane === 'modqn-live-` | B | RED | 轉行為斷言。Playwright 切換至 `sinr-live` 與 `modqn-live-cell-preview` 模式，操作檢驗 `[data-testid="handover-event-rail"]` 內渲染出的事件為 live Walker 來源事件。 |
| 505 | `if (sceneLane === 'modqn-replay-proof') return modqnHandove` | B | RED | 轉行為斷言。Playwright 切換至 `modqn-replay-proof` 模式，檢驗 `[data-testid="handover-event-rail"]` 顯示的事件列標籤為 `producer trace` 且事件源自 producer 資料。 |
| 509 | `const liveTimelineWindowStartSec = demoStartOffset;` | V | RED | 刪除。釘死內部變數賦值；時間軸窗口錨定應透過時間軸顯示數值與 Seek 行為驗證，變數改名或抽入 hook 不影響使用者體驗。 |
| 510 | `simState.simTimeSec - liveTimelineWindowStartSec` | B | RED | 轉行為斷言。在 live Walker 模式下播放或 seek，透過 Playwright 讀取時間軸時間文字（如 `[data-testid="timeline-current-time"]`），驗證其顯示為窗口經過時間而非絕對秒數。 |
| 511 | `demoStartOffsetSec: demoStartOffset` | B | RED | 轉行為斷言。在 live Walker 模式下拖曳時間軸 seek，操作驗證時間軸起始標籤或 telemetry dataset 中的 window offset 保持不變且未被重設。 |
| 551 | `deriveModqnServiceMap({` | V | RED | 刪除。釘死 MainScene 內部的函式呼叫；抽離至專屬 service layer 或 hook 時會假紅，產出正確性直接由下游 UE 覆蓋與遙測驗證。 |
| 552 | `buildModqnCellServiceReadout({` | V | RED | 刪除。釘死 MainScene 內的建構呼叫；邏輯搬移至狀態發布模組時會假紅，其效果已由 SimState 與遙測驗證。 |
| 553 | `modqnCellServiceReadout,` | V | RED | 刪除。僅釘住物件屬性縮寫字面；讀數發布是否正常已由 `el.dataset.modqnServedUeCount` 遙測斷言保護，變數更名無使用者影響。 |
| 555 | `ueCountByCellId={modqnServiceMap.ueCountByCellId}` | B | RED | 轉行為斷言。在 MODQN 模式下透過 Playwright 檢查 `[data-testid="cell-overlay"]` 或 cell badge 上的 UE 數量文字/屬性與服務排程一致。 |
| 556 | `const showCellReassignmentEventArcs = modqnVisualLayers.hand` | V | RED | 刪除。釘死內部暫存變數宣告；此 flag 的開關效果已在行 558 的 visibility 或 3D 渲染層體現，變數宣告本身無防護價值。 |
| 558 | `visible={showCellReassignmentEventArcs}` | B | RED | 轉行為斷言。透過 UI 切換包含與不包含 handoverCues 的 preset，在 Playwright 中檢驗 3D 場景中 reassignment arc 節點的 `visible` 狀態或畫面上 arc 元素之增減。 |
| 560 | `beamConeScope: renderedCellBeamConeScope` | V | RED | 刪除。純屬內部輔助函式傳參字面；實際 scope 行為已由行 566 的 `el.dataset.cellBeamConeScope` 遙測斷言完整保護。 |
| 562 | `beamConeScope={modqnVisualLayers.beamConeScope}` | B | RED | 轉行為斷言。操作切換不同 beamConeScope 之 preset，透過 Playwright 觀察 canvas 上 `data-cell-beam-cone-scope` 遙測屬性及波束錐渲染數量的連動變化。 |

## scripts/validate-phase-c-camera-preset.tsx

| 行號 | 斷言字面（原文照抄，可截到 60 字） | 類別 | 分解衝擊 | 處置建議（繁中，一到兩句，具體） |
| :--- | :--- | :---: | :---: | :--- |
| 260 | `controls.enabled = false` | B | RED | 轉行為斷言。Playwright 觸發 director acquiring 運鏡時模擬 canvas 滑鼠拖曳，驗證相機姿態不隨滑鼠改變（OrbitControls 處於禁用狀態）。 |
| 336 | `from './directorFocusPose'` | V | RED | 刪除。釘死 MainScene.tsx 的 import 路徑字面；相機邏輯抽離至 CameraController 後 MainScene 不再引用它即會假紅，單一真理源已由獨立單元測試保證。 |

## scripts/validate-phase-d-reward-curve.tsx

| 行號 | 斷言字面（原文照抄，可截到 60 字） | 類別 | 分解衝擊 | 處置建議（繁中，一到兩句，具體） |
| :--- | :--- | :---: | :---: | :--- |
| 281 | `import { RewardCurvePanel } from './ui/modqn-training/Reward` | V | RED | 刪除。釘死 import 語句字面；若側邊欄抽成獨立元件（如 `ModqnSidebar`），App.tsx 不再 import 它即會假紅，且元件掛載可由 DOM/testid 行為驗證。 |

實際輸出 23 列
