# Y7：8 個可處置工作項目的來源層偵錯報告

查證基準：目前共用 checkout 的 HEAD 為 `456381997577399e388f17918e37b251068d127b`，分支為 `wip/ee-handover-authority-2026-09-05`。本報告只讀取現行原始碼與既有文件；沒有執行任何 `*:browser` validator，也沒有執行 `check:ee` / `check:visual`。除本檔外未修改任何檔案。

## 逐個處置

### 1. `validate:contact-window-labs:browser` — **修（語意定位；若仍為 0 則 fail closed）**

**判定。** 現行 build 明確掛載一個回首頁的公開 `<a>`；不應退休這個 navigation 契約。validator 應由不等待的 raw CSS `count()` 改成等待公開 link 掛載的可存取名稱定位，再驗證解析後 pathname。這不是繞道：仍是點得到的公開連結，不改 React state、不注入全域值、不 mock 路徑。

**證據。** 失敗斷言在 `scripts/validate-canonical-experiments-browser.ts:23-35`；實際 stage nav 與 home link 在 `src/course/nav/SixActsNav.tsx:31-49`。

```text
$ rg -n 'six-acts-stage-nav|six-acts-nav__stage-home|a\[href="/"\]' src/course/nav/SixActsNav.tsx scripts/validate-canonical-experiments-browser.ts
scripts/validate-canonical-experiments-browser.ts:24:  const nav = page.getByTestId('six-acts-stage-nav');
scripts/validate-canonical-experiments-browser.ts:30:  assert.equal(await nav.locator('a[href="/"]').count(), 1);
src/course/nav/SixActsNav.tsx:35:      data-testid={variant === 'stage' ? 'six-acts-stage-nav' : undefined}
src/course/nav/SixActsNav.tsx:46:        <a className="six-acts-nav__stage-home" href="/" aria-label="回首頁" title="回首頁模擬器">
```

**具體動作。** 在 `scripts/validate-canonical-experiments-browser.ts:30` 以以下語意契約取代單行 raw CSS count：

```ts
const home = nav.getByRole('link', { name: '回首頁' });
await home.waitFor({ state: 'attached', timeout: 30_000 });
assert.equal(await home.count(), 1);
assert.equal(new URL(await home.getAttribute('href') ?? '', page.url()).pathname, '/');
```

現有失敗「CSS selector 得 0」與現行 JSX 矛盾。因此這個修改必須帶條件驗證：若語意 locator 在主 session 仍得 0，不得再放寬斷言；那代表 dev server/route/runtime 與本 checkout 不一致或真的未 mount，應保留紅燈並收集實際 `nav.outerHTML`。

### 2. `validate:golden-flow:browser` — **修**

**判定。** 這是 6 秒邊界上的 torn read。元件每個 animation frame 更新 `reviewWallElapsedSec`，同一次 render 同時產生 elapsed 與 visible-controls；validator 卻用兩次獨立 CDP/DOM 讀取。第一讀可能是 `5.999`、第二讀已跨到 `6.000`，於是由舊 elapsed 算出空集合，卻讀到 `replay,next`。

**證據。** 分離讀取在 `scripts/validate-golden-flow-browser.ts:783-791`；rAF 更新及同-render dataset 在 `src/prototype/golden-flow/GoldenFlowPrototype.tsx:1052-1064,1082-1083,1188-1194`；6 秒契約在 `src/prototype/golden-flow/goldenFlowDirector.ts:686-709,766-780`，單元測試鎖住邊界於 `src/prototype/golden-flow/goldenFlowDirector.test.ts:99-102`。

```text
$ rg -n 'data-control-evaluation-elapsed-sec|data-visible-controls|reviewWallElapsedSec|goldenFlowControlsAvailable\(newNormal' scripts/validate-golden-flow-browser.ts src/prototype/golden-flow
scripts/validate-golden-flow-browser.ts:784:        await page.locator('main').getAttribute('data-control-evaluation-elapsed-sec'),
scripts/validate-golden-flow-browser.ts:789:        await page.locator('main').getAttribute('data-visible-controls'),
src/prototype/golden-flow/GoldenFlowPrototype.tsx:1053:  const [reviewWallElapsedSec, setReviewWallElapsedSec] = useState(0);
src/prototype/golden-flow/GoldenFlowPrototype.tsx:1059:      setReviewWallElapsedSec((performance.now() - reviewMountTime) / 1000);
src/prototype/golden-flow/GoldenFlowPrototype.tsx:1188:      data-visible-controls={visibleControls}
src/prototype/golden-flow/GoldenFlowPrototype.tsx:1193:      data-control-evaluation-elapsed-sec={effectiveElapsedSec.toFixed(3)}
src/prototype/golden-flow/goldenFlowDirector.test.ts:101:assert.deepEqual(goldenFlowControlsAvailable(newNormal, 5.999), []);
src/prototype/golden-flow/goldenFlowDirector.test.ts:102:assert.deepEqual(goldenFlowControlsAvailable(newNormal, 6), ['replay', 'next']);
```

**具體動作。** 把 `scripts/validate-golden-flow-browser.ts:783-791` 改為一次 `locator.evaluate` 原子讀取兩個 attribute，再用該次 elapsed 算期望值：

```ts
const controlSnapshot = await page.locator('main').evaluate(element => ({
  elapsedSec: Number((element as HTMLElement).dataset.controlEvaluationElapsedSec),
  visibleControls: (element as HTMLElement).dataset.visibleControls ?? '',
}));
assert.ok(Number.isFinite(controlSnapshot.elapsedSec), `${beat.id}: control evaluation time is exposed`);
assert.equal(
  controlSnapshot.visibleControls,
  goldenFlowControlsAvailable(beat, controlSnapshot.elapsedSec).join(','),
  `${beat.id}: only beat-authorized controls are exposed`,
);
```

保留 `scripts/validate-golden-flow-browser.ts:846-852` 的 6.2 秒後正向斷言，避免以消除 race 為名削弱授權時序。

### 3. `validate:homepage:multi-candidate:browser` — **修**

**判定。** 首頁 quick buttons 已刻意成為 authored lecture 的入口，不再是 source-backed event index；其計數也刻意不對使用者顯示。validator 若繼續點它並要求 indexed-event count，會跨越自己的 live-truth 邊界。現行 build 另有公開、可操作、source-backed 的 intra/inter timeline marker，且 validator 已先驗證這兩個 marker；應把 flow action 改成點 marker。

**證據。** 過期按鈕邏輯在 `scripts/validate-homepage-multi-candidate-acceptance-browser.ts:468-514`。首頁隱藏 count 在 `src/App.tsx:4084-4094`；quick action 進 authored lecture 在 `src/App.tsx:3329-3352,3372-3376,3412-3416`；其 claim boundary 在 `src/homepage/teaching/handoverTeachingScript.ts:1-23`。相對地，公開 marker 由真實 Walker event index 選出並交給 transport seek：`src/App.tsx:2494-2505`、`src/homepage/controller/teachingTimelineMarkers.ts:64-70`、`src/ui/TimelineBar.tsx:330-350`。validator 自己已在 `scripts/validate-homepage-multi-candidate-acceptance-browser.ts:298-320` 鎖住 marker 的 source owner、kind、source time、click target 與可操作性。

```text
$ rg -n 'scripted teaching story|not user-facing evidence|selectHomepageTeachingTimelineMarkers|data-click-target-sec|current Next button' src/App.tsx src/homepage src/ui/TimelineBar.tsx scripts/validate-homepage-multi-candidate-acceptance-browser.ts
src/App.tsx:2495:  // the same natural event index. Marker clicks seek through the existing
src/App.tsx:2501:    return selectHomepageTeachingTimelineMarkers(liveWalkerDirectorHandoverRailEvents, {
src/App.tsx:3373:    // The homepage buttons own a scripted teaching story, not an index seek.
src/App.tsx:3413:    // The homepage buttons own a scripted teaching story, not an index seek.
src/App.tsx:4085:        // indexed rows is not user-facing evidence and only made the controls
src/homepage/controller/teachingTimelineMarkers.ts:68: * actual seek. No handover decision or second clock is created here.
src/ui/TimelineBar.tsx:342:              data-click-target-sec={String(marker.clickTargetSec)}
scripts/validate-homepage-multi-candidate-acceptance-browser.ts:500:    `${kind}: current Next button must expose a positive indexed-event count or the explicit real intra-trigger contract`,
```

**具體動作。** 在 `scripts/validate-homepage-multi-candidate-acceptance-browser.ts`：

1. 將檔頭「Next buttons」的契約改成「source-backed timeline markers」。
2. 刪除 `waitForButtonReady`（`:468-480`）。
3. 在 `runFlow`（`:482-514`）收集 `before` 後，定位 `[data-testid^="timeline-event-marker-"][data-marker-kind="${kind}"]`，斷言恰有一個且 enabled；讀取並有限數檢查 `data-click-target-sec`；點擊後等待 `.leo-app-shell.dataset.liveTimelineSeekTarget === clickTargetSec.toFixed(3)`。
4. 刪除 `buttonCount` / `buttonTitle` / `realIntraTrigger` 分支。把 `FlowEvidence.buttonCount`（`:116`）改為 `clickTargetSec`，回傳處（`:625`）同步改值。

這條路使用現行公開 UI 與既有 transport，不直接改 state、不發明事件、不把 authored lecture 冒充 live truth。

### 4. `validate:phase-c:lane-experience-bar:browser` — **退休**

**判定。** 它等待的 top-level `LaneExperienceBar` 已被刻意移除；現行靜態 governance 甚至正向要求 App 不 import、不 mount。ADR-002 也已被後續 consolidation supersede。這不是應恢復的 UI 回歸。

**證據。** validator 在 `scripts/validate-phase-c-lane-experience-bar-browser.ts:31-50` 等 bar 並點 segment；元件只剩孤兒檔。現行權威斷言在 `scripts/validate-frontend-scene-lane-governance.ts:2790-2800`；`docs/decisions/ADR-002-nav-surface-vs-lane-authority.md:3-9` 明載 superseded。

```text
$ rg -n "<LaneExperienceBar|from ['\"].*LaneExperienceBar" src --glob '!src/ui/LaneExperienceBar.tsx'
[no matches]

$ rg -n 'LaneExperienceBar is not mounted|retired top-level LaneExperienceBar' scripts/validate-frontend-scene-lane-governance.ts
scripts/validate-frontend-scene-lane-governance.ts:2794:  'App does not retain the retired top-level LaneExperienceBar import',
scripts/validate-frontend-scene-lane-governance.ts:2799:  'LaneExperienceBar is not mounted after the MODQN sub-nav consolidation',
```

**具體動作。** 退休 `scripts/validate-phase-c-lane-experience-bar-browser.ts`，同步移除 `package.json:202` 的 npm key `validate:phase-c:lane-experience-bar:browser`。`.github/workflows/governance.yml` 沒有此項，故 workflow 清單無項目可移除。是否另刪孤兒 `src/ui/LaneExperienceBar.tsx` / styles 是不同的產品清理決定，不是退休 gate 的必要條件。

### 5. `validate:frontend:advanced-drawer-modality:browser` — **退休（退休的是已消失的公開進入路徑，不宣稱 drawer 本體消失）**

**判定。** drawer 本體仍在 MODQN lane mount；但本 gate 唯一進入方式是點已不存在的 `LaneExperienceBar` segment。現行首頁還明載「intentionally does not mount the public SINR/MODQN experience switch」，所以 build 沒有可供此 gate 使用的受支援公開入口。以 URL/localStorage 或直接 state 切 lane 都是題目禁止的繞道。因此現階段應退休這支 browser gate；待產品重新提供公開 MODQN 入口時，以新入口重建 coverage。

**證據。** 過期 click 在 `scripts/validate-frontend-advanced-drawer-modality-browser.ts:37-52`。無 mount 的 grep 同第 4 項。`src/App.tsx:3533-3537` 說明 public switch 刻意不 mount；drawer 只在非 SINR lane 掛載於 `src/App.tsx:4214-4226`，不能把「drawer 尚存在」誤寫成「整項功能刪除」。

```text
$ rg -n 'lane-experience-modqn-live-cell-preview|intentionally does not mount|sceneLane !== .sinr-live.' scripts/validate-frontend-advanced-drawer-modality-browser.ts src/App.tsx
scripts/validate-frontend-advanced-drawer-modality-browser.ts:46:    await page.click('[data-testid="lane-experience-modqn-live-cell-preview"]');
src/App.tsx:3534:  // proof surfaces, but the SINR launch surface intentionally does not mount the
src/App.tsx:4217:          {sceneLane !== 'sinr-live' && (
```

**具體動作。** 退休 `scripts/validate-frontend-advanced-drawer-modality-browser.ts`，同步移除 `package.json:234` 的 npm key `validate:frontend:advanced-drawer-modality:browser`。`.github/workflows/governance.yml` 沒有此項，故 workflow 清單無項目可移除。另須同步修改 `scripts/validate-frontend-scene-lane-governance.ts:3233`，它目前反向釘死該 npm key 必須存在；否則退休後靜態 gate 必紅。

### 6. `validate:live-walker:handover-event-focus:browser` — **保留並標記（QUARANTINED；不是退休，也不是目前的產品回歸）**

**判定。** 它與兩支已 quarantine 的 live director/cinema gate 同根因：live-tab rail 是「日後重建」的 parked feature。依既有處置先例，應保留腳本並加明確 quarantine 註記，不刪腳本、不刪 npm key、不接回 aggregate。

**證據。** 這支 gate 在 `scripts/validate-live-walker-handover-event-focus-browser.ts:144-148` 無條件等 rail。`handoverEventRail` 在 App 雖仍建立（`src/App.tsx:3617-3624`），唯一 JSX mount 是 artifact tab（`src/App.tsx:4477-4491`）；live tab 從 `:4507` 開始但不 mount。`docs/sinr-live-right-sidebar-restore-sdd.md:68-71,89-92` 明載 live rail removed/non-functional/rebuild later。兩支同源 gate 已在 `scripts/validate-phase-c-director-cinematic-live-browser.ts:1-7` 與 `scripts/validate-phase-c-handover-cinema-browser.ts:1-7` 寫出相同 quarantine 契約。

```text
$ rg -n 'QUARANTINED 2026-06-20|handoverEventRail|handover-event-rail' scripts/validate-phase-c-director-cinematic-live-browser.ts scripts/validate-phase-c-handover-cinema-browser.ts scripts/validate-live-walker-handover-event-focus-browser.ts src/App.tsx
scripts/validate-phase-c-director-cinematic-live-browser.ts:1:// QUARANTINED 2026-06-20 — removed from the `validate:live-render` chain (package.json).
scripts/validate-phase-c-handover-cinema-browser.ts:1:// QUARANTINED 2026-06-20 — removed from the `validate:live-render` chain (package.json).
scripts/validate-live-walker-handover-event-focus-browser.ts:147:  await page.locator('[data-testid="handover-event-rail"]').waitFor({ timeout: 30000 });
src/App.tsx:3617:  const handoverEventRail = (
src/App.tsx:4490:                {handoverEventRail}
```

**具體動作。** 在 `scripts/validate-live-walker-handover-event-focus-browser.ts` shebang 後加入同格式 `QUARANTINED 2026-06-20` 檔頭：記錄 live-tab rail 已移除、已知會停在 `:147`、腳本保留供手動/日後重建驗證，功能重建後才接回 `validate:live-render`。保留 `package.json:229` 的 `validate:live-walker:handover-event-focus:browser`；`.github/workflows/governance.yml` 目前沒有它，不做 metadata 變更。靜態 `scripts/validate-live-walker-handover-event-focus.tsx:107-121` 也要求這個 npm key 存在，進一步支持「保留」而非退休。

### 7. W-F G3（兩支）

#### 7a. `validate:phase-c:handover-pulse:render:browser` — **退休**

**判定。** 此 gate 的唯一目的就是要求 legacy natural-pulse carrier 在 root homepage 出現；首頁現行 policy 刻意永遠不 render 該 carrier，並改由 normalized accepted presentation pair 呈現。因此整支 gate 沒有可保留的非過期覆蓋。

**證據。** validator 從 root URL 開始並以 `everRendered`/settled pulse 為唯一正向結果：`scripts/validate-phase-c-handover-pulse-render-browser.ts:60-68,78-114`。`src/scene/MainScene.tsx:3434-3437` 在 homepage 將 natural pulse availability 關掉，`:4042-4049` 再依 policy 回傳空 items；steady policy 在 `src/homepage/controller/homepageSceneGeometryPolicy.ts:129-137` 明定 `renderNaturalPulse: false`，測試於 `src/homepage/controller/homepageSceneGeometryPolicy.test.ts:19-26` 鎖住它，handover-pair 測試也鎖住以 `renderCinemaPair` 取代 pulse（`:40-71`）。

```text
$ rg -n 'naturalPulseAvailable|renderNaturalPulse: false|everRendered' src/scene/MainScene.tsx src/homepage/controller/homepageSceneGeometryPolicy.ts scripts/validate-phase-c-handover-pulse-render-browser.ts
src/scene/MainScene.tsx:3437:    naturalPulseAvailable: !homepageVisualIdentity && recentPrimaryHandoverEvent !== null,
src/homepage/controller/homepageSceneGeometryPolicy.ts:133:    renderNaturalPulse: false,
scripts/validate-phase-c-handover-pulse-render-browser.ts:78:    let everRendered = false; // B: the pulse draws meshes on the live lane
scripts/validate-phase-c-handover-pulse-render-browser.ts:108:    assert.ok(everRendered, 'the ambient pulse rendered >= 1 cone on the live lane (handovers draw, mesh-derived)');
```

**具體動作。** 退休 `scripts/validate-phase-c-handover-pulse-render-browser.ts`；移除 `package.json:187` 的 npm key `validate:phase-c:handover-pulse:render:browser`；從 `package.json:210` 的 `validate:live-render` chain 移除此命令；從 `.github/workflows/governance.yml:95` 移除 `npm run validate:phase-c:handover-pulse:render:browser`。

#### 7b. `validate:phase-c:sinr-live-cells:render:browser` — **修（只移除過期 pulse 子段）**

**判定。** 這支 gate 的 serving-cell render 半段仍保護真實而重要的行為，不能整支退休；只有 G2c ambient pulse 子段已過期。

**證據。** `scripts/validate-phase-c-sinr-live-cell-beams-browser.ts:68-121` 原子驗證 cones、served cells、serving satellites、off-axis、generic cone count 與跨 frame recovery；`:123-156` 才切換到已停用的 legacy pulse。它仍被現行 governance 視為 un-parked cell-cone replacement gate：`scripts/validate-frontend-scene-lane-governance.ts:1849-1866`。

```text
$ rg -n 'healthy frame|G2c ambient|live pulse|cell cones keep rendering' scripts/validate-phase-c-sinr-live-cell-beams-browser.ts
scripts/validate-phase-c-sinr-live-cell-beams-browser.ts:68:    // Wait for ONE healthy frame ATOMICALLY (one DOM read) where the cell cones
scripts/validate-phase-c-sinr-live-cell-beams-browser.ts:121:    assert.ok(conesLater > 0, `cell cones keep rendering across frames (recovered count=${conesLater})`);
scripts/validate-phase-c-sinr-live-cell-beams-browser.ts:123:    // ── G2c ambient live-handover PULSE (the SINGLE handover-visual layer, C3) ──
scripts/validate-phase-c-sinr-live-cell-beams-browser.ts:156:    console.log(`[sinr-live-cell-beams] live pulse: count=${pulse}, meshRendered=${pulseRendered} fired with NO director arm`);
```

**具體動作。** 在 `scripts/validate-phase-c-sinr-live-cell-beams-browser.ts` 移除檔頭 `:29-33` 的 pulse bullet，以及 `:123-156` 的 fast-forward/pulse wait/assert；把 `:164` PASS 訊息改成只宣告 cell-truth cones、fixed cell centres、off-axis 與 live recovery。保留 `package.json:199` npm key、`package.json:210` aggregate 項與 `.github/workflows/governance.yml:101` 清單項。

### 8. `validate:homepage:authority:browser` — **保留並標記（真的 authority 契約；精確故障 conjunct 仍證據不足）**

**判定。** 不應退休或弱化。右 rail 與中央 callout 共用 accepted Walker frame 是 accepted ADR 的現行契約；兩個 surface 及其 datasets 也仍在 build 中。這次紅燈因此應交給 owner 當真實 integration regression（或 dev-server/runtime 與 checkout 不一致）處理。但只靠通用 30 秒 timeout，無法誠實指出是第一個 callout wait，還是第二個 predicate 的哪個 conjunct；目前不能指定產品修復行。

**證據。** `assertSharedWalkerSnapshot` 第一個 wait 在 `scripts/validate-homepage-authority-browser.ts:221-226`；第二個 wait 於 `:227-269` 同時要求節點存在、snapshot/source/phase join、active-link count、candidate receipt、serving identity 與四個 metric 非空。更早的 `assertWalkerHomepage` 已在 `:87-103` 驗證 rail/host/scene 的基本 snapshot join，主流程在 `:893-900` 依序呼叫兩者。產品權威 `docs/decisions/ADR-013-walker-only-homepage-source.md:69-78` 明定右 rail 與中央 callout 必須消費同一 accepted Walker frame。callout 預設開啟於 `src/scene/beamDisplaySpec.ts:92-97,284-287`，掛載條件在 `src/scene/MainScene.tsx:5567-5591`，輸出 identity/formula datasets 在 `src/viz/SinrLiveCellBeamCallouts.tsx:124-186`；rail datasets 在 `src/ui/homepage/HomepageBeamRail.tsx:1368-1397`。

```text
$ rg -n 'beam-callout.*beam-primary|showBeamCallouts \|\| homepageHandoverBeamInfoActive|data-snapshot-id|same accepted Walker' src/scene/MainScene.tsx src/viz/SinrLiveCellBeamCallouts.tsx src/ui/homepage/HomepageBeamRail.tsx docs/decisions/ADR-013-walker-only-homepage-source.md
src/scene/MainScene.tsx:5567:      {(showBeamCallouts || homepageHandoverBeamInfoActive)
src/viz/SinrLiveCellBeamCallouts.tsx:173:              data-testid="beam-callout"
src/viz/SinrLiveCellBeamCallouts.tsx:179:              data-beam-primary={isPrimary ? '1' : '0'}
src/ui/homepage/HomepageBeamRail.tsx:1376:      data-snapshot-id={snapshotId}
docs/decisions/ADR-013-walker-only-homepage-source.md:75:- The right rail and central callouts must consume the same accepted Walker
```

**具體動作。** 先只強化 `scripts/validate-homepage-authority-browser.ts:221-269` 的診斷，不改任何 pass predicate：把兩個 wait 分別包裝；timeout 時用一次 `page.evaluate` 原子輸出下列 named checks 與完整 datasets，再讓原錯誤失敗：

- rail、host、scene、canvas、primary callout、serving row 是否存在；
- snapshot/source-frame/phase 三方 join；
- rail/scene active-data-link count；
- multi-candidate active 時的 accepted snapshot receipt；
- serving row 與 callout 的 satellite/beam identity；
- callout formula-frame 五欄及 serving row 四個 metric 是否非空；
- 所有 callout 的 satellite/beam/cell/primary identity，與 canvas 的 `sinrLiveCellBeamCalloutRenderedCount`。

取得第一個 false 的 named check 後才改 owner code。若第一個 wait 失敗，優先查 `beamInfoItems`/primary identity/callout mount；若第二個 wait 失敗，依 dump 修唯一失配的 join。禁止為了變綠刪 conjunct，也不在此選擇 cellId vs beamId。

## 需要主 session 跑瀏覽器驗證的

以下命令都必須由可啟動 Chromium 的主 session 執行。本 worker 沒有執行它們。

| 提案 | 指令 | 預期結果 |
|---|---|---|
| #1 contact-window labs | `APP_URL=http://localhost:3000 npm run validate:contact-window-labs:browser` | Acts 5/6 的 stage nav 各找到唯一「回首頁」link，pathname 為 `/`，然後繼續通過 labs 斷言。若仍找不到，輸出 `six-acts-stage-nav.outerHTML` 並保留紅燈。 |
| #2 golden flow | `APP_URL=http://localhost:3000 npm run validate:golden-flow:browser` | `new-normal` 的原子 snapshot 在 `<6s` 為空、`>=6s` 為 `replay,next`；既有 6.2 秒正向斷言也通過。 |
| #3 homepage multi-candidate | `APP_URL=http://localhost:3000 npm run validate:homepage:multi-candidate:browser` | intra/inter marker click 各把 `data-live-timeline-seek-target` 設成 marker 的 exact click target，後續仍觀察 accepted snapshot、switching 與 guard/settled phases。若後半失敗，視為 live route 真回歸，不退回 authored quick buttons。 |
| #6 focus quarantine | 現在不要把它當 acceptance 跑；功能重建後才執行 `APP_URL=http://localhost:3000 npm run validate:live-walker:handover-event-focus:browser` | 重建後 live lane 應重新 mount event rail，Focus 能 seek/focus；通過後才解除 quarantine 並接回 aggregate。 |
| #7b SINR live cells | `APP_URL=http://localhost:3000 npm run validate:phase-c:sinr-live-cells:render:browser` | healthy atomic frame 的 cones/served/sats/off-axis/count equality 與 later recovery 全通過；不再等待 legacy pulse。 |
| #7 aggregate metadata | 完成退休/修正後執行 `APP_URL=http://localhost:3000 npm run validate:live-render` | command 展開時不再包含 retired handover-pulse gate，仍包含 SINR live-cells gate。整體可能受本報告範圍外 gate 影響，不能只以 aggregate 最終 exit code 判本提案。 |
| #8 homepage authority | 先套診斷後執行 `APP_URL=http://localhost:3000 npm run validate:homepage:authority:browser` | 若仍 timeout，必須得到第一個 false named check 與同一時刻的 DOM/dataset dump；owner 修復後，rail/scene/callout/serving-row 同一 accepted Walker snapshot 契約全通過。 |

#4、#5、#7a 是退休 gate，本身不需要再跑 browser。metadata 變更後另跑非 browser 的 `npm run validate:frontend:scene-lane-governance`，並以 `rg` 確認被退休 key/script 的引用已同步清乾淨；#5 必須同時處理 `scripts/validate-frontend-scene-lane-governance.ts:3233` 的舊 package-key pin。

## 需要 owner 決定的

1. **移除覆蓋須明示核准：** #4 退休 `validate:phase-c:lane-experience-bar:browser`；#5 退休 `validate:frontend:advanced-drawer-modality:browser`；#7a 退休 `validate:phase-c:handover-pulse:render:browser`。其中 #5 移除的是「目前沒有公開入口可執行的 drawer browser coverage」，不是宣告 drawer 本體不存在；owner 若預計近期恢復公開 MODQN 入口，也可改選 quarantine，但不能以隱藏 state/URL/localStorage 讓現 gate 假綠。
2. **#4 孤兒元件清理：** 是否另刪 `src/ui/LaneExperienceBar.tsx` 與殘留 styles。這不影響 gate 退休，本報告不代替 owner 決定。
3. **#6 維持 parked feature 契約：** 本提案是補 quarantine 標記並保留 npm key。何時重建 live rail、何時重新掛回 `validate:live-render` 仍由 owner 決定；目前不可退休它。
4. **#8 的產品修復點：** 必須先由主 session 取得 named diagnostic。證據不足前不應在 cellId/beamId 之間選邊，也不應移除 authority coverage。
5. **#1 的 source/runtime 矛盾：** 若語意 locator 仍找不到現行 JSX 明載的 home link，owner 應先確認被測 server 的 bundle/route/HEAD；不能繼續改 selector 掩蓋。

## 我查證後發現上面背景敘述有誤的地方

1. 「8 個」若指**工作項目**成立；若指 validator 指令則是 **9 支**：第 7 項 G3 內含 `validate:phase-c:handover-pulse:render:browser` 與 `validate:phase-c:sinr-live-cells:render:browser` 兩支。本報告依題意維持 8 個編號，並在 #7 分成 7a/7b。
2. #1 所述 runtime 現象 `a[href="/"]` 得 0 與目前 checkout 的 `src/course/nav/SixActsNav.tsx:45-48` 明確 JSX 相衝突。沒有主 session 的實際 DOM/bundle 證據前，不能判定背景的實跑紀錄錯誤；只能標記為待驗證的 source/runtime 不一致。
