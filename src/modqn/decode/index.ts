// Pure-TS decode engine — the front-end re-run of the frozen route-B factorial
// decode over RECORDED dense Q (argmax <-> auction, ω re-weighting, k_cap). No
// render / lane / UI imports (purity locked by validate:modqn:decode-parity).
export * from './types';
export * from './scalarize';
export * from './auctionDecode';
