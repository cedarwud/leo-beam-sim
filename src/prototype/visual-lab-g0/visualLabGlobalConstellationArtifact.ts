/**
 * Compatibility entry point for the G0 prototype lane.  The implementation
 * lives in the isolated visual-lab module so the route can consume the same
 * strict artifact contract without importing prototype scene state.
 */
export * from '../../visualLab/globalConstellation';

